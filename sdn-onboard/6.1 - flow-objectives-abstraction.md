# 6.1. Luồng Objectives: abstraction của ONOS

> **Trạng thái:** Content written (Phase B, 2026-04-21)
> **Khối:** VI, Mô hình SDN mới nổi
> **Phần:** Phần 6
> **Ebook mapping:** `plans/ebook-coverage-map.md` → Göransson Ch7.2 (KEEP)
> **Plan:** `plans/sdn-foundation-architecture.md` §3.3 Khối VI
> **Prerequisites:** Phần 6.0, Phần 4.5 (TTP)

---

## Mục tiêu bài học

Sau khi hoàn thành phần này, bạn có thể:

1. **Mô tả (Understand)** Luồng Objectives, abstraction layer của ONOS cho phép application express ý đồ cấu hình ở mức cao hơn OpenFlow luồng entry, tránh TTP complexity và vendor-specific chuỗi xử lý (pipeline).
2. **Phân biệt (Analyze)** 3 objective types (Filtering / Chuyển tiếp / Next) và layered chuyển tiếp model, so sánh với raw luồng table programming của OpenFlow.
3. **Liên hệ (Analyze)** Luồng Objectives với driver layer trong ONOS architecture, cách driver translate objective sang device-specific luồng rule.
4. **Đánh giá (Evaluate)** tại sao Luồng Objectives sự áp dụng hẹp ngoài ONOS, bài học về abstraction layer không phải silver bullet cho SDN fragmentation problem.

---

## Bối cảnh: tầng trừu tượng trên OpenFlow

Phần 4.5 đã phân tích TTP (Table Type Patterns) như attempt của ONF để bridge giữa spec generic và silicon capability fragmented, nhưng fail sự áp dụng vì timing + complexity + vendor incentive issue. Phần 6.0 đã phân tích P4 như mô hình shift thay thế OpenFlow với programmable data plane. Phần này phân tích **cách tiếp cận thay thế thứ ba**: Luồng Objectives của ONOS, giữ OpenFlow làm protocol nhưng add abstraction layer trên controller side.

Triết lý Luồng Objectives: thay vì programmer application write raw OpenFlow luồng entries (phải know match fields, actions, table numbers, priority), application express **high-level ý đồ cấu hình** như "forward traffic from ingress X to egress Y". ONOS driver layer dịch ý đồ cấu hình sang device-specific luồng rules. Driver abstracts hardware heterogeneity từ application code.

Đây là pattern "rising tide" trong software engineering: khi primitive layer too low-level (OpenFlow), introduce abstraction layer to reduce cognitive load. Luồng Objectives success trong ONOS ecosystem (CORD, Trellis) but limited outside.

## 6.1.1 Động lực: TTP không khả thi cho triển khai đa vendor

### Xem lại TTP

Phần 4.5 đã mô tả TTP workflow:
1. ONF define reference TTPs (L2-L3 Edge, Aggregation-Core, WAN PE).
2. Switch advertise supported TTP IDs trong FEATURES_REPLY.
3. Controller choose TTP phù hợp application needs.
4. Controller install flow_mod tuân theo TTP schema.

Vấn đề TTP:
- **Fixed catalog**: thêm pattern mới yêu cầu ONF spec revision, slow process.
- **Vendor implementation**: hardware vendor (Broadcom/Cisco/Juniper) không commit implement TTP layer trong silicon/firmware. Chi phí engineering không justify với limited market demand.
- **Application burden**: application programmer vẫn phải biết TTP schema của switch đang dùng, có nghĩa là aware của switch capability. Không fully abstract.

Kết quả TTP: ONF TS-017 v1.0 (15/08/2014) publish nhưng sự áp dụng thực tế gần zero (Phần 4.5.3).

### Cách tiếp cận của nhóm kiến trúc ONOS

ONOS (Open Network Operating System) của ON.Lab 2014 face cùng problem: application build on ONOS cần portable across switch vendors với heterogeneous chuỗi xử lý (pipeline). TTP không feasible (phụ thuộc vendor implement). ON.Lab decision: **build abstraction trong ONOS controller itself, không on switch side**.

Đây là Luồng Objectives, introduced khoảng 2015 (ONOS 1.x series). Design philosophy:

1. **Application express ý đồ cấu hình ở abstraction cao**: "forward gói tin với X match → Y port". Không specify table ID, priority, match field details.
2. **ONOS framework translate**: framework có compile-time knowledge về switch chuỗi xử lý (pipeline) (via driver class per device model).
3. **Driver emit device-specific luồng**: driver class cho Broadcom OFDPA 2.0 chuỗi xử lý (pipeline) different với driver cho Corsa, Noviflow, etc. Application không biết difference.

Key insight: **abstraction layer locate ở controller (software, flexible) thay vì switch (hardware/firmware, rigid)**. Driver là code trong ONOS framework; adding new switch model = write new driver class, deploy ONOS update, không touch switch firmware.

## 6.1.2 Ba loại objective: filtering, chuyển tiếp, next

### Mô hình chuyển tiếp nhiều tầng

Luồng Objectives define 3 objective types mapping sang 3-layer chuyển tiếp concept:

```
Packet → FilteringObjective (admission) → ForwardingObjective (match logic) → NextObjective (next-hop group) → Egress
```

Mỗi objective type có role cụ thể, tổng hợp tạo complete chuyển tiếp chuỗi xử lý (pipeline).

### FilteringObjective

**Role**: "who can enter this chuỗi xử lý (pipeline)". Admission control ở ingress port.

Tiêu chí khớp:
- VLAN ID (untagged / specific VLAN / priority-tagged).
- MAC address (source, destination).
- IP address optionally.

Operation types:
- **PERMIT**: allow traffic match vào chuỗi xử lý (pipeline).
- **DENY**: drop traffic match.

Example:
```
FilteringObjective {
    key: VLAN 100 on port eth0,
    conditions: [MAC=aa:bb:cc:dd:ee:ff allowed],
    operation: PERMIT
}
```

Ý đồ cấu hình: port eth0 chỉ accept VLAN 100 với specific MAC. ONOS driver translate thành:
- OFDPA chuỗi xử lý (pipeline): luồng install trong VLAN Filtering table (Table 10) và Termination MAC table.
- Corsa chuỗi xử lý (pipeline): different table numbers, same logical effect.

### ForwardingObjective

**Role**: "how to forward matched traffic". Core routing/switching decision.

Flavor types:
- **SPECIFIC**: exact match (dst MAC exact, dst IP exact).
- **VERSATILE**: wildcarded match (ACL, classifier with priority).

Match + action:
```
ForwardingObjective {
    flavor: SPECIFIC,
    selector: dl_dst=aa:bb:cc:dd:ee:ff,
    nextId: 42,  // reference to NextObjective
    priority: 100
}
```

Ý đồ cấu hình: match destination MAC, forward qua next-hop group 42. Application không specify output port trực tiếp; output port là responsibility của NextObjective 42.

Driver translate ForwardingObjective:
- OFDPA: install trong Bridging table (Table 50) hoặc ACL table.
- Native Linux kernel NOS: install route qua netlink.

### NextObjective

**Role**: "next-hop group". Define how traffic egress qua single port, multiple ports (ECMP), or failover.

Group types:
- **SIMPLE**: single output action.
- **HASHED**: hash-based ECMP across buckets.
- **FAILOVER**: primary + backup buckets (like OpenFlow FAST_FAILOVER group type).
- **BROADCAST**: all buckets (like ALL group type).

Example:
```
NextObjective {
    id: 42,
    type: HASHED,
    buckets: [
        { output: eth1, MAC rewrite: 00:00:00:00:00:01 },
        { output: eth2, MAC rewrite: 00:00:00:00:00:02 }
    ]
}
```

Ý đồ cấu hình: ECMP across 2 uplinks với MAC rewrite per-bucket.

### Ghép nối nhiều tầng

Application workflow:

```
1. Install NextObjective 42 (next-hop group).
2. Install ForwardingObjective với nextId=42 (route).
3. Install FilteringObjective permit VLAN 100 (admission).
```

Order matters: NextObjective first (backward reference), ForwardingObjective reference NextObjective. FilteringObjective independent.

Hiểu sai thường gặp:

**Hiểu sai:** "Luồng Objectives = wrapper thin around OpenFlow luồng entries. Application có thể ignore Luồng Objectives, use raw OpenFlow directly."

**Thực tế:** Luồng Objectives abstract away chuỗi xử lý (pipeline) fragmentation meaningful. Raw OpenFlow requires application know table number, match field support, action availability per switch vendor. Luồng Objectives driver encapsulate this knowledge. Application code viết against Luồng Objectives portable; raw OpenFlow code viết for specific chuỗi xử lý (pipeline) (OFDPA) không portable sang different chuỗi xử lý (pipeline) (Corsa, BCM native). Difference is architecturally significant for multi-vendor deployment.

## 6.1.3 Tầng driver: ánh xạ objective sang luồng của thiết bị

### Trách nhiệm của driver

ONOS driver class per switch model (hoặc per chuỗi xử lý (pipeline) family). Driver responsibility:

1. **Chuỗi xử lý (Pipeline) knowledge**: encode silicon/firmware chuỗi xử lý (pipeline) structure (table numbers, valid match fields per table, action support per stage).
2. **Objective translation**: convert FilteringObjective / ForwardingObjective / NextObjective → specific OpenFlow luồng entries.
3. **Group management**: create/update/delete OpenFlow groups corresponding to NextObjective.
4. **Error mapping**: translate device-specific errors (OFPERR_BAD_MATCH on Table 42) back to application-level diagnostics.

### Ví dụ driver: OFDPA 2.0

OFDPA (OpenFlow Data Plane Abstraction) là Broadcom's chuỗi xử lý (pipeline) specification for their switch silicon. OFDPA 2.0 chuỗi xử lý (pipeline) (simplified):

| Table | Purpose |
|-------|---------|
| 0 (Ingress Port) | Port classification |
| 10 (VLAN) | VLAN processing |
| 20 (Termination MAC) | MAC address recognition (for routing) |
| 30 (Unicast Routing) | Unicast L3 chuyển tiếp |
| 50 (Bridging) | L2 bridging |
| 60 (Policy ACL) | ACL |
| 80 (Egress VLAN) | Output VLAN processing |

ONOS OFDPA driver translate FilteringObjective:
- PERMIT VLAN 100 on port eth0 → install luồng trong Table 10 (VLAN) với match `in_port=eth0, vlan_vid=100`, action = goto_table:20.

Translate ForwardingObjective (routing):
- Route 10.0.0.0/24 via next-hop group 42 → install luồng trong Table 30 (Unicast Routing) với match `eth_type=0x0800, ipv4_dst=10.0.0.0/24`, action = group:42.

Translate NextObjective:
- Create OpenFlow group 42 (type=select), buckets = uplink ports.

All internal to driver. Application just calls `ForwardingObjective.Builder ... .build()` và submit to FlowObjectiveService.

### Danh mục driver trong ONOS

ONOS ship drivers cho:
- **OFDPA** (Broadcom): various versions (OFDPA 1.0, 2.0, 3.0).
- **Corsa**: Corsa Red series switch.
- **Noviflow**: Noviflow programmable switch.
- **OpenFlow standard**: fallback for generic OpenFlow switch (limited feature).
- **P4Runtime**: P4-capable switch qua P4Runtime API.

Driver list tại `https://docs.onosproject.org/` + source code `https://github.com/opennetworkinglab/onos/tree/master/drivers`.

### Mô hình phát triển driver

Adding new switch model:
1. Fork ONOS repo, add driver class extending `Pipeliner` abstract class.
2. Implement `filter()`, `forward()`, `next()` methods.
3. Register driver với device manager via `@Component` annotation.
4. Test against switch (interoperability testing).
5. Submit PR upstream.

Cycle time: 2-4 weeks for standard chuỗi xử lý (pipeline) (e.g., new Broadcom OFDPA variant). 3-6 months for novel chuỗi xử lý (pipeline) (e.g., new Noviflow P4-based switch). Much faster than TTP (years) or spec revision (years).

## 6.1.4 Current state: sự áp dụng hẹp ngoài ONOS

### Trellis SD-Fabric

Luồng Objectives thành công rõ nhất trong **Trellis** project: ONF's open-source SD-Fabric for leaf-spine datacenter. Trellis use:
- Mạng hạ tầng (Underlay): leaf-spine IP fabric with BGP unnumbered.
- Control plane: ONOS running Luồng Objectives programming Broadcom OFDPA switches.
- Application: **SEBA** (SDN Enabled Broadband Access) for telco central office PON (Passive Optical Network) virtualization.

Trellis production deployment: Comcast, AT&T Central Office re-architected as Datacenter (CORD) initiative 2016-2020. Small-scale but production.

Trellis documentation: `https://opennetworking.org/trellis/`.

### CORD (Central Office Re-architected as Datacenter)

CORD là ONF telco initiative để replace traditional Central Office với general-purpose DC infrastructure running virtualized telco functions. Components:
- Compute: OpenStack hoặc Kubernetes.
- Mạng hạ tầng (Underlay): Trellis (switch + ONOS + Luồng Objectives).
- VNF (Virtual Network Functions): virtualized BNG, vCSR, vOLT.

CORD documentation: `https://opennetworking.org/cord/`.

### Sự chấp nhận ngoài ONOS

**OpenDaylight**: không adopt Luồng Objectives. ODL apps write raw OpenFlow luồng tables directly. Rationale: ODL's multi-SB architecture (not just OpenFlow but NETCONF, BGP-LS, gNMI) pre-dates Luồng Objectives abstraction.

**Ryu, POX, Faucet**: legacy first-generation controllers, pre-date Luồng Objectives. Write raw OpenFlow.

**Commercial vendor controllers** (Cisco ACI, Juniper Contrail): use native proprietary API, không use OpenFlow standard. Luồng Objectives irrelevant.

**Kubernetes SDN** (OVN-Kubernetes, Calico, Cilium): use native constructs (OVN logical luồng, BGP, eBPF), không adopt Luồng Objectives.

### Evaluation: abstraction không phải silver bullet

Luồng Objectives cho phép portable app across Broadcom OFDPA + Corsa + Noviflow trong ONOS ecosystem. Nhưng:

**Limitation 1**: chỉ portable trong ONOS driver catalog. Switch không có driver = không support. Coverage reality, ONOS driver catalog limited to ~10 switch models.

**Limitation 2**: abstraction leak. ForwardingObjective có flavor SPECIFIC vs VERSATILE là implicit về silicon constraint. Programmer vẫn cần biết chuỗi xử lý (pipeline) structure để optimize.

**Limitation 3**: không thay thế P4. P4 solve fragmentation bằng programmable data plane (compile down); Luồng Objectives solve bằng abstraction at controller (translate at runtime). P4 more powerful long-term but requires silicon support; Luồng Objectives ship today with existing silicon.

### Hiểu sai thường gặp

**Hiểu sai:** "Luồng Objectives là ONOS-specific feature, nên không phải standard."

**Thực tế:** Luồng Objectives là design pattern (layered abstraction trên OpenFlow), applicable to any controller. OpenDaylight choose không adopt là strategic decision, không phải Luồng Objectives không work ngoài ONOS. Pattern có thể port sang ODL hoặc custom controller. "ONOS-specific" là implementation reality, không phải design constraint.

> **Key Topic:** Luồng Objectives thể hiện pattern "abstraction in controller, not in switch" phù hợp với SDN economic reality. Switch firmware update expensive, vendor không incentivized push complexity vào switch (TTP fail minh chứng). Controller abstraction layer cheap to iterate, single codebase change update all users. Luồng Objectives là pragmatic middle path giữa raw OpenFlow (application burden) và P4 (silicon burden). Bài học: khi design abstraction, locate complexity ở layer flexible nhất, không layer rigid nhất.

---

## Mạch nối với phần sau

Phần 6.1 đã phân tích Luồng Objectives: động cơ vượt TTP fragmentation problem, 3 objective types (Filtering/Chuyển tiếp/Next) với layered chuyển tiếp model, ONOS driver layer translate objective sang device luồng mapping, và sự áp dụng reality (thành công trong Trellis/CORD nhưng hẹp ngoài ONOS ecosystem). Khối VI hoàn tất với 2 mô hình SDN mới nổi: P4 programmable data plane (6.0) và Luồng Objectives abstraction (6.1). Khối VII tiếp theo phân tích controller ecosystem chi tiết: Phần 7.0 NOX/POX/Ryu/Faucet first-gen, Phần 7.1 OpenDaylight architecture, Phần 7.2 ONOS service provider scale (deep với Atomix Raft), Phần 7.3 vendor controllers ACI/Contrail (full architecture comparison với open-source controllers).

---

## Tài liệu tham khảo

1. [ONOS Project Documentation](https://docs.onosproject.org/), Open Networking Foundation. Luồng Objectives API reference, driver SDK, core tutorials.
2. [ONOS GitHub Repository](https://github.com/opennetworkinglab/onos), Open Networking Lab. Source code Luồng Objectives implementation + driver classes.
3. [ONF Trellis SD-Fabric](https://opennetworking.org/trellis/), Open Networking Foundation. Production deployment của Luồng Objectives cho leaf-spine DC.
4. [ONF CORD Platform](https://opennetworking.org/cord/), Open Networking Foundation. Central Office Re-architected as Datacenter initiative.
5. [Broadcom OFDPA Pipeline Specification](https://github.com/Broadcom-Network-Switching-Software/OF-DPA), Broadcom. OFDPA 2.0 chuỗi xử lý (pipeline) abstraction specification.
6. [OpenFlow Switch Specification Version 1.3.5](https://opennetworking.org/wp-content/uploads/2014/10/openflow-switch-v1.3.5.pdf), ONF, March 2015. Protocol baseline Luồng Objectives operate over.
7. [ONF TS-017, OpenFlow Table Type Patterns v1.0](https://opennetworking.org/sdn-standards/openflow/), ONF. Contrast reference, TTP approach failed, Luồng Objectives successor attempt.
8. [P4: Programming Protocol-Independent Packet Processors](https://arxiv.org/abs/1312.1719), Bosshart et al., CCR 2014. Alternative mô hình cho cùng fragmentation problem.
9. [ONOS P4Runtime Integration](https://docs.onosproject.org/), ONOS. Luồng Objectives có thể target P4-capable switch qua P4Runtime driver.
10. [Software Defined Networks: A Comprehensive Approach, 2nd edition](https://www.sciencedirect.com/book/9780128045558/software-defined-networks), Göransson, Black, Culver, Morgan Kaufmann 2017. Chương 7.2 phân tích Luồng Objectives.
