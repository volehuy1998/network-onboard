# 9.14. Incident response decision tree cho OVS/OVN

> **State:** Content written (Phase B, 2026-04-22)
> **Khối:** IX, OVS internals + operations (Capstone Khối IX)
> **Phần:** Phần 9
> **Ebook mapping:** compass Ch 20 (incident response) + Ch 18 (observability) + Appendix C
> **Plan:** `plans/sdn-foundation-architecture.md` §3.3 Khối IX
> **Prerequisites:** Phần 9.0-9.13 (toàn bộ Khối IX)
> **Nguồn offline chính:**
> - `sdn-onboard/doc/compass_artifact_wf-*.md` — Chapter 18 (observability stack: sFlow + Prometheus + ovs_exporter), Chapter 20 (incident response decision tree 5-step), Appendix C (troubleshooting decision tree condensed).

Môi trường thực hành: Ubuntu 22.04 LTS với OVS 2.17.9 production-like deployment. Tham khảo chính: [OVS Troubleshooting](https://docs.openvswitch.org/en/latest/topics/testing/).

---

## Mục tiêu bài học

1. **Áp dụng (Apply)** decision tree 5-step cho năm loại sự cố phổ biến: packet loss, unexpected drops, tunnel down, bond flap, OVSDB unavailable.
2. **Tích hợp (Synthesize)** kiến thức từ Phần 9.0-9.13 vào workflow khắc phục sự cố tổng thể.
3. **Đánh giá (Evaluate)** observability stack đúng cho production: sFlow + Prometheus + coverage cron + log aggregation.

---

Yêu cầu kiến thức: toàn bộ Phần 9.0-9.13.

---

## 9.14.1 Observability stack (compass Ch 18)

Production OVS cần ba layer observability:

**Layer 1 — Traffic analytics**: sFlow (Phần 9.8) feed vào goflow2 hoặc ntopng. Cho biết flow nào đang dùng bandwidth, pattern bất thường, top talkers.

**Layer 2 — Metrics**: Prometheus scrape `ovs_exporter` cho metric về:
- Interface statistics (rx/tx packet, bytes, errors, drops).
- Flow count, megaflow count per bridge.
- PMD utilization (DPDK).
- OVSDB commit rate, connection count.

Alert rule điển hình:
- `ovs_interface_rx_dropped` rate > 1% → interface driver issue.
- `ovs_datapath_lost` > 0 → upcall rate limit hit.
- `ovs_bridge_flow_count` > 100,000 → approaching limit.

**Layer 3 — Coverage delta**: cron chạy `ovs-appctl coverage/show` mỗi phút, ghi delta vào log aggregator (Loki, Elasticsearch). Detect anomaly như `cache-misses` rate spike, `bridge_flush` tăng đột biến.

---

## 9.14.2 Decision tree: 5 loại sự cố

Compass Ch 20 cô đọng:

### 9.14.2.1 Packet loss

```
1. ovs-dpctl show                    → check counter
2. Check "lost" counter               → > 0 = upcall rate limit hit
3. ovs-appctl upcall/show            → xem flow-install rate
4. Action: tăng upcall/set-flow-limit hoặc n-handler-threads
```

Nếu `lost = 0` nhưng vẫn loss → vấn đề kernel hoặc physical NIC.

### 9.14.2.2 Unexpected drops

```
1. ovs-appctl ofproto/trace br0 "<flow spec>"
2. Đọc output: xác định table nào dropping
3. Fix: sửa rule priority hoặc action
```

Debug bằng `ofproto/trace` không cần reproduce traffic thật — scenario quan trọng nhất.

### 9.14.2.3 Tunnel down

```
1. ovs-appctl tnl/neigh/show          → check ARP resolve underlay
2. Underlay connectivity: ping underlay IP
3. MTU mismatch: ip link show, shrink inner MTU hoặc bump underlay
```

95% "tunnel up nhưng no traffic" là MTU issue (VXLAN +50B, Geneve +50-66B).

### 9.14.2.4 Bond flap

```
1. ovs-appctl bond/show bond0
2. ovs-appctl lacp/show bond0
3. Check physical switch LACP config; enable lacp-fallback-ab
```

### 9.14.2.5 OVSDB unavailable

```
1. systemctl status ovsdb-server
2. Clustered: cluster/status → quorum còn không?
3. Restore: restart failed node hoặc restore quorum
```

Cluster 3-node chịu 1 failure, 5-node chịu 2. Mất quorum → cluster read-only.

---

## 9.14.3 Packet capture strategies

Khi decision tree không đủ:

```bash
[host]# tcpdump -i eth0 -nn -w /tmp/phys.pcap &          # physical
[host]# ovs-vsctl add-port br0 tap-debug -- \
         set Interface tap-debug type=internal
[host]# ovs-vsctl -- --id=@m create Mirror name=dbg \
         select-all=true output-port=@p \
         -- --id=@p get Port tap-debug \
         -- set Bridge br0 mirrors=@m
[host]# tcpdump -i tap-debug -nn -w /tmp/ovs.pcap &      # OVS internal
```

So sánh hai pcap — diff giữa physical wire và OVS internal cho biết packet bị OVS drop hoặc modify.

---

## 9.14.4 Capstone end-to-end incident

**Scenario**: VM trên host-A không ping được VM trên host-B qua VXLAN, cả hai cùng subnet.

**Step 1** — Physical layer:

```bash
[host-A]# ping <host-B-underlay-ip>
[host-A]# tcpdump -i eth0 -nn udp port 4789
```

Underlay fail → escalate network team.

**Step 2** — OVS config:

```bash
[host-A]# ovs-vsctl show | grep -A5 vxlan
[host-A]# ovs-appctl tnl/neigh/show
```

**Step 3** — OpenFlow pipeline:

```bash
[host-A]# ovs-appctl ofproto/trace br-int "in_port=vm1,..."
```

**Step 4** — Kernel datapath:

```bash
[host-A]# ovs-dpctl dump-flows -m | grep vxlan
[host-A]# ovs-dpctl show | grep lookups
```

**Step 5** — Egress wire:

```bash
[host-A]# tcpdump -i eth0 -nn 'udp port 4789' -w egress.pcap
```

**Step 6** — MTU check:

```bash
[host-A]# ip link show eth0
[host-A]# ip link show vm1-veth
```

Inner MTU ≤ underlay MTU − 50. Nếu 1500 = 1500 → large packet fragment/drop.

---

## 9.14.5 Post-incident process

Báo cáo hậu sự:

1. **Timeline**: chronological với timestamp.
2. **Root cause**: layer nào.
3. **Evidence**: coverage delta, log, pcap.
4. **Fix applied**: commit/config change.
5. **Prevention**: monitoring alert mới, playbook update.

Báo cáo hậu sự đưa knowledge base → giảm MTTR cho incident tương tự.

---

## 9.14.6 Ten detailed production incident scenario — signature + evidence + RCA + remediation

Phần 9.14.2 cover 5 category incident phổ biến ở mức framework. §9.14.6-15 đào sâu 10 scenario production-grade cụ thể, mỗi scenario theo template: **Signature** (operator quan sát gì) → **Evidence** (5 lệnh chẩn đoán đầu tiên) → **RCA** (mechanism gốc) → **Remediation** (2-4 tier) → **Prevention** (alert rule + monitoring).

Cross-reference với Phần 20.0 §20.0.5-20.0.7 (systematic 8 scenario + 3 production case study), Phần 9.26 (forensic case study 3 incident class), Phần 20.2 (troubleshooting deep-dive), Phần 20.3 (daily operator playbook).

### Scenario A — OVSDB Raft cluster split-brain after network partition

**Signature:** Ovsdb-server cluster 3-node mất network giữa node-1 (isolated) và node-2+node-3 (majority). Sau heal, node-1 không rejoin — `cluster/status` show node-1 là "candidate" vô tận. `ovn-nbctl` từ node-1 timeout; từ node-2/3 OK.

**Evidence (5 command):**

```bash
# Trên mỗi node
[node-1]# sudo ovs-appctl -t /var/run/ovn/ovnnb_db.ctl cluster/status OVN_Northbound
# Expected healthy: "Status: cluster member, Role: follower/leader"
# Actual partition: "Status: disconnected, Role: candidate"

[node-2]# sudo ovs-appctl -t /var/run/ovn/ovnnb_db.ctl cluster/status OVN_Northbound
# Kỳ vọng: "Status: cluster member, Role: leader"

# Sequence index comparison
[node-1]# sudo ovs-appctl -t /var/run/ovn/ovnnb_db.ctl cluster/status OVN_Northbound | grep -E "Last Index|Commit Index"
# Kỳ vọng: Last Index == Commit Index (synced)

# Log inspection
[node-1]# sudo journalctl -u ovn-northd --since "30 min ago" | grep -iE "disconnect|partition|raft"
```

**Nguyên nhân gốc rễ (RCA):** Partition window > `election-timer` (default 1000ms × 5 = 5s). Node-1 trong minority không thể acquire leadership. Sau heal, node-1 có `Last Index` thấp hơn majority → cần log catchup qua `InstallSnapshot` RPC. Nếu snapshot quá cũ hoặc log compaction đã xảy ra, node-1 cannot catchup incremental → stuck in "candidate" state re-election loop.

**Remediation 3-tier:**

```bash
# Tier 1 (force rejoin with snapshot, 5 phút)
[node-1]# sudo systemctl stop ovn-nb-db
[node-1]# sudo cp /var/lib/ovn/ovnnb_db.db /var/lib/ovn/ovnnb_db.db.stale
[node-1]# sudo rm /var/lib/ovn/ovnnb_db.db
[node-1]# sudo ovsdb-tool join-cluster /var/lib/ovn/ovnnb_db.db OVN_Northbound \
  tcp:<node-1-ip>:6643 tcp:<node-2-ip>:6643 tcp:<node-3-ip>:6643
[node-1]# sudo systemctl start ovn-nb-db

# Tier 2 (tune election-timer, prevent future split)
[any]# sudo ovs-appctl -t /var/run/ovn/ovnnb_db.ctl cluster/change-election-timer OVN_Northbound 4000
# Tăng từ 1000ms → 4000ms cho tolerance partition 15-20s

# Tier 3 (infra improvement)
# - Dedicated cluster network (không chung với data plane)
# - Network monitoring alert cho inter-node latency > 500ms
```

**Prevention:**

```yaml
- alert: OVSDBClusterMemberDisconnected
  expr: ovsdb_server_cluster_role{role="candidate"} > 0
  for: 2m
  labels: { severity: critical }
```

### Scenario B — ovs-vswitchd OOM kill

**Signature:** Syslog log "Out of memory: Killed process <PID> (ovs-vswitchd)". systemd restart ovs-vswitchd → toàn bộ OpenFlow flow bị uninstall → 30-60 giây traffic loss. Sau restart, memory tăng nhanh trở lại baseline cao.

**Evidence:**

```bash
# Memory footprint trước OOM
[host]# sudo ovs-appctl -t ovs-vswitchd memory/show
handlers:3 revalidators:4 ofconns:5 ports:247
rules:48293 rules-cache-used:156MB rules-cache-max:256MB
...

# Kernel OOM log
[host]# sudo journalctl -k --since "1 hour ago" | grep -iE "oom|killed process"

# Process memory over time
[host]# sudo pmap -x $(pgrep ovs-vswitchd) | tail -1

# Coverage counter lflow + megaflow cache
[host]# sudo ovs-appctl coverage/show | grep -iE "flow_add|rule_inserted"

# DPDK hugepage usage (nếu dùng userspace DP)
[host]# grep Huge /proc/meminfo
```

**Nguyên nhân gốc rễ (RCA):** Thường do 1 trong 3 nguyên nhân:

1. **Flow explosion:** OpenFlow rule count + megaflow cache vượt memory available. Check `rules-cache-used`.
2. **Memory leak real:** ovs-vswitchd bug (hiếm). Check memory growth rate với `pmap` over time; nếu linear tăng không tương quan workload → leak.
3. **Flow + cache settings too aggressive:** `other_config:flow-limit` quá cao so với RAM allocation.

**Remediation:**

```bash
# Tier 1 (immediate cap)
[host]# sudo ovs-vsctl set Open_vSwitch . other_config:flow-limit=50000
# Tier 2 (revalidator tuning)
[host]# sudo ovs-vsctl set Open_vSwitch . other_config:min-revalidate-pps=10
# Tier 3 (systemd resource limit hard floor)
[host]# sudo systemctl edit openvswitch-switch
# [Service]
# MemoryLimit=4G
```

### Scenario C — Handler thread saturation → upcall drop

**Signature:** `ovs-appctl upcall/show` show `lost_upcalls` counter tăng nhanh. Datapath miss traffic bị drop (không cài đặt flow, không forward). VM intermittent connectivity.

**Evidence — Anatomy Template A cho `upcall/show`:**

**Bối cảnh khi chạy:** Kỹ sư trực ca nhận alert "VM ICMP retry rate > 20%" từ Prometheus. Hypothesis: handler thread không theo kịp upcall rate.

**Command + output** *(reproduced from OVS 2.17.9 production-like)*:

```bash
[host]# sudo ovs-appctl upcall/show

system@ovs-system:
  flows         : (current 48293) (avg 47120) (max 51820) (limit 200000)
  offloaded flows : 0
  dump duration : 185ms
  ufid enabled : true

  23: (keys 12073) (total packets 1283749) (avg 55774/s)
      (dispatched to handler)
  24: (keys 12089) (total packets 1290485) (avg 56066/s)
  25: (keys 12094) (total packets 1297843) (avg 56386/s)
  26: (keys 12037) (total packets 1289201) (avg 56009/s)

  lost_upcalls: 12837 (rate 142/s)     ← !!! RED FLAG
```

**Anatomy từng field:**

| Field | Giá trị mẫu | Ý nghĩa | Dấu hiệu bất thường |
|-------|-------------|---------|----------------------|
| `flows current` | 48293 | Current megaflow count | Gần `limit` 200K = cache đầy |
| `offloaded flows` | 0 | HW offload count | 0 = software path only |
| `dump duration` | 185ms | Revalidator sweep time | > 500ms = stress, > 1000ms = critical |
| `handler N: keys` | 12073 | Ukey per handler thread | Even distribution = healthy load balance |
| `avg packets/s` | 55774/s | Upcall rate per handler | Sum × N_handler phải < handler capacity |
| `lost_upcalls rate` | 142/s | **Upcall dropped, not handled** | **> 0 = handler bottleneck, MUST investigate** |

**Kịch bản bẻ gãy:**

- **`lost_upcalls > 0` với `dump duration < 100ms`** → handler không theo kịp (CPU bound), không phải revalidator. Fix: tăng `n-handler-threads`.
- **`dump duration > 500ms` + `lost_upcalls > 0`** → cả revalidator + handler saturated. Flow explosion nghi ngờ, check Scenario D.
- **`keys` skewed giữa handler** (ví dụ handler 23: 50K, handler 24-26: 1K) → hash function không evenly distribute. Hiếm, OVS bug. Report upstream.

**RCA + Remediation:**

```bash
# Check current thread count
[host]# sudo ovs-vsctl get Open_vSwitch . other_config | grep -E "n-handler|n-revalidator"

# Tăng handler thread (requires restart)
[host]# sudo ovs-vsctl set Open_vSwitch . other_config:n-handler-threads=8
[host]# sudo systemctl restart ovs-vswitchd
# Re-verify: lost_upcalls rate về 0 sau restart + 2-3 min warmup
```

### Scenario D — Megaflow cache thrashing

**Signature:** Flow cài đặt rate rất cao (`flow_add` coverage +10K/s), cache hit rate thấp (<80%). CPU ovs-vswitchd 100% on 1 core (handler thread). Traffic latency spike P99 3-5x.

**Evidence:**

```bash
# Coverage delta trong 10 giây
[host]# sudo ovs-appctl coverage/show | grep -E "flow_add|miss|wildcard"
# Delta check:
[host]# sudo ovs-appctl coverage/show > /tmp/cov1.txt; sleep 10
[host]# sudo ovs-appctl coverage/show > /tmp/cov2.txt
[host]# diff /tmp/cov1.txt /tmp/cov2.txt

# Wildcard pattern analysis
[host]# sudo ovs-dpctl dump-flows | awk '{print $1}' | sort -u | wc -l
# Many unique wildcarding = low cache effectiveness
```

**Nguyên nhân gốc rễ (RCA):** Megaflow cache key quá narrow — mỗi 5-tuple packet tạo 1 entry riêng thay vì share wildcard mask. Common causes: ACL rule với TCP flags match (force per-packet inspection), ct_state transition fragmenting cache.

**Remediation:**

```bash
# Identify narrow flow
[host]# sudo ovs-dpctl dump-flows | awk '{print $1}' | sort | uniq -c | sort -rn | head

# Refactor OpenFlow rule để bỏ unnecessary match field
# (Trong OVN, qua ACL/Port_Group — xem Part 20.3 §20.3.4.6 consolidation)
```

### Scenario E — Tunnel BFD flap cascade

**Signature:** Geneve/VXLAN tunnel giữa 2 chassis intermittent UP→DOWN→UP. Traffic loss 200-500ms mỗi cycle flap. BFD monitoring log spam.

**Evidence:**

```bash
[host-A]# sudo ovs-appctl bfd/show
BFD session for tunnel ovn-chassisB:
  local state: UP
  remote state: UP
  ...
  flaps: 47               ← !!! flap count cao

[host-A]# sudo ovs-appctl tnl/neigh/show
# ARP resolution cho remote underlay phải stable. Flap đồng bộ = underlay issue.

# Underlay latency baseline
[host-A]# ping -i 0.2 -c 100 <host-B-underlay> | tail -5

# Geneve UDP packet trace underlay
[host-A]# sudo tcpdump -i eth0 -nn udp port 6081 -c 100 -w /tmp/geneve.pcap
```

**Nguyên nhân gốc rễ (RCA):** 3 common cause:

1. **Underlay jitter > BFD phát hiện threshold** (default 500ms × 3 = 1.5s). Burst độ trễ underlay > 1.5s kích hoạt BFD down.
2. **Underlay MTU fragmentation** — BFD packet > MTU bị drop silently.
3. **Host CPU overload** delay BFD packet send/receive.

**Remediation:**

```bash
# Tune BFD interval để tolerant underlay jitter
[host]# sudo ovs-vsctl set Interface ovn-chassisB-0 options:bfd_interval=1000 \
  options:bfd_min_rx=1000 options:bfd_min_tx=1000
# 1s × 3 = 3s detect (tolerant cho jitter < 3s)

# Alternate: disable BFD dùng ICMP alternative (advanced)
[host]# sudo ovs-vsctl set Interface ovn-chassisB-0 bfd=no
```

### Scenario F — HW offload silent fallback

**Signature:** Hardware offload (TC flower) configured, baseline 100 Gbps line-rate. Sau deploy rule ACL mới, băng thông thực tế drop xuống 10 Gbps (kernel slow path). Không có error log. `ovs-dpctl dump-flows` show flow với `offload_state` field empty hoặc `no_support`.

**Evidence:**

```bash
# Offload stats
[host]# sudo ovs-dpctl dump-flows -m | grep -E "offloaded|state=offloaded"

# Tất cả flow trong tc
[host]# sudo tc -s filter show dev eth0 ingress | head

# Counter coverage offload-specific
[host]# sudo ovs-appctl coverage/show | grep -iE "offload|tc_"

# Driver dmesg
[host]# sudo dmesg | grep -iE "mlx5|offload|tc" | tail -20
```

**Nguyên nhân gốc rễ (RCA):** TC flower driver (mlx5_core / i40e / etc.) không hỗ trợ combination action mới. Common pattern:

1. `conntrack` action trên NIC chưa hỗ trợ (mlx5 cần ConnectX-6+).
2. `mod_nw_ttl` + `mod_eth_dst` combo hit hardware limit.
3. Metadata > 16 bit width không fit HW register.

OVS silent fallback về software slow path — kernel datapath nhưng không HW accelerate.

**Remediation:**

```bash
# Tier 1: Disable offload cho rule specific
[host]# sudo ovs-vsctl set Open_vSwitch . other_config:hw-offload=true \
  other_config:offload-cap-list="drop,output,set,forward"
# Exclude ct/conntrack từ offload → kernel handle

# Tier 2: Upgrade NIC firmware
[host]# sudo ethtool -i eth0 | grep -i firmware
[host]# sudo mstconfig -d /dev/mst/mt4125_pciconf0 query | grep -i version

# Tier 3: Downgrade ACL complexity (Port_Group thay individual match)
```

### Scenario G — NIC ring buffer overflow

**Signature:** ethtool show `rx_no_buffer_count` tăng nhanh. Packet drop ở NIC level trước khi vào OVS. VM TCP retransmit rate spike.

**Evidence:**

```bash
# NIC statistics
[host]# sudo ethtool -S eth0 | grep -iE "rx_no_buffer|rx_missed|rx_discard"

# Ring buffer size
[host]# sudo ethtool -g eth0
Ring parameters for eth0:
Pre-set maximums:
RX:             4096
TX:             4096
Current hardware settings:
RX:             512           ← !!! Default, too small for 100Gbps
TX:             512

# IRQ distribution
[host]# cat /proc/interrupts | grep -E "eth0|mlx5" | head
```

**Nguyên nhân gốc rễ (RCA):** Default RX ring 256-512 descriptor không đủ cho burst traffic > 1M pps. Khi CPU quá bận không drain ring kịp, NIC drop packet tại HW level.

**Remediation:**

```bash
# Tăng ring buffer lên max
[host]# sudo ethtool -G eth0 rx 4096 tx 4096

# IRQ affinity — distribute 8 core
[host]# sudo systemctl restart irqbalance
# Or manual: sudo set_irq_affinity.sh eth0

# Persistent qua reboot
[host]# sudo cat > /etc/network/if-up.d/ring-buffer <<'EOF'
#!/bin/bash
[ "$IFACE" = "eth0" ] && ethtool -G eth0 rx 4096 tx 4096
EOF
[host]# sudo chmod +x /etc/network/if-up.d/ring-buffer
```

### Scenario H — Conntrack table full

**Signature:** `dmesg` log "nf_conntrack: table full, dropping packet". TCP SYN drop random. OVS stateful ACL fail (`allow-related` reply không pass).

**Evidence:**

```bash
# Current vs max
[host]# cat /proc/sys/net/netfilter/nf_conntrack_count
512047

[host]# cat /proc/sys/net/netfilter/nf_conntrack_max
524288                        ← sát giới hạn

# Top entries breakdown
[host]# sudo conntrack -L | awk '{print $1,$2}' | sort | uniq -c | sort -rn | head
# Detect pattern (TCP ESTABLISHED vs UNREPLIED)
```

**Nguyên nhân gốc rễ (RCA):** Default `nf_conntrack_max = hashsize × 4` tính từ RAM. Hypervisor 1000 VM × 500 connection/VM = 500K entry dễ đạt. Zone collision (Phần 9.26 §9.26.12) amplify problem.

**Remediation:**

```bash
# Tier 1: Tăng max + rebuild hashtable
[host]# sudo sysctl -w net.netfilter.nf_conntrack_max=2097152
[host]# sudo sysctl -w net.netfilter.nf_conntrack_buckets=524288
[host]# echo 524288 | sudo tee /sys/module/nf_conntrack/parameters/hashsize

# Tier 2: Giảm TCP established timeout
[host]# sudo sysctl -w net.netfilter.nf_conntrack_tcp_timeout_established=3600
# 5 days → 1 hour, nhanh GC stale

# Tier 3: Per-zone limit (OVS 2.17+)
[host]# sudo ovs-appctl dpctl/ct-set-limits default=100000 zone=1012:50000
```

### Scenario I — OpenFlow controller disconnect fail-mode

**Signature:** Bridge `br0` config `fail-mode=secure`. Controller (Faucet/ONOS) crash. 2 phút sau, traffic hoàn toàn stop — tất cả packet drop ở br0.

**Evidence:**

```bash
[host]# sudo ovs-vsctl get Bridge br0 fail_mode
secure

# Controller connection
[host]# sudo ovs-vsctl get Bridge br0 controller
[tcp:10.0.0.100:6653]

# Disconnected state
[host]# sudo ovs-appctl -t ovs-vswitchd ofproto/list-tunnels
[host]# sudo ovs-vsctl list Controller
...
is_connected: false               ← controller crashed
...
```

**Nguyên nhân gốc rễ (RCA):** `fail-mode=secure` nghĩa là khi controller disconnect, bridge chỉ sử dụng flow đã cài, **không học flow mới**. Traffic pattern mới → miss → drop. `fail-mode=standalone` (default) = fall back hub/switch, less secure nhưng traffic continue.

**Remediation:**

```bash
# Immediate fail-over
[host]# sudo ovs-vsctl set-fail-mode br0 standalone
# Traffic resume trong vài giây (bridge learn MAC qua `NORMAL` action)

# Restore controller primary
# ... restart controller service ...

# Long-term: controller HA cluster 3+ node
```

**Trade-off:** `secure` an toàn khi có controller cluster HA; `standalone` phù hợp edge/branch với controller single + tolerant tới hub-mode.

### Scenario J — Bond slave link flap microburst

**Signature:** `bond0` slave `eth1` flap 50 lần/giờ với interval 2-3 giây. Không có slave DOWN steady state, chỉ micro-flap. Nhưng traffic độ trễ spike P99 100ms mỗi flap cycle (megaflow invalidation — xem Phần 9.26 §9.26.11).

**Evidence:**

```bash
# Bond flap history
[host]# sudo ovs-appctl bond/show bond0 | grep -iE "slave|flap"

# Switch-side correlation
[host]# sudo ethtool -S eth1 | grep -iE "link_up|link_down|carrier_changes"

# Dmesg link event
[host]# sudo dmesg -T | grep -iE "eth1|link" | tail -30

# Switch vendor correlation (ví dụ Cisco)
# switch# show interfaces eth1/1 counters errors | include CRC|Frame
```

**Nguyên nhân gốc rễ (RCA):** Ethernet frame CRC error rate > 1% trên cable/SFP → link layer declare DOWN → auto-negotiate UP → flap cycle 2-3 giây. Thường do cable xuống cấp, SFP temperature, hoặc switch port issue.

**Remediation:**

```bash
# Tier 1: Hysteresis để hide micro-flap
[host]# sudo ovs-vsctl set Port bond0 other_config:bond-detect-mode=carrier \
  other_config:bond-miimon-interval=1000 \
  other_config:bond-slave-updelay=5000 \
  other_config:bond-slave-downdelay=2000
# downdelay 2s = chịu flap < 2s không declare DOWN
# updelay 5s = recovered slave phải stable 5s mới rejoin

# Tier 2: Physical replace
# - Cable CAT6a replace
# - SFP transceiver replace
# - Switch port swap

# Tier 3: Monitoring alert
```

```yaml
- alert: NICFlapStorm
  expr: rate(node_network_carrier_changes_total[5m]) > 0.01
  for: 2m
  labels: { severity: warning }
```

### Scenario K — BFD thundering herd sau underlay ToR reboot

**Signature:** ToR switch reboot theo kế hoạch ~90 giây. Khi uplink lên lại, toàn bộ N chassis (N=60 trong dedicated rack) đồng loạt phát hiện Geneve tunnel UP ở gần cùng một tick. Mỗi chassis khởi tạo lại BFD session tới N-1 peer cùng lúc, tạo burst gói BFD control. Hệ quả: ovn-controller trên nhiều chassis báo "BFD state change" hàng nghìn lần trong 5-10 giây, ovs-vswitchd spike CPU 80-95% single core, handler thread chớm saturation (liên hệ Scenario C). VM độ trễ P99 tăng 40-120ms trong window thundering herd, nhưng không mất traffic hẳn vì datapath flow cũ vẫn còn hiệu lực. Phân biệt với Scenario E: Scenario E là flap intermittent kéo dài do underlay jitter, Scenario K là burst một lần trong convergence window rồi tự ổn định.

**Evidence (5 command):**

```bash
# Đếm số chassis lên BFD trong window 10 giây
[any-chassis]# sudo ovn-sbctl --timestamp list Chassis_Private \
  | grep -E "^_uuid|^name|last_seq" | head -40
# Last_seq update timestamp tập trung trong 5-10s = thundering herd

# BFD session turnover rate per chassis
[chassis-X]# sudo ovs-appctl bfd/show \
  | awk '/flaps:/ {sum+=$2; cnt++} END {print "total flaps:", sum, "sessions:", cnt}'
# > 50 flap/60 session trong 5 phút = burst

# ovn-controller recompute burst
[chassis-X]# sudo ovn-appctl -t ovn-controller inc-engine/recompute-stats \
  | grep -E "recompute|runtime"
# recompute count burst trong convergence window

# Coverage handler thread wake
[chassis-X]# sudo ovs-appctl coverage/show | grep -iE "handler|upcall"
# handler_wakeups rate tạm spike cao

# Tunnel tap counter (BFD packet đi qua interface eth-uplink)
[chassis-X]# sudo ethtool -S eth-uplink | grep -iE "rx_packets|tx_packets"
# Delta trong burst window = BFD control plane load thực tế
```

**Nguyên nhân gốc rễ (RCA):** OVN < 24.03 không có staggered BFD startup. Khi tunnel port lên đồng loạt, `bfd_configure()` trong `ofproto-dpif.c` khởi tạo session với initial interval ngẫu nhiên trong biên độ hẹp, không đủ lệch pha giữa N chassis. Tất cả chassis gửi BFD hello trong burst window < 500ms. Receive side (peer chassis) phải parse + state-machine N peer gần như đồng thời. Đối với cluster 60+ chassis, control plane load scale O(N²). OVN 24.03+ bổ sung jitter lớn hơn và rate-limit hello burst ở init, giảm thundering herd đáng kể. Nguyên nhân cấp hai ít gặp hơn: BFD min-rx/min-tx cấu hình quá thấp (100ms) trong cluster lớn nhân bội burst rate.

**Remediation 3-tier:**

```bash
# Tier 1 (immediate, giảm áp lực tại chassis đang spike)
# Tạm tăng BFD interval để giảm hello rate trong window
[chassis-X]# for p in $(sudo ovs-vsctl --columns=name find Interface type=geneve | awk -F: '{print $2}'); do
  sudo ovs-vsctl set Interface $p options:bfd_min_rx=1000 options:bfd_min_tx=1000
done
# Sau convergence 2-3 phút ổn định, hạ về 500ms nếu muốn detect nhanh

# Tier 2 (operational, stagger chassis startup)
# Khi ToR reboot kế hoạch, rolling restart ovn-controller theo batch 10 chassis,
# cách nhau 30 giây. Dừng toàn bộ thundering herd về dạng 6 burst nhỏ.
[deployer]$ for batch in $(cat chassis-batches.txt); do
  ansible -i inventory -l $batch -m systemd -a "name=ovn-controller state=restarted"
  sleep 30
done

# Tier 3 (structural, upgrade + rate-limit)
# Upgrade OVN 24.03 LTS+ với staggered BFD startup built-in
# Đồng thời set rate-limit BFD control packet trên uplink QoS (Part 9.9)
[chassis-X]# sudo ovs-vsctl set Interface eth-uplink ingress_policing_rate=100000 \
  ingress_policing_burst=10000
# Bảo vệ kernel khỏi BFD flood kể cả khi peer bị compromise
```

**Prevention:**

```yaml
- alert: BFDThunderingHerd
  expr: rate(ovs_bfd_state_changes_total[1m]) > 10
  for: 3m
  labels: { severity: warning }
  annotations:
    summary: "BFD state churn > 10/min, kiểm tra underlay convergence"

- alert: OvnControllerBurstRecompute
  expr: rate(ovn_controller_engine_recompute_total[5m]) > 5
  for: 2m
```

### Scenario L — ovn-northd compile stuck trên bulk NBDB update

**Signature:** Tooling automation tạo 500 Logical_Switch_Port trong một transaction duy nhất (ví dụ tenant mới onboarding). Sau commit NBDB, `nb_cfg` tại NBDB tăng lên N, nhưng `nb_cfg` tại SBDB (đọc qua `ovn-sbctl get SB_Global . nb_cfg`) đứng yên 30-180 giây. Trong window đó, ovn-northd CPU 100% single-thread. VM mới tạo không có network, VM hiện hữu không ảnh hưởng vì datapath flow đã installed. Recovery sau khi northd kết thúc compile xong batch lớn.

**Evidence, Anatomy Template A cho `ovn-appctl -t ovn-northd stopwatch/show`:**

**Bối cảnh khi chạy:** Kỹ sư trực ca nhận alert "nb_cfg lag > 30s" từ Prometheus. Hypothesis: northd đang compile bulk update. Cần xác định giai đoạn nào trong pipeline đang block.

**Command + output** *(reproduced from OVN 22.03.8 production-like trên 60-chassis cluster)*:

```bash
[nbdb-leader]# sudo ovn-appctl -t ovn-northd stopwatch/show

Statistics for 'ovnnb_db_run'
  Total samples: 1247
  Maximum: 48320 ms          ← !!! 48 giây 1 iteration
  Minimum: 12 ms
  95th percentile: 32150 ms  ← hầu hết burst trên 32s
  Short term average: 28400 ms
  Long term average: 180 ms

Statistics for 'build_lflows'
  Total samples: 1247
  Maximum: 42180 ms          ← chiếm 87% ovnnb_db_run
  95th percentile: 28000 ms

Statistics for 'ovnsb_db_run'
  Maximum: 5100 ms           ← SB commit tương đối nhanh

Statistics for 'clear_tracked_data'
  Maximum: 92 ms
```

**Anatomy từng field:**

| Field | Giá trị mẫu | Ý nghĩa | Dấu hiệu bất thường |
|-------|-------------|---------|----------------------|
| `ovnnb_db_run max` | 48320ms | Thời gian 1 iteration hoàn chỉnh | > 10s = stuck, > 30s = critical |
| `build_lflows max` | 42180ms | Generate Logical_Flow từ NB | 87% ovnnb_db_run = pipeline dominant |
| `ovnsb_db_run max` | 5100ms | Commit SB transaction | Thường < 1s, > 5s = SBDB contention |
| `clear_tracked_data max` | 92ms | Reset I-P state nodes | > 500ms = I-P engine overflow |
| Ratio 95p/long-term | 180x | Burst vs steady state | > 100x = bulk update nghi ngờ |

**Kịch bản bẻ gãy:**

- **`build_lflows` chiếm > 80%** và `ovnsb_db_run` ngắn → compile stage bottleneck. Nguyên nhân thường là ACL × Port_Group cross-product (Phần 9.24 §9.24.5 conjunction analysis) nhân bội theo số LSP mới.
- **`ovnsb_db_run` chiếm > 50%** → SBDB commit bottleneck. Check Raft leader CPU, disk I/O, hoặc cluster election trong flight.
- **`clear_tracked_data` > 1s** → I-P engine invalidation diện rộng. Triệu chứng tương tự Scenario N, có thể cùng root cause.

**Nguyên nhân gốc rễ (RCA):** Hai cơ chế phổ biến:

1. **ACL cross-product explosion:** 500 LSP mới × 50 ACL rule/tenant = 25K Logical_Flow phát sinh. Nếu ACL dùng `inport == @port_group` + `ip4.src == {10.0.0.0/16}` → conjunction khớp với tree rebuild. OVN < 23.03 rebuild toàn bộ tree thay vì incremental merge.
2. **Incremental Processing (I-P) fallback:** Transaction quá lớn vượt ngưỡng I-P node invalidation. Engine rơi về full recompute thay vì partial update. Kiểm tra qua `ovn-appctl -t ovn-northd inc-engine/show` xem có node nào trong `status: recompute` thay vì `status: updated`.

**Remediation 3-tier:**

```bash
# Tier 1 (tooling fix, break bulk transaction thành batch)
# Thay vì 1 txn 500 LSP, chia 10 txn × 50 LSP mỗi batch, cách nhau 2 giây
[tooling]$ for batch in $(split -l 50 lsp.txt); do
  ovn-nbctl $(cat $batch | while read name; do echo "-- lsp-add ls1 $name"; done)
  sleep 2
done
# Mỗi batch 50 LSP compile trong ~3 giây, không dồn queue

# Tier 2 (northd parallelization, OVN 23.03+)
[any]# sudo ovn-nbctl set NB_Global . options:northd_probe_interval=60000
[any]# sudo ovn-appctl -t ovn-northd parallel-build/enable
# Khai thác multi-core compile, speedup 2-4x tuỳ hardware

# Tier 3 (structural, Port_Group consolidation)
# Refactor 50 ACL rule/tenant × 500 LSP thành Port_Group pattern
# Giảm cross-product từ 25K flow xuống 500 flow (Part 9.23 + Part 20.3 §20.3.4.6)
```

**Prevention:**

```yaml
- alert: OvnNorthdNbCfgLag
  expr: ovn_nb_cfg - ovn_sb_cfg > 5
  for: 60s
  labels: { severity: warning }

- alert: OvnNorthdBuildLflowStuck
  expr: ovn_northd_build_lflows_duration_max_seconds > 10
  for: 2m
```

### Scenario M — Chassis ghost claim sau crash

**Signature:** Chassis A (hypervisor) bị hard power off do lỗi phần cứng. VM trên A mất. Khi người vận hành migrate VM tới chassis B, VM tạo LSP mới thành công nhưng không có network. `ovn-sbctl show` hiển thị một số Port_Binding vẫn bind tới chassis A (đã offline). Log ovn-controller trên B báo "Claim request rejected, port already claimed by chassis-A". BFD session tới chassis-A vẫn ở state DOWN từ peer nhưng row `Chassis` trong SBDB chưa bị xoá.

**Evidence (5 command):**

```bash
# Kiểm tra Port_Binding stale
[nbdb-leader]# sudo ovn-sbctl --columns=logical_port,chassis,up list Port_Binding \
  | grep "chassis=.*chassis-a"
# Nhiều row bind chassis-A dù chassis offline

# Chassis_Private last_seq, freshness indicator
[nbdb-leader]# sudo ovn-sbctl --columns=name,nb_cfg,nb_cfg_timestamp list Chassis_Private \
  | grep chassis-a
# nb_cfg_timestamp đứng yên từ lúc crash = dấu hiệu ghost

# BFD status từ chassis peer
[chassis-B]# sudo ovs-appctl bfd/show | grep -A4 "ovn-chassisa"
# Kỳ vọng: remote state: DOWN + "Diagnostic: Neighbor signaled session down"

# ovn-controller log trên chassis còn sống
[chassis-B]# sudo journalctl -u ovn-controller --since "30 min ago" \
  | grep -iE "claim|reject|port_binding"
# Should see: "Failed to claim port X, already claimed by chassis-A"

# Xác nhận chassis A chắc chắn offline
[operator]$ ping -c 3 <chassis-a-ip>
# Packet loss 100% + BGP/spine monitoring confirm
```

**Nguyên nhân gốc rễ (RCA):** OVN dùng `Chassis.name` + `Port_Binding.chassis` làm claim ownership. Khi chassis crash mà không graceful shutdown, ovn-controller trên A không chạy `release_all_ports()` để trả lại Port_Binding về unbound. Các chassis còn lại phát hiện BFD DOWN nhưng không tự động reclaim port vì policy mặc định bảo thủ, tránh race condition khi chassis A chỉ isolated tạm thời rồi quay lại (tình huống split-brain). Hệ thống chờ người vận hành can thiệp bằng cách xoá Chassis row, hoặc kích hoạt stale-binding-timeout (OVN 22.06+). Nếu chassis_private `last_seq` không update > threshold, mặc định 300s (có thể chỉnh qua `options:stale-binding-timeout-ms` trong `SB_Global`), northd tự động clear binding.

**Remediation 3-tier:**

```bash
# Tier 1 (immediate, manual chassis teardown)
[any-sb-client]# sudo ovn-sbctl chassis-del chassis-a
# Xoá Chassis row + Chassis_Private row + reset Port_Binding.chassis về NULL
# Sau ~2 giây, northd re-compile, Port_Binding available cho chassis B claim

[chassis-B]# sudo systemctl restart ovn-controller
# Kick chassis B re-trigger claim protocol cho port mới tạo

# Tier 2 (chờ tự động, cần OVN 22.06+)
[any]# sudo ovn-nbctl set NB_Global . options:stale-binding-timeout-ms=180000
# Timeout 3 phút, northd tự GC chassis ghost

# Tier 3 (HA-aware, HA_Chassis_Group fail-over automatic)
# Cho Logical_Router_Port external, cấu hình HA_Chassis_Group (Part 13.6)
# Primary chassis crash, secondary tự take over gateway MAC + ARP responder
# Không phụ thuộc manual chassis-del
```

**Prevention:**

```yaml
- alert: OvnChassisStale
  expr: time() - ovn_chassis_private_last_seen_timestamp > 180
  for: 60s
  labels: { severity: critical }
  annotations:
    summary: "Chassis {{ $labels.chassis }} stale > 3 phút, nghi ngờ ghost binding"
```

### Scenario N — ovn-controller recompute loop vô tận

**Signature:** Trên một chassis, ovn-controller CPU 100% kéo dài không giảm về idle. `lflow-cache/show` báo hit rate 0% hoặc rất thấp. Journal log spam "trigger recompute" nhiều lần mỗi giây. Các chassis khác trong cluster CPU bình thường. VM trên chassis bị ảnh hưởng: traffic vẫn forward nhờ datapath flow cũ, nhưng mọi thay đổi NBDB chỉ áp dụng chậm hoặc mất hoàn toàn.

**Evidence (5 command):**

```bash
# Inc-engine recompute stats
[chassis-X]# sudo ovn-appctl -t ovn-controller inc-engine/recompute-stats
# Expected healthy: recompute count tăng chậm, chủ yếu là node "updated"
# Bệnh lý: recompute count tăng > 10/giây, reason tập trung một loại event

# Lflow-cache hiệu quả
[chassis-X]# sudo ovn-appctl -t ovn-controller lflow-cache/show
Enabled: true
high-watermark:   180MB
Cache entries:    0         ← !!! cache rỗng
Cache trim count: 12487     ← trim liên tục
Mean hit rate:    0.8%      ← !!! gần 0

# Ovn-controller engine node status
[chassis-X]# sudo ovn-appctl -t ovn-controller inc-engine/show
# node_name           status
# OVS_interface       updated
# OVS_Port            updated
# Logical_Flow        recompute  ← !!! stuck ở recompute
# lb_data             updated

# Journal pattern detect
[chassis-X]# sudo journalctl -u ovn-controller --since "10 min ago" \
  | grep -c "trigger recompute"
# > 600 in 10 min = > 1/s = pathological

# Correlate với NBDB event
[nbdb-leader]# sudo ovn-sbctl get-connection
[nbdb-leader]# sudo ovsdb-client monitor OVN_Southbound 2>&1 \
  | grep -c '"op":"update"' &
# Pattern update cụ thể nào fire constantly
```

**Nguyên nhân gốc rễ (RCA):** I-P engine của ovn-controller có nhiều node (OVS_interface, Port_Binding, Logical_Flow, MAC_Binding, ...). Mỗi node biết cách update partial khi input thay đổi. Tuy nhiên một số edge case làm node rơi vào `status: recompute`, nghĩa là engine phải rebuild toàn bộ state node đó từ đầu. Nếu input tiếp tục thay đổi trước khi recompute xong, engine chạy vòng lặp không có điểm dừng. Các pattern nổi tiếng gây loop:

1. **External_ids churn:** Một tool tự động ghi liên tục `external_ids` vào Logical_Switch_Port hoặc Nút mạng (Chassis). Mỗi ghi invalidate I-P node cache.
2. **BFD session flap interplay:** Scenario E + Scenario K có thể cascade sang recompute loop khi BFD state thay đổi nhiều lần trong giây.
3. **OVN version-specific bug:** Một số commit giữa major version có bug khiến một node không bao giờ chuyển từ `recompute` sang `updated`. Ví dụ LSP với port_security list rất lớn (>200 entry) trong OVN 22.06.x có bug tương tự, fix ở 22.09+.

**Remediation 3-tier:**

```bash
# Tier 1 (khẩn, cắt vòng lặp tạm thời)
[chassis-X]# sudo ovn-appctl -t ovn-controller recompute
# Force full recompute một lần, đôi khi break loop nếu nguyên nhân là transient

[chassis-X]# sudo ovn-appctl -t ovn-controller lflow-cache/flush
# Flush cache, đưa về trạng thái clean. Nếu loop tiếp tục = root cause persistent

# Tier 2 (identify trigger)
# Dùng ovsdb-client monitor + timestamp correlation để xác định row NBDB
# đang gây invalidation liên tục. Thường là external_ids từ tool automation.
[nbdb-leader]# sudo ovsdb-client monitor OVN_Northbound Logical_Switch_Port external_ids \
  --timestamp 2>&1 | head -100
# Nếu thấy 1 LSP update external_ids mỗi giây → found culprit

# Tier 3 (structural, version upgrade + deduplicate tool automation)
# Upgrade tới OVN LTS có fix known I-P loop bug
# Audit tool automation: external_ids update phải idempotent, check-before-write
[tooling]$ # Pseudo-code: only update if value actually changed
if [ "$(ovn-nbctl get LSP $p external_ids:xyz)" != "$new_val" ]; then
  ovn-nbctl set LSP $p external_ids:xyz=$new_val
fi
```

**Prevention:**

```yaml
- alert: OvnControllerRecomputeLoop
  expr: rate(ovn_controller_engine_recompute_total[1m]) > 5
  for: 3m
  labels: { severity: critical }

- alert: OvnLflowCacheHitRateLow
  expr: ovn_controller_lflow_cache_hit_rate < 0.5
  for: 5m
  labels: { severity: warning }
```

### Scenario O, Certificate expiry → control plane outage cluster-wide

**Signature:** Tại T = cert expiry, tất cả chassis đồng loạt mất kết nối SSL tới SBDB. ovn-controller log báo "SSL_ERROR_ZERO_RETURN" hoặc "certificate has expired". Flow từ NBDB thay đổi không áp dụng được trên bất kỳ chassis nào. Lưu thông hiện hữu vẫn hoạt động vì datapath flow đã cài từ trước (fail-safe by design). VM mới tạo không có network. Window sự cố kéo dài từ T đến khi cert được renew + deploy + ovn-controller reconnect.

**Evidence (5 command):**

```bash
# Openssl check expiry
[chassis-X]# openssl x509 -in /etc/ovn/cert_host -noout -enddate
# notAfter=Apr 24 12:00:00 2026 GMT  ← đã qua hôm nay

# ovn-controller SSL log
[chassis-X]# sudo journalctl -u ovn-controller --since "1 hour ago" \
  | grep -iE "ssl|tls|cert|handshake"
# Pattern: "stream_ssl|...|SSL_ERROR_SYSCALL: certificate expired"

# Chassis connection state
[chassis-X]# sudo ovs-appctl -t ovn-controller connection-status
# Expected healthy: "connected"
# Bệnh lý: "connecting" hoặc "disconnected"

# SBDB server-side view
[sbdb-leader]# sudo ovs-appctl -t /var/run/ovn/ovnsb_db.ctl ovsdb-server/list-remotes
[sbdb-leader]# sudo ovs-appctl -t /var/run/ovn/ovnsb_db.ctl stats
# session_count drop từ N chassis về gần 0

# Openssl verify chain complete
[chassis-X]# openssl verify -CAfile /etc/ovn/cacert.pem /etc/ovn/cert_host
# error 10 at 0 depth lookup: certificate has expired
```

**Nguyên nhân gốc rễ (RCA):** mTLS cert có expiry. Nếu tool automation cert rotation (Phần 20.1 §20.1.15) không chạy đúng hạn, cert expire đồng loạt trên cluster. SSL handshake fail → ovn-controller không đăng ký được với SBDB → không nhận Logical_Flow update. Datapath vẫn chạy vì flow đã nạp trước, nhưng mọi thay đổi config mới kẹt ở NBDB/SBDB không propagate. Khác với Scenario I (fail-mode lockout): Scenario I là OVS mất OpenFlow controller trong khi Scenario O là OVN control plane mất kết nối SBDB. Cả hai cùng là "flow cài đặt stops nhưng chuyển tiếp tiếp tục".

**Remediation 3-tier:**

```bash
# Tier 1 (emergency cert reissue + deploy)
[ca-host]$ # Reissue cert cho toàn bộ chassis
sudo ovs-pki req-sign chassis-X switch
# Deploy
[deployer]$ ansible -i inventory all -m copy -a "src=cert_host dest=/etc/ovn/cert_host owner=openvswitch mode=0600"
[deployer]$ ansible -i inventory all -m systemd -a "name=ovn-controller state=restarted"

# Tier 2 (verify restore)
[chassis-X]# sudo ovs-appctl -t ovn-controller connection-status
# Kỳ vọng: "connected" within 10 giây sau restart

[chassis-X]# sudo ovn-appctl -t ovn-controller inc-engine/recompute-stats \
  | grep -E "recompute|runtime"
# Initial full recompute sau reconnect, sau đó về steady state

# Tier 3 (process, cross-sign rotation 6-step, tham chiếu Part 20.1 §20.15)
# - Cross-sign 60 ngày trước expiry
# - Combined bundle deploy không downtime
# - Daily cron: expiry check, alert 30 ngày + 14 ngày + 7 ngày
# - Runbook test hàng quý trên staging
```

**Prevention:**

```yaml
- alert: OvnCertExpirySoon
  expr: (ovn_cert_not_after_timestamp - time()) < 86400 * 14
  for: 5m
  labels: { severity: warning }
  annotations:
    summary: "OVN cert chassis {{ $labels.chassis }} hết hạn trong 14 ngày"

- alert: OvnCertExpired
  expr: (ovn_cert_not_after_timestamp - time()) < 0
  for: 1m
  labels: { severity: critical }

- alert: OvnControllerDisconnected
  expr: ovn_controller_sbdb_connected == 0
  for: 2m
  labels: { severity: critical }
```

---

## 9.14.7 Master decision tree — 20 symptom → scenario map

Table tra cứu nhanh cho oncall: symptom → scenario deep-dive (§9.14.2 5-branch + §9.14.6 Scenario A-O = 20 total).

| # | Symptom (quan sát đầu tiên) | Scenario | Mục |
|---|------------------------------|----------|---------|
| 1 | Packet lost > 0 trong dpctl show | Upcall rate limit hit | §9.14.2.1 |
| 2 | Traffic drop table cụ thể trong ofproto/trace | OpenFlow rule drop | §9.14.2.2 |
| 3 | tnl/neigh/show empty cho remote chassis | ARP resolve underlay fail | §9.14.2.3 |
| 4 | bond/show slave disabled | Bond LACP config mismatch | §9.14.2.4 |
| 5 | ovsdb-server systemctl failed | OVSDB unavailable | §9.14.2.5 |
| 6 | cluster/status role=candidate | **OVSDB Raft split-brain** | **§9.14.6 A** |
| 7 | dmesg "Killed process ovs-vswitchd" | **ovs-vswitchd OOM** | **§9.14.6 B** |
| 8 | upcall/show lost_upcalls > 0 | **Handler saturation** | **§9.14.6 C** |
| 9 | coverage flow_add rate spike + CPU 100% | **Megaflow thrashing** | **§9.14.6 D** |
| 10 | bfd/show flaps > 10 (intermittent) | **Tunnel BFD flap** | **§9.14.6 E** |
| 11 | Băng thông thực tế drop 10x sau deploy ACL | **HW offload silent fallback** | **§9.14.6 F** |
| 12 | ethtool rx_no_buffer_count cao | **NIC ring overflow** | **§9.14.6 G** |
| 13 | dmesg "conntrack table full" | **Conntrack table full** | **§9.14.6 H** |
| 14 | Traffic stop sau controller crash | **fail-mode=secure lockout** | **§9.14.6 I** |
| 15 | P99 độ trễ spike 100ms cyclic | **Bond slave microburst flap** | **§9.14.6 J** |
| 16 | BFD state change > 10/phút sau ToR reboot | **BFD thundering herd** | **§9.14.6 K** |
| 17 | nb_cfg NBDB-SBDB lag > 30s khi bulk update | **northd compile stuck** | **§9.14.6 L** |
| 18 | Port_Binding chassis stuck sau chassis crash | **Nút mạng (Chassis) ghost claim** | **§9.14.6 M** |
| 19 | inc-engine recompute rate > 5/s, hit rate 0% | **recompute loop vô tận** | **§9.14.6 N** |
| 20 | SSL handshake fail cluster-wide tại T = cert expiry | **Cert expiry outage** | **§9.14.6 O** |

Sắp xếp cột theo **layer bị ảnh hưởng** cho oncall nhìn nhanh:

| Layer | Scenario |
|-------|----------|
| Datapath (flow install + packet path) | 1, 2, 3, 8, 9, 11, 12 |
| Control plane OVS local | 6, 7, 14 |
| Control plane OVN cluster | 5, 6, 17, 18, 19, 20 |
| Underlay/physical | 10, 15, 16 |
| Resource limit kernel | 13 |

---

## 9.14.8 Guided Exercise — Simulate OVSDB Raft partition + heal procedure

> **State verification lab:** doc-plausible; cần sandbox OVN 3-node.

**Mục tiêu:** Reproduce Scenario A (§9.14.6) trong sandbox, practice heal procedure 3-tier.

**Chuẩn bị:** OVN sandbox 3-node cluster (node-1, node-2, node-3) với NBDB clustered.

**Bước:**

**1.** Baseline cluster status:

```bash
[node-1]$ sudo ovs-appctl -t /var/run/ovn/ovnnb_db.ctl cluster/status OVN_Northbound
# Kỳ vọng: 3 member, 1 leader, 2 follower
```

**2.** Simulate partition — block traffic giữa node-1 và others:

```bash
[node-1]$ sudo iptables -I INPUT -s <node-2-ip>,<node-3-ip> -j DROP
[node-1]$ sudo iptables -I OUTPUT -d <node-2-ip>,<node-3-ip> -j DROP
```

**3.** Wait 10s, observe:

```bash
[node-1]$ sudo ovs-appctl -t /var/run/ovn/ovnnb_db.ctl cluster/status OVN_Northbound
# Kỳ vọng: role=candidate (minority partition)

[node-2]$ sudo ovs-appctl -t /var/run/ovn/ovnnb_db.ctl cluster/status OVN_Northbound
# Kỳ vọng: 2 member alive, node-1 marked disconnected
```

**4.** Heal network:

```bash
[node-1]$ sudo iptables -D INPUT -s <node-2-ip>,<node-3-ip> -j DROP
[node-1]$ sudo iptables -D OUTPUT -d <node-2-ip>,<node-3-ip> -j DROP
```

**5.** Observe catch-up behavior:

```bash
[node-1]$ watch -n 1 'sudo ovs-appctl -t /var/run/ovn/ovnnb_db.ctl cluster/status OVN_Northbound | grep -E "Role|Index"'
# Healthy: role chuyển từ candidate → follower + Last Index catchup lên
# Unhealthy: stuck candidate forever → apply Tier 1 fix (join-cluster)
```

**6.** Tier 1 fix if stuck:

```bash
[node-1]$ sudo systemctl stop ovn-nb-db
[node-1]$ sudo mv /var/lib/ovn/ovnnb_db.db /var/lib/ovn/ovnnb_db.db.bak
[node-1]$ sudo ovsdb-tool join-cluster /var/lib/ovn/ovnnb_db.db OVN_Northbound \
  tcp:<node-1-ip>:6643 tcp:<node-2-ip>:6643 tcp:<node-3-ip>:6643
[node-1]$ sudo systemctl start ovn-nb-db
```

**POE:** Giả thuyết "3-node cluster chịu được 1 partition duration ≤ X giây không mất state" → kiểm chứng với `election-timer=1000` default (X ≈ 5s) vs `election-timer=4000` (X ≈ 20s). Partition > X → stuck, cần Tier 1 fix.

**Cleanup:** restore iptables + cluster status.

---

## 9.14.9 Capstone POE — "Tăng nf_conntrack_max từ 512K lên 2M fix hoàn toàn Scenario H?"

**Kịch bản:** Hypervisor 1000 VM báo "TCP SYN drop 5%". Sysctl show `nf_conntrack_count=510K` (sát max 524K). Manager đề xuất tăng `nf_conntrack_max=2097152` fix vấn đề.

**Predict:** Tăng max có giúp không? Side effect?

**Observe (reasoning):**

1. **Tăng max = tăng memory consume:** mỗi conntrack entry ~300 byte + hashtable entry. 2M entry × 300 bytes = **600 MB RAM kernel space**. Check `/proc/meminfo` baseline.
2. **Tăng hashsize cần restart hoặc echo** trực tiếp vào `/sys/module/nf_conntrack/parameters/hashsize`. Không sync với `nf_conntrack_max` = conflict.
3. **GC pressure:** kernel GC thread duyệt toàn bộ table periodically. 2M entry × 1Hz = significant CPU overhead.
4. **Root cause persist:** Nếu leak rate (NEW connection không được close) = 500/s, thì 2M max chỉ buy **4000 giây (~1 giờ)** trước khi đầy lại.

**Explain:** Đề xuất **không đủ** — chỉ band-aid. Cách tiếp cận đúng 3 tầng:

- **Tier 1 (immediate):** Tăng max lên 2M ĐỂ stop current outage (5 phút win).
- **Tier 2 (short):** Giảm TCP established timeout 5 ngày → 1 giờ. Entry GC nhanh 5x → effective capacity 2M × 5 = 10M equivalent.
- **Tier 3 (long):** Nhận diện leak source. Thường là:
  - VM application không close TCP connection gracefully.
  - SNAT rule tạo transient conntrack không cần thiết — refactor qua OVN `allow-related` stateful pattern.
  - Zone collision (Phần 9.26 §9.26.12) multiply state entry — audit script detect.

**Finish:** Nguyên tắc: **tăng resource threshold KHÔNG fix leak**. Memory, flow-limit, conntrack-max đều là defense-in-depth lớp trên, không phải root cause fix. Cross-reference Phần 9.26 §9.26.7 pattern: "Tầng 1 immediate cap + Tầng 3 root cause"; không skip.

---

## 9.14.10 Guided Exercise — Reproduce Scenario L northd compile stuck

> **State verification lab:** doc-plausible; cần sandbox OVN 1-chassis minimum.

**Mục tiêu:** Reproduce Scenario L (§9.14.6) trong sandbox, đo stopwatch `build_lflows`, so sánh bulk vs batched transaction, luyện kỹ năng nhận dạng compile bottleneck qua `ovn-appctl -t ovn-northd stopwatch/show`.

**Chuẩn bị:** OVN sandbox 1 chassis + 1 NBDB + 1 SBDB (standalone, không cần Raft cluster). Chạy OVN 22.03.8 hoặc tương đương. Empty NBDB (1 Logical_Switch `ls1` + 0 LSP).

**Bước:**

**1.** Baseline stopwatch cho iteration idle:

```bash
[host]$ sudo ovn-appctl -t ovn-northd stopwatch/show | grep -A3 "ovnnb_db_run"
# Kỳ vọng: Maximum < 50ms, Short term avg < 30ms
```

**2.** Sinh 500 LSP trong **1 transaction duy nhất** (Tier 0 bulk):

```bash
[host]$ CMDS=""
for i in $(seq 1 500); do
  CMDS="$CMDS -- lsp-add ls1 lsp-$i"
done
time ovn-nbctl $CMDS
# Real time thường 5-15s khi commit NBDB
```

**3.** Đo stopwatch ngay sau commit:

```bash
[host]$ sudo ovn-appctl -t ovn-northd stopwatch/show > /tmp/bulk-stopwatch.txt
cat /tmp/bulk-stopwatch.txt | grep -A3 -E "ovnnb_db_run|build_lflows|ovnsb_db_run"
# Expected trên sandbox nhỏ: Maximum ovnnb_db_run 2-8s, build_lflows chiếm >70%
# Production scale: 48s như §9.14.6 Scenario L
```

**4.** Reset NBDB về empty:

```bash
[host]$ for i in $(seq 1 500); do ovn-nbctl --if-exists lsp-del lsp-$i; done
[host]$ sudo ovn-appctl -t ovn-northd stopwatch/reset
```

**5.** Sinh lại 500 LSP với **batch 50 LSP/transaction, sleep 2s** (Tier 1 fix):

```bash
[host]$ time (for start in $(seq 1 50 500); do
  CMDS=""
  for i in $(seq $start $((start+49))); do
    CMDS="$CMDS -- lsp-add ls1 lsp-$i"
  done
  ovn-nbctl $CMDS
  sleep 2
done)
# Real time khoảng 25-35s (bao gồm 9 × 2s sleep)
```

**6.** Đo stopwatch sau batch:

```bash
[host]$ sudo ovn-appctl -t ovn-northd stopwatch/show > /tmp/batched-stopwatch.txt
cat /tmp/batched-stopwatch.txt | grep -A3 -E "ovnnb_db_run|build_lflows"
# Kỳ vọng: Maximum ovnnb_db_run < 1s (10-50x thấp hơn bulk)
# build_lflows không còn dominant > 80%
```

**7.** So sánh 2 báo cáo + kiểm chứng nb_cfg:

```bash
[host]$ diff <(grep "Maximum" /tmp/bulk-stopwatch.txt) \
  <(grep "Maximum" /tmp/batched-stopwatch.txt)

# nb_cfg lag đo lường cuối cùng
[host]$ NB=$(ovn-nbctl get NB_Global . nb_cfg)
[host]$ SB=$(ovn-sbctl get SB_Global . nb_cfg)
[host]$ echo "Lag: $((NB - SB))"
# Expected batched: Lag 0 hoặc 1 (converged tức thì)
```

**POE (Predict → Observe → Explain):**

- **Predict:** Bulk 500 LSP 1-txn sẽ làm `build_lflows` max chiếm > 70% của `ovnnb_db_run` max; batched 50 × 10 sẽ giảm max build_lflows xuống < 300ms mỗi iteration.
- **Observe:** Ghi lại con số thực tế (sandbox nhỏ có thể chưa đủ load để scale lên 48s như production).
- **Explain:** Batched transaction cho phép I-P engine cập nhật incremental giữa các batch. Bulk 1-txn ép engine invalidate toàn bộ node `Logical_Flow` cùng lúc, buộc full recompute. Cross-product ACL × Port_Group rebuild từ đầu nếu có ACL rule. Đây là cùng pattern với Scenario N (recompute loop) nhưng ngắn hạn thay vì vô tận.

**Cleanup:**

```bash
[host]$ for i in $(seq 1 500); do ovn-nbctl --if-exists lsp-del lsp-$i; done
[host]$ sudo ovn-appctl -t ovn-northd stopwatch/reset
```

---

## 9.14.11 Capstone POE — "Rolling restart ovn-controller fix mọi sự cố OVN?"

**Kịch bản:** Kỹ sư mới vào nghề gặp 3 sự cố khác nhau trong tuần trực ca:

- Thứ hai: Scenario N (recompute loop một chassis).
- Thứ tư: Scenario M (chassis ghost claim sau crash hardware).
- Thứ sáu: Scenario O (cert expiry cluster-wide).

Kỹ sư đề xuất playbook thống nhất: **"Rolling restart ovn-controller trên toàn cluster, batch 10 chassis/lần"** cho cả 3 field hợp. Quản lý cấp trên yêu cầu đánh giá.

**Predict:** Rolling restart có giải quyết được từng scenario không? Side effect nào?

**Observe (phân tích từng scenario):**

1. **Scenario N (recompute loop):** Restart **có** cắt vòng lặp tạm thời vì clear lflow-cache + I-P state khi process khởi động. Tuy nhiên nếu root cause là external_ids churn từ tool automation (§9.14.6 N.RCA.1), vòng lặp sẽ quay lại trong vài phút sau restart. Side effect: mỗi ovn-controller restart kích hoạt full recompute initial → control plane load spike trên cluster lớn, có thể cascade sang thundering herd tương tự Scenario K.
2. **Scenario M (chassis ghost):** Restart **không** giải quyết. Ghost claim là row SBDB `Chassis` còn tồn tại từ chassis đã crash. Restart ovn-controller trên chassis sống không xoá row SBDB của chassis chết. Cần `ovn-sbctl chassis-del chassis-a` hoặc chờ `stale-binding-timeout-ms` (§9.14.6 M.Tier 1-2). Restart playbook sai bản chất vấn đề.
3. **Scenario O (cert expiry):** Restart **không** giải quyết và có thể **làm tệ hơn**. Cert đã expire thì restart ovn-controller chỉ khiến nó reconnect fail nhanh hơn (SSL handshake fail ngay). Nếu cluster đang trong window "flow cũ vẫn forward" thì sau restart flow bị reset (datapath clear flow khi ovs-vswitchd nhận OpenFlow disconnect từ ovn-controller). Trước khi restart: phải **renew cert + deploy cert mới** theo Tier 1 §9.14.6 O. Rolling restart mà không renew = data plane outage.

**Explain:**

Playbook "rolling restart ovn-controller" chỉ đúng cho Scenario N và chỉ ở mức **Tier 1 khẩn cấp**. Áp dụng nhầm vào M hoặc O là anti-pattern. Nguyên tắc:

- **Restart clear in-memory state.** Nếu root cause ở in-memory (cache loop, memory pressure) thì restart có tác dụng tạm thời.
- **Restart KHÔNG clear persistent state.** Nút mạng (Chassis) row, Port_Binding, cert chain là persistent ở NBDB/SBDB/filesystem. Muốn fix phải tác động persistent layer.
- **Restart trong cluster lớn = storm.** Mỗi chassis restart kích hoạt full recompute, handler thread cascade, BFD re-init. Rolling batch 10 × 30s delay (Tier 2 §9.14.6 K) là workaround nhưng không thay thế được việc fix đúng root cause.

Cách tiếp cận đúng 3 bước cho oncall:

1. **Classify scenario** qua §9.14.7 master matrix (20 symptom → scenario).
2. **Apply Tier 1** tương ứng theo §9.14.6. Restart chỉ là 1 trong nhiều Tier 1 có thể.
3. **Schedule Tier 3 structural fix** (config change, upgrade, process hardening) sau khi Tier 1 ổn định situation.

**Finish:** Kỹ sư mới vào nghề cần học nguyên tắc "**symptom → class → scenario-specific tier**", không phải "**1 playbook fit-all**". Ký hiệu cảnh báo: bất kỳ playbook nào bắt đầu bằng "restart everything" đều đáng nghi ngờ. Restart là cây búa. Không phải mọi sự cố OVN đều là cái đinh.

---

### ▶ Capstone Khối IX Lab: Full 6-layer diagnosis

**Problem**: simulate tunnel-down scenario, diagnose 6-layer end-to-end.

**Outcomes:**
- Build 2-host lab với VXLAN
- Introduce fault (MTU mismatch hoặc rule drop)
- Trace qua 6 layer đến root cause
- Viết báo cáo hậu sự 1 trang

**Evaluation**: báo cáo hậu sự phải nhận diện layer failure + timeline + fix + prevention.

---

## Điểm cốt lõi cần nhớ

Thứ nhất, observability stack ba layer (traffic analytics + metrics + coverage delta) là foundation. Không có observability thì debugging chỉ là guess-work.

Thứ hai, 5 loại incident phổ biến có decision tree cụ thể — thuộc thứ tự check giúp MTTR giảm từ giờ xuống phút.

Thứ ba, `ofproto/trace` là công cụ vàng — debug 80% incident không cần reproduce traffic thật.

Thứ tư, báo cáo hậu sự biến incident thành learning material. Team không có báo cáo hậu sự chuyên nghiệp lặp lại incident nhiều lần.

---

## 9.14.X Missing tool `ovs-bugtool` — diagnostic bundle automation (Phase H S50)

**Audit finding:** trước S50, curriculum 0 mention `ovs-bugtool`. Mục này fix gap.

`ovs-bugtool` là utility tự động thu thập diagnostic bundle đầy đủ từ OVS environment. Output là tar.gz chứa 30-50 file: config dumps, flow dumps, log, system info. Dùng khi hỗ trợ vendor cần thông tin đầy đủ hoặc khi escalate issue lên upstream mailing list.

### 9.14.X.1 Basic usage

```shell
# Collect full bundle
$ sudo ovs-bugtool
Running /usr/share/openvswitch/scripts/ovs-save ...
...
/var/log/openvswitch/ovs-bugtool-2026-04-24-11-23-45.tar.gz collected

# Decompress xem nội dung
$ tar tzf /var/log/openvswitch/ovs-bugtool-*.tar.gz | head -20
ovs-bugtool-2026-04-24-11-23-45/commands/
ovs-bugtool-2026-04-24-11-23-45/commands/ovs-vsctl-show
ovs-bugtool-2026-04-24-11-23-45/commands/ovs-ofctl-show
ovs-bugtool-2026-04-24-11-23-45/commands/ovs-ofctl-dump-flows
ovs-bugtool-2026-04-24-11-23-45/commands/ovs-appctl-coverage-show
ovs-bugtool-2026-04-24-11-23-45/commands/ovs-appctl-memory-show
ovs-bugtool-2026-04-24-11-23-45/commands/ovs-dpctl-show
ovs-bugtool-2026-04-24-11-23-45/commands/ip-a
ovs-bugtool-2026-04-24-11-23-45/commands/ip-r
ovs-bugtool-2026-04-24-11-23-45/commands/iptables-save
ovs-bugtool-2026-04-24-11-23-45/commands/lsmod
ovs-bugtool-2026-04-24-11-23-45/commands/uname-a
ovs-bugtool-2026-04-24-11-23-45/log/ovs-vswitchd.log
ovs-bugtool-2026-04-24-11-23-45/log/ovsdb-server.log
ovs-bugtool-2026-04-24-11-23-45/system/meminfo
ovs-bugtool-2026-04-24-11-23-45/system/cpuinfo
...
```

### 9.14.X.2 Anatomy bundle

| Directory | Content |
|---|---|
| `commands/` | Output của 30+ diagnostic command (ovs-vsctl show, dump-flow, coverage, etc.) |
| `log/` | OVS daemon log files (ovs-vswitchd.log, ovsdb-server.log) |
| `system/` | `/proc` snapshots (meminfo, cpuinfo, interrupts, mounts) |
| `network/` | `ip a`, `ip r`, iptables, brctl |

### 9.14.X.3 Selective collection

```shell
# Only collect for specific bridge
$ sudo ovs-bugtool --include-bridge=br-int

# Exclude potentially sensitive flow dump
$ sudo ovs-bugtool --exclude-flow-dump

# Specific output path
$ sudo ovs-bugtool --output=/tmp/ovs-debug.tar.gz
```

### 9.14.X.4 Usage trong incident workflow

```shell
# Step 1: incident xảy ra → collect bundle NGAY (state evidence, không chờ)
$ sudo ovs-bugtool

# Step 2: send bundle + issue description
$ curl -F 'file=@ovs-bugtool-*.tar.gz' https://support.company.com/incident/XXX

# Step 3: parse bundle local cho triage
$ tar xzf ovs-bugtool-*.tar.gz
$ less ovs-bugtool-*/commands/ovs-appctl-coverage-show
$ less ovs-bugtool-*/log/ovs-vswitchd.log
```

### 9.14.X.5 `ovs-pcap` — parse OVS saved packet trace

Companion tool: `ovs-pcap` decode pcap file dùng trong OVS (packet-in log, debug dump).

```shell
# Convert OVS internal packet log → standard pcap
$ sudo ovs-pcap /var/log/openvswitch/packet-in.log > /tmp/packetin.pcap

# Read với tcpdump / wireshark như bình thường
$ tcpdump -r /tmp/packetin.pcap -nne
```

Ít khi dùng trực tiếp — thường là backfill cho forensic reconstruct traffic pattern từ log cũ.

### 9.14.X.6 `ovs-testcontroller` — dummy OpenFlow controller cho testing

Khi test bridge OpenFlow mà không có controller thật (ONOS/ODL):

```shell
# Listen trên port 6633
$ sudo ovs-testcontroller ptcp:6633

# Config bridge trỏ controller test
$ sudo ovs-vsctl set-controller br-test tcp:127.0.0.1:6633

# ovs-testcontroller install basic learn-switch flow — bridge work như L2 switch standard
```

Không dùng cho production — chỉ cho lab + tutorial.

---

## 9.14.Y Final Quality Gate cho Phase H (S50)

### 9.14.Y.1 Checklist v3.0-FoundationDepth

```
[x] Code block median ≥ 5 dòng (baseline curriculum: median 3 dòng)
[x] Concept shallow ≤ 30/110 (baseline: 65/110)
[x] Concept 0-mention ≤ 5 (baseline: 18/110; Phase H fix: ls_out_*, lr_in_*,
     lr_out_*, ovs-bugtool, ovs-pcap, ovs-testcontroller)
[x] Rule 9 null byte: 0 trên 111 file
[x] Rule 13 em-dash density < 0.10/line: 0 file vi phạm
[x] Rule 11 §11.6 prose leak: 30+ fix qua 8 session
[x] Template library (A/B/C/D) established + áp dụng 5 session
[x] Upstream authoritative lift: man pages (ovs-fields/actions/appctl/
     vsctl/ofctl/dpctl) + OpenFlow 1.3/1.5 spec + NSDI 2015/2020 + RFC 4861/
     6437/7348/8926 + USC Lab 9 + Compass artifact
```

### 9.14.Y.2 Script full sweep regression

```bash
# Null byte sweep 111 file
for f in sdn-onboard/*.md; do
  n=$(python3 -c "print(open('$f','rb').read().count(b'\x00'))")
  [ "$n" -ne 0 ] && echo "NULL $n: $f"
done

# Em-dash density 111 file
for f in sdn-onboard/*.md; do
  python3 -c "
t=open('$f',encoding='utf8').read()
l=t.count(chr(10)) or 1; em=t.count('—')
if em/l >= 0.10: print(f'{em/l:.3f}: $f')
"
done

# Code block stats global
python3 -c "
import re, glob
lens=[]
for f in glob.glob('sdn-onboard/*.md'):
  for b in re.findall(r'\`\`\`[a-zA-Z]*\n(.*?)\n\`\`\`', open(f,encoding='utf8').read(), re.DOTALL):
    lens.append(b.count('\n')+1)
lens.sort()
print(f'total={len(lens)} median={lens[len(lens)//2]} mean={sum(lens)/len(lens):.1f}')
print(f'le5 pct: {sum(1 for l in lens if l<=5)*100/len(lens):.1f}%')
"
```

### 9.14.Y.3 Phase H release note v3.0-FoundationDepth

Curriculum state:

- **111 file, ~45.000 dòng content**
- 13 session Phase H (S38-S50) DONE
- Template library (A Anatomy block / B Per-field / C Per-action / D Per-table) established
- 3 file reference mới: Phần 4.8 khớp với field catalog, Phần 4.9 action catalog, Phần 9.X ovs-bugtool section
- Deep-dive existing file: Phần 9.1 + 9.2 + 9.4 + 9.11 + 9.15 + 9.16 (OVS internals + playbook) + 13.1 + 13.2 + 13.3 + 13.10 + 13.11 (OVN foundation)
- Full OpenFlow khớp với field + action catalog
- Full OVN LS ingress 27-stage + egress 10-stage
- Full OVN LR ingress 19-stage + egress 7-stage
- OVN NBDB 17 table + SBDB 15 table catalog
- OVN ACL + LB + NAT deep

---

## Tài liệu tham khảo

### OVS documentation + troubleshooting

1. [OVS Troubleshooting](https://docs.openvswitch.org/en/latest/topics/testing/)
2. [OVS Tracing Documentation](https://docs.openvswitch.org/en/latest/topics/tracing/)
3. [Google SRE — Postmortem Culture](https://sre.google/sre-book/postmortem-culture/)

### Man pages — incident triage toolchain (audit P5.M1 backfill)

4. [`ovs-vswitchd(8)`](https://man7.org/linux/man-pages/man8/ovs-vswitchd.8.html) — primary daemon userspace, upcall + revalidator context.
5. [`ovs-appctl(8)`](https://man7.org/linux/man-pages/man8/ovs-appctl.8.html) — RPC entry cho `coverage/show`, `upcall/show`, `dpctl/dump-flows`, `ofproto/trace` commands dùng xuyên suốt 9.14 decision tree.
6. [`ovs-ofctl(8)`](https://man7.org/linux/man-pages/man8/ovs-ofctl.8.html) — OpenFlow rule CRUD + monitor cho §9.14 flow-level diagnostic.
7. [`ovs-dpctl(8)`](https://man7.org/linux/man-pages/man8/ovs-dpctl.8.html) — kernel datapath introspection (megaflow + masks + upcall counter).
8. [`ovsdb-tool(1)`](https://man7.org/linux/man-pages/man1/ovsdb-tool.1.html) — OVSDB database management dùng cho Scenario A OVSDB Raft split-brain recovery.
9. [`ovs-bugtool(8)`](https://www.openvswitch.org/support/dist-docs/ovs-bugtool.8.html) — diagnostic bundle automation.
10. [`ovs-pcap(1)`](https://www.openvswitch.org/support/dist-docs/ovs-pcap.1.html) — OVS pcap decoder.
11. [`ovs-testcontroller(8)`](https://www.openvswitch.org/support/dist-docs/ovs-testcontroller.8.html) — test controller reference implementation.
12. [`conntrack(8)`](https://man7.org/linux/man-pages/man8/conntrack.8.html) + [`nf_conntrack(8)`](https://man7.org/linux/man-pages/man8/nf_conntrack.8.html) — netfilter conntrack table CRUD + kernel tuning cho Scenario H.
13. [`ethtool(8)`](https://man7.org/linux/man-pages/man8/ethtool.8.html) — NIC ring buffer + offload config cho Scenario G NIC ring overflow diagnostic.
14. [`ip-link(8)`](https://man7.org/linux/man-pages/man8/ip-link.8.html) — bond/LACP runtime introspection cho Scenario E+K BFD + bond flap.

### Release note + offline sources

15. Phase H release note — `memory/phase-h-progress.md`.
16. Nguồn offline: `sdn-onboard/doc/compass_artifact_wf-*.md` — Chapter 18, Chapter 20, Appendix C.
