# 15.2. Cilium eBPF internals — datapath + service mesh without sidecar

> **Trạng thái:** Skeleton (C5 Architecture Phase, 2026-04-22)
> **Khối:** XV, Expert Extension — Service Mesh + Kubernetes CNI
> **Phần:** Part 15 (Expert Extension)
> **Ebook mapping:** Cilium Documentation + eBPF Summit proceedings + Liz Rice "Learning eBPF" 2023
> **Plan:** `plans/sdn-foundation-architecture.md` §3.3 Block XV
> **Prerequisites:** Part 15.0 (service mesh overview), Part 16.0 (kernel tuning, eBPF basics)

---

## Mục tiêu bài học

Sau khi hoàn thành phần này, bạn có thể:

1. **Mô tả (Understand)** Cilium datapath: eBPF programs attached at tc (traffic control) hook + XDP + socket-level, replacing kube-proxy iptables.
2. **Phân tích (Analyze)** Cilium Service Mesh without sidecar: L7 filter qua Envoy as shared node-level proxy (thay vì per-pod), socket-level mTLS injection.
3. **Áp dụng (Apply)** debug Cilium datapath với `cilium monitor`, `cilium bpf policy get`, `hubble observe` để trace packet path.

---

## 15.2.1 eBPF datapath architecture

*Skeleton — nội dung sẽ bao gồm:* eBPF program attach points (tc ingress/egress, XDP, socket filters, kprobes), eBPF map types (hash, LPM trie, LRU), eBPF verifier constraints (bounded loops, stack size).

## 15.2.2 Cilium CNI vs OVN-K8s

*Skeleton — nội dung sẽ bao gồm:* So sánh kernel datapath (Cilium eBPF) vs userspace + OVS datapath (OVN-K8s), latency profile, NetworkPolicy expressiveness, debugging tooling.

## 15.2.3 kube-proxy replacement

*Skeleton — nội dung sẽ bao gồm:* Cilium's `--kube-proxy-replacement=strict`, hashmap lookup O(1) vs iptables chain traversal O(n), session affinity implementation.

## 15.2.4 Cilium Service Mesh (sidecar-less)

*Skeleton — nội dung sẽ bao gồm:* Shared Envoy proxy per-node (instead of per-pod sidecar), socket-level L7 visibility, mTLS tùy chọn, Ingress Gateway integration.

## 15.2.5 Hubble observability

*Skeleton — nội dung sẽ bao gồm:* Hubble relay + UI, flow event export (eBPF ring buffer → userspace), service map generation, L7 HTTP/gRPC/Kafka visibility.

---

### ▶ Guided Exercise 1: Inspect Cilium eBPF programs

*Skeleton — nội dung sẽ bao gồm:* `bpftool prog list`, identify Cilium-attached programs, dump map content với `bpftool map dump`, verify policy application.

### ▶ Lab 2: Cilium Service Mesh benchmark vs Istio sidecar

*Skeleton — nội dung sẽ bao gồm:* Deploy bookinfo app with Cilium SM (shared Envoy), measure p99 latency vs Istio sidecar, analyze overhead delta (expected 30-50% savings for Cilium).

---

## Tài liệu tham khảo

1. [Cilium Documentation](https://docs.cilium.io/), Cilium project. Official docs with architecture.
2. [Cilium GitHub](https://github.com/cilium/cilium), Cilium project. Source code + integration tests.
3. [Learning eBPF](https://isovalent.com/books/learning-ebpf/), Liz Rice, O'Reilly 2023. Book-length eBPF tutorial.
4. [eBPF Documentation](https://ebpf.io/), eBPF Foundation. Community resources + tooling.
5. [Hubble Observability Platform](https://github.com/cilium/hubble), Cilium project. Observability layer built on Cilium.
6. [Cilium Service Mesh](https://cilium.io/use-cases/service-mesh/), Cilium project. Sidecar-less mesh architecture.
