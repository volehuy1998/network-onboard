# 13.3. OVN ACL, Load Balancer, NAT và Port Groups

> **Trạng thái:** Content written (Phase B, 2026-04-22)
> **Khối:** XIII, OVN foundation
> **Phần:** Part 13
> **Ebook mapping:** compass Ch 17 + ovn-nb(5)
> **Plan:** `plans/sdn-foundation-architecture.md` §3.3 Block XIII
> **Prerequisites:** Part 13.2
> **Nguồn offline chính:**
> - `sdn-onboard/doc/compass_artifact_wf-*.md` — Chapter 17 (ACL, NAT, Load_Balancer, Port_Group).

Môi trường thực hành: Ubuntu 22.04 LTS với OVN 22.03.8. Tham khảo chính: [ovn-nb(5)](https://man7.org/linux/man-pages/man5/ovn-nb.5.html).

---

## Mục tiêu bài học

1. **Áp dụng (Apply)** ACL stateful qua Port_Group — micro-segmentation trong OVN.
2. **Áp dụng (Apply)** Load_Balancer cho L4 traffic balancing với VIP.
3. **Phân biệt (Analyze)** SNAT vs DNAT trong OVN logical router.

---

Kiến thức tiên quyết: Part 13.2, Part 12.2.

---

## 13.3.1 ACL

OVN ACL stateful — tracking connection state qua `ct()` action automatic.

```bash
ovn-nbctl acl-add <ls-or-pg> <direction> <priority> <match> <action>
```

- `<direction>`: `from-lport` (ingress từ VM) hoặc `to-lport` (egress về VM).
- `<priority>`: 0-32767. Higher wins.
- `<action>`: `allow`, `allow-related`, `drop`, `reject`.

`allow-related` = allow + auto-allow reply qua conntrack.

```bash
[admin]# ovn-nbctl acl-add ls-tenant1 from-lport 1000 \
         "ip4.src == 10.0.0.0/24 && tcp.dst == 80" allow-related
[admin]# ovn-nbctl acl-add ls-tenant1 from-lport 500 "1" drop
```

---

## 13.3.2 Port_Group

Port_Group nhóm LSP cho ACL apply chung:

```bash
[admin]# ovn-nbctl pg-add pg-web web-vm1 web-vm2 web-vm3
[admin]# ovn-nbctl acl-add pg-web from-lport 1000 \
         "outport == @pg-app && tcp.dst == 8080" allow-related
```

Shortcut `@pg-web` reference trong match expression.

---

## 13.3.3 Load Balancer

OVN L4 LB:

```bash
[admin]# ovn-nbctl lb-add lb1 10.0.100.100:80 10.0.1.1:8080,10.0.1.2:8080
[admin]# ovn-nbctl ls-lb-add ls-tenant1 lb1
```

- VIP: 10.0.100.100:80.
- Backends: 10.0.1.1:8080, 10.0.1.2:8080.

Hỗ trợ health check (OVN 22.03+). Algorithm: hash dựa 5-tuple inner — consistent hashing cho session persistence.

---

## 13.3.4 NAT

LR hỗ trợ 3 loại:

**SNAT** (source NAT):

```bash
[admin]# ovn-nbctl lr-nat-add lr-tenant1 snat 192.0.2.100 10.0.0.0/24
```

**DNAT_and_SNAT** (floating IP 1-to-1):

```bash
[admin]# ovn-nbctl lr-nat-add lr-tenant1 dnat_and_snat 192.0.2.101 10.0.1.10
```

**DNAT**: thường combine thành dnat_and_snat.

---

## 13.3.5 Gateway router

NAT cần gateway chassis:

```bash
[admin]# ovn-nbctl lrp-set-gateway-chassis lrp-external chassis-1 100
[admin]# ovn-nbctl lrp-set-gateway-chassis lrp-external chassis-2 50
```

Chassis-1 priority 100, chassis-2 priority 50 (failover). Active chassis handle all north-south của LR.

---

## 13.3.6 Workflow 3-tier ACL

```bash
# Port groups
[admin]# ovn-nbctl pg-add pg-web web-vm1 web-vm2
[admin]# ovn-nbctl pg-add pg-app app-vm1 app-vm2
[admin]# ovn-nbctl pg-add pg-db db-vm1

# Allow specific
[admin]# ovn-nbctl acl-add pg-app from-lport 1000 \
         "outport == @pg-app && tcp.dst == 8080" allow-related
[admin]# ovn-nbctl acl-add pg-db from-lport 1000 \
         "outport == @pg-db && tcp.dst == 5432" allow-related

# Default deny
[admin]# ovn-nbctl acl-add pg-web from-lport 1 "ip" drop
[admin]# ovn-nbctl acl-add pg-app from-lport 1 "ip" drop
[admin]# ovn-nbctl acl-add pg-db from-lport 1 "ip" drop
```

---

### ▶ Guided Exercise 15: Micro-segmentation OVN

**Outcomes:** Setup 3-tier tenant với ACL, verify default deny.

**Instructions:**

**1.** Topology setup (Part 13.2 exercise).

**2.** Port groups:

```bash
[central]# ovn-nbctl pg-add pg-web web-vm1
[central]# ovn-nbctl pg-add pg-app app-vm1
[central]# ovn-nbctl pg-add pg-db db-vm1
```

**3.** ACL — chỉ allow web→app:8080 và app→db:5432:

```bash
[central]# ovn-nbctl acl-add pg-app from-lport 1000 \
         "outport == @pg-app && tcp.dst == 8080" allow-related
[central]# ovn-nbctl acl-add pg-db from-lport 1000 \
         "outport == @pg-db && tcp.dst == 5432" allow-related
[central]# ovn-nbctl acl-add pg-web from-lport 1 "ip" drop
[central]# ovn-nbctl acl-add pg-app from-lport 1 "ip" drop
[central]# ovn-nbctl acl-add pg-db from-lport 1 "ip" drop
```

**4.** Verify:

```bash
[central]# ovn-sbctl lflow-list | grep -i "ct\|drop"
```

---

## Điểm cốt lõi cần nhớ

Thứ nhất, ACL stateful tự động qua `allow-related` — conntrack handle reply.

Thứ hai, Port_Group là foundation micro-seg scale. Apply ACL per-group.

Thứ ba, Load_Balancer OVN L4-only. Cho L7, dùng external (HAProxy).

Thứ tư, NAT cần gateway chassis — chokepoint north-south. Scale limited.

**Deep-dive tùy chọn (Expert Extension Block XV, rev 4):** Người đọc chạy OVN trong Kubernetes environment có thể tiếp tục sang [Part 15.1 OVN-Kubernetes CNI deep dive](15.1%20-%20ovn-kubernetes-cni-deep-dive.md) — NetworkPolicy → OVN ACL translation, ovnkube-master/node architecture, ovn-trace debugging workflow. Block XV cũng covers [Part 15.0 Service Mesh Integration](15.0%20-%20service-mesh-integration.md) (Istio vs Cilium comparison với OVN underlay) và [Part 15.2 Cilium eBPF internals](15.2%20-%20cilium-ebpf-internals.md) (eBPF CNI alternative). Block XV là optional track cho platform engineer / CKS candidate.

---

## 13.3.6 OVN ACL stateful deep — match expression + conjunction (Phase H S49)

OVN ACL translate thành OpenFlow logical flow qua stage `ls_in_acl_eval` + `ls_out_acl_eval` (Part 13.2). Section này đi sâu vào match expression syntax + conjunction usage.

### 13.3.6.1 Match expression syntax

OVN ACL match là OVN expression (thường gọi "lflow match syntax") compile thành OpenFlow match. Ngôn ngữ:

```
Operators: && || ! == != < > <= >=
Boolean: 0 / 1
Symbols: inport, outport, eth.src, eth.dst, eth.type,
         ip, ip4, ip6, ip4.src, ip4.dst, ip.proto,
         tcp, tcp.src, tcp.dst, tcp.flags, udp.src, udp.dst,
         arp, arp.op, arp.spa, arp.tpa,
         ct.new, ct.est, ct.rel, ct.rpl, ct.inv, ct.trk,
         ct_mark, ct_label, ct_zone,
         @port_group, $address_set
```

Examples:

```
# Allow TCP from web group to db port 3306, stateful
(inport == @pg_web && ip4.dst == $ip_db && tcp.dst == 3306)

# Block outbound RDP (3389) except admin
(outport == @pg_all && tcp.dst == 3389 && inport != @pg_admin)

# Allow ICMP echo
(icmp4 && icmp4.type == 8 && icmp4.code == 0)

# Drop invalid CT
(ip && ct.inv)
```

### 13.3.6.2 ACL action `allow-related` semantics

OVN có 5 action type trong ACL:

| Action | Semantics |
|---|---|
| `allow` | Stateless allow (không track CT) |
| `allow-related` | Allow + CT commit + allow reply automatically |
| `allow-stateless` | Force stateless (bypass CT even với ip) |
| `drop` | Silent drop |
| `reject` | Send ICMP Destination Unreachable / TCP RST reply |
| `pass` | Skip to next stage (OVN 23.03+) |

**`allow-related` vs `allow`:**

```shell
# allow-related: outbound → commit, inbound reply automatically allowed
$ ovn-nbctl acl-add ls-prod from-lport 1000 \
    "inport == @pg_web && ip4.dst == $ip_internet" allow-related

# Với allow-related, engineer KHÔNG cần add acl for reply
```

```shell
# allow stateless: phải add reply ACL riêng
$ ovn-nbctl acl-add ls-prod from-lport 1000 \
    "inport == @pg_web && ip4.dst == $ip_internet" allow

$ ovn-nbctl acl-add ls-prod to-lport 1000 \
    "outport == @pg_web && ip4.src == $ip_internet" allow
```

`allow-related` gần như luôn là lựa chọn đúng (trừ workload pure stateless như OSPF/BGP).

### 13.3.6.3 Conjunction compression trong practice

Part 4.9 §4.9.25 giới thiệu `conjunction()` action. OVN dùng conjunction automatic khi ACL match kèm Port_Group hoặc Address_Set có nhiều entry.

Ví dụ: Port_Group `@pg_web` có 50 port, `@pg_db` có 20 port:

- **Without conjunction:** 50 × 20 = 1000 OpenFlow rule.
- **With conjunction:** 50 + 20 + 1 = 71 OpenFlow rule.

OVN northd tự sinh conjunction khi match expression có `@pg_X` cross-product. Engineer không cần manual.

Verify qua `ovn-sbctl lflow-list`:

```
  table=8 (ls_in_acl_eval), priority=2001,
    match=(reg0[7] == 1 && (inport == @pg_web)),
    action=(conjunction(42, 1/2);)
  table=8 (ls_in_acl_eval), priority=2001,
    match=(reg0[7] == 1 && (ip4.dst == $ip_db && tcp.dst == 3306)),
    action=(conjunction(42, 2/2);)
  table=8 (ls_in_acl_eval), priority=2001,
    match=(conj_id == 42),
    action=(reg0[17] = 1; next;)
```

---

## 13.3.7 OVN Load_Balancer deep

### 13.3.7.1 Schema deep

```shell
$ sudo ovn-nbctl list Load_Balancer
_uuid               : 7c9d4e2a-...
name                : "lb-webapp"
protocol            : tcp
vips                : {"10.0.0.100:80"="10.0.1.1:8080,10.0.1.2:8080,10.0.1.3:8080"}
ip_port_mappings    : {"10.0.1.1"="lsp-backend-1:10.0.1.1",
                        "10.0.1.2"="lsp-backend-2:10.0.1.2",
                        "10.0.1.3"="lsp-backend-3:10.0.1.3"}
health_check        : [<Service_Monitor_uuid>]
selection_fields    : [ip_dst,tp_dst]
options             : {affinity_timeout="300", event="true",
                        hairpin_snat_ip="169.254.169.254",
                        neighbor_responder="reachable",
                        reject="true", skip_snat="false"}
```

**Key column:**

| Column | Purpose |
|---|---|
| `protocol` | `tcp` / `udp` / `sctp` |
| `vips` | VIP → backend list (comma-separated) |
| `ip_port_mappings` | Backend IP → LSP name mapping (cho hairpin) |
| `health_check` | Reference Service_Monitor cho backend health |
| `selection_fields` | Hash fields cho backend pick (default 5-tuple) |
| `options:affinity_timeout` | Session persistence timeout (giây) |
| `options:event` | Emit Controller_Event trên empty LB |
| `options:hairpin_snat_ip` | SNAT IP khi hairpin (client → VIP → same backend) |
| `options:reject` | Gửi reject thay vì drop khi empty backend |
| `options:skip_snat` | Không SNAT reply (cho DSR pattern) |

### 13.3.7.2 Service_Monitor cho backend health

```shell
# Tạo health check HTTP
$ sudo ovn-nbctl --id=@hc create Load_Balancer_Health_Check \
    vip='"10.0.0.100:80"' \
    options='"interval"="5" "timeout"="2" "success_count"="3" "failure_count"="3"'

$ sudo ovn-nbctl set Load_Balancer lb-webapp health_check=@hc

# Check status
$ sudo ovn-sbctl list Service_Monitor
_uuid       : ...
ip          : "10.0.1.1"
port        : 8080
protocol    : tcp
logical_port: "lsp-backend-1"
src_ip      : "10.0.0.100"
src_mac     : "40:44:00:00:00:01"
status      : online   /* online / offline / error */
```

Health check chạy từ chassis nắm `logical_port`. Status "offline" → OVN northd regenerate lflow bỏ backend đó khỏi `ct_lb_mark()` backends list.

---

## 13.3.8 OVN NAT deep (stateful + stateless)

### 13.3.8.1 SNAT pattern

```shell
# Cả subnet 10.0.0.0/24 SNAT sang external IP 203.0.113.100
$ sudo ovn-nbctl lr-nat-add lr-tenant snat 203.0.113.100 10.0.0.0/24

# Conntrack flow:
# outbound:  10.0.0.5:50000 → 8.8.8.8:443 (new, trk)
#            apply SNAT:  src = 203.0.113.100:32768 (OVN pick free port)
#            commit conntrack zone_lr
# inbound :  8.8.8.8:443 → 203.0.113.100:32768 (est, rpl)
#            reverse SNAT: dst = 10.0.0.5:50000
```

### 13.3.8.2 DNAT_and_SNAT (floating IP pattern)

```shell
# Floating IP 203.0.113.50 ↔ 10.0.0.5 (lsp-vm-web)
$ sudo ovn-nbctl lr-nat-add lr-tenant dnat_and_snat \
    203.0.113.50 10.0.0.5 lsp-vm-web 40:44:00:00:00:50

# Inbound: 8.8.8.8:443 → 203.0.113.50:80 (new, trk)
#          DNAT: dst = 10.0.0.5:80
# Outbound reply: 10.0.0.5:80 → 8.8.8.8:443 (est, rpl)
#                 reverse SNAT: src = 203.0.113.50:80
```

`external_mac` dùng cho Gratuitous ARP emit khi floating IP mới assign. Nếu null → OVN generate random MAC.

### 13.3.8.3 Stateless NAT (rare)

```shell
# Stateless 1-to-1 mapping, không CT tracking
$ sudo ovn-nbctl lr-nat-add lr-tenant dnat 203.0.113.50 10.0.0.5
$ sudo ovn-nbctl set NAT <uuid> options:stateless=true
```

Use case: extremely high throughput LB frontend (L4 DSR). Trade-off: không có flow tracking, reply path khác.

---

## Điểm cốt lõi cần nhớ

Thứ nhất, OVN ACL dùng expression language (lflow match syntax) gồm: inport/outport + eth.* + ip.*/ip4.*/ip6.* + tcp.*/udp.* + ct.* + @port_group + $address_set. Engineer phải thuộc để debug `ovn-sbctl lflow-list`.

Thứ hai, Port_Group là foundation micro-seg scale. Apply ACL per-group.

Thứ ba, Load_Balancer OVN L4-only. Cho L7, dùng external (HAProxy).

Thứ tư, NAT cần gateway chassis — chokepoint north-south. Scale limited.

Thứ năm, `allow-related` là default lựa chọn (không cần manual reply ACL). Conjunction compression tự động khi ACL có Port_Group × Address_Set cross-product.

Thứ sáu, Service_Monitor cho LB health check chạy từ chassis nắm `logical_port` của backend. Backend offline → northd regenerate lflow bỏ backend khỏi `ct_lb_mark()`.

---

## Tài liệu tham khảo

1. [ovn-nb(5) — ACL, Load_Balancer, NAT](https://man7.org/linux/man-pages/man5/ovn-nb.5.html)
2. [ovn-nbctl(8)](https://man7.org/linux/man-pages/man8/ovn-nbctl.8.html)
3. Nguồn offline: `sdn-onboard/doc/compass_artifact_wf-*.md` — Chapter 17.
4. [OVN flow match syntax BNF in ovn-sb(5) §Logical_Flow](https://man7.org/linux/man-pages/man5/ovn-sb.5.html).
5. Part 4.9 §4.9.25 — conjunction() action mechanics.
6. Part 13.2 §13.2.7 — ls_in_acl_eval stage pipeline context.
7. Part 13.11 §13.11.7 — LR pipeline NAT stages (lr_out_snat, lr_out_undnat).
