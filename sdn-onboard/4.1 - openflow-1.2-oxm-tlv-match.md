# 4.1. OpenFlow 1.2: OXM TLV khớp với và controller roles

> **State:** Content written (Phase B, 2026-04-21)
> **Khối:** IV, OpenFlow evolution
> **Phần:** Phần 4
> **Ebook mapping:** `plans/ebook-coverage-map.md` → Göransson Ch5.3.2 (KEEP)
> **Plan:** `plans/sdn-foundation-architecture.md` §3.3 Khối IV
> **Prerequisites:** Phần 4.0

---

## Mục tiêu bài học

Sau khi hoàn thành phần này, bạn có thể:

1. **Phân biệt (Analyze)** 12-tuple fixed (OF 1.0) vs OXM TLV extensible (OF 1.2), nhận ra architectural shift từ rigid header đến flexible metadata.
2. **Giải thích (Understand)** OXM (OpenFlow Extensible Match) structure: class + field + HM + length + value, và cách vendor class 0xFFFF cho phép extension không phá compatibility.
3. **Liệt kê (Remember)** các Các phần mở rộng của khớp với field mới trong 1.2 (ARP detail, ICMP, TCP flags, metadata), và cách controller roles EQUAL/MASTER/SLAVE enable HA deployment.

---

## Bối cảnh: ONF's first spec và shift sang extensible match

Phần 3.2 đã phân tích ONF formation 21/03/2011 và IP transfer OpenFlow spec từ Stanford. Phần 4.0 đã phân tích OF 1.1 (28/02/2011) là spec cuối do Stanford shepherd. OF 1.2 là **spec đầu tiên do ONF công bố** sau khi WG process chuyển đầy đủ vào ONF governance. Release ngày 05/12/2011, 9 tháng sau ONF formation.

Ngoài ý nghĩa organizational (ONF's first), OF 1.2 có shift architectural quan trọng: chuyển từ **12-tuple fixed match** của OF 1.0-1.1 sang **OXM TLV extensible match**. Shift này không phải cosmetic; nó là acknowledgment rằng packet header diversity trong modern networking (ARP, ICMP variants, MPLS, metadata) không fit vào hardcoded 12 fields, cần extension mechanism để future-proof protocol.

OF 1.2 cũng là version đầu tiên có **controller roles** formalized cho HA deployment. Trước 1.2, multi-controller setup là implementation-defined (không chuẩn). OF 1.2 đưa EQUAL/MASTER/SLAVE model với generation_id protocol cho atomic role transition, enable production HA.

---

## 4.1.1 OF 1.2 phát hành 05/12/2011: spec đầu tiên do ONF công bố

### Dòng thời gian và quy trình ONF

OpenFlow Switch Specification Version 1.2 chính thức release ngày **05/12/2011**, wire protocol version **0x03**. Document 85 trang (tăng từ 56 trang của 1.1), reflect scope mở rộng. Copyright page ghi:

> *"OpenFlow Switch Specification, Version 1.2 (Wire Protocol 0x03), December 5, 2011. Copyright © 2011 Open Networking Foundation. All rights reserved."*

Đây là spec đầu tiên ghi copyright ONF thay vì Stanford. Về process, ONF's Technical Steering Committee (TSC) review draft từ Chuyển tiếp Abstractions Working Group (FAWG) và approve release. Đây là proof-of-concept rằng ONF operate được spec process (WG → TSC → member vote → publish) chứ không chỉ đổi tên thương hiệu của Stanford.

Archive hiện tại tại:

```
https://opennetworking.org/wp-content/uploads/2014/10/openflow-spec-v1.2.pdf
```

### Động lực: mở rộng khớp với fields vượt 12-tuple

Trong giai đoạn 2010-2011 deployment, vendor và researcher nhận diện gaps trong OF 1.0-1.1 khớp với structure. Vài pain points cụ thể:

**Gap 1: ARP details beyond just Ethernet type**. OF 1.0 khớp với `dl_type=0x0806` biết packet là ARP, nhưng không khớp với được ARP fields (op, sender IP, target IP). Implementing ARP security rules (anti-spoofing, rate limiting ARP flood) impossible without ARP field match.

**Gap 2: ICMP type/code granular match**. OF 1.0 khớp với `nw_proto=1` biết packet là ICMP, nhưng `tp_src/tp_dst` được reuse cho ICMP_TYPE/ICMP_CODE mà không có clear naming. Confusing và error-prone.

**Gap 3: TCP flags matching**. Firewall và IDS cần khớp với specific TCP flags (SYN flood detection = match `tcp_flags=SYN && !ACK`). OF 1.0 không có TCP flags field, phải workaround qua DPI.

**Gap 4: Metadata cross-table**. Pipeline nhiều table của OpenFlow 1.1 yêu cầu cơ chế truyền state giữa các table, nhưng không có standardized way. Vendor extensions (OVS register) filled gap với cách tiếp cận vendor-specific.

**Gap 5: Future-proof for new protocols**. Hard-coded 12-tuple means mỗi time có protocol mới (VXLAN, NVGRE, Geneve later), phải spec revision. Không scalable.

OXM TLV khớp với addresses cả 5 gaps với một cơ chế thống nhất: variable-length TLV encoding, extensible class space.

## 4.1.2 OXM TLV format

### Cấu trúc

Spec 1.2 §7.2.3 define OXM TLV header 32-bit:

| Offset | Field | Size (bit) | Description |
|--------|-------|------------|-------------|
| 0-15 | oxm_class | 16 | Match class (0x8000=OFPXMC_OPENFLOW_BASIC, >0xFFFF=vendor) |
| 16-22 | oxm_field | 7 | Field identifier within class |
| 23 | HM (has_mask) | 1 | 1 = value+mask, 0 = value only |
| 24-31 | oxm_length | 8 | Length of payload in bytes |

Following header là payload: value (oxm_length byte) + optional mask (nếu HM=1, thêm oxm_length byte mask).

### Các lớp (class) OXM

**OFPXMC_OPENFLOW_BASIC (0x8000)**: ONF-standardized fields. Field identifiers 0-40 spec-defined.

**OFPXMC_EXPERIMENTER (0xFFFF)**: Vendor extensions. Follow by 32-bit experimenter ID (IANA-assigned Private Enterprise Number) để nhận diện vendor, rồi field-specific payload.

**OFPXMC_NXM_0 (0x0000) và OFPXMC_NXM_1 (0x0001)**: Legacy Nicira Extensible khớp với classes, supported để backward compat với OVS switches running OF 1.0-era NXM. Mostly deprecated trong OF 1.2+.

### Các field phổ biến

Spec 1.2 §7.2.3.8 list Basic class fields. Selected entries:

| oxm_field | Name | Size (bit) | Description |
|-----------|------|------------|-------------|
| 0 | OXM_OF_IN_PORT | 32 | Ingress switch port |
| 1 | OXM_OF_IN_PHY_PORT | 32 | Physical port (pre-virtualization) |
| 2 | OXM_OF_METADATA | 64 | Table metadata (cross-table) |
| 3 | OXM_OF_ETH_DST | 48 | Ethernet dest MAC |
| 4 | OXM_OF_ETH_SRC | 48 | Ethernet source MAC |
| 5 | OXM_OF_ETH_TYPE | 16 | EtherType |
| 6 | OXM_OF_VLAN_VID | 12 | VLAN ID |
| 7 | OXM_OF_VLAN_PCP | 3 | VLAN Priority |
| 8 | OXM_OF_IP_DSCP | 6 | IP DSCP |
| 11 | OXM_OF_IP_PROTO | 8 | IP protocol |
| 12 | OXM_OF_IPV4_SRC | 32 | IPv4 source |
| 13 | OXM_OF_IPV4_DST | 32 | IPv4 dest |
| 14 | OXM_OF_TCP_SRC | 16 | TCP source port |
| 15 | OXM_OF_TCP_DST | 16 | TCP dest port |
| 21 | OXM_OF_ARP_OP | 16 | ARP opcode |
| 22 | OXM_OF_ARP_SPA | 32 | ARP sender protocol address |
| 23 | OXM_OF_ARP_TPA | 32 | ARP target protocol address |
| 24 | OXM_OF_ARP_SHA | 48 | ARP sender hardware address |
| 25 | OXM_OF_ARP_THA | 48 | ARP target hardware address |
| 36 | OXM_OF_MPLS_LABEL | 20 | MPLS label |
| 37 | OXM_OF_MPLS_TC | 3 | MPLS traffic class |

OF 1.2 spec define 40 basic fields. OF 1.3 add 4 more (PBB fields + metadata variants). OF 1.5 add packet_type OXM field.

### Nguồn gốc: di sản NXM

OXM format không phải invention from scratch của ONF. Nó kế thừa gần nguyên vẹn từ **NXM (Nicira Extensible Match)**, format mà Open vSwitch đã dùng từ 2008-2009 để extend OF 1.0 khớp với beyond 12-tuple. NXM original design do Ben Pfaff và Justin Pettit (both at Nicira) phát triển, sau khi Nicira phân tích pain points của 12-tuple.

Khi ONF FAWG design OF 1.2 khớp với format, decision straightforward: adopt NXM concept với minor cleanup (rename NXM_NX_* prefix thành OXM_OF_*, tweak numbering). Backward compatibility intentional: vendor đã triển khai OVS + NXM không phải rewrite matching engine, chỉ adapt wire format header. Transition frictionless.

Đây là pattern chung trong OpenFlow history: proven idea từ research/open-source (NXM in OVS) được formalize thành spec official. OXM là reincarnation của NXM, giống như OF 1.0 là reincarnation của GSMP (Phần 2.0), P4 là reincarnation của Active Networking (Phần 2.1).

> **Chủ đề trọng tâm:** OXM TLV extensible khớp với là architectural decision quan trọng hơn bất kỳ feature addition nào. Từ OF 1.2 trở đi, thêm khớp với field mới không cần spec revision breaking, chỉ cần add new oxm_field identifier. OF 1.3 add PBB, OF 1.5 add packet_type, không phá compatibility với 1.2 implementation. Extensibility này essential cho long-term spec viability.

## 4.1.3 Các phần mở rộng của khớp với field: ARP, ICMP, TCP flags, metadata

### ARP detail fields

OF 1.0 khớp với chỉ có `dl_type=0x0806` (detect ARP), không cho phép kiểm tra ARP fields bên trong. OF 1.2 thêm 5 OXM fields cho ARP:

- **ARP_OP (OXM_OF_ARP_OP)**: ARP opcode. Value 1 = Request, 2 = Reply.
- **ARP_SPA (Sender Protocol Address)**: IP address của sender (advertised trong ARP packet).
- **ARP_TPA (Target Protocol Address)**: IP address target mà sender hỏi.
- **ARP_SHA (Sender Hardware Address)**: MAC address của sender.
- **ARP_THA (Target Hardware Address)**: MAC address target (thường 00:00:00:00:00:00 trong Request).

Use cases:

**Anti-ARP-spoofing**: match flow rule cho phép chỉ ARP Reply có SHA chính xác khớp với MAC được gán cho port. Nếu SHA không match, drop.

```
match: eth_type=0x0806, arp_op=2, in_port=10
where arp_sha = <MAC-port-10-authorized>
  → action: forward normal
else → action: drop
```

**ARP rate limiting**: count ARP Request per source MAC, nếu vượt threshold, drop. Prevents ARP flood attack.

**ARP-specific policy**: VLAN A cho phép ARP for 10.0.0.0/24, VLAN B cho phép ARP for 192.168.0.0/16. Match ARP_TPA + VLAN, drop nếu không khớp với policy.

### ICMP detail fields

Tương tự ARP, OF 1.2 thêm:

- **ICMP_TYPE (OXM_OF_ICMPV4_TYPE)**: ICMP message type. Value 8 = Echo Request, 0 = Echo Reply, 3 = Destination Unreachable, 11 = Time Exceeded.
- **ICMP_CODE (OXM_OF_ICMPV4_CODE)**: sub-type within type. Ví dụ Type=3, Code=4 là "Fragmentation Needed".

Use cases:

**ICMP rate limiting**: ping flood detection. Match type=8 (Echo Request), count per source, drop nếu vượt threshold.

**Allow specific ICMP types**: firewall cho phép Echo Request/Reply (ping works) + Destination Unreachable (MTU path discovery), drop others.

**PMTUD preservation**: ensure ICMP Type=3 Code=4 (Fragmentation Needed) không bị firewall drop, critical cho Path MTU Discovery correctness.

### TCP flags field

OF 1.2 thêm **OXM_OF_TCP_FLAGS** (12 bit match, 12 bit mask). Flags field của TCP header bao gồm: URG, ACK, PSH, RST, SYN, FIN, ECE, CWR, NS, và 3 reserved bits.

Use cases:

**SYN flood detection**: match TCP packet với chỉ SYN flag set (không có ACK). Rate limit per source, drop excessive rate.

```
match: eth_type=0x0800, ip_proto=6, tcp_flags=0x002/0x017
# Mask 0x017 = SYN + ACK + FIN + RST + URG bits
# Value 0x002 = only SYN set
  → action: meter:1 (rate limit), forward
```

**Established connection tracking**: match packet có ACK set (established traffic). Allow bypass stateful firewall cho performance.

**Null scan detection**: match TCP với all flags=0 (unusual pattern, indicative of scan). Drop + log.

### Metadata field

**OXM_OF_METADATA** (64-bit): table metadata field introduced trong OF 1.1, formalize trong 1.2 OXM syntax. Dùng cross-table:

- Table 0 WRITE_METADATA=0x01 (ví dụ: tenant ID)
- Table 1 khớp với metadata=0x01, further routing

Metadata persist qua pipeline, không reset giữa table. Enable multi-table state passing without regex tricks.

Tổng quan: OF 1.2 khớp với extensions transform OpenFlow từ basic packet filter (12-tuple) thành general-purpose policy engine có thể express complex security rules, QoS decisions, và multi-table state machines.

## 4.1.4 Controller roles: EQUAL, MASTER, SLAVE

### Bối cảnh triển khai HA

Trong production deployment, một OpenFlow switch thường có nhiều controller configured cho HA:

- **Primary controller**: thường xử lý mọi traffic
- **Backup controller**: standby, take over khi primary down
- **Observer controller**: monitor-only, không programming

Trước OF 1.2, spec không define role semantics. Multiple controller connect đến switch đều có equal access, mỗi controller có thể send FLOW_MOD, nhận PACKET_IN, và các message khác. Không có coordination mechanism, risk conflict (2 controller cùng cài đặt contradictory flow).

OVS và một số controller vendor triển khai proprietary role negotiation, nhưng không interoperable. Cần standardization.

### Ba vai trò được định nghĩa

Spec 1.2 §6.3.5 formalize 3 controller roles:

**EQUAL (default)**: all controllers have equal access. Mỗi controller:
- Có thể send controller-to-switch messages (FLOW_MOD, PACKET_OUT, etc.)
- Nhận async messages (PACKET_IN, FLOW_REMOVED, etc.)
- Nhận error messages riêng (không shared)

Use case: simple development, monitoring scenarios, hoặc khi application đã có external coordination.

**MASTER**: exactly one controller per switch. Master:
- Full read-write access
- Nhận tất cả async messages
- Nhận error messages
- Switch will only accept Primary role modifications from Master

Use case: production deployment, single authoritative controller.

**SLAVE**: read-only observer. Slave:
- Cannot send FLOW_MOD, PACKET_OUT, GROUP_MOD, METER_MOD, etc.
- Can send FEATURES_REQUEST, STATS_REQUEST, BARRIER_REQUEST (query only)
- Does NOT receive PACKET_IN, FLOW_REMOVED, PORT_STATUS async
- Does receive Error messages về own request

Use case: backup controller idle, monitoring controller, distributed controller ONOS where one node is reader.

### generation_id: ngăn race condition

Khi switch có multiple controller và MASTER role changes, cần đảm bảo atomic transition (no split-brain với 2 MASTER simultaneously). OF 1.2 introduce **generation_id** concept:

- Mỗi role change request include generation_id (64-bit counter monotonically increasing)
- Switch track latest generation_id accepted
- Request với generation_id ≤ current = rejected

Protocol:
1. Controller A request MASTER với generation_id=5
2. Switch accept, current generation_id = 5
3. Controller B request MASTER với generation_id=4 (stale)
4. Switch reject với error (ROLE_STALE)
5. Controller B must query current generation_id, request with higher number

Concurrent role requests serialize via generation_id ordering. No split-brain possible.

OFPT_ROLE_REQUEST/REPLY messages specified trong §A.3.10:
- Role: OFPCR_ROLE_NOCHANGE (query only), OFPCR_ROLE_EQUAL, OFPCR_ROLE_MASTER, OFPCR_ROLE_SLAVE
- Generation ID: 64-bit

### Các mẫu hình trong môi trường production

**Active-standby HA**: 2 controller, primary có MASTER, backup có SLAVE. Nếu primary down, backup phát hiện qua echo timeout, request role transition to MASTER. Failover time typical 5-10 seconds.

**Cluster với leader election**: 3+ controller with distributed coordination (ZooKeeper, etcd). Cluster elect leader, leader requests MASTER on every switch, others remain SLAVE. ONOS Atomix uses pattern này (Phần 7.2).

**Multi-region deployment**: controllers trong 2 region, each region có MASTER cho switches local. No single global MASTER.

> **Chủ đề trọng tâm:** Controller roles từ OF 1.2 là prerequisite cho mọi production SDN deployment HA. Trước 1.2, multi-controller setup ad-hoc và unreliable. Từ 1.2, protocol-level guarantee về role atomicity cho phép SDN controller clusters (ODL, ONOS) achieve production-grade availability. Đây là feature "enterprise readiness" không glamorous nhưng essential.

## 4.1.5 Migration OF 1.0 → 1.2: không backward compat wire

### Wire incompatibility

Switch OF 1.0 và switch OF 1.2 không thể trực tiếp communicate với controller của other version. Lý do:

**Header version khác**: OF 1.0 = 0x01, OF 1.2 = 0x03. Message header start với version byte, switch với controller mismatch immediately reject.

**Match structure hoàn toàn khác**: OF 1.0 fixed 40-byte ofp_match struct. OF 1.2 variable-length OXM TLV list. Parser logic khác biệt, không compatible at binary level.

**Instruction framework new**: OF 1.0 có list actions. OF 1.2 có instructions (WRITE_ACTIONS, APPLY_ACTIONS, ...). Semantics khác.

**Message type additions**: OF 1.2 có OFPT_ROLE_REQUEST/REPLY không tồn tại trong 1.0. Controller 1.0 không biết message này.

### Negotiation qua OFPT_HELLO

Spec 1.2 §6.3.1 định nghĩa version negotiation qua HELLO handshake:

1. **Controller connect TCP**: TCP connection established.
2. **Both sides send HELLO**: mỗi bên gửi OFPT_HELLO với supported versions bitmap. Message format mới trong 1.2 cho phép advertise multiple version (bitmap), thay vì single version của 1.0.
3. **Negotiate highest common**: cả hai sides compare bitmap, chọn highest version both hỗ trợ. Nếu không có common version, HELLO_FAILED error và connection close.
4. **Continue with negotiated version**: subsequent messages dùng agreed version.

Bitmap format: 32-bit per chunk, bit N set = hỗ trợ version N+1. Ví dụ bitmap 0x0000000A = hỗ trợ version 2 (OF 1.1) và 4 (OF 1.3), không hỗ trợ 1 (OF 1.0) hay 3 (OF 1.2). Controller/switch có thể advertise arbitrary subset of supported versions.

### Thực tế deployment

**OVS giai đoạn 2012-2014** (OVS 1.9-2.3): supports OF 1.0 và OF 1.3 đồng thời, bỏ qua 1.1 và 1.2. Lý do:
- OF 1.0 cho backward compatibility với existing controller
- OF 1.3 cho new features (LTS version)
- OF 1.1 và 1.2 thời gian ngắn, sự áp dụng thấp, không justify engineering effort

**Hardware vendor**: HP ProCurve + NEC hỗ trợ 1.0 và 1.3 chủ yếu, 1.1/1.2 partial hoặc bỏ qua. Pica8, Centec whitebox switch hỗ trợ 1.3 là focus.

**Controller frameworks**: NOX 2008 chỉ 1.0. POX chỉ 1.0. Ryu hỗ trợ 1.0-1.5. Floodlight 1.0 + 1.3. OpenDaylight từ Helium (10/2014) focus 1.3.

Kết quả: OF 1.2 là "transient version" trong lịch sử. Được release để formalize OXM và controller roles, nhưng market sự áp dụng quick jump từ 1.0 sang 1.3 (LTS). OF 1.2 specific deployment hiếm thấy production. Phần 4.2 sẽ phân tích OF 1.3 và lý do nó trở thành de-facto LTS.

---

## Mạch nối với phần sau

Phần 4.1 đã phân tích OF 1.2 với OXM TLV extensible match, Các phần mở rộng của khớp với field (ARP, ICMP, TCP flags, metadata), controller roles HA, và migration wire incompatibility. Phần 4.2 tiếp theo phân tích OF 1.3, phiên bản LTS được industry đồng thuận, với meter table cho QoS, hỗ trợ PBB cho carrier Ethernet, auxiliary channels, và errata chain 1.3.0-1.3.5 duy trì backward compat 3 năm.

---

## ▶ Guided Exercise — Implement OXM TLV khớp với với OVS (audit P4.B4.1 thực hành)

**Mục tiêu:** Thực nghiệm OXM extensible khớp với trên OVS, verify ARP + TCP flags khớp với mà OF 1.0 không support.

**Bước:**

1. Create bridge với OF 1.3:
   ```bash
   $ sudo ovs-vsctl add-br br-oxm -- set bridge br-oxm protocols=OpenFlow13
   ```
2. Install rule khớp với ARP với OXM fields (OF 1.0 không hỗ trợ ARP op):
   ```bash
   # Drop ARP request from unknown MAC
   $ sudo ovs-ofctl -O OpenFlow13 add-flow br-oxm \
       "table=0,priority=100,arp,arp_op=1,arp_spa=10.0.0.0/24,arp_sha=aa:bb:cc:00:00:01,actions=drop"

   # Verify OXM encoded correctly
   $ sudo ovs-ofctl -O OpenFlow13 dump-flows br-oxm
   # Output shows arp fields inline
   ```
3. Install rule khớp với TCP flags (detect SYN flood):
   ```bash
   $ sudo ovs-ofctl -O OpenFlow13 add-flow br-oxm \
       "table=0,priority=100,tcp,tcp_flags=+syn-ack,actions=meter:1,NORMAL"
   # +syn = SYN bit set
   # -ack = ACK bit not set
   # Match SYN-only packet (connection initiation)
   ```
4. Verify OXM wire encoding qua `ovs-ofctl monitor`:
   ```bash
   $ sudo ovs-ofctl -O OpenFlow13 monitor br-oxm &
   # Generate ARP traffic → observe OXM TLV format
   ```

**POE:**
- **Predict:** OF 1.0 12-tuple có cho phép khớp với `arp_op=1` trực tiếp không?
- **Observe:** Không — OF 1.0 chỉ có 12 fields cố định (L2+L3+L4 basic). Phải dùng NXM extension. OXM TLV của 1.2 chuẩn hóa NXM thành spec standard.
- **Explain:** OXM class + field + length structure extensible — future protocol (VXLAN, Geneve) chỉ cần register new class + field ID, không cần revision spec.

**Cleanup:** `sudo ovs-vsctl del-br br-oxm`

---

## Tài liệu tham khảo

1. [OpenFlow Switch Specification Version 1.2 (Wire Protocol 0x03)](https://opennetworking.org/wp-content/uploads/2014/10/openflow-spec-v1.2.pdf), Open Networking Foundation, December 5, 2011. Spec chính thức 85 trang.
2. [Open vSwitch NXM Extension Reference](https://github.com/openvswitch/ovs/blob/master/include/openflow/nicira-ext.h), Open vSwitch project. NXM origin cho OXM design.
3. [OpenFlow Switch Specification Version 1.1.0](https://opennetworking.org/wp-content/uploads/2013/04/openflow-spec-v1.1.0.pdf), Stanford, February 28, 2011. Predecessor spec.
4. [OpenFlow Switch Specification Version 1.3.0](https://opennetworking.org/wp-content/uploads/2014/10/openflow-spec-v1.3.0.pdf), ONF, April 25, 2012. Successor spec, LTS.
5. [The Design and Implementation of Open vSwitch](https://www.usenix.org/system/files/conference/nsdi15/nsdi15-paper-pfaff.pdf), Pfaff, Pettit, Koponen et al., NSDI 2015. OVS implementation detail cho OXM.
6. [Open Networking Foundation - OpenFlow Specification Archive](https://opennetworking.org/sdn-standards/openflow/), ONF. Full archive of spec versions.
7. [OpenFlow 1.2 Adoption Analysis](https://www.sdnessentials.com/introduction-to-sdn-and-openflow/), SDN Essentials. Industry analysis sự áp dụng patterns.
8. [RFC 7047, OVSDB Management Protocol](https://www.rfc-editor.org/rfc/rfc7047), Pfaff, Davie, IETF, December 2013. Contemporaneous với OF 1.2 sự áp dụng.
9. [Software Defined Networks: A Comprehensive Approach, 2nd edition](https://www.sciencedirect.com/book/9780128045558/software-defined-networks), Göransson, Black, Culver, Morgan Kaufmann 2017. Chương 5.3.2 phân tích OF 1.2.
10. [Controller High Availability Patterns in SDN](https://ieeexplore.ieee.org/document/7057024), survey paper. Generic patterns applied với OF 1.2 controller roles.
