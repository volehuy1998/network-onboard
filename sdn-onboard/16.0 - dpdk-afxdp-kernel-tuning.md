# 16.0. Kernel + DPDK + AF_XDP Performance Tuning

> **Trạng thái:** Skeleton (C5 Architecture Phase, 2026-04-22)
> **Khối:** XVI, Expert Extension — Kernel + DPDK Performance Deep Dive
> **Phần:** Part 16 (Expert Extension)
> **Ebook mapping:** DPDK Programmer's Guide + Linux kernel networking Documentation
> **Plan:** `plans/sdn-foundation-architecture.md` §3.3 Block XVI (Expert Extension)
> **Prerequisites:** Part 9.2 (OVS kernel datapath), Part 9.3 (OVS userspace DPDK), Part 8 (Linux networking primer)

---

## Mục tiêu bài học

Sau khi hoàn thành phần này, bạn có thể:

1. **Phân biệt (Analyze)** ba data path options cho OVS: kernel datapath (default), DPDK userspace, AF_XDP, theo dimension latency/throughput/CPU cost/complexity.
2. **Áp dụng (Apply)** DPDK EAL + PMD setup cho OVS với 2-4 huge pages + CPU pinning trên Ubuntu 22.04.
3. **Đánh giá (Evaluate)** kernel tuning knobs (`net.core.rmem_max`, `net.core.netdev_budget`, `net.core.busy_poll`) so với bypass stack (DPDK).

---

## 16.0.1 Data path comparison: kernel vs DPDK vs AF_XDP

*Skeleton — nội dung sẽ bao gồm:* Throughput benchmarks (10-80 Mpps range), latency profile (kernel ~20μs, DPDK ~2μs, AF_XDP ~10μs), CPU cost model (DPDK burns 100% một core/poll mode vs kernel interrupt-driven).

## 16.0.2 DPDK EAL + PMD internals

*Skeleton — nội dung sẽ bao gồm:* Environment Abstraction Layer, Poll Mode Driver architecture, hugepages allocation (2MB/1GB), NUMA node pinning, mempool lifecycle.

## 16.0.3 AF_XDP zero-copy + XDP programs

*Skeleton — nội dung sẽ bao gồm:* AF_XDP socket architecture, umem (user-space memory) ring, XDP program attach points (driver/native vs SKB), XDP_REDIRECT pattern.

## 16.0.4 Kernel tuning knobs

*Skeleton — nội dung sẽ bao gồm:* `net.core.*` sysctls cho high-packet-rate workload, receive packet steering (RPS), receive flow steering (RFS), busy polling mode, NAPI budget tuning.

## 16.0.5 CPU pinning + NUMA topology awareness

*Skeleton — nội dung sẽ bao gồm:* `lscpu`, `numactl`, `isolcpus` kernel cmdline, OVS PMD thread pinning, IRQ affinity via `/proc/irq/*/smp_affinity`.

## 16.0.6 Profiling với perf + bpftrace

*Skeleton — nội dung sẽ bao gồm:* Measure packet drop via ethtool stats, perf counters cho cache miss, bpftrace probes cho packet latency per-CPU.

---

### ▶ Guided Exercise 1: Benchmark OVS kernel datapath vs DPDK

*Skeleton — nội dung sẽ bao gồm:* Setup 2 VMs với OVS kernel mode, measure throughput với pktgen → 2-3 Gbps; rebuild OVS-DPDK, setup hugepages + CPU pin, measure → 10-20 Gbps.

### ▶ Guided Exercise 2: AF_XDP zero-copy với XDP filter program

*Skeleton — nội dung sẽ bao gồm:* Attach XDP program tại driver level, redirect selected packets qua AF_XDP socket, userspace process packets với libbpf + libxdp.

### ▶ Lab 3: Full performance tuning workflow

*Skeleton — nội dung sẽ bao gồm:* Given 40Gbps NIC + 2-socket server, achieve wire-speed forwarding via combination of: hugepages, CPU isolation, IRQ affinity, OVS-DPDK PMD threads. Document config + benchmarks.

---

## Tài liệu tham khảo

1. [DPDK Programmer's Guide](https://doc.dpdk.org/guides/prog_guide/), DPDK project. Core architecture docs.
2. [Open vSwitch DPDK Install Guide](https://docs.openvswitch.org/en/latest/intro/install/dpdk/), OVS project. Production DPDK setup.
3. [Linux Kernel Networking Documentation](https://www.kernel.org/doc/html/latest/networking/index.html), kernel.org. Authoritative kernel networking reference.
4. [AF_XDP Documentation](https://www.kernel.org/doc/html/latest/networking/af_xdp.html), kernel.org. AF_XDP socket + umem model.
5. [XDP Tutorial](https://github.com/xdp-project/xdp-tutorial), XDP Project. Hands-on XDP programs.
6. [Red Hat Network Performance Tuning Guide](https://access.redhat.com/documentation/en-us/red_hat_enterprise_linux/9/html/monitoring_and_managing_system_status_and_performance/tuning-the-network-performance_monitoring-and-managing-system-status-and-performance), Red Hat. Practical sysctl tuning guide.
