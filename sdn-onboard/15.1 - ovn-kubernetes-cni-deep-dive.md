# 15.1. OVN-Kubernetes CNI — deep dive architecture

> **Trạng thái:** Skeleton (C5 Architecture Phase, 2026-04-22)
> **Khối:** XV, Expert Extension — Service Mesh + Kubernetes CNI
> **Phần:** Part 15 (Expert Extension)
> **Ebook mapping:** OVN-Kubernetes repository docs + Red Hat OpenShift SDN transition docs
> **Plan:** `plans/sdn-foundation-architecture.md` §3.3 Block XV
> **Prerequisites:** Part 13 (OVN foundation), Part 15.0 (service mesh overview), CKA basics

---

## Mục tiêu bài học

Sau khi hoàn thành phần này, bạn có thể:

1. **Mô tả (Understand)** OVN-Kubernetes architecture: ovnkube-master (controller translating K8s resources → OVN NBDB), ovnkube-node (per-node agent), ovnkube-controller (IPAM + route distribution).
2. **Phân tích (Analyze)** NetworkPolicy → OVN ACL translation semantic: ingress/egress rules, podSelector, namespaceSelector, port-range.
3. **Đánh giá (Evaluate)** OVN-K8s vs Calico BGP vs Cilium eBPF: performance, debugging complexity, NetworkPolicy expressiveness, service mesh integration.

---

## 15.1.1 OVN-Kubernetes components

*Skeleton — nội dung sẽ bao gồm:* ovnkube-master daemonset (deployment trong kube-system), ovnkube-node daemonset per-node, northd HA configuration, SBDB per-AZ strategy.

## 15.1.2 NetworkPolicy → OVN ACL translation

*Skeleton — nội dung sẽ bao gồm:* NetworkPolicy resource semantics, translation algorithm (podSelector → Port_Group, namespaceSelector → nested group), default-deny implementation, stateful ACL với conntrack.

## 15.1.3 Service + kube-proxy replacement

*Skeleton — nội dung sẽ bao gồm:* ClusterIP Service → OVN Load_Balancer, NodePort → chassisredirect + DNAT, ExternalTrafficPolicy=Local optimization, ExternalIP annotation.

## 15.1.4 Multi-cluster networking (OVN-IC)

*Skeleton — nội dung sẽ bao gồm:* OVN Interconnect (OVN-IC) cho multi-cluster, transit switch between clusters, VNI namespace isolation.

## 15.1.5 OpenShift-SDN migration → OVN-K8s

*Skeleton — nội dung sẽ bao gồm:* Historical OpenShift-SDN (pre-4.10) với custom CNI, migration path to OVN-K8s (OpenShift 4.12+ default), feature parity notes.

---

### ▶ Guided Exercise 1: Deploy OVN-K8s on kind cluster

*Skeleton — nội dung sẽ bao gồm:* kind create cluster với OVN-K8s CNI preset, verify ovnkube-master + ovnkube-node pods running, `ovn-nbctl show` from master pod, apply NetworkPolicy, observe OVN ACL creation.

### ▶ Lab 2: NetworkPolicy debug + trace

*Skeleton — nội dung sẽ bao gồm:* Apply 3-tier NetworkPolicy (web→app→db), use `ovn-trace` to verify policy path, intentionally break policy + debug với `ovn-sbctl lflow-list`.

---

## Tài liệu tham khảo

1. [OVN-Kubernetes Documentation](https://ovn-kubernetes.io/), OVN-Kubernetes project. Official architecture + deployment.
2. [OVN-Kubernetes GitHub](https://github.com/ovn-kubernetes/ovn-kubernetes), OVN-Kubernetes project. Source code + e2e tests reference.
3. [Red Hat OpenShift Network Operator Documentation](https://docs.redhat.com/en/documentation/openshift_container_platform/latest/html/networking/), Red Hat. OVN-K8s in production OpenShift.
4. [CNI Specification](https://github.com/containernetworking/cni), CNI project. CNI 1.x spec reference.
5. [Kubernetes NetworkPolicy](https://kubernetes.io/docs/concepts/services-networking/network-policies/), Kubernetes. NetworkPolicy reference.
