# v3.9.2 Baseline Reconfirmation (Phase R0)

> **Local OVS repo:** `C:\Users\voleh\Documents\ovs` (checked out at v2.17.9).
> **Commit:** `0bea06d9957e3966d94c48873cd9afefba1c2677` (2024-02-08, "Set release date for 2.17.9").
> **Plan:** `plans/sdn/v3.9.2-ovs-block-cornerstone-sweep.md` Phase R0.
> **Date:** 2026-04-28.
> **Purpose:** Confirm the v3.9.1 Q0 source-verify baseline still applies for the v3.9.2 cornerstone sweep. Replay three spot-check `grep` commands to verify the corrected method (`git checkout v2.17.9` plus `grep -nE "^<fn>" <file>`) still produces the v3.9.1 §0.3 expected output. Verify all four pre-commit hooks are wired and functional on this machine.

---

## 1. Repo state

### 1.1. OVS repo

```
$ cd C:/Users/voleh/Documents/ovs
$ git rev-parse HEAD
0bea06d9957e3966d94c48873cd9afefba1c2677
```

The OVS repo HEAD matches the v3.9.1 Q0 baseline commit exactly. No `git checkout v2.17.9` is needed because the repo is already at that commit. The repo is clean (no uncommitted changes).

### 1.2. Curriculum repo

```
$ cd C:/Users/voleh/Documents/network-onboard
$ git status --short
(only untracked files unrelated to v3.9.2 scope)
$ git log --oneline -1
230357f docs(plans): R-1 v3.9.2 ovs-block cornerstone-sweep plan
```

Branch: `docs/sdn-foundation-rev2`. HEAD is the v3.9.2 plan commit `230357f`, which builds on the v3.9.1 close commit `8c680ec` (CHANGELOG Reckoning #4).

---

## 2. Spot-check grep results

The plan §R0.3 calls for replaying three greps from v3.9.1 §0.3 to verify the corrected method still works.

### 2.1. ovsdb_rbac_(insert|update) in ovsdb/rbac.c

```
$ grep -nE "^ovsdb_rbac_(insert|update)" ovsdb/rbac.c
149:ovsdb_rbac_insert(const struct ovsdb *db, const struct ovsdb_table *table,
308:ovsdb_rbac_update(const struct ovsdb *db,
```

Expected per v3.9.1 §0.3 finding #5 and #6: lines 149 and 308. **MATCH.**

### 2.2. classifier_(insert|lookup) in lib/classifier.c

```
$ grep -nE "^classifier_(insert|lookup)" lib/classifier.c
690:classifier_insert(struct classifier *cls, const struct cls_rule *rule,
938:classifier_lookup__(const struct classifier *cls, ovs_version_t version,
1166:classifier_lookup(const struct classifier *cls, ovs_version_t version,
```

Expected per v3.9.1 §0.3 finding #14: line 690 and line 1166. The internal helper `classifier_lookup__` at line 938 is also surfaced (a bonus, not a violation, because the pattern matches `classifier_lookup` as a prefix). **MATCH.**

### 2.3. raft_run in ovsdb/raft.c

```
$ grep -nE "^raft_run" ovsdb/raft.c | head -2
1930:raft_run(struct raft *raft)
3755:raft_run_reconfigure(struct raft *raft)
```

Expected per v3.9.1 §0.3 finding #7: line 1930. The bonus `raft_run_reconfigure` at line 3755 is a separate function with the same prefix. **MATCH.**

---

## 3. Pre-commit hook status

### 3.1. Hook file present

```
$ ls -la .git/hooks/pre-commit
-rwxr-xr-x 1 voleh 197609 3098 Apr 27 12:29 .git/hooks/pre-commit
```

The hook file is present, executable, and 3098 bytes. The size matches the v3.9.1 Q-1.E hook content (the diff-only `--staged` enforcement architecture).

### 3.2. Hook content references all four scripts

```
$ head -30 .git/hooks/pre-commit
#!/usr/bin/env bash
# Governance pre-commit hook (auto-installed via scripts/pre-commit-install.sh).
#
# Enforces:
#   - GP-6 to GP-10:    anti-gaming check on staged curriculum .md
#   - GP-11 / Rule 16:  rubric leak check on staged curriculum .md
#   - Rule 17 (em-dash): em-dash (U+2014) check on staged curriculum .md
#   - Rule 17 (lang):    language detection check on staged curriculum .md
...
```

All four scripts are referenced. Python interpreter detection prefers `python` over `python3` per the v3.9.1 Q-1.E Windows fix (the Microsoft Store `python3` stub does not have the `lingua` package).

### 3.3. Hook functional check

The four hooks have already proved functional through the 18 commits of plan v3.9.1 plus the plan v3.9.2 entry-point commit `230357f`. No additional sentinel-commit test is needed for v3.9.2 R0.

---

## 4. Verdict

PASS. The v3.9.1 Q0 baseline still applies for the v3.9.2 sweep. All preconditions met for Phase R1.

| Check | Status |
|---|---|
| OVS repo at v2.17.9 | PASS |
| Curriculum repo on `docs/sdn-foundation-rev2` with v3.9.2 plan committed | PASS |
| Spot-check 1 (`ovsdb_rbac_*`) matches v3.9.1 §0.3 | PASS |
| Spot-check 2 (`classifier_*`) matches v3.9.1 §0.3 | PASS |
| Spot-check 3 (`raft_run`) matches v3.9.1 §0.3 | PASS |
| Pre-commit hook installed and references all four scripts | PASS |

---

> **Authority:** plan v3.9.2 Phase R0.
> **Author:** v3.9.2 baseline-reconfirm run, 2026-04-28.
> **Next step:** Phase R1 (9.8 SHA 5ca1ba9 resolution, Q6 carryover).
