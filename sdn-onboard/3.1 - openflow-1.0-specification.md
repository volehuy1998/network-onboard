# 3.1 — OpenFlow 1.0 specification

> **Trạng thái:** Skeleton — nội dung sẽ được viết ở bước S5 theo plan.
> **Khối:** III — Khai sinh OpenFlow
> **Phần:** Part 3
> **Ebook mapping:** `plans/ebook-coverage-map.md` → Goransson Ch5.1-5.2 (KEEP full)
> **Plan:** `plans/sdn-foundation-architecture.md` §3.3 Block III
> **Prerequisites:** Part 3.0

---

## Mục tiêu bài học

Sau khi hoàn thành phần này, bạn có thể:

1. **Mô tả (Understand)** OpenFlow 1.0 wire format: OFPT_HELLO, OFPT_FEATURES_REQUEST, OFPT_FLOW_MOD, OFPT_PACKET_IN, OFPT_PACKET_OUT.
2. **Phân tích (Analyze)** 12-tuple match fields: ingress port, Ethernet src/dst/type/VLAN ID/VLAN PCP, IP src/dst/ToS/protocol, TCP/UDP src/dst port.
3. **Giải thích (Understand)** actions: output (PORT, CONTROLLER, NORMAL, FLOOD, ALL), set-field, drop.
4. **Liên hệ (Analyze)** flow table single-table với limitations → motivation cho multi-table trong OF 1.1.

---

## Dàn bài

## 3.1.1 OpenFlow 1.0 spec công bố 31/12/2009 (Version 1.0.0)

*Sẽ phát triển: ONF predecessor Stanford; timeline 0.8.x → 1.0.0.*

## 3.1.2 Wire protocol: TCP 6633 plain / TCP 6653 TLS

*Sẽ phát triển: TCP port choice history; IANA assignment; TLS optional trong 1.0.*

## 3.1.3 Message types: Hello, Features, FlowMod, PacketIn, PacketOut, FlowRemoved, Error

*Sẽ phát triển: 10+ message types; symmetric (Hello, Echo) vs async (PacketIn, FlowRemoved) vs controller-to-switch.*

## 3.1.4 12-tuple match fields — chi tiết từng field

*Sẽ phát triển: phase từng field với wildcard mask; OFPFW_ALL.*

## 3.1.5 Actions list — semantics

*Sẽ phát triển: ordered action list; OUTPUT variants; SET_VLAN_VID; SET_DL_SRC/DST.*

## 3.1.6 Flow entry: match + priority + counters + timeouts + cookie + actions

*Sẽ phát triển: anatomy of flow entry; idle_timeout vs hard_timeout; cookie for controller bookkeeping.*

## 3.1.7 Single-table limitation và workaround (chaining via FLOOD/resubmit)

*Sẽ phát triển: cross-product explosion; workaround NXM register + resubmit (Open vSwitch specific).*

---

## Tài liệu tham khảo

*Danh mục nguồn sẽ được tổng hợp sau khi hoàn thành nội dung.*
