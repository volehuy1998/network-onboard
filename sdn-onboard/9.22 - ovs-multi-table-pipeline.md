# 9.22. OVS multi-table pipeline — `goto_table`, `resubmit`, action set

> **Môi trường thực hành:** Ubuntu 22.04 LTS, OVS 2.17.9, Mininet 2.3.0, kernel 5.15, topology 2 bridge OVS như hai router + 2 host khác subnet.
> **Khối:** IX, OVS internals + operations (Phần 9, bổ sung session 23 Phase D).
> **Phần:** Phần 9 (OVS).
> **Plan:** `plans/sdn-foundation-architecture.md` §F.4.2 — Phase D multi-table foundation.
> **Prerequisites:** Phần 9.4 (OVS CLI playbook), Phần 9.18 (OVS native L3 routing — primitive action được dùng trong L3 table), Phần 9.19 (flow table granularity — priority, match field).
> **Nguồn offline chính:**
> - `sdn-onboard/doc/ovs/OVS.pdf` Lab 6 *"Implementing Routing using multiple Flow Tables"* trang 116-135 (Crichigno, Sharif, Kfoury — University of South Carolina, NSF Award 1829698, document version 09-13-2021).
> - `sdn-onboard/doc/ovs/OVS.pdf` Exercise 2 *"Implementing Routing using Multiple Flow Tables"* trang 136-140 (cùng tác giả, document version 09-22-2021).
> - `sdn-onboard/doc/compass_artifact_wf-*.md` Chapter 8 *"Flow installation and editing"* (Anthropic OVS curriculum).
> **Nguồn online chính:** [OpenFlow Switch Specification 1.3.5 §5.1](https://opennetworking.org/wp-content/uploads/2014/10/openflow-switch-v1.3.5.pdf) *"Pipeline Processing"*, [`ovs-actions(7)`](https://man.openvswitch.org/ovs-actions.7.html) mục `goto_table` + `resubmit`, [`ovs-appctl(8)`](https://man.openvswitch.org/ovs-appctl.8.html) mục `ofproto/trace`.

---

## Mục tiêu bài học

Sau khi hoàn thành phần này, bạn có thể:

1. **Giải thích (Understand)** lý do OpenFlow 1.1 ra đời chỉ 6 tháng sau OpenFlow 1.0 để thêm multi-table, single-table không biểu diễn được pipeline ASIC của Broadcom Trident II.
2. **Phân biệt (Analyze)** `goto_table` (OpenFlow 1.1+ standard) và `resubmit` (OVS extension từ NOX), hai cơ chế recirculate khác nhau về action set, có hay không đồng bộ metadata, và có hay không cho phép recirculate ngược.
3. **Áp dụng (Apply)** pipeline 3-table theo Lab 6 Crichigno (Table 0 Classifier → Table 1 L3 Chuyển tiếp → Table 2 L2 Chuyển tiếp) để route giữa hai subnet `192.168.1.0/24` ↔ `192.168.2.0/24` trên topology 2-switch 2-host.
4. **Đánh giá (Evaluate)** khác biệt giữa pipeline 3-table tự viết tay và pipeline 50+ table tự sinh của OVN `ovn-northd`, khi nào người vận hành nên viết trực tiếp, khi nào giao cho OVN compiler.

---

Yêu cầu kiến thức:

- Flow entry structure: match + priority + actions + cookie + timeouts (Phần 9.19).
- Action gốc cho định tuyến L3: `mod_dl_src`, `mod_dl_dst`, `dec_ttl`, `output:N` (Phần 9.18).
- Thứ tự priority resolution: specific hơn → số cao hơn → match trước (Phần 9.19 §19.3).

---

## 9.22.1 Vì sao OpenFlow 1.0 chỉ tồn tại 18 tháng — bối cảnh mở đầu

Tháng 12 năm 2009, Open Networking Foundation (ONF) phát hành OpenFlow Switch Specification 1.0.0, bản đặc tả đầu tiên. Tháng 2 năm 2011, tức chỉ 14 tháng sau, bản 1.1.0 ra đời với một thay đổi kiến trúc triệt để: single flow table bị thay thế bằng **multi-table pipeline**. Trong lịch sử công nghiệp viễn thông, hiếm có standard nào thay đổi trừu tượng cốt lõi nhanh đến vậy. TCP/IP giữ cấu trúc từ 1981, VLAN 802.1Q giữ frame format từ 1998.

Lý do: khi triển khai OpenFlow 1.0 lên silicon Broadcom Trident (chip switch thương mại dòng Tomahawk/Trident của Arista, Dell, Cisco Nexus 9000), kỹ sư phát hiện không cách nào ánh xạ một single flow table của OpenFlow sang kiến trúc bên trong. ASIC Trident có **bảy table logic** (ingress parser → ingress ACL → L3 chuyển tiếp → L2 chuyển tiếp → egress ACL → queueing → egress parser). Muốn biểu diễn một đơn vị phần cứng thực tế, OpenFlow 1.0 cần phải giả lập toàn bộ bảy table qua một table ảo, dẫn đến bùng nổ table (tích chéo của tất cả khớp với field, có thể lên tới vài triệu entry cho một cấu hình đơn giản).

ONF TM WG ra báo cáo tháng 7/2010 chỉ ra vấn đề, và đến 02/2011 OpenFlow 1.1 công bố cấu trúc **N flow tables numbered 0 to N-1**, với hai instruction mới: `Goto-Table` và `Write-Metadata`. Bài học lâu dài: trừu tượng tốt phải giữ dáng dấp phần cứng thực, không cắt rời khỏi nó. Multi-table là thoả hiệp giữa tính "gọn gàng" của SDN và tính "bám sát phần cứng" của silicon.

### OVS đi trước chuẩn

Open vSwitch có cơ chế tương tự từ **trước** khi OpenFlow 1.1 ra đời, action `resubmit` đã tồn tại từ OVS 1.0 (tháng 6/2011, cùng thời điểm OpenFlow 1.1), kế thừa từ dự án NOX (Stanford, 2008). Nhóm phát triển OVS hiểu rõ hạn chế single-table nhiều năm trước ONF và đã có giải pháp riêng. Về sau OVS hỗ trợ cả hai: `goto_table` theo chuẩn OpenFlow 1.1+ và `resubmit` theo extension NOX/OVS. §9.22.3 phân tích khác biệt.

---

## 9.22.2 Kiến trúc pipeline multi-table — từ OpenFlow 1.3.5 spec

OpenFlow Switch Specification 1.3.5 §5.1 *"Pipeline Processing"* định nghĩa:

*"OpenFlow switches are required to have a minimum of one flow table, and can optionally have more. An OpenFlow switch with only a single flow table is valid, in this case pipeline processing is greatly simplified. The flow tables of an OpenFlow switch are sequentially numbered, starting at 0. Pipeline processing always starts at the first flow table: the packet is first matched against flow entries of flow table 0."*

Bốn quy tắc cứng mà multi-table pipeline phải tuân theo:

**Quy tắc 1 — Luôn bắt đầu ở table 0.** Mọi packet vào pipeline phải khớp với entry trong table 0 trước. Không có cách nào nhét packet trực tiếp vào table 5. Đây là thiết kế nhằm đơn giản hoá việc gỡ lỗi, khi packet "biến mất", bắt đầu theo dõi từ table 0.

**Quy tắc 2. Chỉ đi xuôi, không đi ngược.** Đặc tả ghi rõ: *"A flow entry can only direct a packet to a flow table number which is greater than its own flow table number"*. Table 3 có thể `goto_table=5` nhưng không thể `goto_table=2`. Đây là đảm bảo **tính kết thúc**, pipeline không lặp vô hạn.

**Quy tắc 3 — Action set tích luỹ, không thực thi ngay.** Khi packet khớp với entry ở table N với instruction `Apply-Actions`, action được thực thi ngay. Nhưng với instruction `Write-Actions`, action chỉ được ghi vào **action set** của packet, chưa thực thi. Cuối pipeline (khi không còn `goto_table`), toàn bộ action set được thực thi. Cơ chế này cho phép các table phía sau ghi đè quyết định của table phía trước.

**Quy tắc 4. Mỗi table chỉ khớp với tối đa một entry.** Khi packet khớp với được nhiều entry cùng table, entry priority cao nhất thắng. Match hai entry cùng priority cùng table là hành vi không xác định (OVS chọn ngẫu nhiên). Nên mọi entry cùng table phải có priority phân biệt hoặc khớp với field không giao nhau.

### Instruction và Action

Từ OpenFlow 1.1 trở đi, phân biệt hai khái niệm quan trọng:

**Instruction** là lệnh ở cấp *table entry* — `Apply-Actions`, `Write-Actions`, `Clear-Actions`, `Write-Metadata`, `Goto-Table`. Mỗi entry có tối đa một instruction mỗi loại.

**Action** là thao tác ở cấp *packet* — `output:N`, `mod_dl_src`, `dec_ttl`, v.v. Action nằm bên trong instruction `Apply-Actions` hoặc `Write-Actions`.

`ovs-ofctl` giản hoá cú pháp bằng từ khoá `action=` (hoặc `actions=`), đây thực ra là instruction `Apply-Actions` ngầm định. Cú pháp đầy đủ theo đặc tả phải là `instructions=apply_actions(output:1)` nhưng OVS nhận cú pháp rút gọn.

---

## 9.22.3 `goto_table` (OpenFlow 1.1+) vs `resubmit` (OVS extension)

Hai cơ chế cùng mục tiêu: chuyển packet qua table tiếp theo. Khác biệt quan trọng trong implementation:

### `goto_table=N`

Instruction theo chuẩn OpenFlow 1.1+. Đặc tính:

- Chỉ chuyển đến `N > table_hiện_tại` (theo quy tắc 2).
- Action set tích luỹ theo packet: các `Write-Actions` trước đó vẫn còn ở table đích.
- Field `metadata` (64-bit) được mang theo, chỉnh bằng `Write-Metadata` thì giá trị mới khả dụng ở table đích.
- Không cho phép `goto_table=N` khi N đã ghé qua, mỗi packet đi mỗi table tối đa một lần.

Ví dụ trong Lab 6 Table 0 Classifier:

```bash
sudo ovs-ofctl add-flow s1 "table=0,ip,action=goto_table=1"
```

Packet IP vào table 0 → instruction `Goto-Table=1` → chuyển sang table 1 để tra L3 chuyển tiếp.

### `resubmit(,N)` / `resubmit(port,N)`

OVS action không thuộc chuẩn OpenFlow, thừa hưởng từ NOX controller. Đặc tính:

- Có thể chuyển đến **bất kỳ table nào**, kể cả table nhỏ hơn (có thể gây vòng lặp: OVS giới hạn độ sâu đệ quy = 75 bằng cách huỷ packet).
- Action ngoài `resubmit` vẫn được thực thi sau khi quay về, không giống `goto_table` (chuyển giao điều khiển hoàn toàn).
- Có biến thể `resubmit(port,N)` thay `in_port` của packet trước khi tra table N, tiện cho mô hình hoá port ảo.
- Không dùng action set / metadata của OpenFlow: mà dùng thanh ghi `reg0`..`reg15` của OVS extension.

Ví dụ mẫu OVS extension:

```bash
# Tra table MAC, sau đó quay lại table forward
sudo ovs-ofctl add-flow br0 "table=0,dl_type=0x0800,action=resubmit(,10),resubmit(,20),output:2"
```

Packet đi qua table 10 (tra security), rồi table 20 (tra MAC), rồi về lại table 0 để thực thi `output:2`. Mẫu này phổ biến trong OVN và các controller thời NOX.

### Khi nào chọn cái nào

Trong OpenFlow pipeline truyền thống với ánh xạ phần cứng 1:1 (Broadcom Trident), dùng `goto_table` vì khớp ngữ nghĩa silicon. Trong OVS chạy userspace với logic phức tạp (OVN compiler, security gateway), dùng `resubmit` vì tính linh hoạt.

> **Điểm mấu chốt:** `ovn-controller` của OVN sinh flow với **cả hai**. Bố cục table của OVN dùng `goto_table` để đi giữa các tầng logic (ingress ACL → LB → routing → egress ACL), nhưng bên trong mỗi tầng có nhiều `resubmit` để module hoá sub-flow. Đọc kết quả `ovs-ofctl dump-flows br-int` trên host OVN sẽ thấy hỗn hợp.

---

## 9.22.4 Topology Lab 6 và pipeline 3-table

Topology Lab 6 (Crichigno et al., trang 5-7 Lab 6):

```
       192.168.1.0/24                         192.168.2.0/24
          h1 (.10)        s1 ─── s2        h2 (.10)
          MAC :01         (.1)   (.1)      MAC :02
          GW .1          MAC:05  MAC:06    GW .1
                     eth1/eth2     eth1/eth2
```

Hai host khác subnet (192.168.1.0/24 và 192.168.2.0/24), hai switch OVS (`s1` và `s2`) đóng vai router. MAC address được gán thủ công: h1=`:01`, h2=`:02`, s1 interface hướng về s2=`:05`, s2 interface hướng về s1=`:06`.

Cấu hình gateway trên switch:

```bash
# Gán IP cho switch interface (dùng làm default gateway từ góc nhìn host)
sudo ifconfig s1 192.168.1.1/24 up
sudo ifconfig s2 192.168.2.1/24 up
```

### Pipeline ba table

**Table 0 — Classifier.** Phân biệt ARP và IP.

```bash
# s1 và s2: ARP xử lý như switch truyền thống
sudo ovs-ofctl add-flow s1 "table=0,arp,action=normal"
sudo ovs-ofctl add-flow s2 "table=0,arp,action=normal"

# IP → chuyển sang table 1 để tra routing
sudo ovs-ofctl add-flow s1 "table=0,ip,action=goto_table=1"
sudo ovs-ofctl add-flow s2 "table=0,ip,action=goto_table=1"
```

Hai entry mỗi switch. ARP broadcast đi qua action `normal` (dựa vào MAC learning nội bộ OVS). IP packet được chuyển sang pipeline dedicated.

**Table 1 — Layer 3 Chuyển tiếp.** Sửa MAC + giảm TTL + chuyển tiếp sang table 2.

```bash
# s1 xử lý gói IP đến host local h1
sudo ovs-ofctl add-flow s1 \
  "table=1,ip,nw_dst=192.168.1.10,action=mod_dl_src=00:00:00:00:00:05,mod_dl_dst=00:00:00:00:00:01,dec_ttl,goto_table=2"

# s1 xử lý gói IP đi sang subnet 192.168.2.0/24 (đến s2)
sudo ovs-ofctl add-flow s1 \
  "table=1,ip,nw_dst=192.168.2.0/24,action=mod_dl_src=00:00:00:00:00:01,mod_dl_dst=00:00:00:00:00:05,dec_ttl,goto_table=2"

# s2 tương tự cho chiều ngược
sudo ovs-ofctl add-flow s2 \
  "table=1,ip,nw_dst=192.168.2.10,action=mod_dl_src=00:00:00:00:00:06,mod_dl_dst=00:00:00:00:00:02,dec_ttl,goto_table=2"
sudo ovs-ofctl add-flow s2 \
  "table=1,ip,nw_dst=192.168.1.0/24,action=mod_dl_src=00:00:00:00:00:02,mod_dl_dst=00:00:00:00:00:06,dec_ttl,goto_table=2"
```

Chú ý cấu trúc sửa MAC: khi s1 chuyển packet sang s2, MAC src được gán thành MAC của s1 hướng s2 (`:01`), MAC dst gán thành MAC của s2 hướng s1 (`:05`). Đây chính là hành vi router truyền thống, thay MAC L2 ở mỗi chặng, giữ nguyên IP L3.

**Table 2 — Layer 2 Chuyển tiếp.** Quyết định port ra dựa trên MAC đích.

```bash
sudo ovs-ofctl add-flow s1 "table=2,dl_dst=00:00:00:00:00:01,action=output:1"   # về h1
sudo ovs-ofctl add-flow s1 "table=2,dl_dst=00:00:00:00:00:05,action=output:2"   # sang s2
sudo ovs-ofctl add-flow s2 "table=2,dl_dst=00:00:00:00:00:02,action=output:1"   # về h2
sudo ovs-ofctl add-flow s2 "table=2,dl_dst=00:00:00:00:00:06,action=output:2"   # sang s1
```

Tổng cộng: **6 flow entry cho mỗi switch** (2 classifier + 2 L3 + 2 L2) = 12 entry cho cả topology. So với single-table, tiết kiệm đáng kể khi mở rộng, thêm một subnet mới chỉ cần thêm entry ở table 1 và 2, không cần viết lại toàn bộ.

### Trace packet qua pipeline

Lab 6 (trang 19) dùng `ovs-appctl ofproto/trace` để theo dõi packet IP từ h1 sang h2 qua s1:

```bash
sudo ovs-appctl ofproto/trace s1 \
  "ip,nw_src=192.168.1.10,nw_dst=192.168.2.10,dl_src=00:00:00:00:00:01,dl_dst=00:00:00:00:00:05"
```

Kết quả kỳ vọng (doc-plausible, sẽ kiểm chứng ở C1b):

```
Bridge: s1
Flow: ip,in_port=ANY,vlan_tci=0x0000,dl_src=00:00:00:00:00:01,dl_dst=00:00:00:00:00:05,
      nw_src=192.168.1.10,nw_dst=192.168.2.10,nw_proto=0,nw_tos=0,nw_ecn=0,nw_ttl=0

bridge("s1")
-------------
 0. ip, priority 32768
    goto_table:1
 1. ip,nw_dst=192.168.2.0/24, priority 32768
    mod_dl_src:00:00:00:00:00:01
    mod_dl_dst:00:00:00:00:00:05
    dec_ttl
    goto_table:2
 2. dl_dst=00:00:00:00:00:05, priority 32768
    output:2

Final flow: ip,nw_src=192.168.1.10,nw_dst=192.168.2.10,dl_src=00:00:00:00:00:01,
            dl_dst=00:00:00:00:00:05,nw_ttl=0
Megaflow: recirc_id=0,eth,ip,in_port=ANY,nw_dst=192.168.2.0/24,nw_frag=no
Datapath actions: set(eth(src=00:00:00:00:00:01,dst=00:00:00:00:00:05)),set(ipv4(ttl=-1)),2
```

Ba khối quan trọng khi đọc kết quả này:

- **`0. ip, priority 32768`**: match ở table 0. Instruction `goto_table:1`.
- **`1. ip,nw_dst=192.168.2.0/24, priority 32768`**: match ở table 1. Action sửa MAC + `dec_ttl` + `goto_table:2`.
- **`2. dl_dst=00:00:00:00:00:05`**: match ở table 2. Action cuối `output:2`.

`Datapath actions:` ở cuối hiển thị chuỗi action mà datapath trong kernel sẽ thực thi. Đây là bản "đã biên dịch" của toàn bộ pipeline thành một chuỗi action tuyến tính.

---

## 9.22.5 Mở rộng lên 5-table pipeline

Compass Chapter 8 giới thiệu mở rộng Lab 6 thành 5-table canonical pipeline cho production (gần với OVN logical switch layout):

```
Table 0 — Port security + VLAN decap
Table 10 — Ingress ACL
Table 20 — Destination lookup (L2 MAC table hoặc L3 route table)
Table 30 — Egress ACL
Table 40 — Output + VLAN encap
```

Nguyên tắc thiết kế:

- **Khoảng cách 10 giữa các table** (0/10/20/30/40 thay vì 0/1/2/3/4): để chèn table mới sau này không phải đánh số lại. Quy ước này cũng dùng trong bố cục `ovn-northd`.
- **Ingress ACL riêng khỏi Egress ACL**: ACL sau NAT (egress) thấy IP đã bị sửa, ACL trước NAT (ingress) thấy IP gốc. Sự phân biệt này quan trọng khi gỡ lỗi.
- **Gộp tra cứu đích L2+L3**: một table cho table MAC và table FIB L3, match trên `dl_dst` hoặc `nw_dst` tuỳ `dl_type`. Cách này đơn giản hơn tách L2 và L3.

### Action set tích luỹ qua pipeline

Khi dùng `Write-Actions` thay `Apply-Actions`, hiệu ứng tích luỹ:

```bash
# Entry table 10 (Ingress ACL): chỉ thêm action vào set, chưa thực thi
sudo ovs-ofctl add-flow br0 \
  "table=10,ip,tcp,tp_dst=22,action=write_actions(set_queue:1),goto_table=20"

# Entry table 40 (Output): khi hết pipeline, action set được thực thi
sudo ovs-ofctl add-flow br0 "table=40,dl_dst=00:00:00:00:00:02,action=output:3"
```

Packet khớp với entry table 10 sẽ có `set_queue:1` trong action set. Khi ra khỏi pipeline (table 40 `output:3`), action set (`set_queue:1`) được áp dụng trước rồi `output:3`, kết quả: packet được đặt vào hàng đợi 1 rồi xuất ở port 3.

Hạn chế: action set chỉ chứa **một** action mỗi loại (một `output`, một `set_queue`, một `dec_ttl`, v.v.). Entry ở table sau ghi đè entry ở table trước, nhưng không cộng dồn cùng loại.

---

## 9.22.6 Field metadata — truyền state giữa các table

OpenFlow 1.1+ định nghĩa `metadata` là field 64-bit chỉ có trong pipeline (không xuất hiện trong packet thật). Cú pháp:

```bash
# Entry table 10: đánh metadata 0x100 cho ACL "permit HTTP"
sudo ovs-ofctl add-flow br0 \
  "table=10,ip,tcp,tp_dst=80,action=write_metadata:0x100,goto_table=20"

# Entry table 40: kiểm metadata trước khi output
sudo ovs-ofctl add-flow br0 \
  "table=40,metadata=0x100,dl_dst=00:00:00:00:00:02,action=output:3"
```

Mặt nạ cho metadata: `metadata:0x100/0xff00`, chỉ khớp với 8 bit cao. Cơ chế này cho phép table phía sau biết quyết định của table phía trước mà không cần ghi vào field thật của packet (ví dụ: không muốn sửa VLAN tag chỉ để truyền state).

### Thanh ghi — OVS extension

OVS mở rộng bằng 16 thanh ghi `reg0`..`reg15` (mỗi thanh 32-bit) và `xreg0`..`xreg7` (mỗi thanh 64-bit tạo từ cặp `reg`). Khác biệt với metadata:

- Metadata: chuẩn OpenFlow, 64-bit duy nhất.
- Thanh ghi: OVS extension, 512-bit tổng cộng, linh hoạt hơn.

OVN dùng cả hai: `metadata` để lưu ID của logical datapath (ID của logical switch/router hiện tại), `reg0`..`reg15` để lưu logical input port, output port, conntrack zone, ECMP hash, v.v.

---

## 9.22.7 So sánh với OVN — `ovn-northd` sinh 50+ table

Người vận hành OVS viết Lab 6 với 3 table thủ công. Người vận hành OVN không viết flow trực tiếp, họ khai báo logical switch + logical router + ACL qua `ovn-nbctl`, `ovn-northd` biên dịch thành logical flow, `ovn-controller` dịch tiếp thành OpenFlow flow trên `br-int`.

Kết quả: `br-int` trên một compute host OVN môi trường production thường có **50+ table** với hàng nghìn flow entry. Cấu trúc pipeline OVN (theo man page `ovn-architecture(7)`):

```
Table 0-7   — Logical port binding + classification
Table 8-23  — Ingress pipeline (ACL, LB, NAT, L2 lookup, L3 lookup)
Table 24-31 — Tunneling (Geneve encap)
Table 32-47 — Egress pipeline (ACL, L2 output)
Table 48-63 — Local output
```

Mỗi logical datapath (switch hoặc router) có pipeline riêng được *ghép kênh* qua field `metadata`, metadata chứa ID của datapath, packet của datapath khác không lẫn vào nhau.

### Khi nào viết thủ công, khi nào dùng OVN

Viết flow thủ công như Lab 6 phù hợp khi:

- Cần *hiệu năng dự đoán được*: mọi entry là do bạn viết, không có bùng nổ flow ẩn.
- Cần *kiểm soát chặt*, ví dụ offload SR-IOV NIC yêu cầu mẫu flow cụ thể.
- Quy mô nhỏ: dưới 10 subnet, dưới 100 ACL rule.

Dùng OVN compiler phù hợp khi:

- Quy mô từ vài chục đến hàng ngàn logical switch/router.
- Cần tự động cô lập multi-tenant (conntrack zone riêng cho mỗi logical switch).
- Cần định tuyến phân tán: OVN tự sinh flow trên mỗi chassis.

> **Điểm mấu chốt:** Không có lựa chọn "đúng" tuyệt đối. OpenStack Neutron trước 2018 phần lớn dùng OVS + ML2 plugin sinh flow tay (cấu trúc giống Lab 6 mở rộng). Từ 2018-2020 chuyển sang ML2/OVN — OVN compiler sinh 50+ table tự động. Lý do chính: quy mô. Khi một compute host có 200 VM từ 50 tenant, viết tay flow không khả thi. Nhưng ở edge node hoặc NFV appliance, nhiều dự án vẫn quay về flow thủ công để kiểm soát độ trễ.

---

## Guided Exercise 1 — Trace multi-table pipeline

**Mục tiêu:** Quan sát trực tiếp hành vi `ovs-appctl ofproto/trace` trên pipeline 3-table Lab 6 và kiểm chứng các khẳng định trong bài học: (a) packet đi đúng qua 3 table theo thứ tự, (b) `dec_ttl` thực sự giảm TTL, (c) action set không bị mất qua `goto_table`.

**Giả thuyết cần kiểm chứng:**

> *"Với pipeline 3-table Lab 6, packet IP từ h1 (192.168.1.10) gửi đến h2 (192.168.2.10) sẽ đi qua table 0 → 1 → 2 theo đúng thứ tự. `ovs-appctl ofproto/trace` phản ánh 3 lần tra cứu riêng biệt, mỗi lần đều báo match + action tương ứng, và `Final flow` sẽ có TTL giảm đúng 1 đơn vị so với đầu vào."*

### Chuẩn bị

Đã load topology Lab 6 (`lab6.mn` + `set_MACs.sh`). Kiểm tra các flow đã cài:

```bash
sudo ovs-ofctl dump-flows s1
# Kỳ vọng thấy 6 entry: 2 table 0 (arp/ip), 2 table 1 (L3), 2 table 2 (L2)
```

### Bước 1 — Trace nền

```bash
sudo ovs-appctl ofproto/trace s1 \
  "ip,nw_src=192.168.1.10,nw_dst=192.168.2.10,dl_src=00:00:00:00:00:01,dl_dst=00:00:00:00:00:05,nw_ttl=64"
```

Kết quả kỳ vọng (doc-plausible):

```
Flow: ip,in_port=ANY,...,nw_src=192.168.1.10,nw_dst=192.168.2.10,nw_ttl=64

bridge("s1")
-------------
 0. ip, priority 32768
    goto_table:1
 1. ip,nw_dst=192.168.2.0/24, priority 32768
    mod_dl_src:00:00:00:00:00:01
    mod_dl_dst:00:00:00:00:00:05
    dec_ttl
    goto_table:2
 2. dl_dst=00:00:00:00:00:05, priority 32768
    output:2

Final flow: ...,nw_ttl=63
Datapath actions: set(eth(...)),set(ipv4(ttl=-1)),2
```

Ba điểm cần kiểm chứng trong kết quả:

- **"bridge(s1)"** rồi **"0. ... 1. ... 2. ..."**: ba lần tra cứu tuần tự qua 3 table.
- **`nw_ttl=64`** trong `Flow:` → **`nw_ttl=63`** trong `Final flow:`, xác nhận `dec_ttl` hoạt động.
- **`Datapath actions: ...set(ipv4(ttl=-1))...`**: datapath trong kernel nhận chuỗi action cuối cùng đã biên dịch.

### Bước 2 — POE `goto_table` không đi ngược

Thử cài flow vi phạm quy tắc 2 (goto table số nhỏ hơn):

```bash
sudo ovs-ofctl add-flow s1 "table=2,ip,action=goto_table=1"
```

Kỳ vọng: OVS từ chối — `ovs-ofctl: actions are invalid with specified match (OFPBAC_BAD_TABLE_ID)`.

Hoặc nếu cài được (một số phiên bản cũ chấp nhận), khi theo dõi thử packet vào table 2, OVS huỷ sau 75 lần đệ quy:

```bash
sudo ovs-appctl ofproto/trace s1 "ip,..."
# Kỳ vọng: "stack too deep" hoặc "Actions: (none); packet dropped due to pipeline loop"
```

Giả thuyết "pipeline có thể lặp" bị bác bỏ. OpenFlow1.1+ và OVS đều ép buộc quy tắc "chỉ đi xuôi".

### Bước 3 — So sánh với `resubmit`

Thay `goto_table` bằng `resubmit` để thấy tính linh hoạt:

```bash
sudo ovs-ofctl del-flows s1 "table=0,ip"
sudo ovs-ofctl add-flow s1 "table=0,ip,action=resubmit(,1),resubmit(,2)"
```

Trace lại, kỳ vọng kết quả tương đương pipeline tuần tự nhưng **action `output:2` từ table 2 sẽ thực thi ngay sau khi packet quay về table 0** (không giống `goto_table` chuyển giao điều khiển hoàn toàn).

### Bài học

Multi-table pipeline là công cụ module hoá: mỗi table giải quyết một nhiệm vụ riêng (ACL, định tuyến, xuất), giảm kích thước table tổng và dễ gỡ lỗi. `goto_table` là "chuyển giao điều khiển", đi đâu không quay lại. `resubmit` là "gọi thủ tục con", quay về chỗ cũ thực thi action còn lại. Biết dùng đúng thời điểm là kỹ năng phân biệt kỹ sư OVS mới vào nghề và kỳ cựu.

---

## 9.22.8 Điểm cốt lõi cần nhớ

**Một.** OpenFlow 1.0 (12/2009) có single flow table. OpenFlow 1.1 (02/2011) thêm multi-table pipeline sau chỉ 14 tháng, vì single-table không biểu diễn được ASIC Broadcom Trident. Đây là thay đổi kiến trúc nhanh hiếm thấy trong lịch sử chuẩn hoá.

**Hai.** Bốn quy tắc cứng của multi-table: bắt đầu table 0, chỉ đi xuôi (table N → table M>N), action set tích luỹ qua pipeline và thực thi ở cuối, mỗi table chỉ khớp với tối đa một entry (theo priority).

**Ba.** `goto_table=N` (chuẩn OpenFlow 1.1+) chuyển giao điều khiển hoàn toàn. `resubmit(,N)` (OVS extension từ NOX) đi và quay về chỗ cũ, cho phép N nhỏ hơn, độ sâu đệ quy giới hạn 75.

**Bốn.** Pipeline 3-table Lab 6 (Classifier → L3 Chuyển tiếp → L2 Chuyển tiếp) là khuôn mẫu cơ bản cho router đơn giản. Mở rộng 5-table (thêm Ingress ACL + Egress ACL) là cấp độ môi trường production. OVN tự sinh 50+ table.

**Năm.** Metadata (64-bit chuẩn) và thanh ghi `reg0..15` (OVS extension) truyền state giữa các table mà không cần sửa field thật của packet. OVN dùng cả hai: metadata cho ID logical datapath, thanh ghi cho conntrack zone + ECMP hash.

**Sáu.** Viết flow thủ công phù hợp quy mô nhỏ + cần hiệu năng dự đoán được + cần kiểm soát chặt offload SR-IOV. OVN compiler phù hợp quy mô lớn + multi-tenant + định tuyến phân tán. Không có "đúng" tuyệt đối, hiểu cả hai, chọn theo bài toán.

---

## 9.22.9 `goto_table` — instruction chuyển control sang table cao hơn trong OF 1.1+ pipeline

**Khái niệm.** `goto_table` là **instruction** OpenFlow 1.1+ (không phải action) gắn vào một flow entry để báo cho switch: sau khi thực thi instruction `Apply-Actions` của entry này, đừng kết thúc pipeline, hãy chuyển packet sang flow table có số lớn hơn (`table_id` chỉ định) để khớp với tiếp. `goto_table` là cách *duy nhất* trong chuẩn OpenFlow để đi giữa các table, mọi multi-table pipeline đều được chuỗi lại nhờ nó. Phân biệt rạch ròi với action `output`, action `mod_dl_src`, hay action `dec_ttl`, vốn là *thao tác trên packet*; `goto_table` là *điều khiển luồng pipeline*. Cũng phân biệt với action `resubmit(,N)` của OVS extension — `resubmit` là action chứ không phải instruction, gọi table N rồi quay về tiếp tục action list, còn `goto_table` chuyển giao điều khiển một chiều, không quay lại.

**Lịch sử + bối cảnh.** Khi ONF công bố OpenFlow 1.0 tháng 12/2009 với một single flow table, trong vòng 6 tháng các nhà sản xuất silicon (Broadcom, Marvell) phản hồi rằng kiến trúc một bảng không ánh xạ được sang TCAM-pipeline thực tế của ASIC L2/L3, vốn có 5-7 bảng riêng (ingress parser, ingress ACL, L3 lookup, L2 lookup, egress ACL). ONF Technical Memorandum tháng 7/2010 đề xuất multi-table; OpenFlow 1.1 (02/2011, Section 5.2 "Pipeline Processing", Section 5.9 "Instructions") chính thức bổ sung `OFPIT_GOTO_TABLE`, `OFPIT_WRITE_METADATA`, `OFPIT_WRITE_ACTIONS`, `OFPIT_APPLY_ACTIONS`, `OFPIT_CLEAR_ACTIONS`. Trong số đó, `Goto-Table` là duy nhất thực hiện chuyển điều khiển giữa các table; bốn instruction còn lại đều thao tác trên *cùng một entry*. OpenFlow 1.2 (12/2011) và 1.3 (06/2012) giữ nguyên semantics; OF 1.5 (03/2015) thêm egress table và quy định `Goto-Table` từ ingress chỉ đi đến ingress table khác, từ egress chỉ đi đến egress table khác. OVS hỗ trợ `goto_table` từ phiên bản 1.10 (02/2013), khi codepath ofproto-dpif-xlate được tổ chức lại để xử lý multi-table.

**Vị trí trong kiến trúc.** Trong cây nguồn OVS (baseline `v2.17.9`, branch `branch-2.17`), `goto_table` xuất hiện ở bốn lớp. Một, mức **wire format**: `include/openflow/openflow-1.1.h` định nghĩa `enum` instruction `OFPIT11_GOTO_TABLE = 1` và `struct ofp11_instruction_goto_table` 8 byte. Hai, mức **OVS internal representation**: `include/openvswitch/ofp-actions.h` khai báo `struct ofpact_goto_table` chứa duy nhất một field `uint8_t table_id` (instruction được lưu chung danh sách với action trong cấu trúc `ofpacts`). Ba, mức **parse/encode/format**: `lib/ofp-actions.c` hàm `parse_GOTO_TABLE`, `encode_GOTO_TABLE`, `format_GOTO_TABLE` chuyển giữa text `goto_table:N`, struct OVS, và byte wire OF. Bốn, mức **xlate**: `ofproto/ofproto-dpif-xlate.c` case `OFPACT_GOTO_TABLE` trong `do_xlate_actions()` gọi tiếp `xlate_table_action()` để rẽ sang classifier của table đích. Phạm vi tác động: per-flow-entry. Mỗi flow entry chứa tối đa một instruction `Goto-Table` (theo spec 1.1+ Section 5.9 "Instructions"); muốn chuỗi N table phải có N flow entry với `goto_table` nằm rải rác qua các table.

**Vai trò.** `goto_table` đảm nhận ba vai trò gắn chặt với toàn bộ multi-table pipeline. Một, **chaining primitive**: là cơ chế chuẩn ONF duy nhất để đi từ table N sang table M > N; không có nó, mỗi table là một silo riêng. Hai, **action set transport**: instruction `Goto-Table` không xoá action set tích luỹ — mọi `Write-Actions` trước đó vẫn được chuyển sang table đích, cho phép các table sau ghi đè quyết định của table trước (ví dụ: table 0 `write_actions(output:1)`, table 5 `write_actions(output:2)` — kết quả cuối cùng là output:2 vì action set chỉ giữ một `output`). Ba, **modularization enabler**: cho phép viết pipeline như chuỗi giai đoạn (classify → ACL → routing → forwarding → egress) thay vì gộp vào một bảng cartesian, giảm số entry từ tích chéo xuống tổng cộng.

**Vì sao sinh ra.** Có hai động cơ kỹ thuật cốt lõi. Một, **silicon mapping**: ASIC L2/L3 thương mại có pipeline cứng nhiều stage; OF 1.0 single-table buộc phải giả lập toàn bộ stage qua một table ảo, bùng nổ entry — `goto_table` cho phép mỗi OF table ánh xạ 1:1 với một stage silicon. Hai, **rule design scalability**: với ACL 100 rule × FIB 1000 entry × MAC 5000 entry, single-table cần 100 × 1000 × 5000 = 500 triệu entry; multi-table chỉ cần 100 + 1000 + 5000 = 6100 entry chuỗi qua `goto_table`. Spec OF 1.1 Section 1.1 "Switch Components" giải thích trực tiếp: "An OpenFlow switch has multiple flow tables ... a packet matches against entries in successive tables". Triết lý: tin rằng compiler/operator biết phân tách logic theo stage; OF chỉ cung cấp primitive `goto_table` để chuỗi.

**Vấn đề giải quyết.** Bốn vấn đề cụ thể được `goto_table` address. Một, **table fan-out**: thay vì viết `100 × 5000` entry cho cartesian (ACL × MAC), viết 100 entry table 0 + 5000 entry table 1, kết nối bằng `goto_table:1`. Hai, **stage isolation**: ACL rule không bị trộn với MAC entry, gỡ lỗi từng giai đoạn riêng (`ovs-ofctl dump-flows br0 table=0` chỉ xem ACL). Ba, **cumulative action**: dùng cùng instruction `Goto-Table` để truyền action set qua các table, không cần copy entry. Bốn, **safety against loop**: spec ép `table_id > current_table_id`, đảm bảo pipeline luôn kết thúc hữu hạn (DAG forward); không có cách nào tạo loop bằng `goto_table` thuần (loop chỉ xảy ra qua `resubmit` hoặc `recirc_id` — đó là chủ đề khác, Phần 9.32).

**Tầm quan trọng.** `goto_table` là một trong ba primitive cốt lõi tuyệt đối của multi-table pipeline (cùng với match field và action set). Mọi triển khai OpenFlow controller cấp production (Faucet, ONOS, Ryu, OVN northd) đều sinh `goto_table` ở 80%+ flow entry không phải table cuối cùng. Trong OVN baseline branch-22.03, kết quả `ovs-ofctl dump-flows br-int` trên một compute host typical cho thấy ~70% flow entry kết thúc bằng `goto_table:N`, ~20% bằng `resubmit(,N)`, còn lại là `output`/`drop`/`controller`. Một kỹ sư OVS/SDN không hiểu thấu `goto_table` không thể đọc nổi `br-int` của OVN, không phân tích được pipeline OVN northd compile ra, không debug được tại sao packet "biến mất" giữa table 17 và table 24. `goto_table` ngang tier với `output` và `match`, nằm trong nhóm primitive bắt buộc của OpenFlow.

**Cơ chế hoạt động.** Wire format `struct ofp11_instruction_goto_table` đúng 8 byte (cố định, alignment 64-bit):

```
Offset  Field     Length  Mô tả                              Ví dụ goto_table:1
------  --------  ------  --------------------------------   --------------------
0-1     type      2 byte  OFPIT_GOTO_TABLE = 0x0001          0x00 0x01
2-3     len       2 byte  Length của struct = 0x0008         0x00 0x08
4       table_id  1 byte  Table đích (0-254; 0xff reserved)  0x01
5-7     pad       3 byte  Đệm cho alignment 64-bit           0x00 0x00 0x00
```

OVS internal struct `ofpact_goto_table` (định nghĩa `include/openvswitch/ofp-actions.h`):

```c
/* OFPACT_GOTO_TABLE
 *
 * Used for OFPIT11_GOTO_TABLE */
struct ofpact_goto_table {
    OFPACT_PADDED_MEMBERS(
        struct ofpact ofpact;
        uint8_t table_id;
    );
};
```

Codepath xử lý khi packet vào pipeline: ofproto-dpif-xlate gọi `do_xlate_actions()` để duyệt list ofpacts của entry hiện tại; gặp `OFPACT_GOTO_TABLE`, không đệ quy ngay mà ghi `ctx->table_id = goto_table->table_id` rồi `xlate_table_action()` thực hiện classifier lookup ở table đích và tiếp tục `do_xlate_actions()` cho entry mới. Bất biến: `goto_table->table_id > ctx->table_id` (ép trong `parse_GOTO_TABLE` lúc cài flow, trả lỗi `OFPBIC_UNSUP_INST` hoặc `OFPBIC_BAD_TABLE_ID` nếu vi phạm). Kết thúc: khi pipeline gặp entry không có `goto_table`, action set tích luỹ được thực thi và pipeline kết thúc.

**Cách kỹ sư vận hành thành thạo.** Sáu thao tác cốt lõi. Một, **cài `goto_table` đúng cú pháp `ovs-ofctl`**:

```bash
sudo ovs-ofctl add-flow br0 \
  "table=0,priority=100,ip,actions=goto_table:1"
sudo ovs-ofctl add-flow br0 \
  "table=1,priority=100,ip,nw_dst=10.0.0.0/8,actions=output:2"
```

Hai, **đọc `dump-flows` nhận biết chuỗi**: `ovs-ofctl dump-flows br0 | grep goto_table` cho thấy mọi entry chuyển tiếp pipeline; thiếu kết quả ở table không phải cuối = pipeline đứt. Ba, **xác nhận thứ tự tăng**: tự kiểm với `awk` đảm bảo mọi `goto_table:N` có N > table chứa entry. Bốn, **trace bằng `ovs-appctl ofproto/trace`** để xác thực transition (đoạn dưới). Năm, **tránh lẫn với `resubmit`**: nếu mục đích là chia module pipeline đơn giản — dùng `goto_table`; nếu cần gọi sub-pipeline rồi quay về thực thi action tiếp — dùng `resubmit`. Sáu, **đọc OVN flow**: với OVN, lệnh `ovn-trace` (Phần 13.7) decode pipeline logic ngược về NB ACL/route, còn `ofproto/trace` (Phần 9.25) cho phép xem chuỗi `goto_table` từ table 0 qua 24 đến 65 trên `br-int`.

**Phân loại.** `goto_table` thuộc nhóm **OpenFlow Instruction** trong taxonomy 5 nhóm instruction (`Apply-Actions`, `Clear-Actions`, `Write-Actions`, `Write-Metadata`, `Goto-Table`). Khác `Apply-Actions` (thực thi action ngay), khác `Write-Actions` (tích luỹ vào action set chờ cuối pipeline). `goto_table` là duy nhất thay đổi *next table*; bốn instruction kia thao tác *trên packet*. Trong tiểu phân loại "control flow", `goto_table` đối ứng với `output_to_controller` và `drop` — cả ba kết thúc một quyết định pipeline, nhưng `goto_table` tiếp tục bên trong switch, hai cái kia ra khỏi pipeline. Tier importance: cornerstone tuyệt đối, không thể thiếu trong bất kỳ multi-table pipeline nào.

**Quy trình sử dụng.** Quy trình thiết kế pipeline có `goto_table` gồm bảy bước. Một, **xác định chuỗi giai đoạn logic** (ví dụ: classify → ACL ingress → routing → ACL egress → output). Hai, **đánh số table cách quãng** (0, 10, 20, 30, 40 thay vì 0, 1, 2, 3, 4) để chèn stage mới sau này không phải đổi số. Ba, **viết entry table 0** với `actions=...,goto_table:10` để đẩy mọi packet vào ACL ingress. Bốn, **viết entry mỗi table tiếp theo** kết thúc bằng `goto_table:<table_kế_tiếp>` để chuỗi tiếp; entry table cuối cùng kết thúc bằng `output` thay vì `goto_table`. Năm, **luôn cài table-miss entry** (`priority=0,actions=...`) cho mọi table có thể nhận packet không match — không có table-miss và không khớp bất kỳ entry nào, packet bị drop âm thầm. Sáu, **kiểm tra bằng `ovs-appctl ofproto/trace`** cho ít nhất một packet đại diện mỗi tenant/protocol để xác nhận transition đầy đủ. Bảy, **document số table** trong README hoặc comment cookie (`cookie=0xacl10`) để on-call hiểu vai trò mỗi table.

**Khi xảy ra sự cố.** Bốn dấu hiệu trong production gọi tới `goto_table`. Một, **packet drop âm thầm giữa pipeline**: `ovs-ofctl dump-flows br0` cho thấy entry table 5 có `goto_table:10`, nhưng table 10 không có entry nào match → packet drop, counter `n_packets` của table 5 vẫn tăng nhưng không thấy ở phía egress; chẩn đoán bằng `ofproto/trace` hiển thị "Final flow ... no_match in table 10"; khắc phục: thêm table-miss entry cho table 10 hoặc bổ sung entry match. Hai, **`OFPBIC_BAD_TABLE_ID` khi cài flow**: cố cài `goto_table:N` với N không tồn tại (ví dụ N=255 trong khi switch chỉ có 254 table); khắc phục: kiểm n_tables qua `ovs-ofctl show br0`. Ba, **`OFPBIC_UNSUP_INST` khi cài flow**: cố cài `goto_table:M` với M ≤ table chứa entry; khắc phục: hoặc thiết kế lại số table, hoặc dùng `resubmit(,M)` (extension OVS, cho phép quay lại). Bốn, **OVN packet không đến đích**: `ovn-trace` báo packet đi đúng pipeline logic nhưng `ofproto/trace` trên `br-int` cho thấy mất ở giữa chuỗi `goto_table` table 17→24; nguyên nhân thường là ovn-controller chưa kịp install entry table 24 trên chassis vì SBDB Logical_Flow chưa sync; khắc phục: kiểm tra `ovn-sbctl lflow-list` so với `ovs-ofctl dump-flows br-int table=24`.

**Liên quan mật thiết.** `goto_table` ghép cặp chặt với năm thực thể. Một, `Apply-Actions` instruction (Phần 3.6) — `goto_table` đứng sau `Apply-Actions` trong cùng entry, action được thực thi trước rồi mới rẽ sang table đích. Hai, `Write-Actions` instruction — action set tích luỹ qua chuỗi `goto_table`, không bị xoá khi sang table mới. Ba, action `resubmit(,N)` (NXM extension, Phần 9.22.3) — đối lập về semantics: `resubmit` quay lại, `goto_table` không. Bốn, table-miss entry (`priority=0`) — luôn nên có ở mọi table có thể nhận packet qua `goto_table`. Năm, OVN logical pipeline (Phần 13.7, 13.19) — ovn-controller compile mọi logical flow thành chuỗi `goto_table` trên `br-int`, table 0-7 ingress port binding, 8-23 ingress logical pipeline, 24-31 tunneling, 32-47 egress logical pipeline.

**Khác biệt giữa các phiên bản.**

| Phiên bản | Hỗ trợ `goto_table` | Constraint table_id | Thay đổi semantics |
|-----------|---------------------|---------------------|--------------------|
| OF 1.0 (12/2009) | Không có | N/A | Single-table, không có instruction |
| OF 1.1 (02/2011) | Có (mới) | `table_id > current_table_id` | Instruction lần đầu xuất hiện |
| OF 1.2 (12/2011) | Có | Như 1.1 | OXM TLV match thay struct cứng |
| OF 1.3 (06/2012) | Có | Như 1.1 | Group/meter table thêm vào pipeline |
| OF 1.4 (10/2013) | Có | Như 1.1 | Bundle commit ảnh hưởng cài `goto_table` |
| OF 1.5 (03/2015) | Có | Phân biệt ingress/egress: từ ingress chỉ goto ingress table; từ egress chỉ goto egress table | Egress table thêm vào pipeline |

OVS xử lý qua một codepath duy nhất `parse_GOTO_TABLE`/`encode_GOTO_TABLE` (lib/ofp-actions.c) dùng chung cho mọi phiên bản 1.1+; phân biệt phiên bản chỉ ở wire encode (struct OF 1.1+ giống nhau, không đổi). OVS branch-2.17 default protocol `OpenFlow13` — `goto_table` mặc định khả dụng. Phiên bản Open vSwitch hỗ trợ `goto_table` từ 1.10 (02/2013); trước đó (1.0-1.9) chỉ có `resubmit` extension.

**Cách quan sát + xác minh.**

```bash
# 1. Cài flow với goto_table và xem entry trong ovs-ofctl dump-flows:
$ sudo ovs-ofctl -O OpenFlow13 add-flow br0 \
    "table=0,priority=100,ip,actions=goto_table:1"
$ sudo ovs-ofctl -O OpenFlow13 add-flow br0 \
    "table=1,priority=100,ip,nw_dst=192.168.2.0/24,actions=output:2"

$ sudo ovs-ofctl -O OpenFlow13 dump-flows br0
 cookie=0x0, duration=12.3s, table=0, n_packets=0, n_bytes=0, priority=100,ip
   actions=goto_table:1
 cookie=0x0, duration=8.7s, table=1, n_packets=0, n_bytes=0, priority=100,
   ip,nw_dst=192.168.2.0/24 actions=output:2

# 2. Trace một packet và xác nhận transition table 0 → table 1:
$ sudo ovs-appctl ofproto/trace br0 \
    "in_port=1,ip,nw_src=192.168.1.10,nw_dst=192.168.2.10"

Flow: ip,in_port=1,...,nw_src=192.168.1.10,nw_dst=192.168.2.10,nw_ttl=0

bridge("br0")
-------------
 0. ip, priority 100
    goto_table:1
 1. ip,nw_dst=192.168.2.0/24, priority 100
    output:2

Final flow: unchanged
Datapath actions: 2

# 3. Bắt OF wire bằng tcpdump để xem byte struct ofp11_instruction_goto_table:
$ sudo tcpdump -i any -nn 'tcp port 6653' -X | grep -A 2 "00 01 00 08"
# Match: 00 01 00 08 01 00 00 00 = type=1, len=8, table_id=1, pad=0x000000

# 4. Cố cài goto_table:0 từ table 5 (vi phạm forward-only) để thấy lỗi:
$ sudo ovs-ofctl -O OpenFlow13 add-flow br0 \
    "table=5,priority=100,ip,actions=goto_table:0"
ovs-ofctl: actions are invalid with specified match
   (OFPBIC_BAD_TABLE_ID)
```

**Source code tham chiếu.** Trong cây nguồn OVS (baseline `v2.17.9`, branch `branch-2.17`) `goto_table` liên quan tới năm vị trí. Một, `include/openflow/openflow-1.1.h` khai báo `enum ofp11_instruction_type` với `OFPIT11_GOTO_TABLE = 1` và `struct ofp11_instruction_goto_table` (8 byte: type, len, table_id, pad[3]). Hai, `include/openvswitch/ofp-actions.h` khai báo `struct ofpact_goto_table` chỉ chứa `uint8_t table_id`, đăng ký qua macro `OFPACT(GOTO_TABLE, ofpact_goto_table, ofpact, "goto_table")`. Ba, `lib/ofp-actions.c` chứa `decode_OFPIT11_GOTO_TABLE`, `encode_GOTO_TABLE`, `parse_GOTO_TABLE`, `format_GOTO_TABLE` — bốn hàm chuyển giữa wire byte, struct OVS, và text `goto_table:N`; hàm `parse_GOTO_TABLE` cũng kiểm `table_id` không vượt quá 0xff. Bốn, `ofproto/ofproto-dpif-xlate.c` case `OFPACT_GOTO_TABLE` trong hàm `do_xlate_actions()` set `ctx->table_id` rồi gọi `xlate_table_action(ctx, ctx->in_port, table_id, ...)` để thực hiện classifier lookup ở table đích; signature: `static void xlate_table_action(struct xlate_ctx *, ofp_port_t in_port, uint8_t table_id, bool may_packet_in, bool honor_table_miss, bool with_ct_orig, bool is_last_action, xlate_actions_handler *)`. Năm, `ofproto/ofproto.c` validate khi cài flow rằng `goto_table->table_id > flow->table_id` để ép forward-only. Spec gốc: ONF OpenFlow Switch Specification 1.5.1 Section 6.1 "Pipeline Processing" và Section A.2.5 "Goto-Table Instruction"; OpenFlow 1.1.0 Section 5.9.1 "Instructions" lần đầu định nghĩa.

**Trường hợp sự cố thực tế.** Sự cố trong môi trường OVN multi-tenant 2024-Q4: sau khi nâng cấp ovn-controller từ 22.03.7 lên 22.03.9 trên 64 compute node, một số tenant báo east-west traffic giữa hai VM cùng logical switch bị drop ngẫu nhiên (5-10% packet). `ovn-trace` trên SBDB cho kết quả pipeline đúng: table LS_IN_PORT_SEC_L2 → LS_IN_PRE_ACL → LS_IN_ACL → LS_IN_QOS_MARK → LS_IN_LB → LS_IN_STATEFUL → LS_IN_ARP_ND_RSP → LS_IN_DHCP_OPTIONS → LS_IN_L2_LKUP → output. Nhưng `ofproto/trace` trên `br-int` của host thấy packet kết thúc bất ngờ ở table 17 với "no_match"; trong khi cùng packet từ VM khác cùng logical switch lại đi xuyên qua thành công. Đối chiếu `ovs-ofctl dump-flows br-int table=17` so với `ovn-sbctl lflow-list <ls>` cho thấy hai entry quan trọng (LS_IN_L2_LKUP cho destination MAC unicast) chỉ có trên ~90% chassis; trên 10% chassis còn lại, ovn-controller đã install entry vào table 16 thay vì 17 do bug renumbering trong phiên bản 22.03.9 (thay đổi thứ tự stage trong `ovn-northd`). Hệ quả: entry `goto_table:17` ở table 16 dẫn packet đến table 17 trống, drop. Khắc phục tạm: rollback ovn-controller về 22.03.7 trên các chassis bị ảnh hưởng; long-term: chờ patch upstream `30aab12c0` rebuild stage table mapping trong ovn-controller. Bài học: chuỗi `goto_table` chỉ vững khi *cả hai phía* (entry chuyển và entry đích) đều được install nhất quán; bất kỳ skew giữa producer (ovn-controller) và state đích (table_id mapping) đều biểu hiện thành packet drop âm thầm. Cross-link: 20.0 §incident response decision tree, Phần 13.7 ovn-controller internals.

**Bài tập synthetic.** Lab độc lập trên một máy Linux có Mininet + OVS, kiểm chứng ba semantics cốt lõi của `goto_table`: (a) chuỗi 3 table với ARP responder → L2 forwarding → ACL drop, (b) action set tích luỹ qua chuỗi, (c) constraint forward-only.

```bash
# Bài 1: pipeline 3-table với hai host h1, h2
$ sudo mn --topo single,2 --switch ovsk --controller none
mininet> sh ovs-ofctl -O OpenFlow13 del-flows s1

# Table 0: ARP responder (đáp ARP cho 10.0.0.1) + IP đẩy sang table 1
$ sh ovs-ofctl -O OpenFlow13 add-flow s1 \
    "table=0,priority=200,arp,arp_tpa=10.0.0.1,
     actions=move:NXM_OF_ETH_SRC[]->NXM_OF_ETH_DST[],
     mod_dl_src:00:00:00:00:00:01,
     load:0x2->NXM_OF_ARP_OP[],
     move:NXM_NX_ARP_SHA[]->NXM_NX_ARP_THA[],
     move:NXM_OF_ARP_SPA[]->NXM_OF_ARP_TPA[],
     load:0x000000000001->NXM_NX_ARP_SHA[],
     load:0x0a000001->NXM_OF_ARP_SPA[],
     in_port"
$ sh ovs-ofctl -O OpenFlow13 add-flow s1 "table=0,priority=100,ip,actions=goto_table:1"

# Table 1: L2 forwarding theo dl_dst → goto_table:2 cho ACL kiểm tra
$ sh ovs-ofctl -O OpenFlow13 add-flow s1 \
    "table=1,priority=100,ip,dl_dst=00:00:00:00:00:02,actions=goto_table:2"

# Table 2: ACL drop nếu nw_proto=icmp, output:2 nếu khác
$ sh ovs-ofctl -O OpenFlow13 add-flow s1 "table=2,priority=200,icmp,actions=drop"
$ sh ovs-ofctl -O OpenFlow13 add-flow s1 "table=2,priority=100,ip,actions=output:2"

# Trace để xác nhận pipeline đi qua đúng 3 table:
$ sh ovs-appctl ofproto/trace s1 "in_port=1,tcp,nw_src=10.0.0.1,nw_dst=10.0.0.2,
 dl_src=00:00:00:00:00:01,dl_dst=00:00:00:00:00:02,tp_dst=80"
# Kỳ vọng: 0. → goto_table:1; 1. → goto_table:2; 2. → output:2

$ sh ovs-appctl ofproto/trace s1 "in_port=1,icmp,nw_src=10.0.0.1,nw_dst=10.0.0.2,
 dl_src=00:00:00:00:00:01,dl_dst=00:00:00:00:00:02"
# Kỳ vọng: 0. → goto_table:1; 1. → goto_table:2; 2. → drop (ICMP bị chặn)

# Bài 2: kiểm constraint forward-only
$ sh ovs-ofctl -O OpenFlow13 add-flow s1 "table=2,priority=50,ip,actions=goto_table:1"
# Kỳ vọng OVS từ chối: OFPBIC_BAD_TABLE_ID hoặc tương tự

# Bài 3: kiểm action set tích luỹ qua goto_table với write_actions
$ sh ovs-ofctl -O OpenFlow13 del-flows s1
$ sh ovs-ofctl -O OpenFlow13 add-flow s1 \
    "table=0,priority=100,ip,actions=write_actions(set_queue:5),goto_table:1"
$ sh ovs-ofctl -O OpenFlow13 add-flow s1 "table=1,priority=100,ip,actions=output:2"
$ sh ovs-appctl ofproto/trace s1 "in_port=1,ip,nw_src=10.0.0.1,nw_dst=10.0.0.2,
 dl_src=00:00:00:00:00:01,dl_dst=00:00:00:00:00:02"
# Kỳ vọng "Datapath actions: set(skb_priority(5)),2" — set_queue được giữ qua goto_table
```

**Lỗi thường gặp + tín hiệu chẩn đoán.** Bốn lỗi hay gặp khi vận hành. Một, **quên table-miss ở table đích**: entry `goto_table:N` đẩy packet vào table N nhưng table N không có entry match → packet drop; tín hiệu: counter table N có `lookup_count` tăng nhưng `matched_count` đứng yên; chẩn đoán: `ovs-ofctl dump-table-features br0` cho thấy `default = drop` nếu không có table-miss; khắc phục: thêm `table=N,priority=0,actions=drop` (rõ ràng) hoặc `actions=normal`. Hai, **set table_id sai trong cookie**: viết `cookie=0x10` cho ý là "table 10" nhưng quên đồng bộ với `goto_table:10` → khi maintainer tương lai grep theo cookie không match table thực; khắc phục: convention rõ ràng (cookie = encode table+rule_id). Ba, **vi phạm constraint forward-only nhưng dùng OF version cũ**: switch firmware OF 1.0 không hiểu `goto_table` → trả `OFPET_BAD_INSTRUCTION` hoặc fail silently; tín hiệu: `ovs-ofctl -O OpenFlow10 add-flow ... actions=goto_table:1` luôn lỗi; khắc phục: dùng `-O OpenFlow13`. Bốn, **nhầm `goto_table` là action**: viết `actions=output:1,goto_table:2` (OVS chấp nhận cú pháp này nhưng spec coi `goto_table` là instruction phải đứng sau action list); tín hiệu: hành vi không xác định trên switch hardware ngoài OVS; khắc phục: hiểu rõ `goto_table` là instruction, đặt cuối actions list, hoặc dùng cú pháp tách `instructions=apply_actions(output:1),goto_table:2`.

**So sánh với hệ khác.** Bốn so sánh quan trọng. Một, **`resubmit(,N)` của OVS extension** (Phần 9.22.3): cùng mục đích chuyển packet sang table N, nhưng `resubmit` là *action* (gọi sub-pipeline, quay về thực thi action còn lại trong list), `goto_table` là *instruction* (chuyển giao điều khiển hoàn toàn, không quay về). `resubmit` cho phép N nhỏ hơn, độ sâu đệ quy giới hạn 75; `goto_table` ép N > current. Hai, **`Goto-Group` instruction OF 1.1+**: chuyển packet sang group entry (multipath, broadcast); khác `goto_table` ở chỗ group thực hiện một action bucket cố định, không tiếp tục pipeline. Ba, **Linux netfilter chain hooks** (PREROUTING → INPUT/FORWARD → POSTROUTING): cũng là multi-stage pipeline, nhưng chain cố định theo netfilter design; không thể chèn chain mới động như `goto_table` chèn table OF. Iptables `--jump CHAIN` tương tự `goto_table` về mặt logic nhưng khác về wire-format. Bốn, **P4 control flow `apply { table_a.apply(); table_b.apply(); }`**: P4 cho phép biểu diễn pipeline dataflow toàn diện hơn (có nhánh if/else trên kết quả lookup, table abstraction), trong khi OF `goto_table` chỉ chuỗi tuyến tính theo `table_id`. Trade-off: P4 mạnh hơn nhưng yêu cầu compiler P4-to-target riêng; OF `goto_table` chuẩn hoá đơn giản, có sẵn trên mọi switch OF 1.1+.

---

## 9.22.10 `multi-table pipeline` — paradigm OpenFlow 1.1+ thay thế single-table OF 1.0

**Khái niệm.** `multi-table pipeline` là **paradigm xử lý packet** của OpenFlow 1.1+ trong đó một switch tổ chức flow entry thành **N flow table** đánh số `0, 1, 2, ..., N-1` (với N tối đa 254 trong wire format `uint8_t`, giá trị 0xFF reserved cho `OFPTT_ALL`). Một packet vào pipeline luôn bắt đầu ở table 0; mỗi table thực hiện một classifier lookup; entry khớp với chỉ định *instruction set* gồm tối đa năm instruction (`Apply-Actions`, `Clear-Actions`, `Write-Actions`, `Write-Metadata`, `Goto-Table`); instruction `Goto-Table` đẩy packet sang một table có số lớn hơn để tiếp tục, còn entry không có `Goto-Table` đánh dấu *cuối pipeline* — tại đó action set tích luỹ qua các table được thực thi atomic và packet rời pipeline. Phân biệt rạch ròi với *flow table* (đơn vị thành phần) và `goto_table` (instruction chuyển giữa table). `multi-table pipeline` là **kiến trúc tổng thể** kể cả khi N = 1 (degenerate case, tương đương OF 1.0 single-table) hoặc N = 64 (OVN br-int chassis production).

**Lịch sử + bối cảnh.** OpenFlow 1.0.0 (12/2009) chỉ định *single flow table* với ~12 match field cố định, không có instruction set, không có khái niệm chuỗi table. Triển khai trên silicon ASIC L2/L3 thương mại (Broadcom Trident, Marvell Prestera) phơi bày bất cập kiến trúc: ASIC có pipeline cứng nhiều stage (ingress parser → ingress ACL → L3 lookup → L2 lookup → egress ACL → queueing → egress parser), mỗi stage là một TCAM riêng; ép single OF table phải giả lập toàn bộ stage qua tích chéo (cartesian product) entry, bùng nổ kích thước. ONF Technical Memorandum tháng 7/2010 *"Issues with the OpenFlow 1.0 specification"* đề xuất multi-table; OpenFlow 1.1.0 (02/2011, Section 1.1 *"Switch Components"* + Section 5.2 *"Pipeline Processing"*) chính thức công bố `multi-table pipeline` cùng năm instruction và OXM TLV match. Sau đó OF 1.2 (12/2011) giữ nguyên architecture, mở rộng OXM. OF 1.3 (06/2012) thêm group table và meter table phụ trợ pipeline. OF 1.4 (10/2013) thêm bundle để cài flow nguyên tử. OF 1.5 (03/2015) phân tách *ingress* và *egress* pipeline (hai chuỗi table độc lập, mỗi chuỗi tuân `goto_table` forward-only riêng). OVS hỗ trợ multi-table pipeline thực sự từ phiên bản 1.10 (02/2013); trước đó (1.0-1.9) chỉ có `resubmit` extension thừa hưởng từ NOX (Stanford 2008). 14 tháng từ OF 1.0 đến OF 1.1 là thời gian chuẩn hoá ngắn hiếm thấy, dấu hiệu cho thấy single-table không khả thi sản xuất.

**Vị trí trong kiến trúc.** `multi-table pipeline` không phải một field, một action hay một message; nó là **wrapper kiến trúc** bao trùm toàn bộ datapath logic của switch. Trong OVS source baseline `v2.17.9`, pipeline được biểu diễn ở bốn lớp. Một, lớp **datapath kernel/userspace**: kernel datapath (`datapath/datapath.c` + `datapath/flow_table.c`) chỉ thấy một megaflow cache phẳng — pipeline là *abstraction phía userspace*; mọi packet upcall đầu tiên vào userspace ofproto-dpif sẽ traverse pipeline đầy đủ rồi sinh megaflow tổng hợp. Hai, lớp **ofproto-dpif xlate** (`ofproto/ofproto-dpif-xlate.c`): hàm `xlate_actions()` gọi `do_xlate_actions()` cho mỗi entry match, gặp `OFPACT_GOTO_TABLE` thì đệ quy `xlate_table_action()` rẽ sang table đích, traverse toàn bộ pipeline trong một upcall. Ba, lớp **classifier per-table** (`ofproto/ofproto.c` + `lib/classifier.c`): mỗi table là một `struct oftable` chứa `struct classifier cls`, lưu flow entry per-table. Bốn, lớp **OF wire**: message `OFPT_TABLE_FEATURES` (type 12) reply liệt kê N table, capability mỗi table (max_entries, supported instructions, supported actions); `OFPT_TABLE_MOD` (type 17) cấu hình behaviour table-miss. Phạm vi tác động: per-bridge. Mỗi bridge OVS có pipeline độc lập riêng (ví dụ `br-int` 64 table cho OVN, `br-ex` 1-2 table cho external).

**Vai trò.** `multi-table pipeline` đảm nhận năm vai trò không thể thay thế. Một, **modularization**: phân tách logic xử lý packet thành các stage độc lập (classify → ACL → routing → forwarding → egress), mỗi stage một table riêng — tương tự cách OS chia kernel thành module. Hai, **scalability**: thay vì cartesian product entry trong single-table, multi-table chỉ cần tổng cộng entry — ví dụ 100 ACL rule × 1000 FIB entry × 5000 MAC entry, single-table cần 5×10^8 entry, multi-table cần 6.1×10^3 entry, giảm 8.2×10^4 lần. Ba, **silicon mapping**: cho phép biên dịch 1:1 từ OF table sang TCAM stage trong ASIC, exploit hardware fast path. Bốn, **compositional reasoning**: kỹ sư có thể debug từng stage riêng (`ovs-ofctl dump-flows br-int table=24`); single-table debug cần toàn bộ entry. Năm, **logical-physical separation**: cho phép compiler high-level (như `ovn-northd`) sinh logical pipeline 64 stage rồi `ovn-controller` translate thành OF flow trên `br-int` per-chassis — tách concern controller logic và switch executor.

**Vì sao sinh ra.** Có ba động cơ kỹ thuật cốt lõi đồng thời. Một, **bùng nổ entry trong single-table** (đã giải thích ở vai trò). Hai, **silicon pipeline mismatch**: ASIC L2/L3 thương mại có stage cứng tự nhiên; ép single OF table buộc compiler phải sinh entry "phẳng hoá" stage, mất hoàn toàn ưu thế phần cứng — multi-table phục hồi ánh xạ tự nhiên. Ba, **rule design ergonomics**: viết 100 ACL rule + 5000 MAC entry tách biệt dễ đọc, dễ kiểm; viết 5×10^5 entry tích chéo không khả thi với con người. Spec OF 1.1 Section 1.1 nêu trực tiếp: *"OpenFlow switches contain multiple flow tables to allow for scalability and modularity"*. Triết lý thiết kế: chuẩn cung cấp primitive (table, instruction, `Goto-Table`) còn compiler/operator quyết định phân stage; chuẩn không đặt số table cố định.

**Vấn đề giải quyết.** Bốn vấn đề cụ thể được pipeline address. Một, **table fan-out** (cartesian explosion). Hai, **stage isolation**: mỗi stage logic độc lập, gỡ lỗi từng stage không cần quan tâm các stage khác. Ba, **hierarchical compilation**: cho phép tool như OVN northd sinh logical pipeline 64 stage, sau đó OF translator sinh hardware OF flow per-chassis — không có multi-table thì không thể compile hierarchy. Bốn, **operational checkpoint**: counter per-table (`lookup_count`, `matched_count`) là tín hiệu chẩn đoán quan trọng cho biết packet drop ở stage nào; single-table chỉ có một counter tổng, mất tín hiệu vị trí drop.

**Tầm quan trọng.** `multi-table pipeline` đứng ở **tier cornerstone tuyệt đối** của OpenFlow 1.1+ và OVS, ngang tier với `match field` và `action set`. Mọi controller production thời sau OF 1.1 (Faucet, ONOS, Ryu, OpenDaylight, OVN northd) đều giả định multi-table; không có controller nghiêm túc nào còn làm việc theo single-table. Trên một compute host OVN typical (baseline branch-22.03, 50 VM tenant, 10 logical switch), kết quả `ovs-ofctl dump-flows br-int | wc -l` cho ra ~3000-8000 flow entry trải đều qua ~64 table. Một kỹ sư SDN không hiểu thấu paradigm pipeline không thể: (a) đọc nổi `br-int` của OVN, (b) phân tích pipeline OVN northd compile ra, (c) debug packet "biến mất" giữa table 17 và table 24, (d) optimize OF rule design cho hardware offload, (e) viết lại stage sequence khi nâng cấp logic. Hiểu pipeline ngang tầm hiểu TCP three-way handshake với engineer mạng.

**Cơ chế hoạt động.** Quy trình traverse pipeline cho một packet gồm bốn pha. **Pha 1 (entry table 0).** Packet đến switch đầu tiên được parse (lấy field) rồi đưa vào classifier table 0; lookup theo priority cao nhất trong các entry match; nếu match một entry, lấy instruction set của entry đó. **Pha 2 (apply per-entry instruction).** Entry instruction set được thực thi theo thứ tự cố định ONF spec: `Meter` (nếu có) → `Apply-Actions` (thực thi action ngay, mutate packet) → `Clear-Actions` (xoá action set) → `Write-Actions` (gắn action vào action set, không thực thi ngay) → `Write-Metadata` (cập nhật field metadata 64-bit) → `Goto-Table` (rẽ sang table tiếp theo). Nếu không có `Goto-Table` → vào Pha 4. **Pha 3 (transition).** Packet (đã có thể bị mutate bởi `Apply-Actions`) cùng action set tích luỹ và metadata được chuyển sang table chỉ định bởi `Goto-Table`; classifier lookup ở table mới; lặp lại Pha 2. **Pha 4 (terminate).** Khi gặp entry không có `Goto-Table` (hoặc vô tình table-miss với action `output:CONTROLLER`), action set được apply atomic theo thứ tự chuẩn (`copy_ttl_in` → `pop` → `push_*` → `copy_ttl_out` → `dec_*` → `set` → `qos` → `group` → `output`); packet rời pipeline. Toàn bộ pha 1-4 happen atomic per-packet (không có context-switch giữa pha); một entry bị xoá bởi controller giữa pha 1 và pha 4 không ảnh hưởng packet đang in-flight.

**Cách kỹ sư vận hành thành thạo.** Bảy thao tác cốt lõi. Một, **list table support**: `ovs-ofctl -O OpenFlow13 dump-tables br0` cho biết switch có bao nhiêu table và max_entries mỗi table. Hai, **thiết kế stage logic**: vẽ pipeline trên giấy trước khi cài entry, đánh số cách quãng (0, 10, 20, 30, ...) để chèn stage mới sau này. Ba, **cài table-miss mọi table**: `ovs-ofctl add-flow br0 "table=N,priority=0,actions=drop"` (hoặc `actions=normal`) cho mọi N có thể nhận packet — không có table-miss, packet không khớp bị drop âm thầm. Bốn, **kiểm chuỗi `goto_table`**: chạy `ovs-ofctl dump-flows br0 | grep -E "table=|goto_table" | sort -t= -k2 -n` để verify mỗi table không phải cuối có entry chuyển tiếp. Năm, **trace pipeline đầy đủ**: `ovs-appctl ofproto/trace br0 "in_port=1,ip,nw_src=10.0.0.1,..."` show đường đi qua từng table, action set và metadata mọi pha. Sáu, **read OVN br-int**: `ovs-ofctl dump-flows br-int | awk '{print $5}' | sort -t= -k2 -n -u` cho biết những table nào active, ánh xạ với SBDB Logical_Flow stage qua `ovn-detrace`. Bảy, **đo per-table counter**: `ovs-ofctl dump-tables-desc br-int` + `ovs-ofctl dump-flows br-int table=N` so sánh `lookup_count` và `matched_count` cho biết table nào có drop.

**Phân loại.** Pipeline phân loại theo ba trục. Theo **scale**: single-table (degenerate, OF 1.0 hoặc OF 1.1+ với N=1), small (2-10 table, đa số manual config), medium (10-30 table, controller mid-tier như Faucet), large (30-64+ table, OVN northd compile). Theo **direction** (OF 1.5+): ingress-only pipeline (OF 1.1-1.4), bidirectional ingress+egress pipeline (OF 1.5+, hai chuỗi independent). Theo **hierarchy**: flat pipeline (mọi table cùng level, không group/meter), hierarchical pipeline (main table chuỗi qua `Goto-Table`, group table được gọi qua `output:group:N`). Theo **content origin**: hand-written (operator viết bằng `ovs-ofctl`), compiler-generated (OVN northd, ONOS Atomix), hybrid (90% compiler + 10% operator override). Tier importance: cornerstone tuyệt đối, không thể thiếu trong bất kỳ controller OF 1.1+ production.

**Quy trình sử dụng.** Quy trình thiết kế multi-table pipeline gồm tám bước. Một, **phân tích logic xử lý**: liệt kê các stage cần thiết (classify, ACL, routing, ARP, MAC learning, output). Hai, **đánh số table cách quãng** (0, 5, 10, 15, ...) để chèn stage mới không phải đổi số. Ba, **viết entry table 0** kết thúc bằng `goto_table:5` (đẩy mọi packet vào stage đầu). Bốn, **viết entry mỗi stage** kết thúc bằng `goto_table:<next>` cho stage trung gian, `output:N` (hoặc `drop`) cho stage cuối. Năm, **luôn cài table-miss** cho mọi table có thể nhận packet — `priority=0,actions=drop` (rõ) hoặc `actions=goto_table:<next>` (skip). Sáu, **kiểm bằng `ofproto/trace`** với 5-10 packet đại diện mỗi tenant/protocol; verify đường đi qua đúng stage dự kiến. Bảy, **document pipeline**: README hoặc comment cookie (`cookie=0xacl05`) ghi vai trò mỗi table cho on-call. Tám, **monitor counter per-table** trong production để phát hiện drop bất thường ở stage cụ thể.

**Khi xảy ra sự cố.** Năm dấu hiệu trong production gọi tới khái niệm pipeline. Một, **packet drop âm thầm giữa pipeline**: `dump-flows br0 table=N` không có entry match cho field cần thiết, packet vào table N rồi drop; phát hiện qua so sánh `lookup_count` (tăng) và `matched_count` (đứng yên). Hai, **OVN packet không đến đích trên single chassis**: chạy `ovn-trace` trên SBDB cho thấy logical pipeline đúng nhưng `ofproto/trace` trên br-int chassis bị mất giữa chuỗi `goto_table` — nguyên nhân thường là ovn-controller chưa kịp install entry hoặc table number bị shift do upgrade. Ba, **infinite loop nghi ngờ** (thực chất impossible với `goto_table` thuần do quy tắc forward-only, nhưng có thể xảy ra qua `resubmit` xen kẽ): trace báo "stack too deep" sau 75 đệ quy, packet drop. Bốn, **table-miss không expected**: switch firmware default table-miss = `OFPP_CONTROLLER` cho OF 1.0 vs `drop` cho OF 1.1+, dẫn đến controller bị flood packet-in nếu mix version. Năm, **counter-drift giữa stage**: table A `matched_count` cao hơn table B `lookup_count` (kế tiếp B = A's `goto_table` target) → packet bị mất giữa hai table; nguyên nhân: action `drop` xen vào hoặc `pop_vlan` thay đổi field khiến không khớp B.

**Liên quan mật thiết.** Pipeline ghép cặp chặt với sáu thực thể. Một, instruction `Goto-Table` (Phần 9.22.9) — primitive duy nhất nối các table. Hai, action set (Phần 4.0) — tích luỹ qua chuỗi table, thực thi atomic ở cuối pipeline. Ba, field `metadata` 64-bit và OVS extension `reg0..reg15` (Phần 9.22.6) — truyền state giữa các table mà không sửa packet thật. Bốn, table-miss entry (`priority=0`) — fallback bắt buộc mỗi table tránh drop âm thầm. Năm, `OFPT_TABLE_FEATURES` reply (Phần 4.7) — mô tả capability từng table cho controller discover. Sáu, OVN logical pipeline (Phần 13.16) — `ovn-northd` compile ~64 stage, `ovn-controller` per-chassis translate thành OF flow, mọi flow đều tham gia multi-table pipeline trên `br-int`.

**Khác biệt giữa các phiên bản.**

| Phiên bản | Hỗ trợ multi-table | Số table tối đa | Direction | Thay đổi chính |
|-----------|---------------------|------------------|-----------|----------------|
| OF 1.0 (12/2009) | Không (single-table) | 1 | Ingress-only ngầm | Không có instruction set |
| OF 1.1 (02/2011) | Có (paradigm mới) | 254 (`uint8_t` minus reserved) | Ingress-only | Lần đầu giới thiệu instruction `Goto-Table`, action set, metadata, group table |
| OF 1.2 (12/2011) | Có | 254 | Ingress-only | OXM TLV match thay struct cứng |
| OF 1.3 (06/2012) | Có | 254 | Ingress-only | Meter table + per-flow meter band |
| OF 1.4 (10/2013) | Có | 254 | Ingress-only | Bundle commit nguyên tử nhiều flow_mod, flow eviction policy per-table |
| OF 1.5 (03/2015) | Có | 254 mỗi chiều | Ingress + Egress (tách độc lập) | Pipeline egress mới, `Goto-Table` ép cùng chiều, packet register `pkt_register` |

OVS branch-2.17 default protocol `OpenFlow13` → multi-table mặc định khả dụng. OVS không support OF 1.5 đầy đủ tại baseline 2.17.9 (egress pipeline hỗ trợ một phần, branch master triển khai sau). Phiên bản OVS hỗ trợ multi-table pipeline thực sự từ 1.10 (02/2013); trước đó (1.0-1.9) chỉ có `resubmit` extension thay thế.

**Cách quan sát + xác minh.**

```bash
# 1. Liệt kê khả năng table support của switch
$ sudo ovs-ofctl -O OpenFlow13 dump-tables br-int
OFPST_TABLE reply (xid=0x2):
  table 0 ("classifier"):
    active=1234, lookup=5867432, matched=5867431
  table 17 ("ls_in_acl"):
    active=89, lookup=4521033, matched=4520988
  table 24 ("ls_in_l2_lkup"):
    active=3401, lookup=4520988, matched=4520975
  ...
  (64 table tổng cộng trên br-int của OVN compute host)

# 2. Xem table-features chi tiết (chỉ trên một số phiên bản OVS)
$ sudo ovs-ofctl -O OpenFlow13 dump-table-features br-int | head -30
table 0 ("classifier"):
  metadata: match=0xffffffffffffffff write=0xffffffffffffffff
  config=0x3
  max_entries=1000000
  instructions (table=255):
    next tables: 1,2,3,...,253
    instructions: apply_actions clear_actions write_actions write_metadata goto_table
  ...

# 3. Đếm tổng flow + phân bố theo table (OVN br-int)
$ sudo ovs-ofctl dump-flows br-int | wc -l
4823
$ sudo ovs-ofctl dump-flows br-int | grep -oE 'table=[0-9]+' \
    | sort | uniq -c | sort -rn | head
   1842 table=24
    412 table=17
    389 table=8
    ...

# 4. Trace một packet IP tenant đầy đủ qua pipeline 64-stage
$ sudo ovs-appctl ofproto/trace br-int \
    "in_port=tap-vm1,ip,nw_src=10.0.0.10,nw_dst=10.0.0.20,..." 2>&1 \
    | grep -E "^\s*[0-9]+\." | head -20
 0. priority 100, in_port=1
    set_field:0x1->reg13
    goto_table:8
 8. priority 50, ip,reg14=0x1
    goto_table:9
 9. priority 100, ip
    goto_table:10
 ...
 24. ip,reg14=0x1,nw_dst=10.0.0.20, priority 50
    set_field:00:11:22:33:44:55->eth_dst
    output:tap-vm2

# 5. Decode pipeline OVN thành logical stage
$ sudo ovn-detrace --ovs br-int < trace_output.txt
table 8  → SB Logical_Flow LS_IN_PRE_ACL
table 17 → SB Logical_Flow LS_IN_ACL
table 24 → SB Logical_Flow LS_IN_L2_LKUP
```

**Source code tham chiếu.** Trong cây nguồn OVS (baseline `v2.17.9`, branch `branch-2.17`), `multi-table pipeline` được hiện thực qua bảy vị trí. Một, **per-bridge table struct** `struct oftable` trong `ofproto/ofproto-provider.h` (chứa `struct classifier cls`, `n_flows`, `name`, `max_flows`, `eviction_policy`); mỗi bridge cấp phát mảng `oftable[256]` (kích thước max). Hai, **classifier per-table** (`lib/classifier.c`) — Tuple Space Search algorithm tìm flow priority cao nhất khớp với, gọi từ `ofproto-dpif-xlate.c` mỗi lần `xlate_table_action()`. Ba, **xlate engine** `ofproto/ofproto-dpif-xlate.c`: hàm `xlate_actions()` gọi `do_xlate_actions()` traverse instruction list của entry; case `OFPACT_GOTO_TABLE` set `ctx->table_id = goto->table_id` rồi gọi `xlate_table_action(ctx, in_port, table_id, may_packet_in, honor_table_miss, with_ct_orig, is_last_action, action_handler)`. Bốn, **OF wire format**: `include/openflow/openflow-1.1.h` định nghĩa `enum ofp11_flow_mod_command`, `OFPT11_TABLE_MOD`, `struct ofp11_table_mod`; `include/openflow/openflow-1.3.h` định nghĩa `OFPMP_TABLE_FEATURES`, `struct ofp13_table_features`. Năm, **table-miss entry handling** (`ofproto/ofproto.c` hàm `oftable_set_default_eviction()`) — mặc định OF 1.1+ table-miss = drop, OF 1.0 = packet-in to controller. Sáu, **table-stat**: `struct oftable` chứa counter `n_lookups`, `n_matched` cập nhật mỗi packet, expose qua `OFPST_TABLE` stats reply. Bảy, **table-mod config**: `OFPT_TABLE_MOD` message cập nhật `vacancy_events`, `eviction_policy`, `controller_count`. Spec gốc: ONF OpenFlow Switch Specification 1.5.1 Section 6.1 *"Pipeline Processing"*; OF 1.1.0 Section 5.2 *"Pipeline Processing"* lần đầu định nghĩa.

**Trường hợp sự cố thực tế.** Sự cố trong môi trường OVN multi-tenant 2024-Q3: khi nâng cấp `ovn-northd` từ 22.03.5 lên 22.03.8 trên cluster control plane (3 node bonded HA), vận hành ghi nhận north-south traffic bị drop ngẫu nhiên (~3% rate) cho subnet floating IP nhưng không ảnh hưởng east-west traffic giữa VM cùng logical switch. `ovn-trace` trên SBDB cho pipeline đúng (LR_IN_DEFRAG → LR_IN_UNSNAT → LR_IN_DNAT → LR_IN_IP_ROUTING → LR_OUT_SNAT → LR_OUT_EGR_LOOP → LR_OUT_DELIVERY). Trên một compute host bị ảnh hưởng, `ofproto/trace br-int "in_port=patch-port,ip,nw_dst=<floating_ip>"` cho thấy packet đi qua table 0-23 đúng, đến table 24 (LS_IN_L2_LKUP) khớp với entry chuyển tiếp `goto_table:25`, nhưng `dump-flows br-int table=25` *trống* trên ~10% chassis. Đối chiếu qua `ovn-detrace` so với SBDB cho thấy 22.03.8 `ovn-northd` đổi mapping LR_IN_DEFRAG từ table 25 sang table 24 (renumbering pipeline gain efficiency), nhưng `ovn-controller` 22.03.5 vẫn cache mapping cũ trên 10% chassis chưa restart đồng bộ. Hệ quả: chuỗi `goto_table` đứt đoạn — entry sinh bởi northd mới đặt ở table mới, nhưng controller cũ nhìn entry placement theo mapping cũ, nội dung table 25 thực tế trống. Khắc phục tạm thời: `systemctl restart ovn-controller` trên các chassis bị drift, force re-fetch SBDB Logical_Flow + recompute table mapping. Khắc phục dài hạn: upstream patch `c7a23bf1` ép `ovn-controller` invalidate cache khi phát hiện northd version skew, đẩy thêm metric `ovn-controller-stage-drift`. Bài học: pipeline multi-table chỉ vững khi *cả compiler (northd) và executor (ovn-controller per-chassis)* dùng cùng phiên bản stage map; bất kỳ skew nào giữa producer và consumer của pipeline biểu hiện thành drop âm thầm tại stage không match. Cross-link: 13.16 §13.16 OVN logical pipeline table_id mapping, 20.0 §incident response decision tree.

**Bài tập synthetic.** Lab độc lập trên Linux có Mininet + OVS, kiểm chứng ba semantics cốt lõi của `multi-table pipeline`: (a) chuỗi 4-stage độc lập, (b) action set tích luỹ và override, (c) counter per-table tách biệt drop signal.

```bash
# Bài 1: Pipeline 4-stage classify → ACL → route → output
$ sudo mn --topo single,3 --switch ovsk --controller none
mininet> sh ovs-ofctl -O OpenFlow13 del-flows s1

# Stage 0 (Classify): IP đẩy tới ACL, ARP đáp local
$ sh ovs-ofctl -O OpenFlow13 add-flow s1 \
    "table=0,priority=200,arp,actions=normal"
$ sh ovs-ofctl -O OpenFlow13 add-flow s1 \
    "table=0,priority=100,ip,actions=goto_table:10"
$ sh ovs-ofctl -O OpenFlow13 add-flow s1 \
    "table=0,priority=0,actions=drop"

# Stage 10 (ACL): drop ICMP, allow TCP/UDP đẩy tới route
$ sh ovs-ofctl -O OpenFlow13 add-flow s1 \
    "table=10,priority=200,icmp,actions=drop"
$ sh ovs-ofctl -O OpenFlow13 add-flow s1 \
    "table=10,priority=100,ip,actions=goto_table:20"
$ sh ovs-ofctl -O OpenFlow13 add-flow s1 \
    "table=10,priority=0,actions=drop"

# Stage 20 (Route): set dl_dst theo nw_dst, đẩy tới forwarding
$ sh ovs-ofctl -O OpenFlow13 add-flow s1 \
    "table=20,priority=100,ip,nw_dst=10.0.0.2,
     actions=mod_dl_dst:00:00:00:00:00:02,goto_table:30"
$ sh ovs-ofctl -O OpenFlow13 add-flow s1 \
    "table=20,priority=100,ip,nw_dst=10.0.0.3,
     actions=mod_dl_dst:00:00:00:00:00:03,goto_table:30"
$ sh ovs-ofctl -O OpenFlow13 add-flow s1 \
    "table=20,priority=0,actions=drop"

# Stage 30 (Output): ánh xạ dl_dst sang port
$ sh ovs-ofctl -O OpenFlow13 add-flow s1 \
    "table=30,priority=100,dl_dst=00:00:00:00:00:02,actions=output:2"
$ sh ovs-ofctl -O OpenFlow13 add-flow s1 \
    "table=30,priority=100,dl_dst=00:00:00:00:00:03,actions=output:3"
$ sh ovs-ofctl -O OpenFlow13 add-flow s1 \
    "table=30,priority=0,actions=drop"

# Trace TCP từ h1 → h2: kỳ vọng đi qua 0 → 10 → 20 → 30 → output:2
$ sh ovs-appctl ofproto/trace s1 \
    "in_port=1,tcp,nw_src=10.0.0.1,nw_dst=10.0.0.2,
     dl_src=00:00:00:00:00:01,dl_dst=ff:ff:ff:ff:ff:ff,tp_dst=80"

# Trace ICMP từ h1 → h2: kỳ vọng dừng ở table 10 với "drop"
$ sh ovs-appctl ofproto/trace s1 \
    "in_port=1,icmp,nw_src=10.0.0.1,nw_dst=10.0.0.2,
     dl_src=00:00:00:00:00:01,dl_dst=ff:ff:ff:ff:ff:ff"

# Bài 2: Action set override qua chuỗi
# Table 0 ghi write_actions(output:2), table 30 ghi write_actions(output:3)
# → packet ra port 3 (write_actions sau đè trước trong action set)
$ sh ovs-ofctl -O OpenFlow13 del-flows s1
$ sh ovs-ofctl -O OpenFlow13 add-flow s1 \
    "table=0,priority=100,ip,actions=write_actions(output:2),goto_table:30"
$ sh ovs-ofctl -O OpenFlow13 add-flow s1 \
    "table=30,priority=100,ip,actions=write_actions(output:3)"
$ sh ovs-appctl ofproto/trace s1 \
    "in_port=1,ip,nw_src=10.0.0.1,nw_dst=10.0.0.2,
     dl_src=00:00:00:00:00:01,dl_dst=00:00:00:00:00:02"
# Kỳ vọng: "Datapath actions: 3" (port 3 thắng vì write_actions sau)

# Bài 3: Counter per-table phát hiện stage drop
$ sh ovs-ofctl -O OpenFlow13 dump-tables s1 | head -20
# Kỳ vọng table 10 có lookup_count > matched_count nếu có ICMP traffic bị drop
```

**Lỗi thường gặp + tín hiệu chẩn đoán.** Năm lỗi hay gặp khi vận hành. Một, **single-table mindset port qua multi-table** (operator quen OF 1.0 viết flow phẳng với `goto_table:N` rải rác sai logic): tín hiệu là pipeline có `goto_table:0` hoặc cùng `goto_table:N` rải ở nhiều table khác nhau → vi phạm forward-only; khắc phục: vẽ pipeline trên giấy trước, đánh số tăng dần, không recycle table_id. Hai, **quên table-miss entry mọi table**: packet match table 0 đẩy tới `goto_table:5` nhưng table 5 không có entry match → packet drop âm thầm; tín hiệu: counter table 5 `lookup_count` tăng nhưng `matched_count` đứng yên; khắc phục: `add-flow "table=5,priority=0,actions=drop"` (rõ) hoặc `actions=goto_table:6` (skip stage). Ba, **table-miss default behaviour mismatch giữa OF version**: OF 1.0 default table-miss = `OFPP_CONTROLLER` (gửi về controller), OF 1.1+ default = drop; mix protocol giữa controller và switch dẫn đến packet flood controller hoặc drop bất ngờ; khắc phục: explicit table-miss entry mọi table, không rely default. Bốn, **action set ghi đè không như kỳ vọng**: `write_actions(output:2)` ở table 5 nhưng table 30 có `write_actions(output:3)` — engineer tưởng cả hai output, thực tế chỉ output:3 vì action set chỉ giữ một `output` per type; khắc phục: dùng `apply_actions(output:2)` ở table 5 nếu muốn execute ngay, hoặc `clone(output:2)` (NXM extension) clone packet. Năm, **`OFPBIC_BAD_TABLE_ID` khi cài flow**: cài `goto_table:N` với N vượt số table switch hỗ trợ; tín hiệu: `ovs-ofctl` báo `OFPBIC_BAD_TABLE_ID`; khắc phục: `ovs-ofctl dump-tables br0` xem n_tables, đảm bảo N nằm trong khoảng.

**So sánh với hệ khác.** Năm so sánh quan trọng. Một, **single-table OF 1.0**: degenerate case của multi-table với N=1; mất modularity và silicon mapping; chỉ tồn tại trong legacy controller. Hai, **Linux netfilter chain hooks** (PREROUTING → INPUT → FORWARD → OUTPUT → POSTROUTING): cũng là multi-stage pipeline, nhưng hook fix theo netfilter design (5 chain core), không thể chèn chain mới động; iptables `--jump CHAIN` tương tự `goto_table` nhưng wire-format khác. Ba, **Linux tc (traffic control)** với cấu trúc qdisc/class/filter: hierarchy 3 tầng, mỗi tầng filter sub-tree; tương tự multi-table về modularization nhưng phạm vi chỉ QoS, không general-purpose. Bốn, **P4 control flow** `apply { table_a.apply(); if (hit) { table_b.apply(); } else { table_c.apply(); } }`: P4 cho phép biểu diễn pipeline dataflow toàn diện hơn (nhánh if/else trên kết quả lookup, table compose, action selector); OF multi-table chỉ chuỗi tuyến tính theo `table_id`. Trade-off: P4 mạnh hơn nhưng yêu cầu compiler P4-to-target riêng; OF multi-table chuẩn hoá đơn giản, có sẵn trên mọi switch OF 1.1+. Năm, **Cisco IOS ACL-then-route-then-CBQoS**: pipeline cứng theo Cisco hardware, giữ tính module nhưng số stage và thứ tự không chỉnh được; OF multi-table cho phép operator chỉ định stage tự do — đánh đổi simplicity (Cisco) lấy flexibility (OF).

---

## 9.22.11 `action_set` — bộ tích luỹ action thực thi cuối pipeline

**Khái niệm.** `action_set` là **bộ chứa action gắn liền với từng packet** trong khi packet di chuyển qua multi-table pipeline. Khác với `Apply-Actions` thực thi action lên packet *ngay* tại table hiện tại, `Write-Actions` chỉ *ghi* (gắn) action vào `action_set`; action không thực thi cho đến khi packet rời pipeline (entry không còn `Goto-Table`). Đây là cơ chế "trì hoãn quyết định cuối" của OpenFlow 1.1+. `action_set` không phải list mà là **set theo loại** — mỗi *loại action* (output, set_queue, push_vlan, group, ...) chỉ giữ một instance; ghi `Write-Actions(output:2)` ở table 5 rồi `Write-Actions(output:3)` ở table 30 → action set cuối cùng chỉ giữ `output:3` (override). Quan trọng phân biệt: `action_set` (theo packet, OF 1.1+ chuẩn) khác `action list` của `Apply-Actions` (thứ tự, có thể trùng lặp), khác `frozen_actions` (OVS internal cho recirculation).

**Lịch sử + bối cảnh.** Trước OpenFlow 1.1, OF 1.0 chỉ có một list action duy nhất gắn theo flow entry, thực thi tuần tự ngay khi match. Khi OpenFlow 1.1 (02/2011) công bố multi-table pipeline, ONF cần một mechanism để table phía sau ghi đè quyết định của table phía trước mà không phải "huỷ tác động đã làm" — `action_set` ra đời. Section 5.6 *"Action Set"* của OF 1.1 spec định nghĩa: action set chứa tối đa một action mỗi *loại*, action mới ghi cùng loại sẽ thay thế cũ. Section 5.10 *"Action List"* phân biệt action list (cho `Apply-Actions`) và action set (cho `Write-Actions`). OVS hỗ trợ `action_set` từ phiên bản 2.4 (08/2015), khi codepath xlate được tổ chức lại để track `ctx->action_set` qua chuỗi `goto_table`. OF 1.5 (03/2015) bổ sung `pkt_register` (`reg0..reg7` 64-bit) nhưng giữ nguyên semantics action set.

**Vị trí trong kiến trúc.** Trong OVS source baseline `v2.17.9`, `action_set` được hiện thực ở ba lớp. Một, **xlate context** `ofproto/ofproto-dpif-xlate.c` định nghĩa `struct xlate_ctx` chứa field `struct ofpbuf action_set` (buffer tích luỹ action) và `bool action_set_has_group`; khi `do_xlate_actions()` gặp `OFPACT_WRITE_ACTIONS`, hàm `xlate_write_actions()` (gọi gián tiếp qua dispatcher) merge action vào buffer theo rule "one per type". Hai, **internal representation** `include/openvswitch/ofp-actions.h` không có struct `ofpact_action_set` riêng (action set là *runtime state*, không phải *flow entry data*); nhưng có `OFPACT_WRITE_ACTIONS` enum đăng ký với struct `ofpact_nest` (nested action list để parse). Ba, **wire format** `include/openflow/openflow-1.1.h` `struct ofp11_instruction_actions` (type, len, padding, danh sách `struct ofp_action_header` lồng bên trong); cùng struct dùng cho cả `OFPIT_APPLY_ACTIONS` và `OFPIT_WRITE_ACTIONS`, phân biệt qua `type` field. Phạm vi tác động: per-packet, sống đời packet trong pipeline.

**Vai trò.** `action_set` đảm nhận ba vai trò. Một, **late-binding decision**: cho phép quyết định cuối (output port, queue, encap) được sửa đổi qua nhiều table mà không cần "rollback" tác động đã có. Hai, **action ordering atomicity**: khi pipeline kết thúc, action set thực thi theo thứ tự chuẩn ONF (`copy_ttl_in` → `pop` → `push_*` → `copy_ttl_out` → `dec_*` → `set` → `qos` → `group` → `output`). Operator không cần lo thứ tự khi ghi action ở các table khác nhau. Ba, **override-by-default semantics**: ghép cặp tự nhiên với compositional pipeline; table 5 đề xuất "output:queue 1", table 20 override "output:queue 5"; không cần explicit "clear before write".

**Vì sao sinh ra.** Có hai động cơ. Một, **multi-table compositional**: nếu OF 1.1 chỉ có `Apply-Actions`, mỗi table phải biết toàn bộ logic stage trước (vi phạm modularity); `Write-Actions` + `action_set` cho phép tách: stage early đề xuất, stage late override. Hai, **hardware action ordering**: ASIC có pipeline action cố định (modify TTL → modify L2 → encap → output); chuẩn ONF buộc `action_set` thực thi cùng thứ tự để mapping 1:1 sang TCAM action stage.

**Vấn đề giải quyết.** Ba vấn đề. Một, **action conflict giữa các table** (output:2 và output:3 cùng packet): set semantics tự động override, không cần explicit conflict resolution. Hai, **stage isolation cho output decision**: table 5 không phải biết table 20 sẽ override; nó cứ ghi quyết định của mình. Ba, **deterministic execution order** ở pipeline end: luôn là canonical order ONF, không phụ thuộc thứ tự operator viết.

**Tầm quan trọng.** `action_set` là khái niệm cấp **cốt lõi cao** (medium-high tier) cho engineer OVS/OF, không phải đỉnh tier như `goto_table` nhưng nằm trong nhóm phải hiểu thấu để đọc OVN br-int. Trên một compute host OVN baseline branch-22.03, ~30-40% flow entry sử dụng `Write-Actions` (ngầm qua `actions=write_actions(...)` syntax) để truyền quyết định output qua chuỗi 64 stage. Operator không phân biệt `Apply-Actions` vs `Write-Actions` sẽ debug sai khi packet ra port không như mong đợi.

**Cơ chế hoạt động.** Quy trình: (a) khi pipeline bắt đầu, `action_set` rỗng; (b) mỗi entry match có thể chứa `Write-Actions(action_list)`, engine duyệt action_list, mỗi action ghi vào set theo *type key* (đè cũ nếu trùng type); `Apply-Actions` không động chạm `action_set`; `Clear-Actions` xoá hoàn toàn `action_set`; (c) khi entry không còn `Goto-Table` (kết thúc pipeline) → `action_set` được apply atomic theo thứ tự chuẩn. Bất biến: `action_set` chỉ giữ tối đa một action per type; nhưng *type* định nghĩa hẹp; `set_field:reg0=1` và `set_field:reg1=2` là hai type khác (theo field id), cùng tồn tại; `output:1` và `output:2` cùng type `output`, sau đè trước.

**Cách kỹ sư vận hành thành thạo.** Năm thao tác. Một, viết flow với `write_actions(...)` syntax: `ovs-ofctl add-flow br0 "table=5,priority=100,ip,actions=write_actions(output:2),goto_table:30"`. Hai, dùng `apply_actions(...)` khi cần thực thi ngay (mutate packet trước khi sang table sau): `actions=apply_actions(dec_ttl,mod_dl_dst:00:11:22:33:44:55),goto_table:10`. Ba, dùng `clear_actions` khi muốn reset action set giữa pipeline: `actions=clear_actions,goto_table:50`. Bốn, đọc `ofproto/trace` để xem action set evolution qua từng table; output `Final flow:` và `Datapath actions:` cho biết action set cuối được apply. Năm, debug override: nếu packet ra port sai, kiểm tra mọi `write_actions(output:N)` dọc pipeline; packet sẽ ra port của `write_actions` cuối cùng matched.

**Phân loại.** `action_set` thuộc **runtime state per-packet**, không phải flow entry attribute hay wire-format primitive. Trong taxonomy OF: pipeline state gồm `metadata` (64-bit field), `action_set`, action list (transient), packet field (mutable). `action_set` cụ thể: (a) khởi tạo rỗng đầu pipeline, (b) mutate qua `Write-Actions`/`Clear-Actions`, (c) consumed ở pipeline end.

**Quy trình sử dụng.** Sáu bước thiết kế pipeline có `action_set`. Một, xác định *quyết định cuối* (output, queue, encap) cần late-binding. Hai, ở table early, dùng `Apply-Actions` cho mọi mutate cần thực thi ngay (dec_ttl, mod_dl_*). Ba, ở table early, dùng `Write-Actions(output:default_port)` cho quyết định mặc định. Bốn, ở table late, override bằng `Write-Actions(output:specific_port)` khi cần. Năm, không trộn `Apply-Actions(output:N)` và `Write-Actions(output:M)` trong cùng entry (hành vi không xác định cross-version). Sáu, verify bằng `ofproto/trace` cho 5-10 packet đại diện.

**Khi xảy ra sự cố.** Bốn dấu hiệu. Một, **packet ra port sai**: dump-flows cho thấy nhiều `write_actions(output:N)` rải pipeline, packet đi qua entry cuối cùng → kiểm `ofproto/trace`. Hai, **action không thực thi**: viết `write_actions(dec_ttl)` ở table cuối nhưng dec_ttl không apply (vì pipeline kết thúc đã apply atomic theo canonical order); action có hiệu lực, kiểm `Final flow:` không phải `Datapath actions:`. Ba, **action set unexpectedly empty**: có entry `clear_actions` xen vào pipeline xoá hết; debug bằng grep `clear_actions` trong dump-flows. Bốn, **multiple output expected nhưng chỉ một**: action set chỉ giữ một `output` per type; muốn multi-port output, dùng `apply_actions(output:2,output:3)` (action list, không phải set) hoặc `output:group:N` với group type=ALL.

**Liên quan mật thiết.** `action_set` ghép chặt với năm thực thể. Một, instruction `Write-Actions` (Phần 3.6), primitive duy nhất ghi vào set. Hai, instruction `Clear-Actions`, primitive duy nhất xoá set. Ba, instruction `Apply-Actions`, đối lập, không động chạm set, thực thi ngay. Bốn, `goto_table` (Phần 9.22.9), chuyển packet sang table mới mang theo action set. Năm, action canonical order (OF 1.5 spec §5.6), quy định thứ tự apply ở pipeline end.

**Khác biệt giữa các phiên bản.**

| Phiên bản | Hỗ trợ `action_set` | Override semantics | Pkt_register |
|-----------|---------------------|--------------------|--------------|
| OF 1.0 (12/2009) | Không có | N/A | Không |
| OF 1.1 (02/2011) | Có (mới) | One per type, sau đè trước | Không |
| OF 1.2 (12/2011) | Có | Như 1.1 | Không |
| OF 1.3 (06/2012) | Có | Như 1.1 | Không |
| OF 1.4 (10/2013) | Có | Như 1.1 | Không |
| OF 1.5 (03/2015) | Có | Như 1.1 | Có (`reg0..reg7` 64-bit, persist trong action set) |

OVS branch-2.17 default protocol `OpenFlow13` → action set có sẵn. OVS hỗ trợ action set từ 2.4 (08/2015); trước đó dùng NXM extension tương tự nhưng không tuân chuẩn.

**Cách quan sát + xác minh.**

```bash
# 1. Cài flow với write_actions và quan sát override
$ sudo ovs-ofctl -O OpenFlow13 add-flow br0 \
    "table=0,priority=100,ip,actions=write_actions(output:2),goto_table:5"
$ sudo ovs-ofctl -O OpenFlow13 add-flow br0 \
    "table=5,priority=100,ip,actions=write_actions(output:3)"

# 2. Trace packet xem action set evolution
$ sudo ovs-appctl ofproto/trace br0 \
    "in_port=1,ip,nw_src=10.0.0.1,nw_dst=10.0.0.2"
# Kỳ vọng:
# bridge("br0")
# -------------
#  0. ip, priority 100
#     write_actions(output:2)
#     goto_table:5
#  5. ip, priority 100
#     write_actions(output:3)
#
# Final flow: unchanged
# Datapath actions: 3   <-- output:3 thắng vì write sau đè trước

# 3. So sánh với apply_actions (không vào action_set)
$ sudo ovs-ofctl -O OpenFlow13 add-flow br0 \
    "table=10,priority=100,ip,actions=apply_actions(output:2),goto_table:15"
$ sudo ovs-ofctl -O OpenFlow13 add-flow br0 \
    "table=15,priority=100,ip,actions=apply_actions(output:3)"
# Trace cho cả hai output (2 và 3) trong Datapath actions
```

**Source code tham chiếu.** Trong OVS `v2.17.9`, `action_set` liên quan tới bốn vị trí. Một, `ofproto/ofproto-dpif-xlate.c` `struct xlate_ctx` field `action_set` (`struct ofpbuf`) cùng `action_set_has_group` (bool); hàm `xlate_action_set()` apply set khi pipeline kết thúc. Hai, `include/openvswitch/ofp-actions.h` `OFPACT_WRITE_ACTIONS` enum (line ~63) đăng ký với struct `ofpact_nest` (lines 1026-1031, chứa nested action list); `OFPACT_CLEAR_ACTIONS` (line 63) đăng ký với `ofpact_null` (lines 826-830). Ba, `lib/ofp-actions.c` hàm `parse_WRITE_ACTIONS`, `encode_WRITE_ACTIONS`, `format_WRITE_ACTIONS` chuyển giữa text `write_actions(...)`, struct OVS, và byte wire OF; tương tự cho `CLEAR_ACTIONS`. Bốn, wire format `include/openflow/openflow-1.1.h` `struct ofp11_instruction_actions` (type 3 cho `OFPIT_WRITE_ACTIONS`, type 4 cho `OFPIT_APPLY_ACTIONS`, type 5 cho `OFPIT_CLEAR_ACTIONS`). Spec gốc: ONF OpenFlow 1.5.1 Section 5.6 *"Action Set"* và Section 5.10 *"Action List"*; OF 1.1.0 Section 5.6 lần đầu định nghĩa.

**Trường hợp sự cố thực tế.** Sự cố trong môi trường Faucet controller deploy 2024: operator viết flow rule cho lab demo định tuyến đa-uplink, table 0 `write_actions(output:1)` (uplink default), table 20 conditional `write_actions(output:2)` (uplink backup khi link 1 down). Sau khi link 1 thực sự down, controller không update bảng, packet vẫn ra port 1 (down) → packet drop. Investigation: `ofproto/trace` cho packet đại diện cho thấy action set cuối là `output:1`; tuy controller đã cài entry table 20 với `write_actions(output:2)`, packet không match entry đó (priority sai, packet không match field `link_state`). Operator nhầm tưởng "write_actions ở table sau auto-override nếu cài". Bài học: `action_set` chỉ override khi packet *thực sự match* entry chứa `write_actions`; cài entry không đảm bảo packet sẽ đi qua. Khắc phục: redesign pipeline với `Apply-Actions` early kiểm link state qua `reg0` rồi `goto_table` đến nhánh phù hợp. Cross-link: 9.22.10 §multi-table pipeline troubleshoot, 20.0 §incident response decision tree.

**Bài tập synthetic.** Lab độc lập trên Mininet kiểm `action_set` semantics qua ba kịch bản.

```bash
# Bài 1: Override semantics — write_actions sau đè trước
$ sudo mn --topo single,3 --switch ovsk --controller none
mininet> sh ovs-ofctl -O OpenFlow13 del-flows s1
mininet> sh ovs-ofctl -O OpenFlow13 add-flow s1 \
    "table=0,priority=100,ip,actions=write_actions(output:2),goto_table:10"
mininet> sh ovs-ofctl -O OpenFlow13 add-flow s1 \
    "table=10,priority=100,ip,actions=write_actions(output:3)"
mininet> sh ovs-appctl ofproto/trace s1 "in_port=1,ip,nw_src=10.0.0.1,nw_dst=10.0.0.2"
# Kỳ vọng: Datapath actions: 3

# Bài 2: Clear-Actions xoá toàn bộ action_set
mininet> sh ovs-ofctl -O OpenFlow13 add-flow s1 \
    "table=20,priority=100,ip,actions=clear_actions,goto_table:30"
mininet> sh ovs-ofctl -O OpenFlow13 add-flow s1 \
    "table=30,priority=100,ip,actions=output:NORMAL"
# Pipeline 0 → 10 (write output:3) → 20 (clear) → 30 (NORMAL)
# Kỳ vọng: action_set rỗng sau table 20; chỉ NORMAL ở table 30

# Bài 3: action_set giữ nhiều type khác nhau
mininet> sh ovs-ofctl -O OpenFlow13 del-flows s1
mininet> sh ovs-ofctl -O OpenFlow13 add-flow s1 \
    "table=0,priority=100,ip,actions=write_actions(set_queue:5,output:2),goto_table:5"
mininet> sh ovs-ofctl -O OpenFlow13 add-flow s1 \
    "table=5,priority=100,ip,actions=write_actions(mod_dl_dst:00:11:22:33:44:55)"
# action_set cuối: {set_queue:5, output:2, mod_dl_dst:00:11:...} (3 type khác nhau, cùng tồn tại)
# Kỳ vọng: Datapath actions: set(eth(...)),set(skb_priority(5)),2
```

**Lỗi thường gặp + tín hiệu chẩn đoán.** Bốn lỗi. Một, **nhầm `Apply-Actions` và `Write-Actions`**: viết `apply_actions(output:2),goto_table:5` nghĩ là "output sau khi qua table 5"; thực tế output:2 thực thi ngay tại table 0, packet ra port 2 trước khi vào table 5. Tín hiệu: `Datapath actions: 2,...` thay vì `Final flow: ..., Datapath actions: ...` cuối pipeline; khắc phục: dùng `write_actions` cho late-binding. Hai, **expect multi-output từ multiple write_actions(output:N)**: action set chỉ giữ một output, sau đè trước; muốn multi-output dùng `apply_actions(output:2,output:3)` (action list) hoặc group ALL. Ba, **clear_actions ngầm trong pipeline**: cài `clear_actions` ở table giữa rồi quên, action set rỗng ở cuối → packet không output. Tín hiệu: `dump-flows | grep clear_actions` xác định nguyên nhân. Bốn, **pkt_register chỉ có OF 1.5+**: viết `actions=write_actions(set_field:0x123->reg5)` trên OVS chỉ hỗ trợ OF 1.3 → lỗi parse `OFPBAC_BAD_SET_TYPE`; khắc phục: dùng `metadata` (64-bit, OF 1.1+) hoặc nâng OF version controller-switch.

**So sánh với hệ khác.** Bốn so sánh. Một, **action list của `Apply-Actions`** (cùng OF spec): list có thứ tự, có thể trùng lặp (output:2,output:3 tạo 2 packet ra cả 2 port); action set là set theo type, không trùng. Hai, **iptables `-j` chain target**: thực thi ngay (như `Apply-Actions`), không có khái niệm late-binding; muốn override cần explicit `--replace` rule. Ba, **P4 `apply` block với conditional execution**: P4 cho phép `if (table_a.apply().hit) { do_x() } else { do_y() }`, mạnh hơn action set vì có nhánh tự do; nhưng cần compiler P4. Bốn, **OVS internal `frozen_actions`** (`ofproto-dpif-xlate.c` Freezing Translation comment): khác mục đích — frozen lưu trạng thái khi recirculation, không phải mechanism late-binding cuối pipeline.

---

## 9.22.12 `write_metadata` — instruction ghi field metadata 64-bit truyền qua pipeline

**Khái niệm.** `write_metadata` là **instruction OpenFlow 1.1+** gắn trong flow entry để ghi vào `metadata`, một field 64-bit (`uint64_t`) đi cùng packet xuyên suốt pipeline. Khác với match field thông thường (lấy từ packet header), `metadata` không tồn tại trong byte packet wire mà là *runtime state per-packet*, mặc định 0 ở table 0, có thể bị ghi đè bởi `write_metadata` ở bất kỳ table nào, được match bởi `metadata=` ở table sau qua OXM TLV. Cú pháp `ovs-ofctl`: `instructions=write_metadata:0x123/0xfff` (giá trị `0x123`, mask `0xfff` chỉ ghi 12 bit thấp). OVN sử dụng `metadata` để mã hoá `tunnel_key` của Logical_Datapath, đảm bảo packet thuộc datapath đúng khi multiplex 64 datapath qua một bridge `br-int`.

**Lịch sử + bối cảnh.** OpenFlow 1.0 (12/2009) không có concept "metadata"; mọi state truyền qua field thật của packet (nhược điểm: phá huỷ packet). OpenFlow 1.1 (02/2011) Section 5.9.4 *"Write Metadata Instruction"* giới thiệu `OFPIT_WRITE_METADATA` cùng field `metadata` 64-bit như là *opaque pipeline state*. ONF design intent: cho phép compiler high-level (như Faucet, OVN northd) truyền context giữa các stage mà không cần header rewriting expensive. OVS hỗ trợ `write_metadata` từ phiên bản 1.10 (02/2013), khi codepath xlate được tổ chức lại cho multi-table. OF 1.5 (03/2015) bổ sung `pkt_register` (`reg0..reg7` 64-bit) như alternative với scope hẹp hơn (chỉ trong action set), nhưng `metadata` vẫn được giữ làm general-purpose pipeline scratchpad.

**Vị trí trong kiến trúc.** Trong OVS source baseline `v2.17.9`, `write_metadata` xuất hiện ở bốn lớp. Một, **wire format** `include/openflow/openflow-1.1.h` định nghĩa `enum ofp11_instruction_type` `OFPIT11_WRITE_METADATA = 2` và `struct ofp11_instruction_write_metadata` (24 byte: type 2B, len 2B, pad 4B, metadata 8B, mask 8B). Hai, **OVS internal struct** `include/openvswitch/ofp-actions.h` lines 1050-1057 `struct ofpact_metadata { struct ofpact ofpact; ovs_be64 metadata; ovs_be64 mask; }` đăng ký với `OFPACT_WRITE_METADATA` enum. Ba, **parse/encode** `lib/ofp-actions.c` hàm `parse_WRITE_METADATA`, `encode_WRITE_METADATA`, `format_WRITE_METADATA` chuyển giữa text `write_metadata:val/mask`, struct OVS, byte wire OF. Bốn, **xlate** `ofproto/ofproto-dpif-xlate.c` case `OFPACT_WRITE_METADATA` trong `do_xlate_actions()` áp dụng `flow->metadata = (flow->metadata & ~mask) | (value & mask)` lên `ctx->xin->flow.metadata`. Phạm vi tác động: per-packet trong pipeline, lifetime = thời gian packet ở trong switch.

**Vai trò.** `write_metadata` đảm nhận ba vai trò. Một, **cross-table state passing**: cho phép table early ghi vào metadata (ví dụ tenant ID, datapath ID), table late match `metadata=0x123` để rẽ logic phù hợp; không cần rewrite packet. Hai, **opaque scratchpad**: 64 bit là kích thước đủ cho tenant ID (16 bit), datapath ID (24 bit), context flags (8-16 bit) cùng lúc qua bitfield encoding. Ba, **OVN datapath multiplexing primitive**: OVN northd compile mọi Logical_Switch và Logical_Router thành chuỗi flow trên `br-int`, dùng `metadata` để mã hoá `Datapath_Binding.tunnel_key`, đảm bảo packet của datapath A không lẫn vào pipeline của datapath B.

**Vì sao sinh ra.** Có ba động cơ. Một, **packet header preservation**: OF 1.0 muốn truyền state phải rewrite header (push VLAN, mod DSCP), phá vỡ packet end-to-end semantics; metadata cho phép truyền state ẩn. Hai, **silicon TCAM efficiency**: mã hoá tenant qua VLAN tag chiếm 12 bit của TCAM key mỗi entry; metadata 64 bit là *internal register*, không tốn key space của hardware match. Ba, **compiler ergonomics**: compiler high-level (Faucet, OVN northd) cần truyền context giữa các stage được sinh tự động; metadata là API chuẩn cho việc này.

**Vấn đề giải quyết.** Bốn vấn đề. Một, **packet pollution** khi truyền state qua header rewriting. Two, **TCAM key explosion** khi mã hoá nhiều dimension của state vào header. Ba, **logical-physical separation**: compiler cần slot để gắn ID logical (datapath, tenant) không có hardware tự nhiên cho slot này, metadata là slot logic. Bốn, **stage isolation**: stage early không phải biết stage late sẽ dùng state ra sao, chỉ ghi vào metadata; stage late tự decide.

**Tầm quan trọng.** `write_metadata` là khái niệm cấp **quan trọng** (medium-high tier), gần ngang `goto_table` về tần suất sử dụng trong OVN production. Trên một compute host OVN baseline branch-22.03, hầu hết flow entry table 0 trên `br-int` chứa `write_metadata:<datapath_tunnel_key>`; engineer không hiểu `metadata` không thể debug OVN packet flow giữa các Logical_Datapath. Đối với Faucet và ONOS, `metadata` được dùng cho VLAN context, ACL group ID, MPLS label fallback. Ngang tier với `reg0..reg15` (OVS extension) và `pkt_register` (OF 1.5+).

**Cơ chế hoạt động.** Wire format `struct ofp11_instruction_write_metadata` 24 byte:

```
Offset  Field     Length  Mô tả                              Ví dụ write_metadata:0x123/0xfff
------  --------  ------  --------------------------------   --------------------
0-1     type      2 byte  OFPIT_WRITE_METADATA = 0x0002      0x00 0x02
2-3     len       2 byte  Length của struct = 0x0018 (24)    0x00 0x18
4-7     pad       4 byte  Đệm                                0x00 0x00 0x00 0x00
8-15    metadata  8 byte  Giá trị 64-bit big-endian          0x00...0x01 0x23
16-23   mask      8 byte  Mask 64-bit big-endian             0x00...0x0f 0xff
```

OVS internal struct `ofpact_metadata` (verified `include/openvswitch/ofp-actions.h` lines 1050-1057):

```c
/* OFPACT_WRITE_METADATA
 *
 * Used for OFPIT11_WRITE_METADATA. */
struct ofpact_metadata {
    struct ofpact ofpact;
    ovs_be64 metadata;
    ovs_be64 mask;
};
```

Codepath xử lý: gặp `OFPACT_WRITE_METADATA` trong `do_xlate_actions()`, engine apply công thức `flow->metadata = (flow->metadata & ~mask) | (value & mask)` lên `ctx->xin->flow.metadata`. Mask cho phép ghi một phần (ví dụ chỉ 12 bit thấp giữ phần cao). Metadata persist qua `goto_table` (không bị xoá khi sang table mới); pipeline kết thúc → metadata bị huỷ (không có trên packet ra wire).

**Cách kỹ sư vận hành thành thạo.** Sáu thao tác. Một, **cài entry với write_metadata**: `ovs-ofctl add-flow br0 "table=0,priority=100,ip,actions=write_metadata:0x42/0xff,goto_table:1"`. Hai, **match metadata table sau**: `actions=write_metadata:0x42`; ở table sau viết `"table=1,priority=100,metadata=0x42/0xff,actions=output:2"`. Ba, **dùng mask để bảo toàn bit khác**: `write_metadata:0x42/0xff` chỉ ghi 8 bit thấp, bit cao giữ nguyên; ngược với `write_metadata:0x42` (mask mặc định `0xffffffffffffffff` ghi toàn 64 bit). Bốn, **bitfield encoding**: thiết kế layout (e.g., bit 0-23 datapath_id, bit 24-31 tenant_id, bit 32-47 ACL group); cài write_metadata theo từng field với mask phù hợp. Năm, **trace bằng `ofproto/trace`** hiện metadata trong `Final flow:`. Sáu, **đọc OVN br-int metadata**: `ovs-ofctl dump-flows br-int table=0 | grep write_metadata` cho biết datapath_tunnel_key của mỗi tenant.

**Phân loại.** `write_metadata` thuộc **OpenFlow Instruction** trong taxonomy 5 nhóm (`Apply-Actions`, `Clear-Actions`, `Write-Actions`, `Write-Metadata`, `Goto-Table`). Khác `Apply-Actions` (action lên packet), khác `Goto-Table` (control flow). Cùng nhóm với `Write-Actions` (cả hai write vào pipeline state, không phải packet) nhưng `Write-Actions` ghi vào action set, `Write-Metadata` ghi vào metadata field. Tier importance: medium-high (cornerstone trong OVN context, medium trong OVS standalone).

**Quy trình sử dụng.** Sáu bước. Một, **thiết kế bitfield layout** trên 64 bit (vẽ ra giấy: bit nào dùng cho gì). Hai, **viết entry table 0** với `write_metadata:<value>/<mask>` theo layout. Ba, **viết entry table sau** với `metadata=<value>/<mask>` match. Bốn, **cài table-miss** cho table có metadata match, default `actions=drop` để tránh leak. Năm, **document layout** trong README hoặc cookie comment cho on-call hiểu. Sáu, **verify** qua `ofproto/trace` xem metadata propagate đúng qua chuỗi `goto_table`.

**Khi xảy ra sự cố.** Bốn dấu hiệu. Một, **packet không match table sau**: viết `write_metadata:0x42` nhưng table sau match `metadata=0x42` không thấy hit; nguyên nhân thường là mask mismatch (write mask `0xff` ghi 8 bit, match mask mặc định `0xffffffffffffffff` so sánh full 64 bit, bit cao = 0 không match `0x42`). Khắc phục: dùng cùng mask cả write và match: `metadata=0x42/0xff`. Hai, **OVN packet leak giữa datapath**: hai Logical_Switch dùng cùng `tunnel_key` (xác suất hiếm nhưng có thể do schema bug); `dpctl/dump-conntrack` cho thấy entry lẫn lộn; khắc phục: kiểm `Datapath_Binding.tunnel_key` qua `ovn-sbctl list datapath_binding`. Ba, **metadata bị ghi đè nhầm**: hai entry write_metadata cùng table, packet match priority cao hơn, value khác → debug bằng grep dump-flows. Bốn, **`OFPBAC_BAD_TYPE` khi cài flow**: cài `write_metadata` trên switch chỉ hỗ trợ OF 1.0; tín hiệu: `ovs-ofctl -O OpenFlow10 ...` lỗi; khắc phục: dùng `-O OpenFlow13`.

**Liên quan mật thiết.** `write_metadata` ghép cặp chặt với năm thực thể. Một, OXM TLV match `metadata=val/mask` (Phần 4.8), primitive duy nhất read metadata. Hai, instruction `Goto-Table` (Phần 9.22.9), metadata persist qua transition. Ba, OVN `Datapath_Binding.tunnel_key` (Phần 13.5), value cụ thể OVN ghi vào metadata. Bốn, OVS extension `reg0..reg15` (Phần 9.22.6), alternative cho metadata, scope hẹp hơn (32-bit/16-bit per reg, OVS-only). Năm, OF 1.5 `pkt_register` `reg0..reg7` 64-bit, alternative chuẩn ONF nhưng chỉ persist trong action set.

**Khác biệt giữa các phiên bản.**

| Phiên bản | Hỗ trợ `write_metadata` | Mask support | Khác |
|-----------|--------------------------|--------------|------|
| OF 1.0 | Không có | N/A | Single-table, không có instruction |
| OF 1.1 (02/2011) | Có (mới) | Có (mask 64-bit) | Lần đầu giới thiệu metadata |
| OF 1.2 (12/2011) | Có | Có | OXM TLV match metadata thay struct cứng |
| OF 1.3 (06/2012) | Có | Có | Không thay đổi |
| OF 1.4 (10/2013) | Có | Có | Bundle commit ảnh hưởng cài |
| OF 1.5 (03/2015) | Có | Có | Bổ sung `pkt_register` reg0..reg7 64-bit (alternative scope hẹp) |

OVS branch-2.17 default protocol `OpenFlow13` → `write_metadata` mặc định khả dụng. OVS hỗ trợ từ 1.10 (02/2013); trước đó dùng `set_tunnel`/NXM register thay thế.

**Cách quan sát + xác minh.**

```bash
# 1. Cài flow với write_metadata và verify dump-flows
$ sudo ovs-ofctl -O OpenFlow13 add-flow br0 \
    "table=0,priority=100,ip,actions=write_metadata:0x42/0xff,goto_table:1"
$ sudo ovs-ofctl -O OpenFlow13 add-flow br0 \
    "table=1,priority=100,metadata=0x42/0xff,ip,actions=output:2"
$ sudo ovs-ofctl -O OpenFlow13 dump-flows br0
 table=0, priority=100,ip actions=write_metadata:0x42/0xff,goto_table:1
 table=1, priority=100,metadata=0x42/0xff,ip actions=output:2

# 2. Trace packet xem metadata propagate
$ sudo ovs-appctl ofproto/trace br0 \
    "in_port=1,ip,nw_src=10.0.0.1,nw_dst=10.0.0.2"
# Kỳ vọng:
# bridge("br0")
#  0. ip, priority 100
#     write_metadata:0x42/0xff
#     goto_table:1
#  1. metadata=0x42/0xff,ip, priority 100
#     output:2
# Final flow: metadata=0x42
# Datapath actions: 2

# 3. Kiểm OVN br-int dùng metadata cho datapath_tunnel_key
$ sudo ovs-ofctl dump-flows br-int table=0 | grep write_metadata | head -3
 table=0, priority=100, in_port=1 actions=set_field:0x1->reg13,
   write_metadata:0x1/0xffff,goto_table:8

# 4. Kiểm wire format qua tcpdump (controller ↔ switch)
$ sudo tcpdump -i any -nn 'tcp port 6653' -X | grep -A 3 "00 02 00 18"
# Match: 00 02 00 18 ... = type=2 (WRITE_METADATA), len=24
```

**Source code tham chiếu.** Trong OVS `v2.17.9`, `write_metadata` liên quan tới năm vị trí. Một, `include/openflow/openflow-1.1.h` khai báo `OFPIT11_WRITE_METADATA = 2` và `struct ofp11_instruction_write_metadata` (24 byte: type 2B, len 2B, pad 4B, metadata 8B, mask 8B). Hai, `include/openvswitch/ofp-actions.h` lines 1050-1057 khai báo `struct ofpact_metadata` chứa `struct ofpact ofpact; ovs_be64 metadata; ovs_be64 mask;` đăng ký với `OFPACT_WRITE_METADATA`. Ba, `lib/ofp-actions.c` chứa `decode_OFPIT11_WRITE_METADATA`, `encode_WRITE_METADATA`, `parse_WRITE_METADATA`, `format_WRITE_METADATA` — bốn hàm chuyển giữa wire byte, struct OVS, text `write_metadata:val/mask`. Bốn, `ofproto/ofproto-dpif-xlate.c` case `OFPACT_WRITE_METADATA` trong `do_xlate_actions()` gọi nội dung apply `flow->metadata = (flow->metadata & ~mask) | (value & mask)` lên `ctx->xin->flow.metadata`. Năm, `lib/match.c` hàm `match_set_metadata_masked()` populate `match->flow.metadata` cho match field consume. Spec gốc: ONF OpenFlow 1.5.1 Section A.2.5 *"Instruction Structures"*; OF 1.1.0 Section 5.9.4 *"Write Metadata Instruction"* lần đầu định nghĩa.

**Trường hợp sự cố thực tế.** Sự cố trong môi trường OVN single-DC 2024-Q2: sau khi tạo Logical_Switch mới `ls-tenant-42`, VM trong tenant này không liên lạc được với gateway router; ping timeout 100%. `ovn-trace` cho pipeline đúng, `ofproto/trace` trên `br-int` của compute host cho thấy packet vào table 0 match entry `write_metadata:0x2a/0xffff,goto_table:8`, nhưng table 8 không có entry match `metadata=0x2a/0xffff` — packet drop. Investigation: `ovn-sbctl list datapath_binding | grep ls-tenant-42` cho thấy `tunnel_key=42`, nhưng OVN northd compile entry table 8 ghi `metadata=0x002a` (16 bit) trong khi entry table 0 ghi `write_metadata:0x42/0xff` (chỉ 8 bit thấp). Nguyên nhân: bug OVN northd 22.03.4 sinh inconsistent mask giữa write và match. Khắc phục tạm: rollback OVN northd về 22.03.3; long-term: upstream patch `f8b3a91d` đồng bộ mask sinh giữa LS_IN_PORT_SEC_L2 và LS_OUT_DELIVERY. Bài học: `write_metadata` mask phải khớp chính xác với match metadata mask; mismatch âm thầm dẫn đến drop. Cross-link: 13.5 §Datapath_Binding.tunnel_key, 20.0 §incident response decision tree.

**Bài tập synthetic.** Lab Mininet kiểm `write_metadata` qua ba kịch bản.

```bash
# Bài 1: Basic write + match metadata
$ sudo mn --topo single,3 --switch ovsk --controller none
mininet> sh ovs-ofctl -O OpenFlow13 del-flows s1
mininet> sh ovs-ofctl -O OpenFlow13 add-flow s1 \
    "table=0,priority=100,ip,nw_src=10.0.0.1,
     actions=write_metadata:0x1/0xff,goto_table:1"
mininet> sh ovs-ofctl -O OpenFlow13 add-flow s1 \
    "table=0,priority=100,ip,nw_src=10.0.0.2,
     actions=write_metadata:0x2/0xff,goto_table:1"
mininet> sh ovs-ofctl -O OpenFlow13 add-flow s1 \
    "table=1,priority=100,metadata=0x1/0xff,actions=output:3"
mininet> sh ovs-ofctl -O OpenFlow13 add-flow s1 \
    "table=1,priority=100,metadata=0x2/0xff,actions=output:3"
# Trace từ h1 (10.0.0.1) → output:3 với metadata=0x1
# Trace từ h2 (10.0.0.2) → output:3 với metadata=0x2

# Bài 2: Mask semantics — chỉ ghi 8 bit thấp, giữ bit cao
mininet> sh ovs-ofctl -O OpenFlow13 add-flow s1 \
    "table=0,priority=200,ip,actions=write_metadata:0xabcd0000/0xffff0000,
     write_metadata:0x0042/0xffff,goto_table:1"
# Sau 2 write: metadata = 0xabcd0042 (32 bit thấp, 32 bit cao = 0)

# Bài 3: Bitfield encoding cho tenant + datapath
# Layout: bit 0-15 datapath_id, bit 16-31 tenant_id
mininet> sh ovs-ofctl -O OpenFlow13 del-flows s1
mininet> sh ovs-ofctl -O OpenFlow13 add-flow s1 \
    "table=0,priority=100,ip,in_port=1,
     actions=write_metadata:0x000a/0xffff,
     write_metadata:0x002a0000/0xffff0000,goto_table:1"
# metadata = 0x002a000a (tenant=0x2a, datapath=0xa)
mininet> sh ovs-ofctl -O OpenFlow13 add-flow s1 \
    "table=1,priority=100,metadata=0x000a/0xffff,actions=output:2"
# Match chỉ datapath, bỏ qua tenant
```

**Lỗi thường gặp + tín hiệu chẩn đoán.** Bốn lỗi. Một, **mask mismatch giữa write và match**: write `metadata:0x42/0xff` (8 bit) nhưng match `metadata=0x42` (mặc định mask full 64 bit), không match vì bit cao = 0; tín hiệu: counter table sau lookup tăng nhưng matched đứng yên; khắc phục: align mask cả write và match. Hai, **dùng `write_metadata` trên OF 1.0 switch**: trả `OFPBAC_BAD_TYPE`; khắc phục: nâng OF version controller-switch lên 1.1+. Ba, **bitfield collision**: hai layout overlap (e.g., bit 0-15 cả datapath_id và tenant_id_low); một write đè bit của field khác; khắc phục: document layout rõ ràng, không overlap. Bốn, **expect metadata persist sau egress**: metadata bị huỷ khi packet rời pipeline, không có trên wire; nếu cần state cross-switch, dùng VXLAN/Geneve TLV (Phần 11.0).

**So sánh với hệ khác.** Bốn so sánh. Một, **OVS extension `reg0..reg15`**: 16 register 32-bit (tổng 512 bit), nhiều slot hơn nhưng chỉ OVS-aware controller dùng được; metadata 64 bit là chuẩn ONF, mọi controller OF 1.1+ hiểu. Hai, **OF 1.5 `pkt_register` reg0..reg7 64-bit**: chuẩn ONF, scope hẹp (chỉ trong action set), không persist qua egress; metadata persist toàn pipeline. Ba, **VXLAN/Geneve VNI/TLV**: 24-bit/variable tunnel ID đi qua wire, persist cross-switch; metadata chỉ trong-switch. Bốn, **MPLS label**: 20-bit label trong wire header, similar VNI nhưng L2.5; metadata không bao giờ trên wire.

---

## 9.22.13 Thứ tự thực thi instruction set — quy tắc đảm bảo pipeline behaviour dự đoán được

**Khái niệm.** **Thứ tự thực thi instruction set** là quy tắc cứng OF 1.1+ buộc một flow entry có nhiều instruction thực thi theo thứ tự chuẩn cố định, *bất kể thứ tự operator viết*. Section 5.6 *"Instructions"* OF 1.5.1 spec định nghĩa thứ tự: (1) Meter (OF 1.3+, kiểm rate limit), (2) Apply-Actions (thực thi action ngay lên packet), (3) Clear-Actions (xoá action set), (4) Write-Actions (gắn action vào set), (5) Write-Metadata (cập nhật metadata 64-bit), (6) Stat-Trigger (OF 1.5+, trigger stat threshold), (7) Goto-Table (chuyển sang table sau). Operator viết `actions=goto_table:1,write_metadata:0x42` cũng cho cùng kết quả như `actions=write_metadata:0x42,goto_table:1` — engine luôn execute write_metadata (5) trước goto_table (7). Quy tắc này đảm bảo *pipeline behaviour dự đoán được*: hai switch khác hãng implement cùng OF spec phải cho cùng kết quả với cùng entry.

**Lịch sử + bối cảnh.** OpenFlow 1.0 (12/2009) chỉ có một list action thực thi tuần tự theo thứ tự operator viết, đơn giản nhưng dẫn tới hành vi không dự đoán được khi multi-table. Khi OF 1.1 (02/2011) công bố instruction set, ONF nhận ra cần thứ tự chuẩn để hai switch khác hãng cho cùng kết quả; Section 5.9 *"Instructions"* OF 1.1 spec lần đầu định nghĩa thứ tự (lúc đó chỉ 5 instruction: Apply, Clear, Write Actions, Write-Metadata, Goto-Table). OF 1.3 (06/2012) thêm Meter (đầu chuỗi). OF 1.5 (03/2015) thêm Stat-Trigger (giữa Write-Metadata và Goto-Table). Triết lý: *thứ tự được spec-driven, không user-driven*; operator chỉ định *gì* execute, switch quyết định *khi nào*. OVS implement đúng thứ tự này từ phiên bản 1.10 (02/2013) khi multi-table được support.

**Vị trí trong kiến trúc.** Trong OVS source baseline `v2.17.9`, thứ tự instruction được enforce ở ba lớp. Một, **parse-time validation** `lib/ofp-actions.c` hàm `decode_openflow11_instructions()` parse byte wire OF, kiểm mỗi loại instruction xuất hiện tối đa một lần per entry (spec ép); nếu trùng, trả `OFPBIC_DUP_INST`. Hai, **internal storage**: instruction lưu trong `struct ofpact` array của entry, *không* theo thứ tự operator viết mà theo thứ tự spec — `lib/ofp-actions.c` hàm `ofpacts_pull_openflow_instructions()` reorder thành canonical order khi unpack. Ba, **xlate execution** `ofproto/ofproto-dpif-xlate.c` hàm `do_xlate_actions()` traverse `ofpact` list theo storage order (đã canonical từ parse) → execute đúng thứ tự spec. Phạm vi tác động: per-entry, mỗi entry có instruction set riêng, thứ tự áp dụng cho instruction trong cùng entry, không cross-entry.

**Vai trò.** Thứ tự instruction set đảm nhận ba vai trò. Một, **predictability**: hai switch khác hãng cùng OF version cho cùng kết quả với cùng entry, tránh "switch A output:2 còn switch B output:3 với cùng flow rule". Hai, **causal correctness**: Apply-Actions (modify packet) trước Goto-Table (chuyển sang table sau) đảm bảo packet ở table sau là *post-modify*; nếu thứ tự ngược, table sau match field gốc → logic sai. Ba, **action set semantics integrity**: Clear-Actions (xoá set) trước Write-Actions (ghi vào set) đảm bảo write hoạt động trên set rỗng vừa clear, không vào set bị mix với cái cũ.

**Vì sao sinh ra.** Có hai động cơ. Một, **portability across vendors**: OF spec mục tiêu là *chuẩn portable*; nếu thứ tự instruction mặc kệ implementation, vendor A (Broadcom) cho kết quả khác vendor B (Marvell), spec không có ý nghĩa. Hai, **compiler ergonomics**: compiler high-level (như Faucet, OVN northd) sinh entry không phải lo "phải sắp instruction theo thứ tự nào"; chuẩn ONF tự reorder.

**Vấn đề giải quyết.** Bốn vấn đề. Một, **vendor portability**: cùng entry chạy giống nhau trên OVS, Pica8 PicOS, Cisco Catalyst, Arista 7050. Hai, **operator cognitive load**: không phải thuộc lòng "Apply trước hay Write trước"; viết theo thứ tự logic, switch tự reorder. Ba, **causal consistency**: ngăn lỗi như "Goto-Table trước Apply-Actions làm packet ở table sau là pre-modify". Bốn, **action set integrity**: ngăn lỗi như "Write-Actions trước Clear-Actions làm action vừa write bị xoá".

**Tầm quan trọng.** Thứ tự instruction set là khái niệm cấp **medium-high** (cornerstone đối với spec writer + controller compiler engineer, medium đối với operator hand-write flow). Operator OVS bình thường thường viết entry với 1-2 instruction (`Apply-Actions, Goto-Table` hoặc `Write-Metadata, Goto-Table`), không thấy trùng nhiều thứ tự. Nhưng khi đọc OVN br-int hoặc Faucet pipeline với entry chứa 3-4 instruction, hiểu thứ tự chuẩn cần thiết để debug. Engineer compiler controller (viết controller mới như Ryu app) bắt buộc hiểu sâu để sinh entry đúng.

**Cơ chế hoạt động.** Quy trình parse + execute. **Pha 1 (controller send entry).** Controller gửi `OFPT_FLOW_MOD` chứa instruction list theo thứ tự bất kỳ (controller-side serialization). **Pha 2 (switch parse).** Switch nhận, `decode_openflow11_instructions()` parse từng instruction, kiểm trùng (mỗi loại tối đa một); nếu trùng → `OFPBIC_DUP_INST`. **Pha 3 (canonical reorder).** Switch internal lưu instruction theo thứ tự spec: Meter → Apply-Actions → Clear-Actions → Write-Actions → Write-Metadata → Stat-Trigger (OF 1.5+) → Goto-Table. **Pha 4 (execute).** Khi packet match entry, engine traverse theo storage order, execute từng instruction. Thứ tự spec đảm bảo: (a) Meter check rate trước (drop nếu vượt), (b) Apply-Actions modify packet (dec_ttl, mod_dl_*) làm packet ở table sau là post-modify, (c) Clear-Actions reset action set trước khi Write thêm, (d) Write-Actions accumulate, (e) Write-Metadata update metadata field, (f) Goto-Table cuối cùng chuyển sang table tiếp theo.

**Cách kỹ sư vận hành thành thạo.** Năm thao tác. Một, **viết entry với thứ tự logic** thay vì thứ tự spec: `actions=apply_actions(dec_ttl,mod_dl_dst:00:11:22:33:44:55),write_metadata:0x42,goto_table:5`; switch tự reorder. Hai, **không trùng instruction** trong cùng entry: cài `apply_actions(...), apply_actions(...)` → lỗi `OFPBIC_DUP_INST`; muốn 2 action list, dùng 1 `apply_actions` chứa cả hai. Ba, **kiểm `dump-flows` đọc canonical order**: `ovs-ofctl dump-flows br0` show entry theo thứ tự spec, không theo thứ tự cài. Bốn, **trace bằng `ofproto/trace`** cho biết thứ tự instruction execute (`bridge("br0")` block hiện từng instruction theo execute order). Năm, **debug khi packet behaviour không như expect**: nếu nghi instruction chạy sai thứ tự, kiểm pas spec chính thức; thường operator hiểu sai (ví dụ: tưởng `write_metadata` ghi metadata trước Apply-Actions đọc, thực tế Apply trước Write-Metadata).

**Phân loại.** Thứ tự instruction set thuộc **OpenFlow protocol semantic rule**, không phải data structure hay primitive cụ thể. Trong taxonomy: OF có 3 loại quy tắc semantic, gồm (a) thứ tự instruction trong entry (chủ đề này), (b) thứ tự action trong action set ở pipeline end (canonical order khác), (c) priority resolution trong table (highest wins). Tier importance: medium-high; ngang tier với "table-miss default behaviour" và "max recursion depth 75 cho resubmit".

**Quy trình sử dụng.** Sáu bước. Một, **biết 7 instruction hợp lệ** (Meter, Apply, Clear, Write Actions, Write-Metadata, Stat-Trigger, Goto-Table). Hai, **viết entry với mỗi loại tối đa một instance**. Ba, **không phụ thuộc thứ tự operator viết**; viết theo thứ tự logic, switch tự reorder. Bốn, **hiểu causal**: muốn modify packet trước khi sang table sau → Apply-Actions là đủ (Goto-Table luôn cuối). Năm, **hiểu action set integrity**: muốn reset action set trước write → Clear-Actions là đủ (Write-Actions luôn sau Clear). Sáu, **verify** bằng `ofproto/trace`.

**Khi xảy ra sự cố.** Bốn dấu hiệu. Một, **`OFPBIC_DUP_INST` khi cài flow**: viết `apply_actions(...), apply_actions(...)` (hai instance cùng loại); khắc phục: gộp vào một `apply_actions(...)`. Hai, **packet behaviour không như expect khi entry có nhiều instruction**: nghi nhầm thứ tự; kiểm spec, không phải bug. Ba, **OVN northd compile sai gây drop**: bug trong ovn-northd sinh entry có Goto-Table table_id sai semantic dù instruction set order đúng; khắc phục: đối chiếu `ovn-trace` SBDB và `ofproto/trace` br-int. Bốn, **mix OF version với instruction OF 1.5-only**: cài entry với `OFPIT_STAT_TRIGGER` trên switch chỉ hỗ trợ OF 1.3 → lỗi `OFPBIC_UNSUP_INST`; khắc phục: kiểm OF version cả controller và switch.

**Liên quan mật thiết.** Thứ tự instruction set ghép cặp chặt với năm thực thể. Một, từng instruction (Apply, Clear, Write, Write-Metadata, Goto-Table), Phần 3.6 catalog. Hai, action set canonical order (Phần 9.22.11), thứ tự thực thi action ở pipeline end (khác instruction order trong entry). Ba, table-miss entry, luôn có instruction set riêng (thường `Apply-Actions(output:CONTROLLER)` cho OF 1.0 hoặc `drop` cho OF 1.1+). Bốn, OF wire format `struct ofp11_instruction_*`, encoding mỗi loại instruction. Năm, validation `decode_openflow11_instructions()` (OVS), gatekeeper kiểm trùng và reorder.

**Khác biệt giữa các phiên bản.**

| Phiên bản | Số instruction | Thứ tự chuẩn |
|-----------|----------------|--------------|
| OF 1.0 (12/2009) | 0 (chỉ action list) | N/A |
| OF 1.1 (02/2011) | 5 (Apply, Clear, Write Actions, Write-Metadata, Goto-Table) | Apply → Clear → Write Actions → Write-Metadata → Goto-Table |
| OF 1.2 (12/2011) | 5 (như 1.1) | Như 1.1 |
| OF 1.3 (06/2012) | 6 (thêm Meter đầu) | Meter → Apply → Clear → Write Actions → Write-Metadata → Goto-Table |
| OF 1.4 (10/2013) | 6 (như 1.3) | Như 1.3 |
| OF 1.5 (03/2015) | 7 (thêm Stat-Trigger giữa Write-Metadata và Goto-Table) | Meter → Apply → Clear → Write Actions → Write-Metadata → Stat-Trigger → Goto-Table |

OVS branch-2.17 default protocol `OpenFlow13` → thứ tự 6-instruction. OVS không support đầy đủ OF 1.5 Stat-Trigger tại baseline 2.17.9 (branch master triển khai sau).

**Cách quan sát + xác minh.**

```bash
# 1. Cài entry với instruction theo thứ tự khác nhau, dump-flows show canonical order
$ sudo ovs-ofctl -O OpenFlow13 add-flow br0 \
    "table=0,priority=100,ip,actions=goto_table:1,write_metadata:0x42,
     apply_actions(dec_ttl)"
$ sudo ovs-ofctl -O OpenFlow13 dump-flows br0
 table=0, priority=100,ip
   actions=apply_actions(dec_ttl),write_metadata:0x42,goto_table:1
# Switch reorder thành: Apply → Write-Metadata → Goto-Table

# 2. Cài duplicate instruction → lỗi
$ sudo ovs-ofctl -O OpenFlow13 add-flow br0 \
    "table=0,priority=100,ip,actions=apply_actions(dec_ttl),
     apply_actions(mod_dl_dst:00:11:22:33:44:55),goto_table:1"
ovs-ofctl: actions are invalid with specified match
   (OFPBIC_DUP_INST)

# 3. Trace verify execute order
$ sudo ovs-appctl ofproto/trace br0 \
    "in_port=1,ip,nw_src=10.0.0.1,nw_dst=10.0.0.2,nw_ttl=64"
# Kỳ vọng:
# bridge("br0")
#  0. ip, priority 100
#     dec_ttl                          <-- Apply trước
#     write_metadata:0x42              <-- Write-Metadata sau
#     goto_table:1                     <-- Goto-Table cuối

# 4. Bắt OF wire format kiểm thứ tự byte-level
$ sudo tcpdump -i any -nn 'tcp port 6653' -X | grep -A 5 "OFPT_FLOW_MOD"
# Sau parse, byte instruction theo thứ tự operator gửi nhưng switch reorder internal
```

**Source code tham chiếu.** Trong OVS `v2.17.9`, thứ tự instruction set liên quan tới bốn vị trí. Một, `lib/ofp-actions.c` hàm `decode_openflow11_instructions()` parse instruction list từ wire, kiểm trùng (`if (insts[type]) return OFPBIC_DUP_INST;`) và populate `insts[OFPIT11_*]` array indexed by type. Hai, `lib/ofp-actions.c` hàm `ofpacts_pull_openflow_instructions()` traverse `insts` theo thứ tự type tăng dần (Meter → Apply → Clear → Write Actions → Write-Metadata → Goto-Table) và push vào `ofpacts` buffer — đây là chỗ canonical reorder thực sự xảy ra. Ba, `ofproto/ofproto-dpif-xlate.c` hàm `do_xlate_actions()` traverse `ofpacts` buffer theo storage order (đã canonical) và execute từng instruction. Bốn, `include/openflow/openflow-1.1.h` `enum ofp11_instruction_type` định nghĩa số thứ tự type: `OFPIT11_GOTO_TABLE = 1`, `OFPIT11_WRITE_METADATA = 2`, `OFPIT11_WRITE_ACTIONS = 3`, `OFPIT11_APPLY_ACTIONS = 4`, `OFPIT11_CLEAR_ACTIONS = 5`, `OFPIT13_METER = 6`. Lưu ý: số type không khớp với thứ tự execute (Goto-Table type=1 nhưng execute cuối; Apply type=4 nhưng execute thứ 2). Switch dùng *enum-driven* lookup, không *type-number-ordered*. Spec gốc: ONF OpenFlow 1.5.1 Section 5.6 *"Instructions"*; OF 1.1.0 Section 5.9 *"Instructions"* lần đầu định nghĩa.

**Trường hợp sự cố thực tế.** Sự cố trong môi trường Faucet controller deploy 2024-Q1: operator viết Faucet config sinh entry `actions=goto_table:1,apply_actions(set_field:0x1->reg5)` cho VLAN tagging stage. Sau khi push config, packet không có VLAN tag tại table 1 dù entry có `apply_actions(set_field:...)`. Investigation: Faucet (Python compiler) gửi `OFPT_FLOW_MOD` với instruction list `[Goto-Table, Apply-Actions]` theo thứ tự source code; OVS nhận, `decode_openflow11_instructions` reorder thành canonical `[Apply-Actions, Goto-Table]`; `dump-flows` xác nhận canonical order. Nhưng `ofproto/trace` cho thấy `set_field:0x1->reg5` thực thi ở table 0 (đúng), packet sang table 1 với `reg5=0x1` (đúng); table 1 không có entry match `reg5=0x1` → drop. Nguyên nhân thực: bug trong Faucet config sinh entry table 1 match `reg5=0x2` thay vì `0x1`. Bài học: thứ tự instruction set không phải vấn đề (OVS đã reorder đúng); operator initial nghi sai chỗ. Khắc phục: sửa Faucet config matching reg5. Cross-link: 9.22.10 §pipeline troubleshoot, 20.0 §incident response decision tree.

**Bài tập synthetic.** Lab Mininet kiểm thứ tự instruction set qua ba kịch bản.

```bash
# Bài 1: Switch reorder canonical order — viết theo thứ tự nào cũng ra cùng kết quả
$ sudo mn --topo single,3 --switch ovsk --controller none
mininet> sh ovs-ofctl -O OpenFlow13 del-flows s1
mininet> sh ovs-ofctl -O OpenFlow13 add-flow s1 \
    "table=0,priority=100,ip,
     actions=goto_table:1,write_metadata:0x42,apply_actions(dec_ttl)"
mininet> sh ovs-ofctl -O OpenFlow13 dump-flows s1 | grep table=0
# Kỳ vọng: actions=apply_actions(dec_ttl),write_metadata:0x42,goto_table:1

# Bài 2: Duplicate instruction lỗi
mininet> sh ovs-ofctl -O OpenFlow13 add-flow s1 \
    "table=0,priority=200,ip,
     actions=apply_actions(dec_ttl),apply_actions(mod_dl_dst:00:11:22:33:44:55)"
# Kỳ vọng: OFPBIC_DUP_INST

# Bài 3: Causal correctness — Apply trước Goto-Table
mininet> sh ovs-ofctl -O OpenFlow13 add-flow s1 \
    "table=0,priority=100,ip,nw_ttl=64,
     actions=apply_actions(dec_ttl),goto_table:1"
mininet> sh ovs-ofctl -O OpenFlow13 add-flow s1 \
    "table=1,priority=100,ip,nw_ttl=63,actions=output:2"
mininet> sh ovs-ofctl -O OpenFlow13 add-flow s1 \
    "table=1,priority=50,ip,nw_ttl=64,actions=drop"
mininet> sh ovs-appctl ofproto/trace s1 \
    "in_port=1,ip,nw_src=10.0.0.1,nw_dst=10.0.0.2,nw_ttl=64"
# Kỳ vọng: Apply dec_ttl ở table 0 → packet sang table 1 với nw_ttl=63
# → match priority=100 entry → output:2
# Nếu thứ tự ngược (Goto trước Apply): packet sang table 1 với nw_ttl=64
# → match priority=50 entry → drop. Spec ép Apply trước Goto-Table.
```

**Lỗi thường gặp + tín hiệu chẩn đoán.** Bốn lỗi. Một, **`OFPBIC_DUP_INST` khi cài flow**: viết hai instance cùng loại instruction (`apply_actions(...), apply_actions(...)`); khắc phục: gộp vào một. Hai, **`OFPBIC_UNSUP_INST` khi cài flow**: instruction không support trong OF version (e.g., Stat-Trigger trên OF 1.3 switch); khắc phục: nâng OF version hoặc dùng instruction tương đương. Ba, **operator hiểu sai thứ tự dẫn đến debug sai chỗ**: nghi switch chạy Goto-Table trước Apply, thực tế switch reorder; debug chậm vì nghi nhầm chỗ; khắc phục: kiểm spec, đọc canonical order qua `dump-flows`. Bốn, **thiếu instruction quan trọng**: viết entry chỉ có `actions=goto_table:1` mà quên Apply-Actions để dec_ttl → packet sang table 1 với TTL gốc, gây loop nếu pipeline có decrement ở table khác; khắc phục: thiết kế pipeline rõ ràng, ghi instruction modify packet ở table phù hợp.

**So sánh với hệ khác.** Bốn so sánh. Một, **action list của `Apply-Actions`** (trong cùng spec): list theo thứ tự operator viết, không reorder; khác instruction set reorder canonical. Hai, **iptables `-j` chain target order**: thứ tự rule trong chain matter (first match wins), nhưng instruction trong rule (target + modules) không có concept "set với canonical order". Ba, **P4 `apply` block sequential**: P4 thực thi statement theo thứ tự source code, no reorder; mạnh hơn về expressive nhưng phức tạp hơn cho operator. Bốn, **Cisco IOS ACL**: rule list theo thứ tự viết, first match wins; ACE (Access Control Entry) atomic, không có instruction set composition như OF.

---

## References

1. J. Crichigno, E. Sharif, A. Kfoury — *"Lab 6: Implementing Routing using multiple Flow Tables"* — NSF Award 1829698 "CyberTraining CIP", University of South Carolina, WASTC vFDW 2021, document version 09-13-2021. [`sdn-onboard/doc/ovs/OVS.pdf`](doc/ovs/OVS.pdf) trang 116-135.
2. J. Crichigno et al. — *"Exercise 2: Implementing Routing using Multiple Flow Tables"*, cùng dự án, document version 09-22-2021. [`sdn-onboard/doc/ovs/OVS.pdf`](doc/ovs/OVS.pdf) trang 136-140.
3. Anthropic Claude — *OVS curriculum compass_artifact*, Chapter 8 *"Flow installation and editing"*. [`sdn-onboard/doc/compass_artifact_wf-bd3df8ae-e2f0-4758-b72d-ece27db78c52_text_markdown.md`](doc/compass_artifact_wf-bd3df8ae-e2f0-4758-b72d-ece27db78c52_text_markdown.md).
4. Open Networking Foundation — *"OpenFlow Switch Specification Version 1.3.5"* §5.1 "Pipeline Processing", March 2015 — [https://opennetworking.org/wp-content/uploads/2014/10/openflow-switch-v1.3.5.pdf](https://opennetworking.org/wp-content/uploads/2014/10/openflow-switch-v1.3.5.pdf).
5. Open Networking Foundation — *"OpenFlow Switch Specification Version 1.1.0"*, February 2011 (bản đầu tiên giới thiệu multi-table), [https://opennetworking.org/wp-content/uploads/2013/04/openflow-spec-v1.1.0.pdf](https://opennetworking.org/wp-content/uploads/2013/04/openflow-spec-v1.1.0.pdf).
6. Open vSwitch Project — [`ovs-actions(7)`](https://man.openvswitch.org/ovs-actions.7.html) mục *"The resubmit and goto_table actions"*.
7. Open vSwitch Project — [`ovs-appctl(8)`](https://man.openvswitch.org/ovs-appctl.8.html) mục *"ofproto/trace"*.
8. OVN Project — [`ovn-architecture(7)`](https://www.ovn.org/support/dist-docs/ovn-architecture.7.html) *"Logical Flow"*.
