# 9.25. OVS flow debugging — `ofproto/trace`, `dpif/show`, hygiene với `replace-flows`/`diff-flows`

> **Môi trường thực hành:** Ubuntu 22.04 LTS, OVS 2.17.9, kernel 5.15, một bridge `br0` với 100-500 flow entry cố ý cài nhiều rule để mô phỏng môi trường production.
> **Khối:** IX, OVS internals + operations (Part 9, bổ sung session 24 Phase D).
> **Phần:** Part 9 (OVS).
> **Plan:** `plans/sdn-foundation-architecture.md` §F.4.5 — Phase D flow debugging toolbox.
> **Prerequisites:** Part 9.1 (OVS userspace vs kernel module), Part 9.2 (megaflow vs microflow), Part 9.4 (OVS CLI playbook), Part 9.15 (megaflow caching + revalidator), Part 9.19 (flow entry 6 thành phần), Part 9.22 (multi-table pipeline), Part 9.23 (priority resolution), Part 9.24 (conntrack state).
> **Nguồn offline chính:**
> - `sdn-onboard/doc/ovs/OpenVSwitch.pdf` NSRC *"Open vSwitch"* slide deck, trang 7-21 — các slide "Debugging the Database", "Flow Debugging", "Tracing Flow (ICMP Allowed / TCP allowed / TCP denied)" (NSRC Network Startup Resource Center, minh hoạ firewall 4 rule + 3 trace case).
> - `sdn-onboard/doc/compass_artifact_wf-*.md` Chapter 10 *"Flow-table hygiene: monitor, replace-flows, diff-flows"* + Chapter Q *"Datapath introspection"* + Chapter L *"Logging and debugging với ovs-appctl"* + Chapter R *"ovs-appctl reference tour"* (Anthropic OVS curriculum).
> **Nguồn online chính:** [`ovs-appctl(8)`](https://man.openvswitch.org/ovs-appctl.8.html) mục `ofproto/trace` + `dpif/show` + `upcall/show`, [`ovs-ofctl(8)`](https://man.openvswitch.org/ovs-ofctl.8.html) mục `monitor` + `replace-flows` + `diff-flows`, [docs.openvswitch.org tracing tutorial](https://docs.openvswitch.org/en/latest/topics/tracing/).

---

## Mục tiêu bài học

Sau khi hoàn thành phần này, bạn có thể:

1. **Giải thích (Understand)** vì sao đọc `ovs-ofctl dump-flows` 2000 dòng không phải cách gỡ lỗi flow table hiệu quả, và vì sao `ofproto/trace` cho kết quả trong vài giây.
2. **Phân tích (Analyze)** cấu trúc kết quả `ofproto/trace`, khối `bridge(...)`, thứ tự match qua các table, `Datapath actions`, `Final flow`, `Megaflow`, và đọc được từng khối nói gì về đường đi gói tin.
3. **Phân biệt (Differentiate)** ba lệnh dump flow khác nhau: `ovs-ofctl dump-flows` (OpenFlow entry trong userspace), `ovs-dpctl dump-flows` (megaflow cache trong kernel), `ovs-appctl bridge/dump-flows` (bao gồm cả flow ẩn như in-band, fail-open).
4. **Áp dụng (Apply)** workflow vệ sinh flow table trong môi trường production: `ovs-ofctl monitor` theo dõi thay đổi thời gian thực, `diff-flows` so sánh trước khi áp dụng, `replace-flows` thay nguyên bảng nguyên tử.
5. **Đánh giá (Evaluate)** ba tình huống NSRC tracing — ICMP được phép, TCP port 80 được phép, TCP port khác bị chặn, để đối chiếu hành vi kỳ vọng với kết quả `ofproto/trace`.

---

Kiến thức tiên quyết:

- Kiến trúc OVS hai tầng: `ovs-vswitchd` (userspace) quyết định, kernel module (datapath) cache (Part 9.1-9.2).
- Megaflow wildcarded match khác với OpenFlow flow exact match (Part 9.15).
- Flow entry 6 thành phần: match + priority + actions + cookie + timeouts + counters (Part 9.19).
- Action set tích luỹ qua multi-table pipeline (Part 9.22).

---

## 9.25.1 Vì sao `dump-flows` 2000 dòng là hạ sách — bối cảnh mở đầu

Tháng 11 năm 2021, một nhóm vận hành OpenStack tại một hãng viễn thông Đông Nam Á gặp sự cố: VM mới tạo không ping được gateway của subnet. Log `nova-compute` sạch, `neutron-server` không báo lỗi, `ovn-controller` ghi nhận binding thành công. Kỹ sư on-call chạy `ovs-ofctl dump-flows br-int`, ra 2.847 dòng. Anh đọc từng dòng trong 45 phút, không tìm ra. Đến khi cấp cao hơn vào ca, chỉ mất 3 giây gõ `ovs-appctl ofproto/trace br-int "in_port=12,ip,nw_src=10.0.0.10,nw_dst=10.0.0.1"`. Kết quả phơi bày ngay: một flow entry do script canary của đội bảo mật cài sai priority đang chặn traffic trước khi đến table NAT. Thời gian giải quyết nếu biết công cụ: 3 giây. Thời gian giải quyết bằng cách đọc dump-flows: 45 phút.

Bài hôm nay là bộ công cụ gỡ lỗi flow table mà mọi kỹ sư SDN phải thuộc lòng: `ofproto/trace`, `dpif/show`, `monitor`, `replace-flows`, `diff-flows`. Đây không phải tính năng nâng cao. Đây là vũ khí sơ cấp. Đọc dump-flows từng dòng trong flow table 500 rule là hạ sách cuối cùng, không phải bước đầu tiên.

### Vì sao dump-flows không đủ

Compass Chapter 10 ghi rõ bài toán: *"Flow tables can become incredibly complex, but OVS has tools to make it easier to debug."* Ba lý do cụ thể khiến đọc dump-flows không hiệu quả:

**Một — số lượng flow entry tăng theo cấp số nhân với quy mô.** OVN compute host có 50-80 tenant thì `ovn-controller` sinh 2.000-10.000 flow entry. Đọc tuyến tính tốn vài giờ.

**Hai — thứ tự match không hiển thị trực quan.** Dump-flows in theo table và priority giảm dần, nhưng đường đi thực của một gói tin qua pipeline có thể đi qua 8 table và kích hoạt 15 entry liên tiếp. Thứ tự đó không đọc ra được từ dump-flows.

**Ba — match field có wildcard và mask.** Một flow `ip,nw_src=10.0.0.0/8,priority=100` match tất cả IP trong subnet `10.0.0.0/8`, nhưng khi tra gói tin cụ thể 10.0.0.5, người đọc phải tự tính mask bằng tay, rất dễ nhầm.

`ofproto/trace` giải tất cả ba vấn đề trong một lệnh: nó giả lập gói tin đi qua pipeline, theo đúng thứ tự, áp dụng đúng mask, và in ra kết quả cuối cùng.

---

## 9.25.2 `ofproto/trace` — giả lập gói tin qua pipeline

`ovs-appctl ofproto/trace` là lệnh chạy qua ovs-appctl socket (thường `/var/run/openvswitch/ovs-vswitchd.pid.ctl`), yêu cầu `ovs-vswitchd` giả lập việc xử lý một gói tin hypothetical mà không thực sự gửi gói tin nào qua datapath. Cú pháp cơ bản theo `ovs-appctl(8)`:

```
ovs-appctl ofproto/trace <bridge> "<flow-spec>"
```

Trong đó `<flow-spec>` là chuỗi match field kiểu OVS, ví dụ:

```bash
sudo ovs-appctl ofproto/trace br-int \
  "in_port=12,ip,nw_src=10.0.0.10,nw_dst=10.0.0.1,tcp,tp_dst=80"
```

`ovs-vswitchd` sẽ:

1. Parse flow-spec thành bộ giá trị match field đầy đủ.
2. Đi qua pipeline bắt đầu table 0, tra match từng table.
3. Ghi lại từng match + action thực thi.
4. Thu thập action set cuối cùng.
5. In kết quả ra stdout.

Không có gói tin nào được gửi thật. Đây là simulation thuần tuý, an toàn trong môi trường production (không tạo side effect trên datapath).

### Vì sao gọi qua `ovs-appctl` thay vì `ovs-ofctl`

`ovs-ofctl` là công cụ OpenFlow chuẩn, giao tiếp với `ovs-vswitchd` qua giao thức OpenFlow (TCP/6633 hoặc Unix socket). `ovs-appctl` là công cụ management của chính OVS, giao tiếp qua Unix socket nội bộ. `ofproto/trace` là lệnh quản trị, không thuộc OpenFlow spec → chỉ có ở `ovs-appctl`. Một số phiên bản OVS cũ cho phép `ovs-ofctl ofproto/trace` như shortcut nhưng cú pháp chuẩn vẫn là `ovs-appctl ofproto/trace`.

---

## 9.25.3 Cú pháp flow-spec đầy đủ

Flow-spec truyền vào `ofproto/trace` là chuỗi match field cách nhau bằng dấu phẩy, dùng cú pháp giống `ovs-ofctl add-flow` nhưng KHÔNG có phần `actions=`. Các trường tối thiểu bắt buộc phụ thuộc vào layer bạn muốn trace:

**Chỉ L2:**
```
in_port=1,dl_src=aa:bb:cc:00:00:01,dl_dst=aa:bb:cc:00:00:02,dl_type=0x0800
```

**L2 + L3 IPv4:**
```
in_port=1,ip,nw_src=10.0.0.1,nw_dst=10.0.0.2,nw_proto=17
```

Chú ý: khi dùng alias `ip`, OVS tự gán `dl_type=0x0800`. Tương tự `arp` → `dl_type=0x0806`, `ipv6` → `dl_type=0x86dd`.

**L2 + L3 + L4 TCP:**
```
in_port=1,tcp,nw_src=10.0.0.1,nw_dst=10.0.0.2,tp_src=52134,tp_dst=80
```

Alias `tcp` tự gán `dl_type=0x0800,nw_proto=6`. Các alias khác: `udp` → `nw_proto=17`, `icmp` → `nw_proto=1`, `sctp` → `nw_proto=132`.

**Conntrack state (Part 9.24):**
```
in_port=1,tcp,nw_src=10.0.0.1,nw_dst=10.0.0.3,ct_state=+trk+est,ct_zone=7
```

### Các trường nào bắt buộc

`ofproto/trace` không yêu cầu đầy đủ mọi trường, nhưng thiếu trường nào thì trường đó sẽ mặc định 0. Nếu flow entry trong bảng match trên `nw_src` cụ thể mà bạn không khai báo `nw_src` trong flow-spec, gói tin giả lập sẽ có `nw_src=0.0.0.0`, có thể không match ý định.

Quy tắc thực dụng: **khai báo mọi trường mà flow rule trong bảng có match**. Nếu không chắc, dùng output `ovs-ofctl dump-flows` tham khảo các match field và khai báo đầy đủ trong flow-spec.

### Tuỳ chọn bổ sung

```
ovs-appctl ofproto/trace br0 "<flow>" [--ct-next '<ct-next-spec>'] [-generate]
```

- `-generate`: nếu pipeline có action `learn`, với `-generate` thì `ofproto/trace` sinh ra flow thật tương ứng; không có thì chỉ trace đường đi.
- `--ct-next`: mô phỏng trạng thái conntrack cho lần gặp `ct()` tiếp theo, hữu ích khi trace flow có `ct_state=+trk+est` nhưng chưa có entry conntrack thật.

---

## 9.25.4 Đọc kết quả `ofproto/trace` — bốn khối chính

Kết quả trả về của `ofproto/trace` có cấu trúc cố định bốn khối:

```
Flow: <flow-spec đầy đủ sau khi OVS điền mặc định>

bridge("br-int")
-------------
 0. <match trong table 0>
    <action thực thi>
 1. <match trong table 1>
    <action thực thi>
 ...

Final flow: <flow sau khi mọi field-modification áp dụng>
Megaflow: <flow với wildcard mask do OVS tính toán để cache>
Datapath actions: <chuỗi action tuyến tính cho kernel datapath>
```

### Khối 1: `Flow:`

Đây là flow-spec đầy đủ sau khi OVS điền giá trị mặc định cho các trường bạn không khai báo. Ví dụ nếu bạn truyền vào `in_port=1,tcp,tp_dst=80`, khối `Flow:` sẽ hiển thị:

```
Flow: tcp,in_port=1,vlan_tci=0x0000,dl_src=00:00:00:00:00:00,dl_dst=00:00:00:00:00:00,nw_src=0.0.0.0,nw_dst=0.0.0.0,nw_tos=0,nw_ecn=0,nw_ttl=0,tp_src=0,tp_dst=80
```

Kiểm tra khối này để biết OVS hiểu flow-spec của bạn thế nào, mọi trường không khai báo thành 0, có thể là nguyên nhân không match flow thực.

### Khối 2: `bridge(...)` và chuỗi lookup

Đây là phần quan trọng nhất, trình tự match qua pipeline. Format:

```
bridge("br-int")
-------------
 0. ip,nw_dst=10.0.0.0/24, priority 32768
    goto_table:1
 1. ip,nw_dst=10.0.0.1, priority 40000
    mod_dl_src:aa:bb:cc:dd:ee:ff
    mod_dl_dst:11:22:33:44:55:66
    dec_ttl
    goto_table:2
 2. dl_dst=11:22:33:44:55:66, priority 32768
    output:3
```

Mỗi dòng bắt đầu bằng số. Đó là ID table. Sau là flow rule match (hiển thị các match field đã match) + priority. Dòng tiếp theo thụt lề liệt kê action thực thi. Action `goto_table:N` kết thúc việc match ở table hiện tại, chuyển sang table N.

### Khối 3: `Final flow:`

Sau khi mọi action `mod_*` / `dec_ttl` / `set_field` đã áp dụng, đây là flow cuối cùng trước khi xuất gói tin. So sánh với `Flow:` để thấy trường nào bị thay đổi:

```
Final flow: tcp,in_port=1,...,dl_src=aa:bb:cc:dd:ee:ff,dl_dst=11:22:33:44:55:66,nw_ttl=63
```

Ba thay đổi dễ thấy: MAC src, MAC dst (do `mod_dl_*`), TTL giảm từ 64 xuống 63 (do `dec_ttl`).

### Khối 4: `Megaflow:` và `Datapath actions:`

`Megaflow:` là flow wildcarded mà OVS sẽ install vào kernel cache sau khi xử lý gói tin thật có cùng đặc điểm. Megaflow luôn có mask ít chính xác hơn match gốc. OVS chỉ giữ các trường thực sự tham gia quyết định. Ví dụ:

```
Megaflow: recirc_id=0,eth,ip,in_port=1,nw_dst=10.0.0.0/24,nw_frag=no
```

`nw_src` không xuất hiện trong megaflow vì không flow rule nào match trên `nw_src`. `tp_dst` cũng không. OVS bỏ vì không cần thiết. Chi tiết megaflow construction ở Part 9.15.

`Datapath actions:` là chuỗi action tuyến tính mà kernel datapath sẽ thực thi khi nhận gói tin thực có cùng megaflow match. Đây là kết quả "đã biên dịch" của toàn bộ pipeline:

```
Datapath actions: set(eth(src=aa:bb:cc:dd:ee:ff,dst=11:22:33:44:55:66)),set(ipv4(ttl=-1)),3
```

Ba action: set MAC (kết hợp `mod_dl_src` + `mod_dl_dst`), set IPv4 (giảm TTL), output:3. Kernel datapath không hiểu OpenFlow. Nó thực thi chuỗi action datapath-level này.

> **Điểm mấu chốt:** Khi gỡ lỗi, luôn đọc ba chỗ cụ thể: (a) table ID đầu tiên — gói tin đi vào có match flow như kỳ vọng không; (b) `Final flow:` — trường nào bị sửa; (c) `Datapath actions:` — có `drop` cuối không. Nếu thấy `Datapath actions: drop` thì pipeline kết thúc bằng việc huỷ gói tin.

---

## 9.25.5 Ba lệnh dump flow — so sánh bài bản

OVS có ba lệnh dump flow, dùng cho mục đích khác nhau. Nhầm lẫn giữa chúng là nguồn gốc của nhiều buổi debug lãng phí.

### `ovs-ofctl dump-flows <bridge>`

Dump OpenFlow flow entry mà userspace `ovs-vswitchd` quản lý. Đây là bảng thật sự quyết định đường đi gói tin. Output:

```bash
sudo ovs-ofctl dump-flows br-int
```

```
cookie=0x0, duration=12345.67s, table=0, n_packets=8834, n_bytes=917136, idle_age=2, priority=100,ip,nw_src=10.0.0.0/8 actions=goto_table:10
cookie=0x5, duration=11234.12s, table=10, n_packets=7812, n_bytes=809432, priority=50,tcp,tp_dst=80 actions=output:3
cookie=0x0, duration=12345.67s, table=0, n_packets=156, n_bytes=17248, priority=0 actions=NORMAL
```

Mỗi dòng là một flow entry. Đọc từng trường:
- `cookie=0xN`: nhãn nhóm flow (Part 9.19 §19.4).
- `duration=Ns`: thời gian entry đã tồn tại.
- `n_packets`/`n_bytes`, bộ đếm match.
- `idle_age`: số giây kể từ lần match cuối.
- `priority=N,<match>`: priority + match field.
- `actions=...`: chuỗi action.

### `ovs-dpctl dump-flows [datapath]`

Dump **megaflow cache** trong kernel datapath. Không phải flow entry OpenFlow. Đây là cache của `ovs-vswitchd` đã biên dịch xuống kernel cho các gói tin đã đi qua:

```bash
sudo ovs-dpctl dump-flows
```

```
recirc_id(0),in_port(2),eth(src=aa:bb:cc:00:00:01,dst=aa:bb:cc:00:00:02),eth_type(0x0800),ipv4(src=10.0.0.1,dst=10.0.0.2,frag=no), packets:1234, bytes:123456, used:0.123s, actions:3
```

Ba khác biệt so với `ovs-ofctl dump-flows`:
- **Format khác**: sử dụng cú pháp datapath (match field trong ngoặc đơn).
- **Ít entry hơn**: cache chỉ chứa megaflow của gói tin đã thấy, không phải toàn bộ bảng OpenFlow.
- **Wildcard hẹp hơn**: megaflow chỉ wildcard những trường thực sự không cần thiết, nên có thể rất cụ thể.

Dùng `ovs-dpctl dump-flows` để debug tại sao gói tin cụ thể bị drop ở kernel (thay vì đọc 2.000 flow OpenFlow). Nếu megaflow không có entry cho gói tin đó, kernel sẽ upcall lên userspace → `ovs-vswitchd` tra bảng OpenFlow đầy đủ.

### `ovs-appctl bridge/dump-flows <bridge>`

Hiển thị **mọi flow**, bao gồm các flow ẩn mà `ovs-ofctl dump-flows` không hiện. OVS ngầm cài một số flow cho các tính năng:
- **In-band control**: flow cho phép controller traffic (OpenFlow packet-in/packet-out) đi qua bridge ngay cả khi flow table người dùng rỗng.
- **Fail-open**: khi mất kết nối controller, flow `actions=NORMAL` được cài để bridge tiếp tục forward.
- **STP / LLDP**: flow capture BPDU và LLDP gửi lên controller.

```bash
sudo ovs-appctl bridge/dump-flows br-int
```

Output có thêm cột `duration` dạng `n/a` cho flow vĩnh viễn, và match có thể chứa `hidden=true` flag nội bộ.

Theo compass Chapter L: *"The bridge/dump-flows command is essential when debugging controller behavior, if dump-flows (user-level) shows empty but traffic still passes, the hidden flows are doing the work."*

### Bảng so sánh nhanh

| Lệnh | Tầng | Nội dung | Khi nào dùng |
|------|------|----------|--------------|
| `ovs-ofctl dump-flows` | Userspace OpenFlow | Flow entry do người dùng cài + controller cài | Kiểm tra flow entry bạn đã `add-flow` |
| `ovs-dpctl dump-flows` | Kernel megaflow cache | Megaflow đã biên dịch cho gói tin đã thấy | Debug tại sao gói tin cụ thể drop ở kernel |
| `ovs-appctl bridge/dump-flows` | Userspace + hidden flows | Tất cả flow, kể cả in-band/fail-open | Kiểm tra flow ẩn controller tự cài |

---

## 9.25.6 `ovs-appctl dpif/show` — sức khoẻ datapath

Khi pipeline quá nặng, kernel upcall nhiều, cache đầy → hiệu năng xấu. `dpif/show` cho thấy thống kê sức khoẻ datapath:

```bash
sudo ovs-appctl dpif/show
```

```
system@ovs-system:
  lookups: hit:45234567 missed:234561 lost:12
  flows: 2345
  masks: hit:45234567 total:32 hit/pkt:32.00
  port 0: ovs-system (internal)
  port 1: br-int (internal)
  port 2: veth0
  port 3: veth1
```

Ba chỉ số quan trọng:
- **`lookups:` hit/missed/lost**: số lần tra cache thành công / miss (đẩy lên userspace) / lost (drop vì cache đầy). `missed` cao có nghĩa nhiều gói tin phải upcall, hiệu năng giảm.
- **`flows:`**: số megaflow hiện trong cache. Mặc định giới hạn 200.000 (có thể điều chỉnh bằng `ovs-appctl upcall/set-flow-limit`).
- **`masks: hit/pkt`**: trung bình một gói tin phải quét qua bao nhiêu mask trong tuple space search. Số cao (> 40) là dấu hiệu flow table quá phân mảnh.

Compass Chapter Q nhấn mạnh: *"Too many upcalls relative to packet rate means flows are being evicted too fast; raise with `ovs-appctl upcall/set-flow-limit 500000`. Tune handler and revalidator thread counts via `other_config:n-handler-threads` and `other_config:n-revalidator-threads`."*

### Các lệnh `dpif/` liên quan

```bash
ovs-appctl dpif/show-dp-features    # Xem tính năng datapath hỗ trợ (conntrack, IPsec, v.v.)
ovs-appctl upcall/show              # Số upcall đang chờ xử lý
ovs-appctl upcall/set-flow-limit N  # Giới hạn megaflow cache
ovs-appctl revalidator/wait         # Chờ tất cả revalidator xong (xoá flow stale)
ovs-appctl revalidator/purge        # Xoá tất cả megaflow ngay lập tức
```

---

## 9.25.7 Vệ sinh flow table — `monitor`, `replace-flows`, `diff-flows`

Trong môi trường production, việc thay đổi flow table phải **đếm được, đảo ngược được, nguyên tử được**. Ba công cụ OVS cung cấp để đạt điều này:

### `ovs-ofctl monitor <bridge> watch:`

Stream thay đổi flow theo thời gian thực ra stdout:

```bash
sudo ovs-ofctl monitor br-int watch:
```

Để terminal này chạy nền. Khi ai đó (hoặc controller) gọi `add-flow`, `mod-flow`, `del-flows`, bạn sẽ thấy event ngay:

```
NXST_FLOW_MOD: ADDED: table=0, priority=100, in_port=1, dl_type=0x0800, actions=output:2
NXST_FLOW_MOD: DELETED: table=0, priority=50, in_port=3, actions=drop
```

Dùng khi debug controller: chạy `monitor` song song với ứng dụng controller để thấy chính xác flow nào bị cài / xoá khi user click một nút trong UI.

### `ovs-ofctl replace-flows <bridge> <file>`

Thay **nguyên** bảng flow bằng nội dung file. Nguyên tử, không bao giờ thấy trạng thái trung gian:

```bash
# Chuẩn bị file flow entry
cat > /tmp/new-flows.txt <<EOF
table=0,priority=100,ip,nw_dst=10.0.0.1,actions=output:3
table=0,priority=50,actions=NORMAL
EOF

# Thay toàn bộ
sudo ovs-ofctl replace-flows br-int /tmp/new-flows.txt
```

Khác với `add-flow` (chỉ thêm) hoặc `add-flows` file (chỉ thêm hàng loạt), `replace-flows`:
- Xoá mọi flow hiện tại (trừ flow ẩn in-band).
- Cài flow mới từ file.
- Tất cả trong một transaction, không có khoảng trống khi bảng rỗng.

Lệnh này dùng trong triển khai kiểu blue-green flow table: sinh file flow mới, review, `replace-flows` để cutover.

### `ovs-ofctl diff-flows <bridge> <file>`

**So sánh mà không áp dụng**, hiển thị flow nào trong file KHÁC với bảng hiện tại:

```bash
sudo ovs-ofctl diff-flows br-int /tmp/new-flows.txt
```

Output kiểu diff:
```
-table=0, priority=100, ip, nw_dst=10.0.0.2, actions=output:4
+table=0, priority=100, ip, nw_dst=10.0.0.1, actions=output:3
 table=0, priority=50, actions=NORMAL
```

`-` là flow sẽ bị xoá nếu `replace-flows`, `+` là flow sẽ được thêm, không dấu là giữ nguyên.

Compass Chapter 10 viết: *"`ovs-ofctl diff-flows br0 new.flows` shows what would change without applying it, always run this in production before `replace-flows`."* Đây là quy tắc bắt buộc: không `replace-flows` mà không `diff-flows` trước.

### Quy trình vệ sinh flow table chuẩn

```
1. ovs-ofctl dump-flows br-int > current.txt     # Chụp bảng hiện tại
2. (sinh new.txt từ controller / script)
3. ovs-ofctl diff-flows br-int new.txt            # Kiểm tra diff
4. (review diff, xác nhận với đội)
5. ovs-ofctl replace-flows br-int new.txt         # Áp dụng nguyên tử
6. (giám sát dump-flows, kiểm chứng lưu lượng thực)
7. Nếu sự cố: ovs-ofctl replace-flows br-int current.txt   # Rollback
```

---

## 9.25.8 Ba ví dụ NSRC tracing — firewall 4 rule

Slide NSRC OpenVSwitch (trang 17-20) trình bày một firewall đơn giản: chặn mọi TCP trừ port 80, cho phép non-TCP, cho phép mọi gói từ port 2. Bốn flow rule:

```bash
# Flow A: Chuyển TCP từ port 1 sang giai đoạn tiếp theo của pipeline
sudo ovs-ofctl add-flow br0 "priority=100,tcp,in_port=1,actions=resubmit:4000"

# Flow B: Cho phép TCP port 80 (implicit drop mọi port khác)
sudo ovs-ofctl add-flow br0 "priority=100,tcp,in_port=4000,tp_dst=80,actions=NORMAL"

# Flow C: Cho phép mọi traffic non-TCP từ port 1
sudo ovs-ofctl add-flow br0 "priority=90,in_port=1,actions=NORMAL"

# Flow D: Cho phép mọi traffic từ port 2
sudo ovs-ofctl add-flow br0 "priority=100,in_port=2,actions=NORMAL"
```

Chú ý: `resubmit:4000` gọi một "virtual port" 4000 mà không tồn tại thật. Đây là kỹ thuật dùng `resubmit(port,table)` để đánh dấu pipeline stage. Trong OVS hiện đại cách này được thay bằng `goto_table` (Part 9.22), nhưng NSRC slide vẫn dùng phong cách cũ để minh hoạ.

### Ba tình huống trace

**Tình huống 1 — ICMP vào từ port 1 (cho phép):**

```bash
sudo ovs-appctl ofproto/trace br0 "in_port=1,icmp"
```

Kỳ vọng:
- Flow C match (priority 90, in_port=1, action `NORMAL`).
- Flow A không match vì không phải TCP.
- `Datapath actions: <NORMAL processing output>`.

**Tình huống 2. TCP port 80 vào từ port 1 (cho phép):**

```bash
sudo ovs-appctl ofproto/trace br0 "in_port=1,tcp,tp_dst=80"
```

Kỳ vọng:
- Flow A match (priority 100, tcp, in_port=1): action `resubmit:4000`, đổi `in_port` thành 4000 rồi resubmit.
- Flow B match (priority 100, tcp, in_port=4000, tp_dst=80): action `NORMAL`.
- `Datapath actions: <NORMAL>`.

**Tình huống 3. TCP port 22 vào từ port 1 (bị chặn):**

```bash
sudo ovs-appctl ofproto/trace br0 "in_port=1,tcp,tp_dst=22"
```

Kỳ vọng:
- Flow A match: resubmit sang port 4000.
- Sau resubmit: flow B không match (vì `tp_dst=22` khác 80). Flow C không match (vì `in_port=4000` khác 1). Flow D không match (vì `in_port=4000` khác 2).
- Không có flow nào match → implicit drop.
- `Datapath actions: drop`.

Đây chính là khuôn mẫu để gỡ lỗi firewall: trace từng loại lưu lượng kỳ vọng, kiểm chứng kết quả có đúng action như thiết kế.

---

## Guided Exercise 1 — Debug flow table có cookie conflict và priority sai

**Mục đích:** Quan sát trực tiếp cách `ofproto/trace` phơi bày một bug phổ biến trong flow table, priority cài ngược khiến ACL deny rule không bao giờ match, và so sánh thời gian gỡ lỗi bằng `ofproto/trace` (vài giây) với việc đọc `dump-flows` (vài phút).

**Giả thuyết cần kiểm chứng:**

> *"Khi một flow ACL deny cài với priority thấp hơn flow forward mặc định, `ofproto/trace` sẽ cho thấy flow forward match trước, deny không bao giờ được kích hoạt. `Datapath actions` sẽ hiển thị output bình thường thay vì `drop`. Đây chính là bug kinh điển 'rule không hoạt động' mà đọc dump-flows khó phát hiện."*

### Chuẩn bị

Tạo bridge test với 3 port:

```bash
sudo ovs-vsctl add-br test-br
sudo ip link add veth0 type veth peer name veth0-peer
sudo ip link add veth1 type veth peer name veth1-peer
sudo ip link add veth2 type veth peer name veth2-peer
sudo ovs-vsctl add-port test-br veth0 -- add-port test-br veth1 -- add-port test-br veth2
```

### Bước 1 — Cài bộ flow đúng (ACL deny priority cao)

```bash
sudo ovs-ofctl del-flows test-br

# Flow 1: ACL deny h3 (port 3) → h1 (port 1), priority 40000
sudo ovs-ofctl add-flow test-br \
  "priority=40000,ip,in_port=3,nw_dst=10.0.0.1,actions=drop"

# Flow 2: Default forwarding
sudo ovs-ofctl add-flow test-br "priority=100,actions=NORMAL"
```

Trace traffic h3 → h1, kỳ vọng drop:

```bash
sudo ovs-appctl ofproto/trace test-br \
  "in_port=3,ip,nw_src=10.0.0.3,nw_dst=10.0.0.1"
```

Kết quả (doc-plausible):
```
 0. ip,in_port=3,nw_dst=10.0.0.1, priority 40000
    drop

Datapath actions: drop
```

ACL hoạt động đúng.

### Bước 2 — Cài bộ flow có bug (priority ngược)

Mô phỏng tình huống kỹ sư mới gõ nhầm priority ACL:

```bash
sudo ovs-ofctl del-flows test-br

# Flow 1: ACL deny (SAI — priority chỉ 50, thấp hơn default 100)
sudo ovs-ofctl add-flow test-br \
  "priority=50,ip,in_port=3,nw_dst=10.0.0.1,actions=drop"

# Flow 2: Default forwarding (priority 100 cao hơn)
sudo ovs-ofctl add-flow test-br "priority=100,actions=NORMAL"
```

Trace lại cùng traffic:

```bash
sudo ovs-appctl ofproto/trace test-br \
  "in_port=3,ip,nw_src=10.0.0.3,nw_dst=10.0.0.1"
```

Kết quả (doc-plausible):
```
 0. priority 100
    NORMAL
     -> forwarding to learned port

Datapath actions: 1
```

**ACL drop không xuất hiện trong trace**, flow 1 không bao giờ match vì priority 50 < 100 của flow 2. `Datapath actions: 1` nghĩa là gói tin được forward đến port 1.

### Bước 3 — So sánh với đọc `dump-flows`

```bash
sudo ovs-ofctl dump-flows test-br
```

Kết quả:
```
cookie=0x0, duration=12.3s, table=0, n_packets=0, n_bytes=0, priority=100 actions=NORMAL
cookie=0x0, duration=23.4s, table=0, n_packets=0, n_bytes=0, priority=50,ip,in_port=3,nw_dst=10.0.0.1 actions=drop
```

Đọc dump-flows có thể nhìn ra priority ngược, nhưng chỉ khi bạn **biết** priority mặc định là 32768 và flow-forward là 100. Trong flow table 500 entry, việc này cực khó phát hiện bằng mắt. `ofproto/trace` hiển thị **trực tiếp** rule nào match, không cần so sánh priority trong đầu.

### Bước 4 — POE "đọc dump-flows đủ debug"

Giả thuyết ngược cần bác bỏ:

> *"Tôi không cần `ofproto/trace` — đọc `dump-flows` đủ để tôi biết rule nào chặn gói tin."*

Phản chứng: với flow table chỉ 2 rule như trên, mắt tốt có thể đọc được priority ngược. Nhưng scale lên 50 rule với `cookie` nhóm, `idle_timeout` khác nhau, `hard_timeout` khác nhau, match field dài 10 cột → phân tích priority trong đầu là bất khả thi. Test:

```bash
# Cài 50 flow rule random priority 50-150
for i in $(seq 1 50); do
  p=$((RANDOM % 100 + 50))
  sudo ovs-ofctl add-flow test-br \
    "priority=$p,ip,nw_src=10.0.${i}.0/24,actions=output:1"
done

# Dump ra xem
sudo ovs-ofctl dump-flows test-br | wc -l    # > 50 dòng
sudo ovs-ofctl dump-flows test-br | head -10
```

Giờ thử xác định bằng mắt rule nào sẽ match gói tin `nw_src=10.0.25.5`: mất vài phút, dễ nhầm.

Dùng `ofproto/trace`:

```bash
sudo ovs-appctl ofproto/trace test-br "ip,nw_src=10.0.25.5,nw_dst=1.1.1.1"
```

2 giây có kết quả chính xác rule nào match.

### Bài học

Đọc dump-flows từng dòng là kỹ năng đọc tĩnh; `ofproto/trace` là kỹ năng mô phỏng động. Tĩnh không thấy ảnh hưởng của priority. Động phơi bày ngay. Quy tắc thực dụng: khi flow table > 20 entry, luôn `ofproto/trace` trước, đọc `dump-flows` chỉ để kiểm chứng match expression của entry bạn đã xác định.

---

## 9.25.9 So sánh với OVN — debug logical flow

OVN có hai tầng flow: **logical flow** (trong OVSDB `Southbound_DB.Logical_Flow`) và **OpenFlow flow** (trên `br-int` của mỗi chassis). `ovs-appctl ofproto/trace br-int` chỉ trace tầng OpenFlow. Để trace tầng logical, OVN có lệnh riêng:

```bash
ovn-trace --detailed "inport == \"lsp-vm1\" && ip4.src == 10.0.0.10 && ip4.dst == 10.0.0.1"
```

`ovn-trace` giả lập gói tin đi qua logical pipeline, ingress ACL → LB → routing → egress ACL, mà chưa xuống OpenFlow. Kết quả hiển thị mỗi logical stage + action tương ứng.

Khi debug OVN sự cố, workflow hai bước:
1. `ovn-trace`, xác định logical flow nào quyết định.
2. `ovs-appctl ofproto/trace br-int`, kiểm chứng OpenFlow thực thi đúng logical flow.

Nếu logical trace đúng nhưng OpenFlow trace sai → lỗi ở `ovn-controller` dịch Logical Flow sang OpenFlow. Nếu cả hai đúng mà traffic vẫn sai → lỗi ở datapath kernel → `ovs-dpctl dump-flows` + `dpif/show`.

---

## 9.25.10 Điểm cốt lõi cần nhớ

**Một.** Đọc `ovs-ofctl dump-flows` 2.000 dòng là hạ sách — `ofproto/trace` cho kết quả chính xác trong vài giây. Luôn trace trước khi đọc dump.

**Hai.** `ofproto/trace` chạy qua `ovs-appctl`, không thuộc OpenFlow spec. Nó giả lập gói tin đi qua pipeline mà không tạo side effect trên datapath, an toàn trong môi trường production.

**Ba.** Kết quả `ofproto/trace` có 4 khối: `Flow:` (flow-spec đã điền mặc định), `bridge(...)` (chuỗi lookup qua các table), `Final flow:` (flow sau mọi field-modification), `Datapath actions:` (chuỗi action tuyến tính cho kernel).

**Bốn.** Ba lệnh dump flow khác nhau: `ovs-ofctl dump-flows` (userspace OpenFlow), `ovs-dpctl dump-flows` (kernel megaflow cache), `ovs-appctl bridge/dump-flows` (bao gồm flow ẩn in-band/fail-open). Chọn đúng lệnh theo tầng bạn đang debug.

**Năm.** `dpif/show` cho thấy sức khoẻ datapath — `lookups hit/missed/lost`, số `flows`, trung bình `masks hit/pkt`. `missed` cao hoặc `masks hit/pkt > 40` là dấu hiệu flow table phân mảnh hoặc cache quá nhỏ.

**Sáu.** Vệ sinh flow table trong môi trường production: `ovs-ofctl monitor watch:` theo dõi thay đổi thời gian thực; `diff-flows` so sánh trước khi áp dụng (bắt buộc trong production); `replace-flows` thay nguyên bảng nguyên tử. Không `replace-flows` mà không `diff-flows` trước.

**Bảy.** Debug OVN có hai tầng: `ovn-trace` cho logical flow, `ovs-appctl ofproto/trace br-int` cho OpenFlow. Nếu logical đúng mà OpenFlow sai → lỗi `ovn-controller` dịch; nếu cả hai đúng mà traffic sai → lỗi datapath → `ovs-dpctl dump-flows`.

---

## References

1. NSRC (Network Startup Resource Center), *"Open vSwitch"* slide deck, minh hoạ Flow Debugging + Tracing Flow (ICMP Allowed / TCP allowed / TCP denied). [`sdn-onboard/doc/ovs/OpenVSwitch.pdf`](doc/ovs/OpenVSwitch.pdf) trang 7-21.
2. Anthropic Claude — *OVS curriculum compass_artifact*, Chapter 10 *"Flow-table hygiene: monitor, replace-flows, diff-flows"* + Chapter Q *"Datapath introspection"* + Chapter L *"Logging and debugging with ovs-appctl"* + Chapter R *"ovs-appctl reference tour"*. [`sdn-onboard/doc/compass_artifact_wf-bd3df8ae-e2f0-4758-b72d-ece27db78c52_text_markdown.md`](doc/compass_artifact_wf-bd3df8ae-e2f0-4758-b72d-ece27db78c52_text_markdown.md).
3. Open vSwitch Project — [`ovs-appctl(8)`](https://man.openvswitch.org/ovs-appctl.8.html) mục `ofproto/trace`, `bridge/dump-flows`, `dpif/show`, `upcall/show`, `upcall/set-flow-limit`.
4. Open vSwitch Project — [`ovs-ofctl(8)`](https://man.openvswitch.org/ovs-ofctl.8.html) mục `dump-flows`, `monitor`, `replace-flows`, `diff-flows`, `add-flows`.
5. Open vSwitch Project — [`ovs-dpctl(8)`](https://man.openvswitch.org/ovs-dpctl.8.html) mục `show`, `dump-flows`.
6. Open vSwitch Project — [Tracing Packets through the OpenFlow Pipeline](https://docs.openvswitch.org/en/latest/topics/tracing/), tutorial chính thức về `ofproto/trace`.
7. OVN Project — [`ovn-trace(8)`](https://www.ovn.org/support/dist-docs/ovn-trace.8.html), công cụ trace logical flow song song với `ofproto/trace`.
