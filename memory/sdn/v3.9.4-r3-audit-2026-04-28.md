# v3.9.4 Phase R3 Axis-16 Audit Log (Block 9 axis-20 group, 5 files)

> **Files audited:** sdn-onboard/9.16 - ovs-connection-manager-controller-failover.md, sdn-onboard/9.17 - ovs-performance-benchmark-methodology.md, sdn-onboard/9.18 - ovs-native-l3-routing.md, sdn-onboard/9.19 - ovs-flow-table-granularity.md, sdn-onboard/9.20 - ovs-vlan-access-trunk.md.
> **Baseline:** local OVS repo at v2.17.9, commit `0bea06d9957e3966d94c48873cd9afefba1c2677` (2024-02-08).
> **Method:** plan v3.9.1 §Q7.2 (locked). The corrected method per plan v3.9.1 §0.3 was applied: `cd C:/Users/voleh/Documents/ovs && git checkout v2.17.9 && grep -nE "^<fn>" <path>`, with wider grep fallback when the column-0 anchor returned empty.
> **Date:** 2026-04-28.

---

## 1. Summary

| Metric | Count |
|---|---|
| Total citations audited | 19 |
| VERIFIED | 18 |
| WRONG_LINE | 0 |
| WRONG_PATH | 0 |
| WRONG_NAME | 0 |
| WRONG_TYPE | 0 |
| FABRICATED | 1 |
| **Confirmed violations** | **1** |
| **Error rate** | **5.3 percent** |
| **FAB rate** | **5.3 percent** |

**Mid-batch escalation gate per plan v3.9.4 §3.7:** FAB rate 5.3 percent and total-violation rate 5.3 percent are both far below the calibrated thresholds (FAB 40 percent, total 70 percent). Gate **NOT triggered**. R3 fix proceeds.

The error rate is dramatically lower than the v3.9.3 R1 prior of 50.3 percent. R3 files (the axis-20 group) were authored with lighter source-code attribution (mostly cross-domain comparisons, fewer internal function citations) and what citations exist were carefully chosen at a higher level of abstraction (function names without inline line numbers, path-only references, hyperlinks to specific upstream commits).

---

## 2. Per-file findings

### 2.1. 9.16 - ovs-connection-manager-controller-failover.md (447 lines)

Two path-only references at lines 26 and 430:

- Line 26: "trong `ofproto-dpif.c` và `connmgr.c`". Both files exist in the OVS repo at v2.17.9 (verified by `ls ofproto/connmgr.c` and `ls ofproto/ofproto-dpif.c`). Path-only citation. **VERIFIED.**
- Line 430: "OVS Source, connmgr.c" with hyperlink to `github.com/openvswitch/ovs/blob/main/ofproto/connmgr.c`. Path-only. **VERIFIED.**

No axis-16 source-code citation block (`#### Source code tham chiếu` style) exists. No fix needed.

### 2.2. 9.17 - ovs-performance-benchmark-methodology.md (761 lines)

Six citations in §"Upstream source code (OVS v2.17.9 + Linux 5.15)" at line 731 onward:

| # | Citation | Verification | Status |
|---|----------|--------------|--------|
| 1 | `net/openvswitch/datapath.c::ovs_dp_process_packet()` | exists at `datapath/datapath.c:234` (OVS out-of-tree equivalent of mainline `net/openvswitch/datapath.c`); the curriculum's URL points to `git.kernel.org` mainline v5.15, which is the correct path for that hyperlink | **VERIFIED** |
| 2 | `net/openvswitch/flow_table.c::ovs_flow_tbl_lookup_stats()` | `datapath/flow_table.c:667` | **VERIFIED** |
| 3 | `net/openvswitch/actions.c::ovs_execute_actions()` | `datapath/actions.c:1541` | **VERIFIED** |
| 4 | `ofproto/ofproto-dpif-upcall.c::udpif_upcall_handler()` | line 809 | **VERIFIED** |
| 5 | `ofproto/ofproto-dpif-upcall.c::udpif_revalidator()` | declared at line 347, defined later in the same file | **VERIFIED** |
| 6 | `ofproto/ofproto-dpif-xlate.c::xlate_actions()` | line 7885 | **VERIFIED** |
| 7 | `lib/classifier.c::classifier_lookup__()` | line 938 | **VERIFIED** |

All seven citations VERIFIED. No fix needed.

Note: the curriculum cites mainline kernel paths (`net/openvswitch/...`) for the kernel-side functions while the local repo has only the OVS out-of-tree (`datapath/...`). The function names match in both trees. The hyperlinks correctly point to mainline kernel v5.15. No path correction needed under Rule 14 §14.3 because the curriculum's URLs are explicit about the source tree.

### 2.3. 9.18 - ovs-native-l3-routing.md (493 lines)

Section "9.18.X `dec_ttl` action source-level" at line 298 onward. One real citation embedded in pseudo-code commentary:

| # | Citation | Verification | Status |
|---|----------|--------------|--------|
| 8 | `ofproto/ofproto-dpif-xlate.c::compose_dec_ttl()` | line 5269 | **VERIFIED** |

The pseudo-code block at lines 306-319 references `execute_controller_action()` and `update_ip_checksum()` but the block is explicitly labeled "Pseudo-code" so these are descriptive, not citations to verify. Out of audit scope.

### 2.4. 9.19 - ovs-flow-table-granularity.md (521 lines)

Two citations in §"Upstream source code (OVS v2.17.9)" at line 505 onward:

| # | Citation | Verification | Status |
|---|----------|--------------|--------|
| 9 | `lib/classifier.c::classifier_lookup__()` | line 938 | **VERIFIED** |
| 10 | `ofproto/ofproto-dpif-upcall.c` (path-only) | path exists | **VERIFIED** |

### 2.5. 9.20 - ovs-vlan-access-trunk.md (548 lines)

Two function citations:

| # | Citation | Verification | Status |
|---|----------|--------------|--------|
| 11 | `ofproto/ofproto-dpif-xlate.c::xlate_normal()` | line 3083 | **VERIFIED** |
| 12 | `lib/ofp-actions.c::parse_push_vlan()` (line 373) | `grep -rn "parse_push_vlan" lib/ ofproto/` returns no result; the closest is `parse_set_vlan_vid` at line 1618 and the action enum `OFPACT_PUSH_VLAN` at line 462 | **FABRICATED** |

The push_vlan parsing in v2.17.9 happens inside `parse_set_vlan_vid` and the OpenFlow action enum `OFPACT_PUSH_VLAN`. There is no top-level function named `parse_push_vlan`.

**Fix decision per Rule 14 §14.4 Option C:** drop the fabricated function name and reword to point at the actual parsing flow.

---

## 3. Mid-batch checkpoint (per plan v3.9.4 §3.7)

- Threshold (calibrated): FAB rate above 40 percent OR total-violation rate above 70 percent.
- Observed: FAB rate 5.3 percent, total-violation rate 5.3 percent.
- Decision: continue. R3 fix proceeds.

---

## 4. Fix scope

One fix in 9.20. Reword the line-373 citation to drop `parse_push_vlan()` and point to the real parsing flow (`parse_set_vlan_vid` plus `OFPACT_PUSH_VLAN` enum). GP-13 in-commit English rewrite covers the level 3 sub-section that contains the fix.

Estimated effort: less than 30 minutes.

---

> **Authority:** plan v3.9.4 Phase R3.
> **Author:** v3.9.4 R3 audit, 2026-04-28.
> **Next step:** R3 fix commit (1 finding in 9.20).
