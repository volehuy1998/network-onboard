# v3.12 R1.N Run 1.A Audit Log (Form A, 13.14 first half)

**Date:** 2026-04-29
**Plan:** v3.12 curriculum-wide English migration, Phase R1.N Run 1.A
**Scope:** Lines 1-263 of `sdn-onboard/13.14 - ovn-nbctl-sbctl-reference-playbook.md` (header, Learning objectives, §13.14.1 CLI architecture, §13.14.2 ovn-nbctl 97 subcommands in 12 groups). Lines 264+ deferred to Run 1.B.

## Per-file evidence

| File | Range | Vi chunks closed (staged-diff) | Em-dashes removed (staged-diff) | Mixed Language-status callout | Identifiers preserved | Pre-commit hooks (staged-diff) |
|------|-------|--------------------------------|---------------------------------|------------------------------|-----------------------|--------------------------------|
| `sdn-onboard/13.14 - ovn-nbctl-sbctl-reference-playbook.md` | L1-L263 (partial) | 99 prose chunks (lang_check `--staged` PASS, 0 non-English) | 0 in staged diff (em_dash_check `--staged` PASS) | Added at L3 | `ovn-nbctl`, `ovn-sbctl`, `--db=`, `OVN_NB_DAEMON`, `--detach`, `--pidfile=`, `--log-file=`, `--verbose`, `--timeout=`, `--no-wait`, `--dry-run`, all 97 subcommand names (`ls-add`, `lsp-set-addresses`, `lr-route-add`, `lb-add`, `acl-add`, `pg-add`, `dhcp-options-create`, `qos-add`, `meter-add`, `ha-chassis-group-add-chassis`, `copp-add`, `set-connection`, `set-ssl`, OVSDB primitives `list`/`find`/`get`/`set`/`add`/`remove`/`clear`/`create`/`destroy`/`wait-until`), table column names (`Logical_Switch`, `Logical_Router`, `Port_Binding`, `Datapath_Binding`, `Logical_Flow`, `Chassis`, `DHCP_Options`, `Port_Group`, `HA_Chassis_Group`, `Load_Balancer`), source paths (`utilities/ovn-nbctl.c` 7244 lines, `utilities/ovn-sbctl.c` 1528 lines, `ctl-libs/ctl-lib.c`), version baseline branch-22.03 | em_dash PASS, lang_check PASS, rubric_leak PASS |

## Partial-state note

This file remains in mixed-language transition state until Run 1.B closes:

- **Now English (post Run 1.A):** Header block, Learning objectives, §13.14.1.1, §13.14.1.2, §13.14.1.3, §13.14.2.1 through §13.14.2.11.
- **Still Vietnamese (Run 1.B):** §13.14.3 ovn-sbctl, §13.14.4 anatomy, §13.14.5, and Phase D / v3.5 / v3.6 backfill subsections (§13.14.6 through §13.14.12 plus references and cross-links).

The Language-status callout at line 3 documents this state explicitly per plan v3.9.1 §8.3.

## Validation summary

- `em_dash_check.py --staged`: PASS (0 em-dash in staged diff).
- `lang_check.py --staged`: PASS (99 prose chunks, 0 non-English).
- `rubric_leak_check.py`: PASS (file-level).
- Whole-file scans still flag 11 em-dashes and 188 non-English chunks, all in lines 264+ which are out of Run 1.A scope.

## Effort

- Edit calls: 4 (header + objectives, §13.14.1, §13.14.2.1-2.2, §13.14.2.3-2.11).
- Hours: ~0.4.
- Halt signals: none.

## Files closed in Run 1.B retry2 (13.14 partial closure, 2026-04-29)

| File | Range | Vi chunks closed (staged-diff) | Em-dashes removed (staged-diff) | Mixed Language-status callout | Identifiers preserved | Pre-commit hooks (staged-diff) |
|------|-------|--------------------------------|---------------------------------|------------------------------|-----------------------|--------------------------------|
| `sdn-onboard/13.14 - ovn-nbctl-sbctl-reference-playbook.md` | §13.14.3 - §13.14.9.2 plus §13.14.9.3 heading (partial) | ~76 prose chunks across two commits (lang_check `--staged` PASS each commit, 0 non-English) | 0 in staged diff (em_dash_check `--staged` PASS each commit) | Updated to Mixed at L3 honestly listing remaining Vietnamese sections | All 15 ovn-sbctl subcommands (`init`, `chassis-add`, `chassis-del`, `lsp-bind`, `lsp-unbind`, `lflow-list`, `dump-flows`, `count-flows`, `ip-multicast-flush`, `set-connection`, etc.), 10 Anatomy outputs (`ovn-nbctl show`, `ovn-sbctl list Chassis`, `ovn-sbctl list Port_Binding`, `ovn-sbctl lflow-list`, `ovn-nbctl lr-route-list`, `ovn-nbctl acl-list`, `ovn-nbctl lb-list`, `ovn-nbctl ha-chassis-group-list`, `ovn-nbctl get NB_Global . nb_cfg`, `ovn-sbctl find Port_Binding chassis=[]`), all SBDB tables (`Chassis`, `Port_Binding`, `Datapath_Binding`, `Logical_Flow`, `MAC_Binding`, `Multicast_Group`, `Service_Monitor`, `Encap`, `Gateway_Chassis`, `HA_Chassis`, `HA_Chassis_Group`, `RBAC_Role`, `RBAC_Permission`, `SB_Global`), all 30+ ovn-nbctl options (`--db`, `--leader-only`, `--no-leader-only`, `--shuffle-remotes`, `--no-wait`, `--wait=sb`, `--wait=hv`, `--print-wait-time`, `--commit-retry`, `--unix-lock`, `--inactivity-probe`, `--retry`, `-t/--timeout`, `--if-exists`, `--may-exist`, `--if-not-exists`, `-f/--format`, `-d/--data`, `--no-headings`, `--pretty`, `--bare`, `--id=@NAME`, `-v/--verbose`, `--log-file`, `--syslog-target`, `--syslog-method`, `-p/--private-key`, `-c/--certificate`, `-C/--ca-cert`, `-h/--help`, `-V/--version`, `--daemon`, `-u`, `OVN_NB_DAEMON`), source path `utilities/ovn-detrace.in`, branch baseline `branch-22.03` | em_dash PASS, lang_check PASS, rubric_leak PASS |

### Partial-state note (continued)

After Run 1.B retry2, the file is in mixed-language transition state:

- **Now English (post Run 1.A plus Run 1.B retry2):** Header block (Language-status updated to honest Mixed form), Learning objectives, §13.14.1, §13.14.2 (all 11 sub-sections), §13.14.3 ovn-sbctl 15 subcommands table plus OVSDB primitives note, §13.14.4 Anatomy 1 through 10, §13.14.5 decision matrix, §13.14.6 Guided Exercise S67' New tenant plus multi-tier ACL, §13.14.7 Capstone POE Phase I.B1, §13.14.8 core points, §13.14.9.1 all ovn-nbctl options across 8 groups, §13.14.9.2 ovn-trace simulator deep coverage, §13.14.9.3 heading.
- **Still Vietnamese (deferred to follow-up Run 1.B continuation):** §13.14.9.3 body (ovn-detrace deep), §13.14.10 source-code citations Phase J.5.d, §13.14.11 cluster plus transaction-guard option deep dive (5 sub-sections), §13.14.12 ovn-trace plus ovn-detrace advanced options (5 sub-sections).

### Validation summary (Run 1.B retry2)

- Two commits: `13511ad` (sections §13.14.3-§13.14.8) and `284363a` (§13.14.9.3 heading plus Language-status revert to honest Mixed form).
- `em_dash_check.py --staged`: PASS on both commits (0 em-dash in staged diff).
- `lang_check.py --staged`: PASS on both commits (24 plus 53 = 77 total prose chunks newly English, 0 non-English in staged diffs).
- `rubric_leak_check.py`: PASS on both commits.
- Whole-file lang_check still flags 56 chunks at lines 1024+, all in §13.14.9.3 body and §13.14.10-§13.14.12, which are out of scope for this retry2 due to time constraints.

### Effort (Run 1.B retry2)

- Edit calls: ~16 successful (the rest of the planned ~30 hit "modified since read" errors due to concurrent agent activity from R1.M working on 9.2; this fragmented progress).
- Hours: ~1.0.
- Halt signals: "modified since read" race with concurrent agents; mitigated by re-Reading before each Edit and by committing partial progress in two batches before the file state could drift further.

### Notes for follow-up Run 1.B continuation

- ~56 Vietnamese chunks remain in §13.14.9.3, §13.14.10, §13.14.11, §13.14.12.
- ~11 em-dashes remain in those same lines and must be replaced together with the prose translation.
- The Language-status callout at line 3 currently lists exactly which sections are English and which remain Vietnamese; the follow-up agent must update it to "English (full migration complete)" only after all four remaining sections are translated and lang_check `--all` returns PASS for the whole file.
