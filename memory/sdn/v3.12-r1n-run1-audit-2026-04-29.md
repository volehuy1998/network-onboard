# v3.12 R1.N Run 1.A Audit Log (Form A, 13.14 first half)

**Date:** 2026-04-29
**Plan:** v3.12 curriculum-wide English migration, Phase R1.N Run 1.A
**Scope:** Lines 1-263 of `sdn-onboard/13.14 - ovn-nbctl-sbctl-reference-playbook.md` (header, Learning objectives, §13.14.1 CLI architecture, §13.14.2 ovn-nbctl 97 subcommands in 12 groups). Lines 264+ deferred to Run 1.B.

## Per-file evidence

| File | Range | Vi chunks closed (staged-diff) | Em-dashes removed (staged-diff) | Mixed Language-status callout | Identifiers preserved | Pre-commit hooks (staged-diff) |
|------|-------|--------------------------------|---------------------------------|------------------------------|-----------------------|--------------------------------|
| `sdn-onboard/13.14 - ovn-nbctl-sbctl-reference-playbook.md` | L1-L263 (partial) | 99 prose chunks (lang_check `--staged` PASS, 0 non-English) | 0 in staged diff (em_dash_check `--staged` PASS) | Added at L3 | `ovn-nbctl`, `ovn-sbctl`, `--db=`, `OVN_NB_DAEMON`, `--detach`, `--pidfile=`, `--log-file=`, `--verbose`, `--timeout=`, `--no-wait`, `--dry-run`, all 97 subcommand names (`ls-add`, `lsp-set-addresses`, `lr-route-add`, `lb-add`, `acl-add`, `pg-add`, `dhcp-options-create`, `qos-add`, `meter-add`, `ha-chassis-group-add-chassis`, `copp-add`, `set-connection`, `set-ssl`, OVSDB primitives `list`/`find`/`get`/`set`/`add`/`remove`/`clear`/`create`/`destroy`/`wait-until`), table column names (`Logical_Switch`, `Logical_Router`, `Port_Binding`, `Datapath_Binding`, `Logical_Flow`, `Chassis`, `DHCP_Options`, `Port_Group`, `HA_Chassis_Group`, `Load_Balancer`), source paths (`utilities/ovn-nbctl.c` 7244 lines, `utilities/ovn-sbctl.c` 1528 lines, `ctl-libs/ctl-lib.c`), version baseline branch-22.03 | em_dash PASS, lang_check PASS, rubric_leak PASS |

## Partial-state note

This file remains in mixed-language transition state until Run 1.B closes:

- **Now English (post Run 1.A):** Header block, Learning objectives, §13.14.1.1, §13.14.1.2, §13.14.1.3, §13.14.2.1 through §13.14.2.11.
- **Still Vietnamese (Run 1.B):** §13.14.3 ovn-sbctl, §13.14.4 anatomy, §13.14.5, and Phase D / v3.5 / v3.6 backfill subsections (§13.14.6 through §13.14.12 plus references and cross-links).

The Language-status callout at line 3 documents this state explicitly per plan v3.9.1 §8.3.

## Validation summary

- `em_dash_check.py --staged`: PASS (0 em-dash in staged diff).
- `lang_check.py --staged`: PASS (99 prose chunks, 0 non-English).
- `rubric_leak_check.py`: PASS (file-level).
- Whole-file scans still flag 11 em-dashes and 188 non-English chunks, all in lines 264+ which are out of Run 1.A scope.

## Effort

- Edit calls: 4 (header + objectives, §13.14.1, §13.14.2.1-2.2, §13.14.2.3-2.11).
- Hours: ~0.4.
- Halt signals: none.
