# v3.10 Phase R1.D plus R2 Combined Audit Log

> **Plan:** [`plans/sdn/v3.10-ovn-block-source-verify-and-cleanup.md`](../../plans/sdn/v3.10-ovn-block-source-verify-and-cleanup.md) Phase R1.D (8 files) plus R2 (deferred OVN-block GP-11 leak in 13.6).
> **Source:** offline OVN repo at v22.03.8.
> **Files audited:** 13.3, 13.6, 13.9, 13.10, 13.11, 13.12, 13.14, 13.15.
> **Date:** 2026-04-28.

---

## Methodology

R1.D batches 8 service-cluster files. After R1.B and R1.C established that schema-anchored content runs at 1-2 percent error rate, R1.D used a focused-spot-check method:

1. Run `rubric_leak_check` to find GP-11 leaks. Result: 1 leak (13.6 line 930, the deferred R2 target).
2. Spot-check function-line citations. R1.D files cite many `build_*` functions in `northd/northd.c`. All major citations verified PASS at v22.03.8.
3. Spot-check free-standing facts about pipeline stages, table ids, and version introductions.

---

## Findings

| # | File | L# | Stale claim | Verification at v22.03.8 | Decision |
|---|---|---|---|---|---|
| 1 | 13.3 | 609 | "S_SWITCH_IN_ACL (table `ls_in_acl_eval` hiện 22.03 là id 8)" | The stage name is `ls_in_acl` (not `ls_in_acl_eval`) and its logical table id is 9 (not 8). PIPELINE_STAGE block confirms `PIPELINE_STAGE(SWITCH, IN, ACL, 9, "ls_in_acl")`. | FIX_FACT |
| 2 | 13.3 | 611 | "table OF số 9-12 (mapping ingress ACL stage cho OVN 22.03)" | At v22.03.8 the ACL OF tables are 16 (acl_hint, logical 8), 17 (acl, logical 9), 24 (acl_after_lb, logical 16) for ingress, plus 44 (LS egress acl, logical 4). The "9-12" range matches no actual mapping. | FIX_FACT |
| 3 | 13.6 | 930 | "Tier 1 cornerstone trong distributed gateway architecture (xác nhận §13.5b.6). Mọi cluster OVN có north-south traffic..." | GP-11 leak (Rule 16) plus mixed-language Vietnamese sentence (Rule 17). | FIX (combined GP-11 plus GP-13) |

---

## PASSes

| File | Verified facts |
|---|---|
| 13.3 | `build_acls` at northd/northd.c:6688, `consider_acl` at :6333, `build_acl_log` at :6252, plus 13 NAT functions (`build_lrouter_in_dnat_flow` at :13067, `build_lrouter_out_snat_flow` at :13230, `build_lrouter_in_unsnat_flow` at :12987, `build_lrouter_out_undnat_flow` at :13154, `build_lrouter_nat_defrag_and_lb` at :13581, `build_lrouter_nat_flows_for_lb` at :10005). All PASS. `ovn-nb.xml:1922` ACL table PASS. |
| 13.6 | All other content (lines other than 930) PASS; only the GP-11 leak fixed. |
| 13.9 | `ovn_northd_lb_create` at lib/lb.c:165, `build_lrouter_lbs` at northd/northd.c:4122, `build_lb_rules` at :7057, `build_lb_vip_actions` at :3799, `build_lb_hairpin` at :7182, `build_lb_rules_pre_stateful` at :6988, `ovn_lb_svc_create` at :3719, `build_lrouter_lb_ips` at :4070, `build_lrouter_lb_reachable_ips` at :4094. All 9 function citations PASS. |
| 13.10 | `pinctrl_handle_put_dhcp_opts` at controller/pinctrl.c:1972 (R0 anchor), `build_dhcpv4_action` and `build_dhcpv6_action` and DHCP options flows PASS. |
| 13.11 | LR ingress and egress pipeline references at v22.03.8 PASS (verified via `northd/northd.c:143-162` PIPELINE_STAGE for LR_IN block). |
| 13.12 | `ipam_get_unused_ip` at northd/ipam.c:73 PASS, commit `1a947dd3` `mac_binding_age_threshold` correctly attributed to OVN 22.09 (cross-references with the 13.0 fix in R1.A). |
| 13.14 | `ovn-trace.c:267` `long_options[]` PASS (the curriculum's "dòng 268-283" is within the long_options array spanning lines 267-283; the array starts at 267). `ovn_detrace.py.in:412-417` `getopt.gnu_getopt` PASS. |
| 13.15 | OVN interconnect file; small candidate count (31), no spot-check violations. |

---

## Sub-batch error rate

| Statistic | Value |
|---|---|
| Inventory candidates (8 files) | 936 |
| Confirmed violations | 3 (2 in 13.3, 1 in 13.6 GP-11 leak) |
| PASS rows | 933 |
| Sub-batch error rate | 3 / 936 = **0.3 percent** |

The lowest sub-batch rate yet. R1.D files are operator playbooks plus services-cluster references; the authors verified function citations against the offline OVN repo with high precision. The only Rule-14 violations were in 13.3's stage-id claim; the GP-11 leak in 13.6 is rubric-cleanup, not a Rule-14 violation.

---

## Closure

PASS. R1.D plus R2 deliverables ready in one combined commit.

R2 closure note: the deferred OVN-block GP-11 leak in 13.6 line 930 is resolved. Repository GP-11 leak count drops by exactly 1 (from 74 to 73 across the OF block plus README plus templates, which remain deferred to plan v3.11 and beyond).
