# 10.0. OVSDB protocol RFC 7047: schema và transactions

> **Trạng thái:** Skeleton, nội dung sẽ được viết ở bước S5 theo plan.
> **Khối:** X, OVSDB management
> **Phần:** Part 10
> **Ebook mapping:** RFC 7047 + external
> **Plan:** `plans/sdn-foundation-architecture.md` §3.3 Block X
> **Prerequisites:** Part 9.1

---

## Mục tiêu bài học

Sau khi hoàn thành phần này, bạn có thể:

1. **Mô tả (Understand)** OVSDB protocol RFC 7047 (December 2013), JSON-RPC over TCP/SSL/UNIX.
2. **Liệt kê (Remember)** 10 operations: insert, select, update, mutate, delete, wait, commit, abort, comment, assert.
3. **Phân biệt (Analyze)** monitor vs monitor_cond, conditional watch.

---

## Dàn bài

## 10.0.1 RFC 7047 overview

*Sẽ phát triển: RFC publication Dec 2013 by Pfaff/Davie; JSON-RPC 1.0; connection methods (TCP 6640, SSL 6641, UNIX socket).*

## 10.0.2 Schema language: tables, columns, types, constraints

*Sẽ phát triển: JSON schema format; atomic types; composite types (set, map); strong typing; referential integrity.*

## 10.0.3 10 transaction operations

*Sẽ phát triển: mỗi operation's JSON payload; semantics; error handling; atomic transaction boundary.*

## 10.0.4 Monitor + monitor_cond protocol

*Sẽ phát triển: replication semantics; initial snapshot; incremental updates; conditional monitoring (Open_vSwitch 2.6+).*

## 10.0.5 Real schemas: Open_vSwitch, OVN_Northbound, OVN_Southbound

*Sẽ phát triển: ba schemas quan trọng; bảng chính; quan hệ foreign key; forward reference Block XIII.*

---

## Tài liệu tham khảo

*Danh mục nguồn sẽ được tổng hợp sau khi hoàn thành nội dung.*
