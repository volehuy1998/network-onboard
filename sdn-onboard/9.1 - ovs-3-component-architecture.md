# 9.1. Ba thành phần của Open vSwitch: ovs-vswitchd, ovsdb-server, kernel module

> **Trạng thái:** Skeleton, nội dung sẽ được viết ở bước S5 theo plan.
> **Khối:** IX, OpenvSwitch internals
> **Phần:** Part 9
> **Ebook mapping:** External gap
> **Plan:** `plans/sdn-foundation-architecture.md` §3.3 Block IX
> **Prerequisites:** Part 9.0

---

## Mục tiêu bài học

Sau khi hoàn thành phần này, bạn có thể:

1. **Liệt kê (Remember)** ba thành phần: `ovs-vswitchd` (user), `ovsdb-server` (config DB), kernel module `openvswitch.ko`.
2. **Giải thích (Understand)** Netlink genl family giữa userspace ↔ kernel.
3. **Phân biệt (Analyze)** config plane (ovsdb-server) vs control plane (ovs-vswitchd) vs data plane (kernel).

---

## Dàn bài

## 9.1.1 Three-component overview

*Sẽ phát triển: architecture diagram; ba processes/modules; lifecycle khởi động; vai trò mỗi tầng.*

## 9.1.2 ovs-vswitchd process model

*Sẽ phát triển: main loop epoll; handler/revalidator threads; OpenFlow agent; connection to controller.*

## 9.1.3 ovsdb-server + OVSDB protocol (Block X details)

*Sẽ phát triển: cấu hình schema Open_vSwitch; JSON-RPC endpoints; forward reference Block X.*

## 9.1.4 Kernel module openvswitch.ko

*Sẽ phát triển: datapath implementation trong kernel; upcall to userspace; flow table in kernel.*

## 9.1.5 Netlink genl family for upcall/flow-install

*Sẽ phát triển: generic Netlink family "ovs_datapath", "ovs_flow", "ovs_vport"; syscall boundary.*

---

## Tài liệu tham khảo

*Danh mục nguồn sẽ được tổng hợp sau khi hoàn thành nội dung.*
