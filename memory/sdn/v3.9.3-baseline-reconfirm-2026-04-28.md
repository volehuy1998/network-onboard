# v3.9.3 Baseline Reconfirmation (Phase R0)

> **Local OVS repo:** C:\Users\voleh\Documents\ovs (checked out at v2.17.9).
> **Commit:** 0bea06d9957e3966d94c48873cd9afefba1c2677 (2024-02-08).
> **Plan:** plans/sdn/v3.9.3-ovs-block-cornerstone-sweep-continuation.md Phase R0.
> **Purpose:** Confirm the v3.9.1 Q0 baseline plus the v3.9.2 R0 reconfirmation still apply for the v3.9.3 continuation sweep.
> **Date:** 2026-04-28.

---

## 1. Repo state

### 1.1. OVS repo

```
$ cd C:/Users/voleh/Documents/ovs
$ git rev-parse HEAD
0bea06d9957e3966d94c48873cd9afefba1c2677
```

The OVS repo is at the v2.17.9 release commit (annotated tag `v2.17.9`, commit dated 2024-02-08, message "Set release date for 2.17.9"). This matches the baseline locked in plan v3.9.1 Q0 and reconfirmed in plan v3.9.2 R0.

### 1.2. Curriculum repo

```
$ cd C:/Users/voleh/Documents/network-onboard
$ git log --oneline -1
7173cf5 chore(meta): R7 v3.9.2 CHANGELOG Reckoning #5 entry, OVS-block sweep partial closure

$ git branch --show-current
docs/sdn-foundation-rev2

$ git status --short | grep -v "^??"
(no output: zero modified or staged tracked files at R0 entry)
```

The curriculum repo HEAD is on `docs/sdn-foundation-rev2` at commit `7173cf5`, the v3.9.2 R7 Reckoning #5 entry. The working tree is clean of tracked-file changes. Untracked files are unrelated GitHub workflow files plus the v3.9.3 plan itself.

---

## 2. Spot-check grep results

All three greps run against the OVS repo at v2.17.9 (HEAD `0bea06d9957e3966d94c48873cd9afefba1c2677`).

### 2.1. classifier_insert and classifier_lookup

```
$ grep -nE "^classifier_(insert|lookup)" lib/classifier.c
690:classifier_insert(struct classifier *cls, const struct cls_rule *rule,
938:classifier_lookup__(const struct classifier *cls, ovs_version_t version,
1166:classifier_lookup(const struct classifier *cls, ovs_version_t version,
```

Match: `classifier_insert` at line 690 and `classifier_lookup` at line 1166. Expected output per plan v3.9.2 R0 §3.1: PASS.

### 2.2. raft_run

```
$ grep -nE "^raft_run" ovsdb/raft.c | head -5
1930:raft_run(struct raft *raft)
3755:raft_run_reconfigure(struct raft *raft)
```

Match: `raft_run` at line 1930 and `raft_run_reconfigure` at line 3755. Expected output per plan v3.9.2 R0 §3.2: PASS.

### 2.3. ovsdb_rbac_insert and ovsdb_rbac_update

```
$ grep -nE "^ovsdb_rbac_(insert|update)" ovsdb/rbac.c
149:ovsdb_rbac_insert(const struct ovsdb *db, const struct ovsdb_table *table,
308:ovsdb_rbac_update(const struct ovsdb *db,
```

Match: `ovsdb_rbac_insert` at line 149 and `ovsdb_rbac_update` at line 308. Expected output per plan v3.9.2 R0 §3.3: PASS.

---

## 3. Pre-commit hook status

```
$ ls -la .git/hooks/pre-commit
-rwxr-xr-x 1 voleh 197609 3098 Apr 27 12:29 .git/hooks/pre-commit

$ head -20 .git/hooks/pre-commit
#!/usr/bin/env bash
# Governance pre-commit hook (auto-installed via scripts/pre-commit-install.sh).
#
# Enforces:
#   - GP-6 to GP-10:    anti-gaming check on staged curriculum .md
#   - GP-11 / Rule 16:  rubric leak check on staged curriculum .md
#   - Rule 17 (em-dash): em-dash (U+2014) check on staged curriculum .md
#   - Rule 17 (lang):    language detection check on staged curriculum .md
```

The pre-commit hook is installed, executable (mode `-rwxr-xr-x`), and references all four required scripts (anti_gaming, rubric_leak, em_dash, lang). The Python resolution logic (prefer `python` then fall back to `python3`) introduced by v3.9.1 Phase Q-1.E remains in place per the head comment.

---

## 4. Verdict

PASS. The v3.9.1 Q0 source-verify baseline plus the v3.9.2 R0 reconfirmation still apply for the v3.9.3 continuation sweep. No drift detected. Plan v3.9.3 may proceed to Phase R1 (9.4 and 9.11 axis-16 sweep, R2.B carryover from plan v3.9.2).

The corrected method per plan v3.9.1 §0.3 (`git checkout v2.17.9` plus `grep -nE "^<fn>" <file>`, anchored at column 0 per OVS coding style) is preserved and operative.
