# 8.0 — Linux namespaces và cgroups cho network isolation

> **Trạng thái:** Skeleton — nội dung sẽ được viết ở bước S5 theo plan.
> **Khối:** VIII — Linux networking primer
> **Phần:** Part 8
> **Ebook mapping:** External gap — không có trong Goransson ebook
> **Plan:** `plans/sdn-foundation-architecture.md` §3.3 Block VIII
> **Prerequisites:** RHCSA cơ bản

---

## Mục tiêu bài học

Sau khi hoàn thành phần này, bạn có thể:

1. **Mô tả (Understand)** 7 Linux namespaces: pid, net, mnt, uts, ipc, user, cgroup.
2. **Áp dụng (Apply)** `ip netns` + `unshare` để tạo isolated network namespace.
3. **Phân biệt (Analyze)** network namespace vs veth vs bridge — primitives của container networking.

---

## Dàn bài

## 8.0.1 Linux namespaces: 7 loại

*Sẽ phát triển: pid, net, mnt, uts, ipc, user, cgroup; kernel 3.8+ có đủ bảy; relation với clone(2) flags.*

## 8.0.2 Network namespace: ip netns, /var/run/netns

*Sẽ phát triển: setns(2) syscall; persistent netns file; `ip netns exec` wrapper.*

## 8.0.3 unshare + nsenter

*Sẽ phát triển: util-linux tools; unshare --net; nsenter để join existing namespace.*

## 8.0.4 cgroups v1 vs v2 — network bandwidth class

*Sẽ phát triển: cgroup v1 net_cls + net_prio; cgroup v2 unified; tc dùng classid.*

### ▶ Guided Exercise 1: Tạo 2 netns + veth pair, ping xuyên

*Sẽ phát triển: full POE walkthrough; Predict (không có route → fail); Observe (ping success sau add route); Explain.*

---

## Tài liệu tham khảo

*Danh mục nguồn sẽ được tổng hợp sau khi hoàn thành nội dung.*
