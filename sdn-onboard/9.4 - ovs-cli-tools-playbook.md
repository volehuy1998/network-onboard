# 9.4 — OVS CLI tools: playbook troubleshooting 6 lớp

> **Trạng thái:** Skeleton — nội dung sẽ được viết ở bước S5 theo plan.
> **Khối:** IX — OpenvSwitch internals
> **Phần:** Part 9
> **Ebook mapping:** External gap
> **Plan:** `plans/sdn-foundation-architecture.md` §3.3 Block IX
> **Prerequisites:** Part 9.3

---

## Mục tiêu bài học

Sau khi hoàn thành phần này, bạn có thể:

1. **Áp dụng (Apply)** `ovs-vsctl`, `ovs-ofctl`, `ovs-appctl`, `ovs-dpctl`, `ovsdb-client`.
2. **Phân biệt (Analyze)** 6-layer troubleshooting: physical → kernel → megaflow → OpenFlow → OVSDB → logical.
3. **Diagnose (Evaluate)** một issue bằng `ofproto/trace` + `appctl upcall/show`.

---

## Dàn bài

## 9.4.1 ovs-vsctl patterns (config wheel)

*Sẽ phát triển: add-br, add-port, set Interface options; transaction semantics; common patterns.*

## 9.4.2 ovs-ofctl add-flow/mod-flow/del-flows + NXM extensions

*Sẽ phát triển: OpenFlow flow management; NXM vs OXM; cookie/priority patterns.*

## 9.4.3 ovs-appctl introspection: ofproto/trace, upcall/show, bond/show

*Sẽ phát triển: unix socket control channel; ofproto/trace simulation; upcall statistics.*

## 9.4.4 ovs-dpctl debugging: dump-flows, dump-conntrack

*Sẽ phát triển: kernel datapath inspection; conntrack zone enumeration; flow statistics.*

## 9.4.5 ovsdb-client monitor raw

*Sẽ phát triển: OVSDB change stream monitoring; JSON-RPC notifications; debug ovsdb issues.*

## 9.4.6 Six-layer troubleshooting playbook

*Sẽ phát triển: L1 physical → L2 kernel driver → L3 megaflow → L4 OpenFlow → L5 OVSDB → L6 logical (OVN); decision tree.*

### ▶ Trouble Ticket 1: Packet drop diagnosis với ofproto/trace

*POE Lab: simulate packet drop, use ofproto/trace to walk through tables, identify dropping action.*

### ▶ Capstone Block IX Lab 2

*POE 6-layer Lab: Build 3-bridge OVS topology, chạy throughput benchmark kernel vs DPDK, trace megaflow hit rate với `ovs-appctl dpctl/dump-flows -m`.*

---

## Tài liệu tham khảo

*Danh mục nguồn sẽ được tổng hợp sau khi hoàn thành nội dung.*
