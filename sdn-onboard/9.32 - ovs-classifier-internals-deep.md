# 9.32. OVS classifier internals deep (TSS + subtable + Patricia trie)

> **Trạng thái skeleton:** Phase F v3.7-Reckoning, 2026-04-26. Content fill ở Phase G cohort C2 (cornerstone classifier + thread).

## Mục tiêu bài học

Sau Phần này, học viên có thể:

1. Mô tả cụ thể TSS (Tuple Space Search) algorithm: subtable build, mask consolidation, classifier traversal order.
2. Phân biệt classifier (userspace ofproto-dpif) vs datapath flow table (kernel megaflow).
3. Đo classifier traversal cost qua coverage/show counter + identify hot subtable.
4. Tune classifier với specific OF rule pattern để minimize subtable count.

## Yêu cầu kiến thức

- Phần 9.1 (3-component architecture), 9.2 (kernel datapath megaflow), 9.15 (TSS overview).

## Cấu trúc Phần (TODO Phase G cohort C2)

### §9.32.1 TSS algorithm core

- Tuple space concept
- Per-mask subtable mapping
- Classifier traversal priority order
- Mask consolidation invariant

### §9.32.2 Subtable internals

- struct cls_subtable trong lib/classifier.c
- Hash table per-mask
- Add/remove flow → subtable update

### §9.32.3 Patricia trie optimization

- Prefix trie cho IP address match
- Skip subtable subset based on trie pruning
- Performance impact

### §9.32.4 Classifier vs datapath relationship

- Userspace classifier (ofproto-dpif) → install megaflow into kernel datapath
- Megaflow expand from classifier output (not 1-to-1)
- Revalidator role bridge

### §9.32.5 Anatomy: coverage/show classifier counter

- ovs-appctl coverage/show | grep classifier
- Sample output interpretation
- Hot subtable detection

### §9.32.6 Tuning classifier rule design

- Avoid mask explosion (each unique mask = 1 subtable)
- Conjunction action reduce cartesian flow rule
- Best practice: small subtable count via conjunctive matching

### §9.32.7 Source code anchor (Rule 14)

- `lib/classifier.c` cls_classifier_lookup, cls_classifier_insert
- `lib/cmap.c` cmap hash table (subtable backing)
- `lib/pvector.c` priority sorted vector

## Cross-references (TODO populate Phase G)

- Phần 9.1, 9.2, 9.15
- Phần 9.4 (ovs-vsctl playbook)
- Phần 9.11 (ovs-appctl reference)
- Phần 9.26 (revalidator storm forensic)

> **Ghi chú:** Skeleton for Phase G fill.
