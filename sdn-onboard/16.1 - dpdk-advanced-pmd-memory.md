# 16.1. DPDK Advanced — PMD + mempool + NUMA topology

> **Trạng thái:** Skeleton (C5 Architecture Phase, 2026-04-22)
> **Khối:** XVI, Expert Extension — Kernel + DPDK Performance Deep Dive
> **Phần:** Part 16 (Expert Extension)
> **Ebook mapping:** DPDK Programmer's Guide + DPDK Sample Applications + Intel DPDK performance reports
> **Plan:** `plans/sdn-foundation-architecture.md` §3.3 Block XVI
> **Prerequisites:** Part 16.0 (performance tuning overview), Part 9.3 (OVS-DPDK basics)

---

## Mục tiêu bài học

Sau khi hoàn thành phần này, bạn có thể:

1. **Mô tả (Understand)** DPDK Poll Mode Driver architecture: hot-loop, RX/TX queue ring, burst processing (default 32 packets), DMA descriptors.
2. **Áp dụng (Apply)** setup hugepages 2MB vs 1GB, NUMA node pinning cho workers, memory channel interleaving, mempool creation với power-of-2 sizes.
3. **Phân tích (Analyze)** CPU cache effects: cache line aligned data structures, false sharing avoidance, prefetch hints.

---

## 16.1.1 PMD hot loop internals

*Skeleton — nội dung sẽ bao gồm:* `rte_eth_rx_burst()` loop, polling RX queue, burst size (default 32), per-queue lock-free ring buffer implementation.

## 16.1.2 Hugepages 2MB vs 1GB

*Skeleton — nội dung sẽ bao gồm:* TLB overhead (regular 4KB page → many TLB entries), 2MB huge (1024× reduction), 1GB gigantic (for large mempools), boot-time vs runtime allocation.

## 16.1.3 Memory channels + NUMA affinity

*Skeleton — nội dung sẽ bao gồm:* Intel 2-socket systems với 4-6 memory channels per socket, interleaving vs pinning, `numactl --hardware` inspection, DPDK `--lcores` + `--socket-mem` flags.

## 16.1.4 Cache line alignment + false sharing

*Skeleton — nội dung sẽ bao gồm:* 64-byte cache line, struct padding for alignment, false sharing between PMD threads, RTE_CACHE_LINE_SIZE macro usage.

## 16.1.5 Multi-queue + RSS scaling

*Skeleton — nội dung sẽ bao gồm:* Receive Side Scaling (RSS) hash symmetric, multi-queue NIC (Intel E810 256 queues), queue-to-CPU pinning strategy, flow director for specific flow steering.

---

### ▶ Guided Exercise 1: Setup OVS-DPDK với 1GB hugepages + 2 PMD threads

> **Trạng thái lab verification:** Output dựa trên OVS-DPDK tuning guide + Intel performance reports; chờ verify lab thực.

**Mục đích:** Production-grade DPDK setup. 1GB hugepages minimize TLB misses cho large mempools, 2 PMD threads trên isolated CPUs ensure consistent polling performance.

**Chuẩn bị:**
- Physical server với 2-socket CPU, 16+ GB RAM, NIC support DPDK
- Ubuntu 22.04 LTS, grub access
- Verify CPU topology: `lscpu --extended`

**Mô hình lab:**

```
┌──────────────────────────────────────────────────┐
│  2-socket server                                 │
│                                                  │
│  Socket 0 (NUMA 0):       Socket 1 (NUMA 1):     │
│  ┌──────────────────┐     ┌──────────────────┐   │
│  │ CPU 0,1 (HT)     │     │ CPU 8,9 (HT)     │   │
│  │ CPU 2,3 (HT)←PMD │     │ CPU 10,11 (HT)   │   │
│  │ CPU 4,5 (HT)←PMD │     │ CPU 12,13 (HT)   │   │
│  │ CPU 6,7 (HT)     │     │ CPU 14,15 (HT)   │   │
│  │                  │     │                  │   │
│  │ 4× 1GB hugepages │     │ 0 hugepages      │   │
│  │ on socket 0      │     │ (NIC on socket 0)│   │
│  └──────────────────┘     └──────────────────┘   │
│         ▲                                        │
│         │ PCIe Gen3 x8                           │
│    ┌────┴─────┐                                  │
│    │ NIC      │                                  │
│    │ 40 Gbps  │                                  │
│    └──────────┘                                  │
└──────────────────────────────────────────────────┘
```

**Bước thực hiện:**

**1.** Check CPU topology để identify NUMA nodes + siblings:

```bash
[host]# lscpu --extended
CPU NODE SOCKET CORE L1d:L1i:L2:L3 ONLINE
0   0    0      0    0:0:0:0     yes
1   0    0      0    0:0:0:0     yes    # HT sibling of CPU 0
2   0    0      1    1:1:1:0     yes
3   0    0      1    1:1:1:0     yes    # HT sibling of CPU 2
...
```

Identify: CPU 2 (physical core 1 on socket 0), CPU 4 (core 2), CPU 6 (core 3). Skip CPUs 1,3,5,7 (HT siblings).

**2.** Configure grub với 1GB hugepages + isolcpus + IOMMU:

```bash
[host]# sed -i 's/GRUB_CMDLINE_LINUX_DEFAULT="\(.*\)"/GRUB_CMDLINE_LINUX_DEFAULT="\1 hugepagesz=1G hugepages=4 default_hugepagesz=1G intel_iommu=on iommu=pt isolcpus=2,4"/' /etc/default/grub
[host]# update-grub
[host]# reboot
```

**3.** Verify sau reboot:

```bash
[host]$ grep HugePages /proc/meminfo
HugePages_Total:       4
HugePages_Free:        4
Hugepagesize:    1048576 kB

[host]$ cat /sys/devices/system/cpu/isolated
2,4

[host]$ cat /proc/cmdline | tr ' ' '\n' | grep -E 'huge|iommu|isolcpus'
hugepagesz=1G
hugepages=4
default_hugepagesz=1G
intel_iommu=on
iommu=pt
isolcpus=2,4
```

**4.** Configure OVS-DPDK:

```bash
[host]# ovs-vsctl set Open_vSwitch . other_config:dpdk-init=true
[host]# ovs-vsctl set Open_vSwitch . other_config:dpdk-socket-mem=1024,0
[host]# ovs-vsctl set Open_vSwitch . other_config:pmd-cpu-mask=0x14
# 0x14 = binary 00010100 = bits 2 và 4 set → CPUs 2 và 4
[host]# systemctl restart openvswitch-switch
```

**5.** Verify PMD threads:

```bash
[host]# ovs-appctl dpif-netdev/pmd-stats-show | grep 'core_id'
pmd thread numa_id 0 core_id 2
pmd thread numa_id 0 core_id 4
```

**6.** Check CPU usage — PMD threads hiển thị 100%:

```bash
[host]# top -bn1 -p $(pgrep ovs-vswitchd)
%CPU: 200  (2 PMD threads, each 100%)
```

**Output mong muốn:**
- 1GB hugepages successfully allocated
- 2 PMD threads pinned to CPU 2,4
- 100% CPU usage on PMD cores (poll mode constant)
- Bridge `br0-dpdk` với `datapath_type=netdev` functioning

**Bài học học được:**

Thứ nhất, **1GB hugepages** giảm TLB pressure đáng kể — 1GB page cover cùng memory area với 512 của 2MB page, 262144 của 4KB page. Critical cho mempool sizes > 1GB.

Thứ hai, **Avoid HT siblings**: HyperThreading means 2 logical CPUs share 1 physical core. Pin PMD to một HT sibling, workload khác đến sibling kia → thread contention. Always pin PMD to physical core alone.

Thứ ba, **isolcpus remove cores khỏi scheduler** — guarantee PMD không bị preempted by unrelated workload. Combined với cgroup cpuset cho additional isolation (userspace apps cũng không run trên isolated cores).

**Cleanup:**

```bash
[host]# ovs-vsctl set Open_vSwitch . other_config:dpdk-init=false
[host]# systemctl restart openvswitch-switch
# Remove grub args if reverting completely (reboot required)
```

---

### ▶ Lab 2: Tune DPDK performance for 40Gbps wire-speed

**Same as Lab 16.0-Lab3 — reference that Capstone lab for full iterative tuning methodology.**

**Additional focus cho Lab 16.1-Lab2:**
- Deep-dive vào DPDK-specific tuning parameters (not covered in 16.0)
- NUMA-aware mempool allocation
- Multi-queue RSS với symmetric hash cho bidirectional flows
- Flow Director cho specific flow steering

**Advanced parameters to explore:**
```bash
# Increase mempool size cho high-rate scenario
ovs-vsctl set Open_vSwitch . other_config:dpdk-socket-mem=2048,2048  # both NUMA

# Pin PMD to exact physical cores (skip HT siblings)
ovs-vsctl set Open_vSwitch . other_config:pmd-cpu-mask=0x54  # cores 2,4,6

# Multi-queue on physical NIC
ovs-vsctl set Interface dpdk-p0 options:n_rxq=4

# Flow Director for steering specific flows
ethtool -N ens1 flow-type tcp4 dst-port 5001 action 0
```

**Expected delta from advanced tuning:** +20-30% throughput over basic 16.0-Lab3 setup, achieving true wire-speed (40 Gbps) với headroom cho burst traffic.

---

## Tài liệu tham khảo

1. [DPDK Programmer's Guide](https://doc.dpdk.org/guides/prog_guide/), DPDK project. PMD + mempool + memory internals.
2. [DPDK Sample Applications](https://doc.dpdk.org/guides/sample_app_ug/), DPDK project. l3fwd, testpmd references.
3. [OVS-DPDK Advanced Tuning](https://docs.openvswitch.org/en/latest/topics/dpdk/phy/), OVS project. Production tuning guide.
4. [Intel DPDK Performance Tuning Guidelines](https://www.intel.com/content/www/us/en/developer/articles/technical/dpdk-performance-optimization-guidelines-whitepaper.html), Intel. Practical performance whitepaper.
5. [DPDK GitHub](https://github.com/DPDK/dpdk), DPDK project. Source code + test apps reference.
