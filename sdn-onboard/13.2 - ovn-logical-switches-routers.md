# 13.2. OVN logical switches, routers và logical flow pipeline

> **State:** Content written (Phase B, 2026-04-22)
> **Khối:** XIII, OVN foundation
> **Phần:** Phần 13
> **Ebook mapping:** compass Ch 17 + ovn-nb(5)
> **Plan:** `plans/sdn-foundation-architecture.md` §3.3 Khối XIII
> **Prerequisites:** Phần 13.1
> **Nguồn offline chính:**
> - `sdn-onboard/doc/compass_artifact_wf-*.md` — Chapter 17 (Logical_Switch, Logical_Router, LSP, LRP essentials).

Môi trường thực hành: Ubuntu 22.04 LTS với OVN 22.03.8. Tham khảo chính: [ovn-nb(5)](https://man7.org/linux/man-pages/man5/ovn-nb.5.html).

---

## Mục tiêu bài học

1. **Mô tả (Understand)** Logical_Switch như L2 domain và Logical_Router như L3 router — abstraction trên OVS.
2. **Áp dụng (Apply)** connect LS qua LR với LRP.
3. **Phân biệt (Analyze)** OVN 22.03 pipeline 24 ingress + 27 egress tables.

---

Yêu cầu kiến thức: Phần 13.1.

---

## 13.2.1 Logical Switch

Logical Switch là L2 domain — equivalent với VLAN, nhưng programmable và scale unlimited.

```bash
[admin]# ovn-nbctl ls-add ls-tenant1
[admin]# ovn-nbctl lsp-add ls-tenant1 vm1
[admin]# ovn-nbctl lsp-set-addresses vm1 "aa:bb:cc:00:00:01 10.0.0.1"
[admin]# ovn-nbctl lsp-set-port-security vm1 "aa:bb:cc:00:00:01 10.0.0.1"
```

**LSP addresses**: `<MAC> <IP1> <IP2> ...` — LSP có thể multiple IP.

Special LSP type:
- `type=router`: connect đến LRP.
- `type=localnet`: connect đến physical network.
- `type=vtep`: connect đến hardware VTEP.

---

## 13.2.2 Logical Router

```bash
[admin]# ovn-nbctl lr-add lr-tenant1
[admin]# ovn-nbctl lrp-add lr-tenant1 lrp-ls1 aa:bb:cc:00:00:fe 10.0.0.254/24
[admin]# ovn-nbctl lsp-add ls-tenant1 ls1-lrp -- \
         set Logical_Switch_Port ls1-lrp type=router \
         options:router-port=lrp-ls1 \
         addresses=router
```

- `lrp-add <lr> <lrp-name> <mac> <cidr>`.
- Peer LSP phải có `type=router` và `options:router-port=<lrp-name>`.

LR không physical — distributed across chassis.

---

## 13.2.3 Connect multiple subnets

Typical tenant có multiple LS (web, app, db) connect qua một LR:

```bash
[admin]# ovn-nbctl ls-add ls-web
[admin]# ovn-nbctl ls-add ls-app
[admin]# ovn-nbctl ls-add ls-db
[admin]# ovn-nbctl lr-add lr-tenant1

# LRP mỗi subnet:
[admin]# ovn-nbctl lrp-add lr-tenant1 lrp-web aa:bb:cc:00:01:fe 10.0.1.254/24
[admin]# ovn-nbctl lrp-add lr-tenant1 lrp-app aa:bb:cc:00:02:fe 10.0.2.254/24
[admin]# ovn-nbctl lrp-add lr-tenant1 lrp-db aa:bb:cc:00:03:fe 10.0.3.254/24

# Peer LSP:
[admin]# ovn-nbctl lsp-add ls-web ls-web-lrp \
         -- set Logical_Switch_Port ls-web-lrp type=router \
            options:router-port=lrp-web addresses=router
# Tương tự app, db
```

Traffic giữa VM trong ls-web và ls-db đi qua lr-tenant1 — routing tự động.

---

## 13.2.4 Static route

```bash
[admin]# ovn-nbctl lr-route-add lr-tenant1 10.1.0.0/16 10.0.1.100
```

Default route:

```bash
[admin]# ovn-nbctl lr-route-add lr-tenant1 0.0.0.0/0 10.0.1.1
```

---

## 13.2.5 Pipeline evolution

OVN version khác nhau (con số kết hợp Logical Switch + Logical Router stages, tổng hợp từ `ovn-northd.8` man page và `northd/northd.c` tại mỗi tag; con số cụ thể có thể lệch 1-2 stage tùy minor version backport — verify ở baseline triển khai):

- **OVN 20.06**: ≈ 20 LS ingress + 25 LS egress.
- **OVN 22.03** (baseline curriculum): **≈ 23 LS ingress + 9 LS egress + 20 LR ingress + 7 LR egress** (verified qua `mcp__github__get_file_contents(northd/northd.c, ref=v22.03.8)`, grep `"ls_in_*"` + `"lr_in_*"`).
- **OVN 24.03+**: mở rộng pipeline với một số stage mới (ví dụ PMTUD + output_large_pkt_detect, xem commit history `northd/northd.c`); structure chia nhỏ thành nhiều file `en-lflows-*.c`.

Phần 19 phân tích chi tiết diff multichassis + PMTUD pipeline giữa 22.03 và 24.03.

---

## 13.2.6 Provider network (localnet)

Connect logical network với physical VLAN:

```bash
[admin]# ovn-nbctl ls-add ls-provider
[admin]# ovn-nbctl lsp-add ls-provider provider-port \
         -- lsp-set-addresses provider-port unknown \
         -- lsp-set-type provider-port localnet \
         -- lsp-set-options provider-port network_name=physnet1
```

`network_name=physnet1` khớp với với `external_ids:ovn-bridge-mappings=physnet1:br-ex`.

---

### ▶ Guided Exercise 14: 3-LS + 1-LR tenant

**Outcomes:** Build 3-tier tenant với logical routing.

**Instructions:**

**1.** Topology:

```bash
[central]# ovn-nbctl ls-add ls-web
[central]# ovn-nbctl ls-add ls-app
[central]# ovn-nbctl ls-add ls-db
[central]# ovn-nbctl lr-add lr-tenant1
```

**2.** LRP:

```bash
[central]# ovn-nbctl lrp-add lr-tenant1 lrp-web 40:44:00:00:01:01 10.0.1.254/24
[central]# ovn-nbctl lrp-add lr-tenant1 lrp-app 40:44:00:00:02:01 10.0.2.254/24
[central]# ovn-nbctl lrp-add lr-tenant1 lrp-db 40:44:00:00:03:01 10.0.3.254/24
```

**3.** Peer LSP (loop):

```bash
[central]# for ls in web app db; do
  ovn-nbctl lsp-add ls-$ls ls-$ls-lrp \
    -- set Logical_Switch_Port ls-$ls-lrp type=router \
       options:router-port=lrp-$ls addresses=router
done
```

**4.** VM ports:

```bash
[central]# ovn-nbctl lsp-add ls-web web-vm1
[central]# ovn-nbctl lsp-set-addresses web-vm1 "aa:bb:cc:01:01:01 10.0.1.10"
```

**5.** Verify:

```bash
[central]# ovn-sbctl lflow-list lr-tenant1 | wc -l
```

**6.** Cleanup: `ovn-nbctl lr-del lr-tenant1 && ovn-nbctl ls-del ls-web ls-app ls-db`

---

## 13.2.7 Logical Switch Ingress pipeline — 27 stage exhaustive (Phase H S46)

OVN translate mỗi Logical_Switch thành một chuỗi OpenFlow table trên br-int. Mục này breakdown từng stage theo Template D (per `ovn-architecture(7)` baseline, OVN 22.03).

### 13.2.7.1 Danh sách 27 stage LS ingress

| T | Stage name | Purpose | Match example |
|---|---|---|---|
| 0 | `ls_in_check_port_sec` | Port security incoming check (drop if MAC/IP mismatch LSP) | `inport=="p1" && eth.src==MAC` |
| 1 | `ls_in_apply_port_sec` | Apply verdict từ check_port_sec | `reg0[15] == 0` pass |
| 2 | `ls_in_lookup_fdb` | Consult FDB for known src MAC | `inport=="p1" && eth.src==MAC_A` |
| 3 | `ls_in_put_fdb` | Insert new MAC into FDB (learn action) | `reg0[11] == 0` — chưa known |
| 4 | `ls_in_pre_acl` | Pre-ACL CT track commit (set reg0[0]=trk) | `ip` |
| 5 | `ls_in_pre_lb` | Pre-LB CT track (for VIP traffic) | `ip4.dst == VIP` |
| 6 | `ls_in_pre_stateful` | Commit gói tin to conntrack zone | `reg0[0] == 1` |
| 7 | `ls_in_acl_hint` | Hint generation to reduce redundant CT commit | `ct.est && ct.rpl` |
| 8 | `ls_in_acl_eval` | ACL evaluation (direction=from-lport) | `ip4.dst==10.0.0.5 && tcp.dst==80` |
| 9 | `ls_in_acl_action` | Apply ACL verdict (drop, reject, allow) | `reg0[17] == 1` |
| 10 | `ls_in_qos` | QoS mark/meter | `inport=="p1" && ip.dscp_set` |
| 11 | `ls_in_lb_aff_check` | Load balancer affinity check (pinned session) | `ct.new && ip4.dst == VIP` |
| 12 | `ls_in_lb` | LB VIP → backend select (hash-based) | `ct.new && ip4.dst == VIP` |
| 13 | `ls_in_lb_aff_learn` | Learn affinity entry for persistence | `ct.new && reg0[9] == 1` |
| 14 | `ls_in_acl_after_lb_eval` | Re-evaluate ACL after LB rewrite | `ip4.dst == backend_IP` |
| 15 | `ls_in_acl_after_lb_action` | Apply verdict after LB | - |
| 16 | `ls_in_stateful` | Final CT commit với ct_mark / ct_label | `reg0[1] == 1` |
| 17 | `ls_in_pre_hairpin` | Detect hairpin (VIP → backend same LS = loop risk) | - |
| 18 | `ls_in_nat_hairpin` | NAT hairpin SNAT for local traffic | - |
| 19 | `ls_in_hairpin` | Redirect hairpin back to originator | - |
| 20 | `ls_in_arp_rsp` | ARP responder (local reply cho gateway IP) | `arp.op==1 && arp.tpa==GW_IP` |
| 21 | `ls_in_dhcp_options` | DHCPv4/v6 options lookup | `inport=="p1" && udp.dst==67` |
| 22 | `ls_in_dhcp_response` | DHCP response gói tin generation | `reg0[3] == 1` |
| 23 | `ls_in_dns_lookup` | DNS query processing | `udp.dst==53` |
| 24 | `ls_in_dns_response` | DNS response generation | `reg0[4] == 1` |
| 25 | `ls_in_external_port` | Bridging to external port (localnet) | `outport=="extport"` |
| 26 | `ls_in_l2_lkup` | Destination MAC lookup → set outport | `eth.dst == MAC_B` |

### 13.2.7.2 Template D cho `ls_in_acl_eval` (T8) — stage phổ biến nhất

| Attribute | Giá trị |
|---|---|
| **Table** | T8 (logical); physical ~T16 trong OVN 22.03 |
| **Stage name** | `S_SWITCH_IN_ACL_EVAL` trong `northd/northd.h` |
| **Purpose** | Evaluate ACL rule từ `NBDB.ACL` với direction=from-lport |
| **Match criteria** | ACL expression compiled thành OpenFlow match |
| **Typical actions** | `reg0[17]=1;` (allow), `reg0[17]=0;` (drop), `reject { ... };` (generate ICMP) |
| **Produced by** | `ovn-northd` từ `NBDB.ACL` direction=from-lport |
| **State input** | `reg0[0]` (ct_hint), `reg0[9]` (ct_est) |
| **State output** | `reg0[17]` (acl_allow flag) |

**Logical flow sample** (`ovn-sbctl lflow-list`):

```
  table=8 (ls_in_acl_eval), priority=65535,
    match=(!ct.est && ct.rel && !ct.rpl && ct_mark.blocked == 0),
    action=(reg0[17] = 1; next;)
  table=8 (ls_in_acl_eval), priority=2001,
    match=(inport == @pg_web && ip4.dst == $ip_db && tcp.dst == 3306),
    action=(reg0[17] = 1; next;)
  table=8 (ls_in_acl_eval), priority=2000,
    match=(ip),
    action=(reg0[17] = 0; next;)
```

Anatomy: priority 65535 cho established+related CT; 2001 cho user ACL; 2000 default drop. `@pg_web` là Port_Group reference; `$ip_db` là Address_Set reference.

**Kịch bản bẻ gãy:** traffic dự kiến allow nhưng drop → check `ovn-trace` output, thường do ACL `allow-related` thiếu hoặc fall through default 2000.

### 13.2.7.3 Template D cho `ls_in_lb` (T12) — Load balancer

| Attribute | Giá trị |
|---|---|
| **Purpose** | LB VIP → backend selection qua `ct_lb_mark` action |
| **Match** | `ct.new && ip4.dst == VIP && tcp.dst == vport` |
| **Actions** | `ct_lb_mark(backends=<ip>:<port>...);` |
| **Produced by** | `NBDB.Load_Balancer` |
| **State input** | `ct.new` flag |

**Logical flow:**

```
table=12 (ls_in_lb), priority=100,
  match=(ct.new && ip4.dst == 10.0.0.100 && tcp.dst == 80),
  action=(ct_lb_mark(backends=10.0.1.1:8080,10.0.1.2:8080,10.0.1.3:8080);)
```

Backend hash deterministic per connection (5-tuple). Session affinity qua `ls_in_lb_aff_*` stage.

### 13.2.7.4 Stage `ls_in_arp_rsp` (T20) deep

```
table=20 (ls_in_arp_rsp), priority=50,
  match=(arp.op == 1 && arp.tpa == 10.0.0.1 /* gateway */),
  action=(eth.dst = eth.src;
          eth.src = aa:bb:cc:dd:ee:ff;
          arp.op = 2;
          arp.tha = arp.sha;
          arp.sha = aa:bb:cc:dd:ee:ff;
          arp.tpa = arp.spa;
          arp.spa = 10.0.0.1;
          outport = inport;
          flags.loopback = 1;
          output;)
```

Anatomy: 10-step action chain đổi ARP Request → Reply + reflect về ingress port. So sánh với OpenFlow equivalent Phần 4.9 §4.9.19 (classic OpenFlow ARP responder 7-action).

---

## 13.2.8 Logical Switch Egress pipeline — 10 stage (gap foundation được fix)

**Audit finding:** trước S46, curriculum 0 file mention `ls_out_*`. Mục này là gap foundation critical được fix.

OVN egress pipeline chạy sau `ls_in_l2_lkup` set outport, đi qua 10 stage trước khi thoát tunnel/bridge.

### 13.2.8.1 Danh sách 10 stage LS egress

| T | Stage name | Purpose |
|---|---|---|
| 0 | `ls_out_pre_lb` | Egress pre-LB tracking (reverse LB direction) |
| 1 | `ls_out_pre_acl` | Egress pre-ACL CT track |
| 2 | `ls_out_pre_stateful` | Egress CT commit |
| 3 | `ls_out_acl_hint` | Egress ACL hint |
| 4 | `ls_out_acl_eval` | Egress ACL evaluation (direction=to-lport) |
| 5 | `ls_out_acl_action` | Apply egress verdict |
| 6 | `ls_out_qos` | Egress QoS mark/meter |
| 7 | `ls_out_stateful` | Final egress CT commit |
| 8 | `ls_out_port_sec_ip` | Port security outgoing IP check |
| 9 | `ls_out_port_sec_l2` | Port security outgoing L2 check |

### 13.2.8.2 `ls_out_acl_eval` vs `ls_in_acl_eval`

Key difference: egress match **`outport`** thay vì `inport`. NBDB ACL direction quyết định:

```shell
# Egress ACL từ web sang db
$ sudo ovn-nbctl --direction=to-lport acl-add ls-prod 1000 \
    "outport == @pg_db && ip4.src == $ip_web && tcp.dst == 3306" allow-related
```

Logical flow tương ứng:

```
table=4 (ls_out_acl_eval), priority=1000,
  match=(outport == @pg_db && ip4.src == $ip_web && tcp.dst == 3306),
  action=(reg0[17] = 1; next;)
```

### 13.2.8.3 `ls_out_port_sec_ip` + `ls_out_port_sec_l2`

Port security egress side — đảm bảo LSP chỉ nhận gói tin với MAC/IP phù hợp với `addresses` config. Ngăn receiver-side spoofing.

```
table=8 (ls_out_port_sec_ip), priority=90,
  match=(outport == "lsp-vm-db" && eth.dst == aa:bb:cc:dd:db && ip4.dst == 10.0.0.10),
  action=(next;)

table=9 (ls_out_port_sec_l2), priority=50,
  match=(outport == "lsp-vm-db" && eth.dst == aa:bb:cc:dd:db),
  action=(output;)

table=9 (ls_out_port_sec_l2), priority=0,
  match=(1),
  action=(drop;)  /* default drop không match address */
```

### 13.2.8.4 End-to-end pipeline trace

```
Packet arrive host X → Geneve decap → br-int physical T8 →
Ingress pipeline T0-T26 (ls-prod-1) →
  inport=p1, stage 0-26, outport set T26 →
Egress pipeline T0-T9 (ls-prod-1) →
  outport=p2, final CT commit, port_sec check →
Physical T65 → wire
```

Xem Phần 9.27 cho 3-tier diagnostic (ovn-trace logical + ofproto/trace physical + dpif/dump-flow datapath).

---

## 13.2.9 Đếm logical flow vs OpenFlow translation

```shell
$ sudo ovn-sbctl lflow-list ls-prod-1 | grep "^  table" | wc -l
142

$ sudo ovs-ofctl -O OpenFlow15 dump-flows br-int | grep "cookie=0x<dp_uuid>" | wc -l
387
```

Ratio logical → physical ≈ 1:2.7 cho LS với ACL medium. Với LB nhiều backend + group table → 1:5 hoặc cao hơn.

---

## 13.2.X Anatomy Template A — `ovn-nbctl ls-list` + `ovn-sbctl lflow-list` (v3.2 expansion)

**Bối cảnh khi chạy:** Verify LS + LR có đầy đủ port + ACL + compile đúng pipeline sau khi provisioning 3-tier app.

**Command 1 — `ovn-nbctl ls-list` + `ls-show`:**

```shell
$ sudo ovn-nbctl ls-list
7c9d4e2a-... (ls-tenant-web)
5e8f1920-... (ls-tenant-app)
ab3c4d51-... (ls-tenant-db)

$ sudo ovn-nbctl ls-show ls-tenant-web
switch 7c9d4e2a-... (ls-tenant-web)
    port lsp_web_01
        addresses: ["aa:bb:cc:00:00:01 10.0.0.5"]
    port lsp_web_02
        addresses: ["aa:bb:cc:00:00:02 10.0.0.6"]
    port ls-to-lr-tenant
        type: router
        router-port: lrp-ls-web
```

**Command 2 — `ovn-sbctl lflow-list <ls>` (pipeline compiled):**

```shell
$ sudo ovn-sbctl lflow-list ls-tenant-web | head -30
Datapath: "ls-tenant-web" (7c9d4e2a-...)  Pipeline: ingress
  table=0 (ls_in_port_sec_l2    ), priority=100 ,
    match=(eth.src == $<pg_ls-tenant-web_ports> && inport == <pg_ls-tenant-web_ports>),
    action=(next;)
  table=1 (ls_in_port_sec_ip    ), priority=90  , ...
  table=8 (ls_in_acl            ), priority=1000,
    match=(ip && ct.new && outport == @pg_web && tcp.dst == 8080),
    action=(reg0[1] = 1; next;)
  table=19 (ls_in_l2_lkup        ), priority=50  ,
    match=(eth.dst == aa:bb:cc:00:00:01),
    action=(outport = "lsp_web_01"; output;)

Datapath: "ls-tenant-web" (7c9d4e2a-...)  Pipeline: egress
  table=0 (ls_out_pre_lb         ), priority=0   , match=(1), action=(next;)
  table=8 (ls_out_port_sec_ip    ), priority=90  , ...
```

**9-attribute anatomy:**

| Field | Giá trị mẫu | Ý nghĩa | Red flag nếu |
|-------|-------------|---------|--------------|
| `Datapath` line | `"ls-tenant-web" (7c9d4e2a-...)` | Identify LS | Missing = LS chưa compile trong SBDB |
| `Pipeline` | `ingress` / `egress` | Direction stage | Chỉ có 1 pipeline = northd chưa complete |
| `table=N` | 0-26 ingress, 0-9 egress | Stage table ID trong pipeline | Gap trong numbering = stage missing (bug) |
| Stage name | `ls_in_port_sec_l2`, `ls_in_acl` | Semantic stage purpose | Name mismatch với OVN version expected = version skew |
| `priority` | 0 (default) - 32767 (max) | Within-table matching priority | Priority 0 = catch-all default |
| `match` | `eth.src == $<pg>` | OVN expression language | Invalid expression = compile error trong log |
| `action` | `next;` / `outport = ...; output;` | Action pipeline | Missing `next;` = pipeline dead-end |
| ACL stage count | 1 ACL = 2 OpenFlow rule | Ratio NBDB → SBDB | Ratio không scale theo formula = conjunction không apply |
| Stage traversal | 0 → 1 → ... → 26 → egress 0 → ... → 9 | Sequential pipeline | Skip stage = gap trong numbering |

**Kịch bản bẻ gãy:**

- **Datapath ingress có nhưng egress không có**: northd compile incomplete. Wait 2-3s, nếu persist → check ovn-northd log.
- **Table=8 ls_in_acl empty khi có ACL trong NBDB**: ACL match expression syntax error, northd reject. Check log `/var/log/ovn/ovn-northd.log`.
- **Priority=1000 in multiple ACL overlap**: undefined order. Fix: dùng priority distinct per ACL.
- **Stage ls_in_l2_lkup table=19 priority=50 fallback to flood**: MAC unknown, ARP không responder bắt được. VM với `addresses=unknown` fallback flood.

---

## 13.2.Y Hiểu sai + Key Topic (v3.2 expansion)

> **Hiểu sai phổ biến 1:** *"Logical Router là một server vật lý — physical chassis dedicated routing."*
>
> **Thực tế:** Logical Router (LR) là **distributed abstraction** — routing logic cài trên mọi chassis. Khi VM trên chassis A gửi gói tin tới LR (destination IP match LR network), gói tin stay local trên chassis A, execute LR pipeline (decrement TTL, ARP resolve next-hop, output qua LRP), không cross chassis vô ích. Chỉ khi LR cần route tới external (north-south NAT), traffic mới đi qua chassisredirect → gateway chassis. Đây là "distributed LR" core benefit của OVN so với traditional L3 via physical router.

> **Hiểu sai phổ biến 2:** *"OVN LS + LR khác Neutron router + subnet — không thể migrate legacy Neutron sang OVN."*
>
> **Thực tế:** OVN Neutron ML2-OVN driver (`networking-ovn`) automatically map Neutron network + router + subnet → OVN LS + LR + LRP + static route. Migration path có tool + doc official. Red Hat OpenStack Platform 16 (2020) default migrate legacy ML2-Linuxbridge sang ML2-OVN. Pain points: (1) Floating IP behavior thay đổi (distributed DNAT_and_SNAT thay vì centralized), (2) Security Group translation edge case (stateful rule semantic khác nhau), (3) DHCP agent removal (OVN native DHCP). Xem RHOSP migration guide.

> **Chủ đề trọng tâm:** 27 ingress + 10 egress stage của LS pipeline là OVN compiler core output. Mỗi stage có semantic mục đích rõ ràng: port_security (anti-spoofing), FDB learn (MAC awareness), pre-CT (conntrack state check), ACL (policy), QoS (rate limit), LB (load balance + hairpin), stateful commit, ARP responder, DHCP/DNS reply, L2 lookup (MAC → port). Biết stage sequence giúp debug: ví dụ "gói tin reach L2 lookup nhưng không forward" → stage 19 `ls_in_l2_lkup` empty match → MAC table issue. Phần 13.2 (full pipeline table Phase H) là reference cho mọi stage.

---

## 13.2.Z ▶ Capstone POE — 3-tier tenant end-to-end ping trace (v3.2 expansion)

**Mục tiêu:** Trace full gói tin journey từ VM tenant-web → VM tenant-db qua LS + LR + multiple pipeline stage, confirm policy enforcement đúng.

**Scenario:** 3-tier tenant `tenant-T1` với LS-web + LS-app + LS-db + LR-tenant connect cả 3 LS. ACL allow web→app:8080, app→db:5432. Test ping từ web → app (allowed), ping từ web → db (blocked).

**Predict:**

Web → App:
- Gói tin enter LS-web pipeline ingress (27 stage): port_sec → FDB learn → ACL (hit allow-related rule với pg_web → pg_app) → L2 lookup (output port ls-to-lr).
- Transition sang LR-tenant ingress pipeline: routing decision (dst IP trong LS-app subnet) → output LRP-app.
- Transition sang LS-app pipeline egress: ACL (allow reply) → port_sec → L2 lookup → output lsp_app_01.
- Kỳ vọng: ping success, 2-5ms độ trễ (3 pipeline traversal).

Web → DB:
- Gói tin enter LS-web pipeline ingress: port_sec pass → ACL evaluate...
- ACL table=8 priority=1000 match `pg_web → pg_app` → không match vì outport=pg_db.
- ACL table=8 priority=1 match `ip` → drop action.
- Gói tin dropped ở stage ACL.
- Kỳ vọng: ping fail, no reply.

**Observe:**

```bash
# Ping web → app
vm_web$ ping -c 5 10.0.1.5  # lsp_app_01
# Kỳ vọng: 5 packet received, ~2ms RTT.

# Ping web → db
vm_web$ ping -c 5 10.0.2.5  # lsp_db_01
# Kỳ vọng: 100% packet loss.

# Trace packet drop
central$ sudo ovn-trace ls-tenant-web \
    'inport=="lsp_web_01" && eth.src==aa:bb:cc:00:00:01 && \
     ip4.src==10.0.0.5 && ip4.dst==10.0.2.5 && ip.ttl==64 && \
     tcp.src==54321 && tcp.dst==5432'

# Output shows pipeline traversal:
# ... ingress ls_in_port_sec_l2 (pass) ...
# ... ingress ls_in_acl priority=1 match=(ip) action=drop ...
# PACKET DROPPED
```

**Explain:**

- ACL policy enforce đúng: web → app allowed (priority 1000 specific rule), web → db denied (priority 1 default-deny).
- Trace confirm drop ở stage ingress table=8 (ls_in_acl) của LS-web, trước khi gói tin reach LR.
- Distributed LR: không traffic cross-chassis nếu web + app cùng chassis.

**Falsification test:**

Remove default-deny ACL priority=1, add mismatch allow rule:
```bash
central$ sudo ovn-nbctl acl-del ls-tenant-web from-lport 1 "ip"
```

Retry ping web → db. Kỳ vọng: ping success (no default-deny). Confirms ACL priority semantics — default-deny là required guardrail. Không có default-deny = open firewall.

**Finish:**

Nguyên tắc: **"Default-deny + specific-allow pattern là Zero Trust foundation"**. Apply mọi OVN tenant. Pattern này scale qua Port_Group + conjunction, verify qua ovn-trace regularly, alert khi ACL rule-set lệch trạng thái từ baseline.

---

## Điểm cốt lõi cần nhớ

Thứ nhất, LS = L2 domain, LR = L3 router. Connect qua LRP + peer LSP type=router.

Thứ hai, LR distributed across chassis — không physical router điểm nghẽn.

Thứ ba, Pipeline table count thay đổi theo OVN version. 22.03 baseline (24+27).

Thứ tư, `localnet` LSP connect logical với physical VLAN qua bridge mapping.

Thứ năm, LS ingress pipeline 27 stage có structure: port_sec → FDB learn → pre-CT → ACL eval/action → QoS → LB (với affinity) → stateful commit → hairpin → ARP rsp → DHCP/DNS → external → L2 lookup. Mỗi stage có purpose rõ ràng, OVN flag condition chuyển giữa stage.

Thứ sáu, LS egress pipeline 10 stage có pattern đối xứng với ingress về ACL + CT + QoS nhưng match direction `outport`. Port security chạy cuối (T8 IP + T9 L2) để enforce receiver-side filtering.

---

## Tài liệu tham khảo

1. [ovn-nb(5) man page](https://man7.org/linux/man-pages/man5/ovn-nb.5.html)
2. [ovn-nbctl(8)](https://man7.org/linux/man-pages/man8/ovn-nbctl.8.html)
3. [OVN Architecture](https://man7.org/linux/man-pages/man7/ovn-architecture.7.html)
4. Nguồn offline: `sdn-onboard/doc/compass_artifact_wf-*.md` — Chapter 17.
