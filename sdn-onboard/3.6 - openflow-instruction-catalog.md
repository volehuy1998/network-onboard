# 3.6. OpenFlow instruction catalog (6 instruction)

> **Trạng thái:** Phase G v3.7-Reckoning Batch 10 (cohort C4 instruction part), 2026-04-26.

## Mục tiêu bài học

Sau Phần này, học viên có thể:

1. Phân biệt instruction (set of instructions) vs action (set vs list of actions).
2. Order of evaluation pipeline: Apply-Actions → Clear-Actions → Write-Actions → Write-Metadata → Goto-Table → Meter.
3. Use case khi nào dùng instruction nào (Apply vs Write semantics).
4. Debug instruction misuse qua ofproto/trace.

## Yêu cầu kiến thức

- Phần 3.1 (OF 1.0 spec), 4.0 (OF 1.1 multi-table + groups), 4.7 (OVS programming), 9.22 (multi-table pipeline).

---

## §3.6.1 Apply-Actions instruction (cornerstone DEEP-20)

**Axis 1 concept.** Instruction execute action list immediately (modify packet now). Loại: OF instruction, OF 1.1+.

**Axis 7 importance.** Tier 1. Most-used instruction trong OF flow rule.

**Axis 8 mechanism.** Apply-Actions specifies action list executed in-order on current packet copy. Modifications visible to downstream instructions in same flow rule.

**Axis 14.** OF 1.1 introduce. Stable since.

**Axis 17.** Production: typical flow rule có Apply-Actions với output, set_field, dec_ttl. Common pattern.

**Axis 18.** `ovs-ofctl add-flow br0 'priority=100,ip,actions=set_field:00:11:22:33:44:55->eth_dst,output:1'` exercise.

**Axis 20.** Cisco IOS class-map action immediate similar concept.

---

## §3.6.2 Clear-Actions instruction (cornerstone)

**Axis 1 concept.** Clear accumulated action set. Loại: OF instruction, OF 1.1+.

**Axis 7 importance.** Tier 1 medium. Critical cho complex multi-table pipeline reset.

**Axis 8 mechanism.** Reset action set to empty. Useful before re-evaluating in different table context.

**Axis 14.** OF 1.1 introduce.

**Axis 17.** Pipeline reset case: table 0 set action_set, table 5 wants different action — Clear-Actions reset trước Write-Actions mới.

**Axis 18.** `actions=clear_actions,goto_table:5` exercise.

**Axis 20.** Cisco IOS no inheritance similar concept.

---

## §3.6.3 Write-Actions instruction (cornerstone)

**Axis 1 concept.** Merge action list into action set (deferred execution at pipeline end). Loại: OF instruction.

**Axis 7 importance.** Tier 1. Action set deferred execution model fundamental.

**Axis 8 mechanism.** Write-Actions merges actions into action set. Action set dedup (only 1 action per type, e.g., 1 output). Final execution after pipeline ends.

**Axis 14.** OF 1.1 introduce.

**Axis 17.** Production: most flow with goto_table chain use Write-Actions để defer output until last table.

**Axis 18.** `actions=write_actions(output:1),goto_table:1` exercise.

**Axis 20.** Cisco IOS deferred queue action analog.

---

## §3.6.4 Write-Metadata instruction (cornerstone)

**Axis 1 concept.** Write to 64-bit metadata register (passes between tables). Loại: OF instruction.

**Axis 7 importance.** Tier 1. Metadata persistence cross-table critical cho multi-stage pipeline.

**Axis 8 mechanism.** Write-Metadata sets value + mask:
```
write_metadata:0x12345678/0xffff0000
```
Lower bits unchanged, upper bits set 0x1234.

**Axis 14.** OF 1.1 introduce.

**Axis 17.** Production: Write-Metadata used cho tagging packet across pipeline stages (e.g., tenant ID tag).

**Axis 18.** `actions=write_metadata:0x1,goto_table:1` then table 1 match metadata=0x1.

**Axis 20.** Cisco IOS forwarding-class similar concept.

---

## §3.6.5 Goto-Table instruction (cornerstone)

**Axis 1 concept.** Continue pipeline at specified next table. Loại: OF instruction.

**Axis 7 importance.** Tier 1. Multi-table pipeline foundation.

**Axis 8 mechanism.** `goto_table:N` advance to table N. N must > current table (forward only). OVS extension `resubmit` allows backward.

**Axis 14.** OF 1.1 introduce. OF 1.5 packet_type-aware variant.

**Axis 17.** Standard 5-table OVS pipeline use goto_table chain (cited 9.22).

**Axis 18.** `actions=goto_table:1` then table 1 has additional rule.

**Axis 20.** Cisco IOS table chain via class-map similar.

---

## §3.6.6 Meter instruction (cornerstone)

**Axis 1 concept.** Redirect packet to meter for rate-limit / DSCP-remark before continuing. Loại: OF instruction, OF 1.3+.

**Axis 7 importance.** Tier 1. QoS/DSCP foundation.

**Axis 8 mechanism.** `meter:N` invoke Meter table entry N. Meter has bands (DROP / DSCP_REMARK) based on rate threshold.

**Axis 14.** OF 1.3 introduce. OF 1.5 enhance.

**Axis 17.** Production: rate-limit egress traffic per VM via meter.

**Axis 18.** `ovs-ofctl add-meter br0 meter=1,kbps,band=type=drop,rate=10000` + `actions=meter:1,output:1`.

**Axis 20.** Cisco IOS policer/shaper analog. Linux tc qdisc similar concept.

---

## Cross-references

- Phần 3.1 §1.0 spec foundation
- Phần 4.0 §multi-table introduction
- Phần 4.7 §OVS programming examples
- Phần 9.22 §OVS multi-table pipeline
- Phần 9.25 §ofproto/trace instruction visibility
- Phần 4.9 §action catalog (Apply-Actions list source)
- Phần 9.32 §ofproto-dpif xlate (xlate handle instruction)

## Tài liệu tham khảo

- ONF OpenFlow Switch Specification 1.3.5 §5.3 Pipeline Processing
- `lib/ofp-actions.c` instruction parser
- `lib/ofp-print.c` instruction print

> **Cohort C4 instruction part:** 6/6 instruction full DEEP-20 treatment.
