# 9.1. Kiến trúc ba thành phần của Open vSwitch: ovs-vswitchd, ovsdb-server và kernel module

> **Language status:** Mixed (Vietnamese with partial English).
> The two source-code reference paragraphs at lines 493 and 801 are written in English (post-2026-04-28).
> Other sections remain in Vietnamese until plan v3.12 completes the curriculum-wide migration.
> See [`plans/sdn/v3.9.2-ovs-block-cornerstone-sweep.md`](../plans/sdn/v3.9.2-ovs-block-cornerstone-sweep.md) for the migration roadmap.

> **State:** Content written (Phase B, 2026-04-21)
> **Khối:** IX, OpenvSwitch internals
> **Phần:** Phần 9
> **Ebook mapping:** External gap + compass Ch 1 (four-layer model) + compass Ch L (vlog) + compass Ch Q (datapath introspection) + USC Lab 1 + USC Lab 9
> **Plan:** `plans/sdn-foundation-architecture.md` §3.3 Khối IX
> **Prerequisites:** Phần 9.0
> **Nguồn offline chính:**
> - `sdn-onboard/doc/compass_artifact_wf-*.md` — Chapter 1 (four-layer mental model), Chapter L (logging với ovs-appctl), Chapter Q (datapath introspection).
> - `sdn-onboard/doc/ovs/OVS.pdf` + `OVS.txt` — Crichigno et al. *Open Virtual Switch Lab Series* (University of South Carolina, NSF Award 1829698): Lab 1 (Linux namespaces + OVS daemon lắng nghe netlink), Lab 9 (OVS Kernel Datapath: slow path + fast path).
> - `sdn-onboard/doc/ovs/OpenVSwitch.pdf` + `OpenVSwitch.txt` — USC/WASTC OVS reference companion.
> - `sdn-onboard/doc/ovs/Day 4-lab3-Introduction to Open vSwitch.pdf` + `.txt` — WASTC 2021 thực hành lab 3 giới thiệu thành phần OVS.
> - `sdn-onboard/doc/ovs/Day 4- Motivation and Introduction to Open vSwitch.pdf" + ".txt" — WASTC 2021 workshop slides về kiến trúc OVS.

Môi trường thực hành: Ubuntu 22.04 LTS với OVS 2.17.9. Tham khảo chính: [docs.openvswitch.org — Architecture](https://docs.openvswitch.org/en/latest/intro/what-is-ovs/), [man ovs-vswitchd(8)](https://man7.org/linux/man-pages/man8/ovs-vswitchd.8.html), [man ovsdb-server(1)](https://man7.org/linux/man-pages/man1/ovsdb-server.1.html).

---

## Mục tiêu bài học

Sau khi hoàn thành phần này, bạn có thể:

1. **Ghi nhớ (Remember)** ba thành phần cốt lõi cấu thành OVS — `ovs-vswitchd` (tiến trình chuyển tiếp ở tầng userspace), `ovsdb-server` (cơ sở dữ liệu cấu hình dựa trên JSON-RPC), và kernel module `openvswitch.ko`, cùng vai trò cụ thể của từng thành phần trong vòng đời xử lý gói tin (packet).
2. **Giải thích (Understand)** cơ chế Upcall thông qua Netlink generic family (`ovs_datapath`, `ovs_flow`, `ovs_vport`): khi kernel gặp tình trạng cache miss → gửi packet lên userspace → `ovs-vswitchd` quyết định hành vi (flow action) → đẩy các dòng chảy (flow entry) xuống kernel.
3. **Phân biệt (Analyze)** ba tầng trong mô hình tư duy bốn lớp của OVS: tầng cấu hình (Config plane - OVSDB), tầng điều khiển (Control plane - OpenFlow trong ovs-vswitchd), và tầng dữ liệu (Data plane - kernel datapath). Tầng thứ tư (Wire - những gì thực sự đi ra khỏi NIC) sẽ được phân tích sâu ở Phần 9.2.
4. **Áp dụng (Apply)** các lệnh kiểm tra trạng thái (state) cho từng thành phần: `ps`, `lsmod`, `ovs-appctl memory/show`, `ovs-appctl coverage/show`, `ovs-dpctl show` để chẩn đoán và xử lý sự cố.

---

Yêu cầu kiến thức:

- Nắm vững về tiến trình (process), lời gọi hệ thống (syscall), và kernel module trong Linux.
- Hiểu về cơ chế Netlink socket.
- Phân biệt được sự khác biệt về ngữ cảnh giữa userspace và kernelspace (context switch).
- Lịch sử phát triển và các phiên bản của OVS (Phần 9.0).

---

## Bối cảnh: Vì sao OVS lại chia thành ba thành phần riêng biệt?

Một câu hỏi thường gặp khi mới tiếp cận OVS là: "Trong khi Linux bridge chỉ là một kernel module đơn lẻ, tại sao OVS lại cần tới ba thành phần khác nhau?". Câu trả lời nằm ở yêu cầu kiến trúc trọng yếu mà OVS phải đáp ứng ngay từ những ngày đầu thiết kế (2007): **khả năng thay đổi chính sách chuyển tiếp (forwarding policy) từ xa một cách linh hoạt mà không cần khởi động lại hệ thống hay làm gián đoạn kết nối hiện tại**.

Linux bridge giải quyết bài toán "Switch học địa chỉ L2 cơ bản" với các hành vi cố định. Mọi thay đổi về cấu hình (như thêm VLAN, chỉnh mức ưu tiên STP) đều phải thông qua lệnh `brctl` từ userspace, lệnh này sẽ trực tiếp thay đổi trạng thái trong kernel qua cơ chế ioctl. Cách tiếp cận này đơn giản nhưng thiếu linh hoạt, không có database hay tiến trình quản lý trung gian.

OVS phải xử lý những bài toán phức tạp hơn nhiều: một bộ điều khiển SDN (controller) có thể đẩy xuống **hàng nghìn luật OpenFlow** chỉ trong một giây. Các luật này cần được kiểm tra tính hợp lệ (validate), lưu trữ bền vững, biên dịch thành các flow entry cho kernel và phải có khả năng hoàn tác (rollback) nếu phát sinh lỗi. Những xử lý phức tạp này (như chạy state machine, phân tách dữ liệu JSON, quản lý kết nối TCP) không thể thực hiện trực tiếp trong kernel. Đó là lý do vì sao cần có một tiến trình chạy ở tầng userspace (userspace daemon).

Bên cạnh đó, các thông tin cấu hình (bridge, port, VLAN tag, QoS) đòi hỏi **khả năng lưu trữ bền vững sau khi khởi động lại** và tính chất **giao dịch nguyên tử (atomic transaction)**. Chẳng hạn, khi thêm hai port cùng lúc, hoặc cấu hình một hàng đợi (queue) tham chiếu đến chính sách QoS, tất cả phải cùng thành công hoặc cùng bị hủy bỏ để đảm bảo tính nhất quán. Giải pháp tối ưu chính là sử dụng cơ chế giao dịch của cơ sở dữ liệu. Tuy nhiên, OVS cần một loại database **nhẹ, có sơ đồ dữ liệu (schema) rõ ràng và hỗ trợ thông báo theo thời gian thực**. Do đó, Nicira đã tự thiết kế OVSDB, và `ovsdb-server` chính là tiến trình độc lập đảm nhiệm việc quản lý tệp tin database này.

Cuối cùng, tầng dữ liệu (data plane) - nơi thực hiện việc chuyển tiếp gói tin thực tế - bắt buộc phải nằm ở **kernel** để đạt được hiệu năng cao (băng thông 10+ Gbps). Việc bắt mỗi gói tin đi qua network stack của kernel phải chuyển sang userspace để xử lý là quá tốn kém (mỗi lần chuyển đổi ngữ cảnh context switch và sao chép dữ liệu tiêu tốn khoảng 2 microsecond, quá chậm đối với tốc độ hàng triệu gói tin mỗi giây). Vì vậy, OVS sử dụng kernel module `openvswitch.ko` để vận hành "đường truyền nhanh" (fast path).

Kết quả là chúng ta có ba thành phần, mỗi thành phần đảm nhận một nhiệm vụ chuyên biệt và giao tiếp với nhau qua các giao diện (interface) rõ ràng.

---

## 9.1.1 Tổng quan về ba thành phần và cơ chế tương tác

Mô hình tư duy bốn lớp (four-layer mental model) là công cụ cực kỳ hiệu quả để bao quát kiến trúc OVS khi cần xử lý sự cố:

```
Lớp 1 — Config plane:     OVSDB (ovsdb-server)
                          "Cần tồn tại những gì"
Lớp 2 — Control plane:    OpenFlow (ovs-vswitchd)
                          "Gói tin nên được xử lý như thế nào"
Lớp 3 — Datapath cache:   Kernel module openvswitch.ko (hoặc userspace netdev)
                          "Những gì fast path đã học được"
Lớp 4 — Wire:             Card mạng vật lý hoặc giao diện ảo
                          "Những gì thực sự đi ra khỏi NIC"
```

> **Lưu ý quan trọng:** Phần lớn công việc chẩn đoán lỗi trong OVS là **so sánh giữa bốn lớp này để tìm ra điểm mâu thuẫn**. Ví dụ, nếu OVSDB báo bridge có 3 port, nhưng OpenFlow table chỉ ghi nhận 2 port, trong khi kernel datapath lại thấy 3 port, thì rõ ràng đang có sự **mâu thuẫn trạng thái (state mismatch)** giữa lớp 1 và lớp 2 — thường là do vấn đề đồng bộ giữa `ovsdb-server` và `ovs-vswitchd`. Việc hiểu rõ vai trò của từng lớp giúp chúng ta thu hẹp phạm vi tìm lỗi một cách nhanh chóng.

**Sơ đồ luồng xử lý gói tin đi qua OVS (Sử dụng kernel datapath)**:

```
┌─────────────────────────────────────────────────────────────────┐
│                     Userspace                                    │
│                                                                   │
│  ┌────────────────┐       ┌──────────────────────────┐          │
│  │ ovsdb-server   │◄─────►│ ovs-vswitchd             │          │
│  │ (config DB)    │ JSON-  │  - OpenFlow pipeline    │          │
│  │ /etc/openvswi- │  RPC  │  - classifier (TSS)      │          │
│  │ tch/conf.db    │       │  - handler threads        │          │
│  └────────────────┘       │  - revalidator threads   │          │
│                            └────────┬─────────────────┘          │
│                                     │                             │
└─────────────────────────────────────┼─────────────────────────────┘
                             Netlink genl
                          (ovs_datapath, ovs_flow,
                             ovs_vport families)
                                      │
┌─────────────────────────────────────▼─────────────────────────────┐
│                     Kernel space                                   │
│                                                                     │
│  ┌──────────────────────────────────────────────┐                 │
│  │  openvswitch.ko                               │                 │
│  │                                                │                 │
│  │   Gói tin đến ──► Tra cứu Megaflow cache      │                 │
│  │                    │                           │                 │
│  │                    ├── hit  ──► Thực thi action│                 │
│  │                    │                           │                 │
│  │                    └── miss ──► Upcall lên user│                 │
│  │                                    │           │                 │
│  │                                    ▼           │                 │
│  │                              ovs-vswitchd      │                 │
│  │                                                │                 │
│  └──────────────────────────────────────────────┘                 │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

Ba thành phần này không kết nối trực tiếp theo kiểu bắc cầu đơn thuần. `ovs-vswitchd` đóng vai trò trung tâm: nó kết nối với `ovsdb-server` qua Unix socket JSON-RPC (để đọc cấu hình và nhận thông báo thay đổi), đồng thời kết nối với kernel module qua Netlink genl family (để cài đặt các dòng chảy và nhận Upcall khi gặp cache miss). Lưu ý rằng `ovsdb-server` không bao giờ giao tiếp trực tiếp với kernel, và kernel cũng không bao giờ đọc dữ liệu từ OVSDB.

---

## 9.1.2 ovs-vswitchd: Tiến trình chuyển tiếp chính ở tầng userspace

`ovs-vswitchd` là tiến trình đóng vai trò quan trọng nhất, nơi tập trung hầu hết logic xử lý của OVS. Đây là một tệp thực thi (binary) duy nhất, chạy dưới dạng một tiến trình đơn nhưng sử dụng cơ chế đa luồng (multi-threading) để xử lý các khối lượng công việc song song.

**Vòng lặp chính (Main loop)** của `ovs-vswitchd` dựa trên cơ chế epoll và poll timer. Trong mỗi chu kỳ lặp:

1. Kiểm tra kết nối OVSDB xem có thay đổi về cấu hình hay không.
2. Kiểm tra kết nối OpenFlow từ controller để nhận các yêu cầu như `flow_mod`, `packet_out`.
3. Kiểm tra Netlink socket để nhận các Upcall từ kernel datapath.
4. Kiểm tra Unix socket `/var/run/openvswitch/ovs-vswitchd.ctl` để nhận lệnh điều khiển từ `ovs-appctl`.
5. Thực hiện các tác vụ định kỳ (như cân bằng tải bond, gửi gói tin STP BPDU).

Trong khi luồng chính xử lý hầu hết các sự kiện, một số công việc đặc thù được tách ra các **nhóm luồng (thread pool) riêng biệt**:

- **Handler threads**: Đảm nhận việc xử lý các gói tin Upcall từ kernel khi gặp cache miss. Mỗi khi có Upcall, các luồng này sẽ phân tích gói tin, duyệt qua OpenFlow pipeline, biên dịch thành megaflow entry và đẩy xuống kernel qua Netlink.
- **Revalidator threads**: Định kỳ (mặc định mỗi 500ms) quét toàn bộ megaflow cache trong kernel để kiểm tra tính hợp lệ của từng entry (ví dụ: luật flow có thay đổi không, đã quá thời gian chờ idle chưa) và xóa bỏ các entry đã cũ (stale).
- **OpenFlow worker threads**: Xử lý các kết nối OpenFlow từ controller.

Sử dụng lệnh `ovs-appctl memory/show` giúp chúng ta xem chi tiết mức độ chiếm dụng bộ nhớ của từng thành phần như classifier (bảng flow), handlers, revalidators. Lệnh này cực kỳ hữu ích khi cần chẩn đoán hiện tượng tràn bộ nhớ.

```bash
[host]# ovs-appctl -t ovs-vswitchd memory/show
handlers:4 ports:5 revalidators:4 rules:127 ...
```

Đọc các chỉ số từ `ovs-appctl coverage/show` giúp phát hiện những điểm bất thường: chẳng hạn nếu tỷ lệ `cache-misses` tăng vọt trong khi `flow-lookup` giảm, điều đó có nghĩa là lưu lượng mạng đang tạo ra quá nhiều dòng chảy nhỏ (microflow) khác nhau, khiến cơ chế cache không phát huy được hiệu quả.

---

## 9.1.3 ovsdb-server: Cơ sở dữ liệu cấu hình nhẹ nhàng

`ovsdb-server` là tiến trình "nhẹ ký" nhất về mặt chiếm dụng tài nguyên. Nhiệm vụ duy nhất của nó là cung cấp dịch vụ cho một tệp tin cơ sở dữ liệu OVSDB thông qua giao thức JSON-RPC.

Tệp database của OVS nằm tại `/etc/openvswitch/conf.db`, với sơ đồ dữ liệu (schema) quy định các bảng quan trọng như `Bridge`, `Port`, `Interface`, `Controller`, v.v.

Các thành phần giao tiếp với `ovsdb-server` qua Unix socket hoặc TCP/SSL. Có hai đối tượng chính thường xuyên tương tác:

- **`ovs-vsctl`**: Công cụ dòng lệnh dành cho quản trị viên. Mỗi lệnh thực thi được coi là một giao dịch (transaction) duy nhất, đảm bảo tính nguyên tử (thành công tất cả hoặc không gì cả).
- **`ovs-vswitchd`**: Kết nối với `ovsdb-server` ngay khi khởi động và đăng ký theo dõi (monitor) toàn bộ database. Bất kỳ thay đổi nào trong database sẽ được đẩy (push) theo thời gian thực về `ovs-vswitchd` mà không cần phải thăm dò (polling) liên tục.

Một điểm mấu chốt cần nhớ: OVSDB chỉ lưu trữ **ý đồ cấu hình** (ví dụ: người dùng muốn tạo bridge X), chứ không lưu trữ **trạng thái vận hành thực tế** (ví dụ: bridge đó đang có bao nhiêu lưu lượng qua lại). Các số liệu thống kê vận hành sẽ được `ovs-vswitchd` tính toán tức thời và cung cấp qua các cột đặc biệt, nhưng chúng không được lưu trữ vĩnh viễn xuống đĩa cứng.

---

## 9.1.4 openvswitch.ko: Tầng dữ liệu trong kernel (Fast Path)

Kernel module `openvswitch.ko` chính là nơi thực thi việc chuyển tiếp gói tin ở tốc độ cao (fast path). Khi một gói tin đi vào card mạng, kernel sẽ chuyển giao cho module này xử lý.

`openvswitch.ko` quản lý hai cấu trúc dữ liệu quan trọng trong bộ nhớ kernel:

**1. Bảng dòng chảy (Flow table) trong kernel:** Thực chất đây là một bản Megaflow cache. Mỗi entry bao gồm các trường khớp (match key) với các mặt nạ (mask), danh sách các hành động (action) cần thực hiện và các bộ đếm thống kê.

**2. Bảng cổng (Port table):** Ánh xạ giữa số hiệu cổng OpenFlow và con trỏ card mạng tương ứng trong hệ thống.

Khi một gói tin đến mà không tìm thấy entry nào khớp trong cache (cache miss), kernel sẽ không hủy gói tin ngay mà thực hiện **Upcall gửi lên tầng userspace**. Thông điệp Upcall này chứa gói tin gốc cùng các thông tin bổ trợ. `ovs-vswitchd` sẽ tiếp nhận, xử lý qua OpenFlow pipeline để tìm ra hành động phù hợp, biên dịch thành một megaflow entry mới và **đẩy ngược xuống kernel**. Những gói tin tương tự sau đó sẽ khớp trực tiếp trong kernel, giúp việc chuyển tiếp trở nên cực nhanh.

> **Giải thích thêm:** Upcall là cơ chế giúp OVS đạt được cả hai mục tiêu: tốc độ (nhờ fast path trong kernel) và sự linh hoạt (nhờ logic phức tạp ở userspace). Theo các nghiên cứu thực tế, tỷ lệ hit cache thường đạt trên 99%, nghĩa là chỉ một phần rất nhỏ gói tin đầu tiên của một dòng chảy mới phải chuyển lên userspace, chi phí này sau đó được bù đắp bởi tốc độ xử lý hàng nghìn gói tin tiếp theo ngay trong kernel.

Kernel datapath thường có hai loại:
- **`datapath_type=system`** (mặc định): Sử dụng kernel module, hiệu năng thường đạt 5-10 Gbps tùy phần cứng.
- **`datapath_type=netdev`**: Sử dụng tầng dữ liệu ở userspace (thường kết hợp với DPDK). Gói tin sẽ bỏ qua kernel đi thẳng vào userspace, cho phép đạt băng thông cực lớn (50+ Gbps) nhưng đòi hỏi tài nguyên CPU chuyên dụng cao hơn.

---

## 9.1.5 Netlink genl family: Cầu nối giữa Userspace và Kernel

Netlink là cơ chế giao tiếp đặc biệt trong Linux dùng để trao đổi thông tin giữa tầng người dùng và nhân hệ điều hành. OVS sử dụng một lớp nâng cao gọi là **Generic Netlink (genl)**.

OVS đăng ký ba "họ" (family) Netlink chính:
- **`ovs_datapath`**: Quản lý vòng đời của datapath (tạo/xóa bridge ở tầng kernel).
- **`ovs_flow`**: Quản lý các flow entry trong datapath. Các gói tin Upcall cũng đi qua họ này.
- **`ovs_vport`**: Quản lý các cổng ảo (interface) được gán vào bridge.

Trong quá trình vận hành, nếu bạn thấy các thông báo lỗi liên quan đến Netlink, đó thường là dấu hiệu của việc **mâu thuẫn phiên bản** giữa tiến trình `ovs-vswitchd` và kernel module `openvswitch.ko` (ví dụ: vừa nâng cấp phần mềm OVS nhưng chưa cập nhật module kernel tương ứng).

---

## Tóm tắt những điểm cốt lõi

1. **Vì sao phải chia ba?** Bởi vì mỗi tầng có đặc thù riêng: Cấu hình cần sự bền vững (OVSDB), logic điều khiển phức tạp cần sự linh hoạt của userspace (ovs-vswitchd), và chuyển tiếp dữ liệu cần tốc độ tối đa của kernel.
2. **Mô hình tư duy 4 lớp:** Luôn so sánh giữa Cấu hình (OVSDB) → Điều khiển (OpenFlow) → Cache (Datapath) → Đường truyền thực tế (Wire). Mọi lỗi vận hành đều nằm ở sự mâu thuẫn giữa các lớp này.
3. **Cơ chế Upcall:** Là "linh hồn" của OVS, giúp hài hòa giữa tính năng và hiệu năng.
4. **Bộ công cụ chẩn đoán:** Hãy thuộc lòng các lệnh `ovs-vsctl`, `ovs-ofctl`, `ovs-dpctl` và `ovs-appctl`. Đó là những "ống nghe" giúp kỹ sư biết chính xác hệ thống đang gặp vấn đề ở đâu.

---

## 9.1.X Nội tại của ofproto-dpif và connmgr (Bổ sung Phase H)

Mục này cung cấp chi tiết về hai hệ thống con (subsystem) quan trọng: **ofproto-dpif** (chuyển đổi OpenFlow pipeline sang datapath) và **connmgr** (quản lý kết nối).

### 9.1.X.1 Kiến trúc ofproto-dpif trong ovs-vswitchd

Tiến trình `ovs-vswitchd` được phân thành 5 lớp chính (từ trên xuống dưới):

```
┌─────────────────────────────────────┐
│ bridge.c                            │  ← Quản lý vòng đời Bridge + đồng bộ OVSDB
├─────────────────────────────────────┤
│ ofproto/ofproto.c                   │  ← Xử lý giao thức OpenFlow
├─────────────────────────────────────┤
│ ofproto/ofproto-dpif.c              │  ← ofproto nhận biết datapath
│ ofproto/ofproto-dpif-xlate.c        │  ← Chuyển đổi hành động (Action translation)
│ ofproto/ofproto-dpif-upcall.c       │  ← Xử lý Upcall + revalidator
├─────────────────────────────────────┤
│ ofproto/connmgr.c                   │  ← Quản lý kết nối tới OF controller
│ lib/classifier.c                    │  ← Công cụ phân loại (TSS + trie)
├─────────────────────────────────────┤
│ lib/dpif.c (+ dpif-netlink.c,       │  ← Trừu tượng hóa giao diện Datapath
│             dpif-netdev.c)          │
└─────────────────────────────────────┘
```

### 9.1.X.2 Giải mã đầu ra `ovs-appctl dpif/show`

Lệnh này giúp kiểm tra trạng thái vận hành của datapath:

```shell
$ sudo ovs-appctl dpif/show
system@ovs-system:
  bridge br-int/0:
    flows: (current 1247) (avg 1250) (max 1350) (limit 200000)
    br-int 65534/<local>: (internal)
    tap-vm-web 1/1: (internal)
    ovn-huyvl-0 3/3: (geneve: remote_ip=10.200.0.12, csum=true, key=flow)
```

Lưu ý sự khác biệt giữa số hiệu cổng OpenFlow (persistent) và số hiệu cổng datapath (do kernel cấp phát động).

### 9.1.X.3 Vai trò của Connection Manager (connmgr)

Connmgr chịu trách nhiệm thiết lập các kết nối TCP/SSL tới bộ điều khiển, quản lý việc bầu chọn vai trò (MASTER/SLAVE) và phân phối các thông báo bất đồng bộ như `PACKET_IN`.

### 9.1.X.4 Mô hình đa luồng tổng hợp

- **Main thread:** Đồng bộ OVSDB và chạy chu kỳ ofproto.
- **Handler threads:** Xử lý các Upcall từ kernel.
- **Revalidator threads:** Dọn dẹp và cập nhật lại các dòng chảy trong kernel.
- **PMD threads:** Chỉ dùng với DPDK để thăm dò hàng đợi card mạng.

---

## 9.1.Y Công cụ xlate trong ofproto-dpif (Bổ sung Phase I)

**Xlate engine** là thành phần dịch gói tin và bảng OpenFlow thành danh sách hành động datapath. Đây là con đường xử lý quan trọng nhất của OVS ở tầng userspace.

### 9.1.Y.1 Khi nào xlate được kích hoạt?

1. **Khi có Upcall từ kernel:** Khi kernel không tìm thấy dòng chảy khớp cho gói tin đầu tiên.
2. **Khi Revalidator quét định kỳ:** Để kiểm tra xem các luật có thay đổi hay không.
3. **Khi chạy lệnh `ofproto/trace`:** Dùng để chẩn đoán luồng đi của gói tin một cách thủ công.

### 9.1.Y.2 Cơ chế làm việc của xlate_actions()

Hàm này nhận đầu vào là thông tin gói tin và trả về danh sách các hành động cho datapath cùng với một "mặt nạ" (megaflow mask). Trong quá trình duyệt qua các bảng OpenFlow, xlate sẽ đánh dấu các trường dữ liệu nào đã được kiểm tra để tạo ra mặt nạ Megaflow tối ưu nhất.

### 9.1.Y.3 Các cơ chế bảo vệ trong xlate

Để tránh các sai sót trong cấu hình luật (như tạo vòng lặp vô hạn), OVS đặt ra các giới hạn cứng:
- **Độ sâu đệ quy tối đa:** 64 cấp (đối với hành động `resubmit`).
- **Tổng số lần resubmit tối đa:** 4096 lần.
Nếu vượt quá, quá trình xử lý sẽ bị hủy bỏ để bảo vệ hệ thống không bị treo.

### 9.1.Y.4 Tại sao phải tách biệt giữa Kernel và Userspace?

Kernel datapath thực chất rất "ngây thơ", nó không biết OpenFlow là gì. Nó chỉ thực thi những hành động cơ bản do userspace đẩy xuống. Việc tách biệt này giúp:
- Kernel giữ được sự đơn giản và tốc độ cao.
- Dễ dàng nâng cấp logic xử lý ở userspace mà không cần can thiệp vào kernel.
- Tận dụng cơ chế Megaflow để giảm thiểu số lần phải chuyển lên userspace xử lý.

---

## 9.1.Z ovs-vswitchd, daemon userspace của Open vSwitch (treatment chuyên sâu)

> **Phạm vi:** Treatment chuyên sâu (mở rộng 2026-04-26) cho keyword `ovs-vswitchd` cornerstone. Đây là tiến trình userspace duy nhất tích hợp toàn bộ logic forwarding của OVS: vòng đời bridge, OpenFlow protocol, flow classifier, OFPT_PACKET_IN/PACKET_OUT, megaflow translation, dpif client, OVSDB IDL subscriber. Cross-link mật thiết với §9.1.2 (giới thiệu sơ lược), §9.1.X (5 lớp ofproto-dpif), §9.1.X.4 (mô hình đa luồng), §9.1.Y (xlate engine), §9.1.AB (dpif), §9.2 (kernel datapath megaflow là nội tại datapath provider của vswitchd), §9.4 (CLI playbook), §10.1 (ovsdb-server cluster mà vswitchd subscribe). Curriculum baseline OVS v2.17.9.

**Khái niệm.** `ovs-vswitchd` là một daemon C single-process, multi-thread, build từ `vswitchd/ovs-vswitchd.c` cùng với rất nhiều file thư viện trong cây mã nguồn (`vswitchd/bridge.c`, `ofproto/*`, `lib/*`). Đây là **một tiến trình duy nhất per host** chịu trách nhiệm: (a) đọc cấu hình từ `ovsdb-server` qua giao thức OVSDB JSON-RPC, (b) tạo và duy trì các struct ofproto đại diện cho từng bridge, (c) chấp nhận kết nối OpenFlow từ các controller (tcp:, ssl:, unix:) hoặc chạy chế độ "fail-mode standalone" khi không có controller, (d) duyệt OpenFlow pipeline để chuyển đổi packet đầu tiên của mỗi flow thành tập datapath action, (e) cài megaflow xuống datapath qua interface `dpif`, (f) quan sát + thu hồi megaflow stale qua thread revalidator. `ovs-vswitchd` KHÔNG phải kernel module, KHÔNG phải database, KHÔNG phải CLI một lần dùng rồi thoát; nó là daemon nền chạy liên tục, lifecycle gắn với host. Không có `ovs-vswitchd`, kernel module `openvswitch.ko` chỉ còn là một mảnh code trong kernel không nhận được rule nào, và một lát sau khi `flow-restore-wait` hết, mọi megaflow tồn dư bị purge, traffic ngừng hoàn toàn.

**Lịch sử + bối cảnh.** Tiền thân của `ovs-vswitchd` là chương trình `vswitchd` đời đầu (2009-2010) trong source tree Nicira, vốn là một ứng dụng C tích hợp cả config storage. Năm 2010 Nicira tách config storage ra thành `ovsdb-server` riêng, phần điều khiển + forwarding logic giữ tên `ovs-vswitchd`; đây là khởi điểm kiến trúc 3-thành-phần (`ovs-vswitchd`, `ovsdb-server`, `openvswitch.ko`) còn nguyên đến hôm nay. Năm 2012 `ovs-vswitchd` được refactor để dùng dpif (xem §9.1.AB) thay cho trực tiếp Netlink, mở đường cho userspace datapath. Năm 2014 (OVS 2.0) `ovs-vswitchd` chuyển sang mô hình đa luồng: tách handler thread và revalidator thread khỏi main thread, đồng thời ra mắt megaflow architecture; đây là bước nhảy hiệu năng lớn nhất trong lịch sử OVS. Năm 2015 NSDI paper "The Design and Implementation of Open vSwitch" (Pfaff, Pettit, et al.) chính thức công bố kiến trúc `ovs-vswitchd` và đặt nó vào sơ đồ tham chiếu của tài liệu học thuật. Từ 2016 trở đi, các bản 2.x bổ sung tunable: `n-handler-threads`, `n-revalidator-threads`, `flow-restore-wait`, `vlan-limit`, `dpdk-init`. Sau khi Open vSwitch chuyển sang Linux Foundation (2016), `ovs-vswitchd` tiếp tục là điểm tích hợp cho conntrack, recirc, NSH, OVN. Đến v2.17.9 (LTS hiện hành của series 2.17), kiến trúc daemon đã ổn định nhiều năm, tunable thay đổi rất ít.

**Vị trí trong kiến trúc.** Trong sơ đồ ba thành phần OVS, `ovs-vswitchd` nằm ở giữa, là cầu nối duy nhất giữa nguồn cấu hình (OVSDB) và data plane thực tế (kernel datapath hoặc userspace netdev):

```
┌───────────────────┐  JSON-RPC qua Unix socket  ┌────────────────────┐
│ ovs-vsctl         │ ─────────────────────────► │ ovsdb-server       │
│ ovn-controller    │                            │ /etc/openvswitch/  │
│ neutron-ovs-agent │                            │   conf.db          │
└───────────────────┘                            └────────┬───────────┘
                                                          │ OVSDB IDL monitor (push)
                                                          ▼
       ┌─────────────────────────────────────────────────────────────────┐
       │                       ovs-vswitchd (single process)             │
       │ ┌───────────────────────────────────────────────────────────┐   │
       │ │ Main thread: bridge_run → ofproto_run → unixctl → idl_run │   │
       │ │   (poll_block trên epoll fd của tất cả nguồn sự kiện)     │   │
       │ ├───────────────────────────────────────────────────────────┤   │
       │ │ N handler threads → udpif_upcall_handler()                │   │
       │ │ M revalidator threads → udpif_revalidator()               │   │
       │ │ (với DPDK) PMD threads polling RX queue                   │   │
       │ ├───────────────────────────────────────────────────────────┤   │
       │ │ Subsystem: ofproto, ofproto-dpif, classifier, connmgr,    │   │
       │ │   netdev, dpif (vtable), tnl_*, ct (userspace conntrack)  │   │
       │ └───────────────────────────────────────────────────────────┘   │
       └────────┬────────────────────────────────────────┬────────────────┘
                │ OpenFlow tcp:/ssl:/unix:               │ dpif_* API
                ▼                                        ▼
        ┌───────────────────┐                ┌─────────────────────────┐
        │ OF controller     │                │ Datapath provider       │
        │ (ovn-controller,  │                │  - dpif-netlink → ko    │
        │  Faucet, Ryu...)  │                │  - dpif-netdev → DPDK/  │
        └───────────────────┘                │    userspace netdev     │
                                             └─────────────────────────┘
```

Scope: per-host, một instance `ovs-vswitchd` quản tất cả bridge trên host (không có chuyện chạy nhiều `ovs-vswitchd` đồng thời cho cùng một bộ bridge). Lifecycle: long-running daemon, init script (`/etc/init.d/openvswitch-switch` hoặc `openvswitch-switch.service`) khởi động sau `ovsdb-server` và load module `openvswitch.ko`. Restart `ovs-vswitchd` thường gây gián đoạn data plane vài giây trừ khi bật `flow-restore-wait`.

**Vai trò.** Sáu vai trò cốt lõi: (a) **OVSDB IDL subscriber**, đăng ký monitor lên các table `Open_vSwitch`, `Bridge`, `Port`, `Interface`, `Controller`, `Manager`, `Flow_Table`, `Mirror`, ... đảm bảo khi user gõ `ovs-vsctl add-br br0`, `ovs-vswitchd` thấy thay đổi gần như tức thời và tự reconfigure. (b) **OpenFlow speaker**, chạy connmgr lắng nghe + connect controller; nói OpenFlow 1.0 đến 1.5 tuỳ negotiation `OFPT_HELLO`. (c) **Classifier owner**, mỗi bridge có một classifier TSS (Tuple Space Search) lưu OF rule, được hỏi trên upcall và trên trace. (d) **Datapath programmer**, khi flow miss xảy ra, vswitchd dịch OF pipeline ra datapath action set rồi gọi `dpif_flow_put()` cài megaflow. (e) **Revalidator + cleaner**, định kỳ quét megaflow trong datapath để xoá entry không còn hợp lệ. (f) **Telemetry surface**, expose Unix control socket `/var/run/openvswitch/ovs-vswitchd.<pid>.ctl` cho `ovs-appctl` hỏi `coverage/show`, `memory/show`, `dpif/show`, `vlog/set`, `bond/show`, `bridge/dump-flows`, ... mọi câu hỏi vận hành thực tế đều đi qua đây.

**Vì sao sinh ra.** Động cơ thiết kế nhắm trả lời ba câu hỏi đồng thời. Câu hỏi 1, "OpenFlow pipeline phức tạp tới mức nào không thể nhúng thẳng vào kernel?" Câu trả lời: rất phức tạp (hàng vạn dòng C, multi-table, group, meter, conntrack, NAT, recirc, sample), kernel không nên gánh. Câu hỏi 2, "Nếu logic ở userspace thì làm sao đạt tốc độ data plane?" Câu trả lời: cache lookup trong kernel (megaflow) chỉ cho gói đầu tiên đi userspace, còn lại đi fast path. Câu hỏi 3, "Làm sao tách hẳn cấu hình ra để hot-reload không restart toàn bộ stack?" Câu trả lời: OVSDB tách riêng, `ovs-vswitchd` subscribe và react. Trước OVS, các implementation L2 switch trên Linux như `bridge-utils` chôn cấu hình trong sysfs/ioctl, không có "OpenFlow brain" và không có khái niệm controller. `ovs-vswitchd` ra đời để hợp nhất cả ba lời giải vào một daemon duy nhất, đủ nhỏ để hiểu được, đủ lớn để chứa toàn bộ data plane pluggability cần cho SDN.

**Vấn đề giải quyết.** Cụ thể `ovs-vswitchd` giải bốn lớp vấn đề: (1) **Phân tách config khỏi forwarding**, OVSDB chứa "ý đồ", `ovs-vswitchd` thực thi ý đồ đó; admin có thể chỉnh DB mà không cần hiểu chi tiết forwarding. (2) **OpenFlow + non-OpenFlow đồng tồn tại**, một bridge có thể có controller (mode=secure) hoặc tự forward MAC learning (mode=standalone), `ovs-vswitchd` lo cả hai code path. (3) **Datapath agnostic**, qua dpif (xem §9.1.AB) cùng một `ovs-vswitchd` chạy được trên kernel module Linux, trên userspace netdev, hoặc trên DPDK PMD. (4) **Operability**, mọi tín hiệu vận hành của OVS đều được expose qua `ovs-appctl` socket, do đó `ovs-vswitchd` đóng vai trò "single point of observability" cho toàn bộ stack OVS.

**Tầm quan trọng.** Đây là khái niệm cốt lõi tuyệt đối, ngang `megaflow`, `ct_state`, `recirc_id`, `dpif`, `ofproto-dpif xlate`. Một engineer không hiểu rõ vai trò của `ovs-vswitchd` sẽ không phân biệt nổi: (i) đâu là rule trong OVSDB và đâu là rule do controller cài runtime, (ii) tại sao restart `ovs-vswitchd` sẽ mất megaflow trong vài giây, (iii) tại sao log `ovs-vswitchd.log` lại quan trọng hơn `dmesg` khi debug forwarding. Trong cấu trúc 320+ keyword của curriculum, `ovs-vswitchd` là "trục dọc" của khối IX (OVS internals), mọi keyword khác như `bridge_run`, `ofproto_run`, `udpif_upcall_handler`, `coverage/show`, `memory/show`, `flow-restore-wait`, ... đều quay quanh nó.

**Cơ chế hoạt động.** Trình tự khởi động và vòng đời chi tiết của `ovs-vswitchd`:

```
1. Init script khởi động ovsdb-server trước, load module openvswitch.ko (nếu type=system).
2. exec ovs-vswitchd (binary từ /usr/sbin/ovs-vswitchd) với argv:
     unix:/var/run/openvswitch/db.sock
     --pidfile=/var/run/openvswitch/ovs-vswitchd.pid
     --log-file=/var/log/openvswitch/ovs-vswitchd.log
     --detach --monitor --no-chdir
3. main() (vswitchd/ovs-vswitchd.c) thực hiện:
   a. set_program_name("ovs-vswitchd"), ovsthread_id_init.
   b. dns_resolve_init(true), parse_options(argc, argv) → trả về remote = unix:/var/run/.../db.sock.
   c. fork qua daemonize_start(true), parent thoát, child trở thành daemon, redirect stderr về log file.
   d. unixctl_server_create(NULL, &unixctl_server) → mở /var/run/.../ovs-vswitchd.<pid>.ctl
      cho ovs-appctl gửi command về.
   e. bridge_init(remote) → tạo IDL handle qua ovsdb_idl_create(),
      đăng ký các table cần monitor, init các subsystem (LACP, bond, CFM, BFD, STP, LLDP, RSTP).
   f. Vòng lặp chính: while (!exiting) {
        memory_run();        # nhẩm lại heap profile để memory/show có dữ liệu
        bridge_run();        # đọc IDL changes, reconfigure, gọi ofproto_run
        unixctl_server_run(); # phục vụ ovs-appctl
        netdev_run();        # poll netdev statistics, link state
        memory_wait(); bridge_wait(); unixctl_server_wait(); netdev_wait();
        poll_block();        # block trên epoll cho tới khi có sự kiện
        if (should_service_stop()) exiting = true;
      }
   g. Shutdown: bridge_exit(), unixctl_server_destroy(), ovsrcu_exit(), dns_resolve_destroy().
4. Thread phụ:
   - Handler thread (n-handler-threads, mặc định = max(2, n_cores) chia theo công thức trong
     ofproto/ofproto-dpif-upcall.c::udpif_set_threads): mỗi thread chạy udpif_upcall_handler()
     loop gọi dpif_recv() rồi xlate_actions() rồi dpif_flow_put().
   - Revalidator thread (n-revalidator-threads, mặc định ≈ n_handlers/4 + 1): mỗi thread chạy
     udpif_revalidator() loop dump megaflow → re-translate → giữ hoặc xoá.
   - Khi datapath_type=netdev có DPDK: PMD thread per-CPU pinned, polling RX queue.
```

Bridge_run thực hiện công việc gì cụ thể trong mỗi vòng lặp? Đọc thay đổi OVSDB qua `ovsdb_idl_run(idl)`, kiểm tra `idl_seqno` đổi thì gọi `bridge_reconfigure()` để add/del bridge, add/del port, cập nhật tunable; tiếp đó iterate tất cả bridge, mỗi bridge gọi `ofproto_run(br->ofproto)` để xử lý OpenFlow message từ controller (qua connmgr), service expire, flush queued packet_out, cập nhật rule statistics. Lock chính của vswitchd là `ofproto_mutex` bảo vệ flow table; một số path đọc dùng RCU (`ovsrcu`).

**Cách kỹ sư vận hành thành thạo.** Một engineer thành thạo cần phản xạ năm thao tác. Thao tác 1, kiểm tra trạng thái daemon: `systemctl status openvswitch-switch` rồi `ovs-appctl version` so với `dpkg -l openvswitch-switch` để chắc binary trên đĩa và process đang chạy cùng version (sau khi `apt upgrade` chưa restart sẽ lệch). Thao tác 2, đọc thread map: `top -H -p $(pgrep ovs-vswitchd)` để thấy đầy đủ main + handler + revalidator + URCU + IPsec; nếu thấy thread `revalidator` ăn CPU 100% → flow churn cao, kiểm tra `coverage/show`. Thao tác 3, đọc `coverage/show` và quan tâm các counter: `bridge_reconfigure`, `flow_extract`, `miss_queue_overflow`, `upcall_flow_limit_hit`, `revalidator_purge`, `rev_revalidate`, `txn_unchanged`. Thao tác 4, đọc `memory/show` để biết phần lớn RAM nằm ở đâu (handlers, revalidators, ports, rules, ofconns); con số `rules` lớn bất thường ám chỉ controller đang push rule không kiểm soát. Thao tác 5, biết đường tới log: `/var/log/openvswitch/ovs-vswitchd.log`, hiểu format `<timestamp>|<seq>|<module>|<level>|<message>`, dùng `ovs-appctl vlog/set <module>:file:dbg` để bật debug riêng module (`ofproto`, `bridge`, `dpif`, `connmgr`, `udpif`) thay vì bật toàn bộ.

**Phân loại.** Có thể phân loại các trạng thái và chế độ vận hành của `ovs-vswitchd`: (a) **Theo chế độ datapath**, `system` (kernel ko), `netdev` (userspace), `dummy` (test). (b) **Theo connection mode**, có controller (in-band/out-of-band), không controller (standalone, học MAC). (c) **Theo lifecycle phase**, starting (đọc IDL, build initial bridge), steady (vòng lặp main), reconfiguring (giữa lần đọc OVSDB delta và cài flow mới), shutting-down (drain + bridge_exit). (d) **Theo cấu hình tuning**, vanilla (không set `n-handler-threads`/`n-revalidator-threads`), DPDK-tuned (set `dpdk-init=true`, pin PMD core), HA (bật `flow-restore-wait` cho restart không mất traffic).

**Quy trình sử dụng.** Workflow "khởi tạo OVS từ con số 0 đến bridge first packet":

```
Bước 1: Đảm bảo hai daemon ngụ ý sẵn.
        $ systemctl status openvswitch-switch
        $ ps -ef | grep -E "ovsdb-server|ovs-vswitchd"
        Dự kiến thấy 2 process, ovs-vswitchd có nhiều thread con.

Bước 2: Đếm thread của ovs-vswitchd để xác nhận multi-thread.
        $ ps -L -p $(pgrep -f "/usr/sbin/ovs-vswitchd") -o pid,tid,comm
        Dự kiến thấy main, handler1..N, revalidator1..M, urcu, ipsec_monitor.

Bước 3: Tạo bridge và port.
        $ ovs-vsctl add-br br0
        Hành vi nội bộ: ovs-vsctl ghi vào OVSDB; ovsdb-server emit update tới
        ovs-vswitchd qua IDL; bridge_run() phát hiện idl_seqno đổi, gọi
        bridge_reconfigure() tạo struct bridge mới, gọi ofproto_create("br0").
        $ ovs-vsctl add-port br0 eth1
        Hành vi nội bộc: bridge_reconfigure thấy Port row mới, gọi dpif_port_add.

Bước 4: Quan sát ovs-vswitchd đã thấy bridge chưa.
        $ ovs-appctl bridge/dump-flows br0
        $ ovs-appctl dpif/show

Bước 5: Đẩy traffic và quan sát thread hoạt động.
        $ ovs-appctl coverage/show | grep -E "(facet|upcall|flow_extract)"
        Counter facet_revalidate, flow_extract, upcall_flow_limit_hit thay đổi
        từng giây khi traffic nhiều.

Bước 6: Quan sát log boot sequence.
        $ less /var/log/openvswitch/ovs-vswitchd.log
        Tìm dòng "ovs|...|bridge|INFO|bridge br0: added interface br0 ...",
        và "ofproto|INFO|system@ovs-system: opened dpif".
```

Best practice: KHÔNG chạy `ovs-vswitchd` thủ công trên production; luôn để init script quản lifecycle, để `--monitor` tự respawn khi crash. Anti-pattern: thay đổi `Open_vSwitch.other_config:n-handler-threads` rồi reload SIGHUP, vì option này chỉ đọc một lần ở `bridge_init`; muốn thay đổi cần restart daemon (hoặc set trước khi start).

**Khi xảy ra sự cố.** `ovs-vswitchd` là điểm bắt buộc nhớ trong các tình huống: (a) Bridge tạo trên `ovs-vsctl` mà packet không forward, kiểm tra trước tiên `pgrep ovs-vswitchd`; nếu daemon chết, traffic vẫn chạy được vài chục giây nhờ megaflow tồn dư trong kernel rồi mất hết. (b) Daemon đang chạy nhưng OF rule push không có hiệu lực, kiểm tra `coverage/show | grep ofctl_rate_limit_drop` xem connmgr có đang rate-limit hay không. (c) `ovs-vswitchd.log` spam `revalidator|WARN|flow limit hit, dropping flows`, tức megaflow đã chạm `flow-limit` (mặc định 200000); cần tuning. (d) CPU 100% trên main thread, thường là `bridge_reconfigure` lock hold quá lâu vì OVSDB dump rule lớn; chuyển sang ovs-vsctl-bench hoặc giảm rule churn. (e) Memory growth không dừng, dùng `memory/show` định kỳ ghi log; nếu `rules` tăng vô hạn thì controller push không xoá. Restart daemon bằng `systemctl restart openvswitch-switch` mà KHÔNG bật `flow-restore-wait` sẽ tạo data plane gap ~1-3 giây.

**Liên quan mật thiết.** `ovs-vswitchd` đan xen với gần như mọi keyword trong khối IX và một phần khối X: (a) `ovsdb-server` (nguồn cấu hình, vswitchd subscribe IDL, xem §9.1.3 và §10.1). (b) `openvswitch.ko` (đối tác phía dưới qua dpif system provider, xem §9.1.4 và §9.2). (c) `dpif` (lớp trừu tượng giữa, xem §9.1.AB). (d) `ofproto-dpif xlate` (hàm gọi mạch nhất khi xử lý upcall, xem §9.1.Y và §9.32). (e) `udpif_upcall_handler` và `udpif_revalidator` (worker thread chạy bên trong vswitchd). (f) `connmgr` (subsystem trong vswitchd phụ trách OF connection, xem §9.1.X.3). (g) `ovs-appctl` (CLI client nói chuyện qua control socket, xem §9.4 và §9.11). (h) `flow-restore-wait` (Open_vSwitch.other_config column ảnh hưởng restart behavior). (i) `coverage/show`, `memory/show` (telemetry expose qua control socket).

**Khác biệt giữa các phiên bản.**

| Phiên bản OVS | Hành vi `ovs-vswitchd` đáng chú ý |
|---------------|-----------------------------------|
| 1.x (2010-2013) | Single-threaded core loop, chưa có megaflow, rule cache giới hạn |
| 2.0 (2014) | Multi-thread (handler + revalidator), megaflow, dpif-netdev xuất hiện |
| 2.4 (2015) | Tunable `n-handler-threads`, `n-revalidator-threads` qua Open_vSwitch.other_config |
| 2.5 (2016) | Conntrack tích hợp, action `ct()` available trong xlate |
| 2.6 (2016) | EMC/SMC (Exact/Signature Match Cache) trong dpif-netdev cho DPDK |
| 2.8 (2017) | Tunnel NSH support, mở rộng connmgr negotiation OpenFlow 1.5 |
| 2.10 (2018) | Cache hierarchy expose qua dpif feature, more granular vlog modules |
| 2.13 (2020) | LTS, một bug rare gây revalidator memory leak (đã fix bản sau) |
| 2.17.9 (baseline) | LTS hiện hành, kiến trúc daemon ổn định, tunable đầy đủ, OVN tested |
| 3.x | Tiếp tục stable, không thay đổi shape kiến trúc; thêm capability negotiation |

Kiến trúc daemon nhìn từ ngoài (process tree, thread set, control socket) gần như không đổi suốt giai đoạn 2.x → 3.x; thay đổi chủ yếu nằm trong subsystem (xlate, conntrack, dpif feature).

**Cách quan sát + xác minh.** Sáu cách kiểm chứng `ovs-vswitchd` đang khoẻ mạnh và hoạt động đúng:

```
(1) Process tree (single process, multi-thread):
$ pgrep -af ovs-vswitchd
3247 ovs-vswitchd: monitoring pid 3248 (healthy)
3248 ovs-vswitchd unix:/var/run/openvswitch/db.sock --pidfile=...

$ ps -L -p 3248 -o tid,comm,pcpu | head
  TID COMMAND         %CPU
 3248 ovs-vswitchd     0.4    ← main thread (bridge_run + ofproto_run)
 3249 urcu1            0.0
 3250 ovs-vswitchd     0.0
 3251 handler2         0.2    ← udpif_upcall_handler
 3252 handler3         0.1
 3253 handler4         0.2
 3254 revalidator5     0.5    ← udpif_revalidator
 3255 revalidator6     0.4

(2) Lifetime + uptime:
$ ovs-appctl version
ovs-vswitchd (Open vSwitch) 2.17.9
$ stat -c '%y' /var/run/openvswitch/ovs-vswitchd.pid
2026-04-20 03:14:22.000000000 +0700        ← thời điểm khởi động gần nhất

(3) Internal counter:
$ ovs-appctl coverage/show | head -20
Event coverage, avg rate over last: 5 seconds, last minute, last hour ...
bridge_reconfigure       0.0/sec   0.000/sec    0.000/sec   total: 8
ofproto_dpif_xlate       42.5/sec  43.123/sec   42.901/sec  total: 1287345
upcall_flow_limit_hit     0.0/sec   0.000/sec    0.000/sec   total: 0
rev_revalidate            5.2/sec   5.111/sec    5.092/sec   total: 168944
flow_extract             52.3/sec  53.011/sec   52.840/sec  total: 1547223

(4) Heap snapshot:
$ ovs-appctl memory/show
handlers:4 ofconns:1 ports:7 revalidators:2 rules:127 udpif keys:218

(5) OF connection state:
$ ovs-appctl ofproto/list
br-int
$ ovs-appctl bridge/list-controllers br-int
unix:/var/run/openvswitch/br-int.mgmt   (for trace tool only)
ssl:10.0.0.5:6653                       (CONNECTED, role=other)

(6) Log sample khi handler thread cài megaflow:
$ tail -5 /var/log/openvswitch/ovs-vswitchd.log
2026-04-22T08:14:11.220Z|00342|bridge|INFO|bridge br-int: added interface tap-vm-web on port 1
2026-04-22T08:14:11.221Z|00343|ofproto|INFO|system@ovs-system: opened dpif
2026-04-22T08:14:11.301Z|00344|connmgr|INFO|br-int<->ssl:10.0.0.5:6653: connecting...
2026-04-22T08:14:11.521Z|00345|connmgr|INFO|br-int<->ssl:10.0.0.5:6653: connected
2026-04-22T08:14:11.825Z|00346|udpif(handler3)|DBG |received upcall: dp_packet ... (action set installed)
```

**Source code tham chiếu.** Theo Rule 14 dùng function name anchor (line drift across version). Baseline OVS v2.17.9:

- `vswitchd/ovs-vswitchd.c`, entry point. Function `main()` parse argv, daemonize, gọi `bridge_init()` rồi vào main loop. Function `parse_options()` xử lý long options như `--mlockall`, `--pidfile`, `--log-file`. Block comment đầu file mô tả "Main program for ovs-vswitchd".
- `vswitchd/bridge.c`, lifecycle bridge và OVSDB IDL. Function `bridge_init(remote)` mở IDL connection. Function `bridge_run()` chạy mỗi vòng main loop, đọc IDL, gọi `bridge_reconfigure()` khi cấu hình đổi, sau đó iterate `ofproto_run()` cho mỗi bridge. Function `bridge_reconfigure()` orchestrate add/del bridge, add/del port, đặt datapath_id, mirror, MAC table, NetFlow, sFlow, IPFIX. Function `bridge_exit()` cleanup khi shutdown.
- `ofproto/ofproto.c`, OF protocol layer. Function `ofproto_create()` tạo struct ofproto cho bridge. Function `ofproto_run()` chạy mỗi vòng, expire rule, run connmgr, flush packet_out queue. Function `ofproto_destroy()` cleanup.
- `ofproto/ofproto-dpif.c`, dpif provider của ofproto. Class `ofproto_dpif_class` ghép vào registry qua `ofproto_class_register()`. Function `construct()` tạo struct ofproto_dpif khi bridge mới được tạo, gọi `dpif_open()` từ §9.1.AB.
- `ofproto/ofproto-dpif-upcall.c`, handler + revalidator. Function `udpif_create()` được gọi từ ofproto-dpif construct. Function `udpif_set_threads(udpif, n_handlers, n_revalidators)` đặt số thread (mặc định tính theo `count_cpu_cores()`). Function `udpif_start_threads()` `pthread_create` các handler (`udpif_upcall_handler`) và revalidator (`udpif_revalidator`).
- `ofproto/connmgr.c`, OpenFlow connection manager. Function `connmgr_create()` tạo struct, function `connmgr_run()` chấp nhận TCP/SSL connect từ controller.
- `lib/unixctl.c`, control socket framework cho `ovs-appctl`. Function `unixctl_server_create()` mở Unix socket. Function `unixctl_command_register()` đăng ký command name (`coverage/show`, `memory/show`, ...).

**Trường hợp sự cố thực tế.** Một deployment OpenStack (4 controller + 25 compute, mỗi compute ~80 VM, OVN as Neutron driver) ghi nhận: vào thời điểm cao điểm trafffic, `ovs-vswitchd` trên một số compute node ăn 100% CPU trên main thread, dẫn tới VM ping nhau bị giật ngắt quãng, ovn-controller log "ovsdb client transaction took too long". Investigation: `top -H -p $(pgrep ovs-vswitchd)` thấy main thread saturated, handler/revalidator nhàn rỗi. `ovs-appctl coverage/show` cho thấy `bridge_reconfigure` chạy 4-5 lần mỗi giây (bình thường < 1 lần/phút). Nguyên nhân gốc: ovn-controller đang push flow update với tần suất rất cao (do ARP-update churn từ một số VM bị crash loop), mỗi update trigger OVSDB transaction, mỗi transaction gây IDL update tới `ovs-vswitchd`, mỗi update gọi `bridge_reconfigure` để re-validate config. Mitigation: (i) xác định VM crash loop, dừng VM đó; (ii) tuning `Open_vSwitch.other_config:vlan-limit`; (iii) trong release sau bật `ofproto_pipeline_pause` patch đề xuất upstream cho phép ovn-controller batch update. Lesson: triệu chứng "ovs-vswitchd CPU 100%" mà handler/revalidator vẫn nhàn là dấu hiệu kinh điển của main thread bị reconfigure storm; tool đầu tay là `top -H` rồi `coverage/show` tìm `bridge_reconfigure` count; nếu cao thì gốc nằm ở phía controller hoặc OVSDB writer chứ không phải data plane.

**Bài tập synthetic.**

```
Lab Z1, "Khởi động ovs-vswitchd, đo thread, restart không gián đoạn"
       (~50 phút, một host Ubuntu 22.04 LTS với OVS 2.17.9):

Topology dự kiến:
  - 1 host vật lý hoặc VM, đã apt install openvswitch-switch.
  - 1 bridge br0 với 2 internal port p1, p2 đặt netns h1, h2.
  - Không cần controller; bridge chạy fail-mode standalone.

Bước 1: Kiểm tra hai daemon đã chạy.
        $ systemctl status openvswitch-switch
        $ pgrep -af ovs-vswitchd
        $ pgrep -af ovsdb-server

Bước 2: Tạo bridge + 2 port.
        $ ovs-vsctl add-br br0
        $ ovs-vsctl add-port br0 p1 -- set Interface p1 type=internal
        $ ovs-vsctl add-port br0 p2 -- set Interface p2 type=internal
        $ ip netns add h1; ip link set p1 netns h1; ip -n h1 addr add 10.0.0.1/24 dev p1; ip -n h1 link set p1 up
        $ ip netns add h2; ip link set p2 netns h2; ip -n h2 addr add 10.0.0.2/24 dev p2; ip -n h2 link set p2 up

Bước 3: Đếm thread con của ovs-vswitchd.
        $ ps -L -p $(pgrep -f /usr/sbin/ovs-vswitchd) -o tid,comm | sort -u
        Dự kiến thấy: main process tid, urcu, handler*, revalidator*, ipsec_monitor (nếu có).

Bước 4: Đẩy traffic và quan sát coverage.
        Tab A: $ ip netns exec h1 ping -i 0.001 10.0.0.2
        Tab B: $ watch -n1 "ovs-appctl coverage/show | head -25"
        Quan sát flow_extract, ofproto_dpif_xlate tăng theo thời gian.

Bước 5: Quan sát ofproto_dpif_xlate từng handler.
        $ ovs-appctl upcall/show
        Dự kiến thấy mỗi handler: dispatch <count>, exec <us>, ...

Bước 6: Test restart không gián đoạn (flow-restore-wait).
        Tab A vẫn ping. Tab B:
        $ ovs-vsctl set Open_vSwitch . other_config:flow-restore-wait=true
        $ systemctl restart openvswitch-switch
        $ ovs-vsctl remove Open_vSwitch . other_config flow-restore-wait
        Quan sát Tab A: số gói mất rất ít (so với restart thông thường mất 1-3s liên tục).

Bước 7: Predict, Observe, Explain.
        Predict: thread set sẽ tái tạo sau restart; megaflow trong kernel bị giữ lại nhờ
                 flow-restore-wait cho tới khi userspace reconnect.
        Observe: pid mới khác pid cũ, megaflow cũ vẫn forward một thời gian, sau đó được
                 revalidator kiểm tra và giữ lại nếu vẫn match OF rule.
        Explain: ovs-vswitchd chỉ là "người cài flow"; một khi flow đã trong kernel, gói
                 vẫn được forward dù process userspace tạm vắng mặt; flow-restore-wait
                 ngăn handler thread mới purge megaflow tồn dư trước khi reconnect xong.

Pass criteria: học viên (i) liệt kê được thread map của ovs-vswitchd, (ii) giải thích được
              tại sao restart không phá huỷ traffic ngay khi có flow-restore-wait, (iii)
              đọc được coverage counter và biết counter nào ý nghĩa gì.
```

**Lỗi thường gặp + tín hiệu chẩn đoán.**

- *Triệu chứng* `ovs-vsctl add-br` thành công nhưng port không forward. *Nguyên nhân* `ovs-vswitchd` đã crash hoặc chưa start. *Chẩn đoán* `pgrep ovs-vswitchd` không có kết quả; `journalctl -u openvswitch-switch` thấy "ovs-vswitchd|ERR|...". *Khắc phục* `systemctl restart openvswitch-switch`; nếu lặp lại, đọc log core dump.
- *Triệu chứng* CPU main thread 100%, handler/revalidator nhàn. *Nguyên nhân* Reconfigure storm do controller hoặc ovs-vsctl writer push thay đổi quá nhiều. *Chẩn đoán* `top -H -p $(pgrep ovs-vswitchd)` để xác định thread; `ovs-appctl coverage/show | grep bridge_reconfigure`. *Khắc phục* Giảm tần suất writer phía trên; trong tình huống xấu nhất, restart ovs-vswitchd để bẻ chu kỳ.
- *Triệu chứng* `ovs-vswitchd.log` spam `revalidator|WARN|flow limit hit`. *Nguyên nhân* Megaflow vượt giới hạn `flow-limit` mặc định 200000. *Chẩn đoán* `ovs-appctl dpif/show -s` đọc `flows: (current ...)`, `memory/show` đọc `rules`. *Khắc phục* `ovs-vsctl set Open_vSwitch . other_config:flow-limit=400000`; hoặc kiểm tra controller có cài rule không có timeout.
- *Triệu chứng* Restart `ovs-vswitchd` gây mất traffic vài giây. *Nguyên nhân* `flow-restore-wait` chưa bật, kernel megaflow bị purge khi userspace mới khởi động và revalidator quét. *Chẩn đoán* `ovs-vsctl get Open_vSwitch . other_config` không thấy `flow-restore-wait=true`. *Khắc phục* Bật trước restart, gỡ sau restart khi `ovs-appctl ofproto/list` đã thấy đủ bridge.
- *Triệu chứng* `ovs-vswitchd` memory tăng theo thời gian, sau vài tuần OOM. *Nguyên nhân* Controller push rule không xoá, hoặc bug rare trong revalidator (đã thấy ở 2.13). *Chẩn đoán* Theo dõi `ovs-appctl memory/show` định kỳ, ghi log cột `rules` và `udpif keys`. *Khắc phục* Upgrade lên LTS mới hơn (2.17 LTS), audit controller logic.

**So sánh với hệ khác.** Các hệ khác giải bài "userspace daemon điều phối kernel data plane" theo cách khác:

- **Linux bridge + brctl**: không có daemon userspace tương đương, mọi cấu hình nằm trong sysfs/ioctl, không có OpenFlow brain. `ovs-vswitchd` thay thế hoàn toàn vai trò này khi dùng OVS.
- **Cumulus Linux switchd**: tương đương ở level "userspace daemon program kernel forwarding", nhưng switchd nói chuyện với SwitchAPI driver hardware ASIC thay vì kernel module software; mô hình split control/data tương tự.
- **VPP `vpp_main` process** (FD.io): VPP cũng là daemon userspace, tổ chức dạng vector packet processing graph; khác với `ovs-vswitchd` ở chỗ VPP tự run cả data plane userspace, không có bipartite "kernel fast path + userspace control" như OVS default.
- **Cilium agent**: daemon userspace program eBPF data plane, tương đương ở vai trò "userspace daemon program data plane" nhưng dùng eBPF thay vì OpenFlow + kernel module.
- **NSX-T netcpa + nestdb-ops**: VMware NSX có pair daemon tương đương `ovs-vswitchd` + `ovsdb-server`; thực ra nguồn gốc Nicira → VMware nên kiến trúc gần giống, chỉ khác tên process và protocol.
- **Cisco IOS XR `BGP-RIB ↔ FIB downloader`**: gần như không tương đương; IOS XR tích hợp control + forwarding trong proprietary firmware, không có "userspace daemon → kernel module via stable interface" tách rạch ròi.

---



---

## 9.1.AB dpif, lớp trừu tượng giữa userspace ofproto-dpif và datapath provider

> **Phạm vi:** Treatment chuyên sâu (mở rộng 2026-04-26) cho keyword `dpif` (Datapath Interface). Keyword này là cornerstone của OVS internals vì là điểm duy nhất trong code base nơi userspace forwarding logic tách rời khỏi datapath implementation cụ thể (kernel module openvswitch.ko, userspace netdev provider, hoặc các provider tương lai). Cross-link mật thiết với §9.1.X (5-lớp ofproto-dpif), §9.1.X.2 (`dpif/show` output), §9.2 (kernel datapath megaflow là nội tại của một dpif provider), §9.32 §9.32.4 (compact architecture sketch trước đây). Curriculum baseline OVS v2.17.9.

**Khái niệm.** `dpif` là viết tắt của "DataPath InterFace", một abstract C interface định nghĩa trong `lib/dpif.h` và `lib/dpif-provider.h`. dpif đại diện cho "một switch nhìn từ phía userspace": tập hợp port, một flow table có khả năng cài/xóa/dump entry, một upcall channel để nhận packet không khớp flow, và một execute path để inject packet. dpif KHÔNG phải là kernel module, KHÔNG phải là một daemon, mà là **một bộ function pointer + struct layout** mà các provider concrete (`dpif-netlink`, `dpif-netdev`) phải implement. Nhờ dpif, mọi tầng phía trên trong `ovs-vswitchd` (bridge.c, ofproto.c, ofproto-dpif.c, ofproto-dpif-xlate.c, ofproto-dpif-upcall.c) chỉ cần biết "gọi dpif_flow_put rồi nhận kết quả", không cần biết flow đó kết thúc ở Linux kernel hay ở thread polling DPDK port. Loại: userspace abstraction layer (vtable pattern), file ngôn ngữ C, không phải database table, không phải binary, không phải daemon.

**Lịch sử + bối cảnh.** dpif xuất hiện trong giai đoạn refactor lớn đầu tiên của OVS (2010-2012) khi Nicira nhận ra mô hình "ovs-vswitchd biết trực tiếp cách nói chuyện với kernel module" (qua `lib/dpif-linux.c` đời đầu) là điểm chặn cho việc port OVS lên môi trường mới. Năm 2012 xuất hiện `dpif-netdev` cho phép chạy datapath thuần userspace; năm 2014 NSDI 2015 paper (Pfaff et al.) đã chính thức hoá dpif như "the datapath interface" trong sơ đồ kiến trúc. Năm 2014-2015 dpif-netdev được mở rộng để hỗ trợ DPDK PMD; sau này có thêm các provider thử nghiệm (windows, dummy cho test). Mỗi lần OVS thêm một datapath backend mới, không cần đụng vào ofproto, mà chỉ cần implement đủ các function pointer trong `dpif_class`. Đây là một trong những refactor có giá trị duy trì lâu dài nhất của OVS, vì kiến trúc này vẫn còn nguyên sau hơn một thập kỷ.

**Vị trí trong kiến trúc.** Trong sơ đồ 5 lớp của `ovs-vswitchd` (đã trình bày ở §9.1.X.1), dpif nằm ở **lớp dưới cùng userspace**, ngay trên ranh giới với data plane:

```
bridge.c                          (lớp 1, vòng đời bridge)
  └─ ofproto/ofproto.c            (lớp 2, OF protocol semantics)
      └─ ofproto/ofproto-dpif.c   (lớp 3, ofproto biết về dpif)
          ├─ ofproto-dpif-xlate.c (dịch OF → odp action)
          ├─ ofproto-dpif-upcall.c (nhận upcall từ dpif)
          └─ ofproto-dpif-rid.c   (recirc id pool)
              ↓ chỉ gọi qua dpif_* API
          ──────────────────────────
          lib/dpif.c               (lớp 4, dispatch wrapper)
              ↓ vtable dpif_class
          ──────────────────────────
          lib/dpif-netlink.c       (lớp 5a, kernel provider)
              ↓ Netlink genl
              kernel openvswitch.ko
          lib/dpif-netdev.c        (lớp 5b, userspace provider)
              ↓ in-process function call
              userspace netdev / DPDK PMD thread pool
```

Scope: per-`ovs-vswitchd`-process, mỗi process có thể mở nhiều dpif instance song song (mỗi bridge một dpif, hoặc nhiều bridge share một dpif type tuỳ provider). Một host thường có 1 dpif type chính (`system` cho kernel, `netdev` cho userspace), không trộn lẫn ngẫu nhiên trên cùng bridge.

**Role (rewritten in English by the 2026-04-28 source-citation cleanup).** Four core roles: (a) **Provider dispatch.** `lib/dpif.c` keeps a registry of `dpif_class` instances (via `dp_register_provider()`) and resolves a type name (`system`, `netdev`) to the corresponding class when `dpif_open()` is called. (b) **API uniformity.** Every function call from the layer above the datapath (install a flow, dump flows, add a port, receive an upcall, execute a packet) goes through one unified set of `dpif_*` symbols; `ofproto-dpif` never calls `dpif_netlink_*` or `dpif_netdev_*` directly. (c) **Capability negotiation.** `dpif` exposes a feature matrix via `dpif_class.cache_get_supported_levels`, the `dpif_supports_*` helpers, and the command `ovs-appctl dpif/show-dp-features`, so xlate knows whether the current provider supports a given action (ct, recirc, push_mpls, sample). (d) **Lifecycle and ownership.** `dpif` manages the datapath-instance life cycle: `dpif_create`, `dpif_open`, `dpif_close`, so `ofproto` does not have to handle cleanup itself.

**Vì sao sinh ra.** Động cơ thiết kế là tách "what to do" ra khỏi "how to do it". Trước dpif, code OVS đời đầu (2009-2010) có hard-coded `linux_*` function gọi thẳng Netlink, làm việc port sang FreeBSD, port sang Hyper-V, port sang DPDK đều phải sửa hàng nghìn dòng tại nhiều nơi. dpif đảo ngược dependency: mọi nơi phía trên gọi qua interface, mọi nơi phía dưới chỉ cần implement interface. Đây là instance kinh điển của Dependency Inversion Principle áp dụng vào systems C. Lợi ích cụ thể đã chứng minh: (i) thêm DPDK datapath chỉ cần một file `dpif-netdev.c` lớn mà không phải sửa ofproto.c, (ii) thêm test backend (`dpif-dummy`) cho unit test ofproto mà không cần kernel module, (iii) thêm AF_XDP hỗ trợ thử nghiệm như một nhánh trong `dpif-netdev`.

**Vấn đề giải quyết.** Cụ thể dpif giải quyết bốn lớp vấn đề: (1) **Tính khả chuyển đa nền**, OVS không bị khoá vào Linux kernel; provider có thể là kernel module, là userspace polling, là hardware accelerator gateway. (2) **Khả năng test**, ofproto-dpif có thể test bằng `dpif-dummy` (in-memory) không cần root, không cần load kernel module. (3) **Capability divergence handling**, các provider khác nhau hỗ trợ khác nhau (ví dụ kernel có ct() từ 2.5, userspace netdev có ct() muộn hơn, một số tunnel type chỉ kernel hoặc chỉ userspace); xlate đọc capability matrix mà ra quyết định "emulate" hay "delegate". (4) **Performance isolation**, khi DPDK PMD thread polling không thể block trên I/O, dpif-netdev có cơ chế gọi async không khoá main thread của ovs-vswitchd; tách qua interface giúp ofproto code không phải tự xử lý sự khác biệt blocking model.

**Tầm quan trọng.** Đây là khái niệm cốt lõi tuyệt đối của OVS internals. Không hiểu dpif thì không thể đọc được flow chi tiết của upcall, không debug được kernel-vs-userspace mismatch, không hiểu được tại sao cùng một OF rule lại có hành vi khác nhau giữa hai host (host A có `datapath_type=system`, host B có `datapath_type=netdev`). Mức độ quan trọng ngang `megaflow`, `ct_state`, `recirc_id`, `ofproto-dpif xlate` trong nhóm cốt lõi OVS. Trong cấu trúc 320+ keyword của curriculum, dpif là "trục dọc" gắn kết toàn bộ Khối IX (OVS internals).

**Cơ chế hoạt động.** Trình tự per-bridge từ lúc `ovs-vsctl add-br br0` đến lúc packet đầu tiên được forward:

1. `bridge.c` đọc OVSDB, biết `Bridge.datapath_type = "system"` (mặc định khi chưa set).
2. `ofproto.c` gọi `ofproto_create()`, ofproto-dpif chọn dpif provider qua `dp_register_provider()` registry.
3. `lib/dpif.c::dpif_open("br0", "system", &dpif)` lookup `dpif_netlink_class`, gọi `dpif_netlink_class.open(...)`. Kernel module nhận message Netlink genl `OVS_DP_CMD_NEW`, tạo struct datapath trong `net/openvswitch/datapath.c`.
4. `ofproto-dpif.c` gọi `dpif_port_add()` cho mỗi `Port` row trong OVSDB; provider gửi `OVS_VPORT_CMD_NEW`.
5. ofproto cài initial flow table 0 với `dpif_operate(struct dpif_flow_put)`. Provider netlink translate sang `OVS_FLOW_CMD_NEW`.
6. ofproto-dpif-upcall.c spawn handler thread, mỗi thread block trên `dpif_recv()` chờ upcall.
7. Khi packet đầu tiên đến, kernel megaflow miss → upcall `OVS_PACKET_CMD_MISS` → handler thread `dpif_recv` trả về `struct dpif_upcall` chứa packet + key.
8. Handler chạy xlate, sinh datapath actions, gọi `dpif_flow_put()` cài megaflow + `dpif_execute()` để forward packet đầu tiên (vì kernel không tự forward khi miss).
9. Mọi packet sau cùng flow đi đường fast path trong kernel, không qua dpif (dpif chỉ tồn tại ở userspace).

`dpif_class` (vtable) trong `lib/dpif-provider.h` (OVS v2.17.9) tổ chức theo nhóm:

```
struct dpif_class {
    const char *type;                    /* "system", "netdev", "dummy" */
    int (*init)(void);                   /* Provider registration */

    /* Datapath lifecycle */
    int  (*open)(const struct dpif_class *, const char *name,
                 bool create, struct dpif **);
    void (*close)(struct dpif *);
    int  (*destroy)(struct dpif *);

    /* Port management */
    int (*port_add)(struct dpif *, struct netdev *, odp_port_t *port_no);
    int (*port_del)(struct dpif *, odp_port_t);
    int (*port_query_by_number)(const struct dpif *, odp_port_t,
                                struct dpif_port *);

    /* Flow table */
    int (*flow_flush)(struct dpif *);
    struct dpif_flow_dump *(*flow_dump_create)(const struct dpif *, ...);
    int (*flow_dump_next)(struct dpif_flow_dump_thread *,
                          struct dpif_flow *flows, int max_flows);
    void (*operate)(struct dpif *, struct dpif_op **ops, size_t n_ops, ...);
        /* ^ batched flow_put / flow_del / execute */

    /* Upcall / packet I/O */
    int  (*recv_set)(struct dpif *, bool enable);
    int  (*recv)(struct dpif *, uint32_t handler_id,
                 struct dpif_upcall *, struct ofpbuf *buf);
    void (*recv_wait)(struct dpif *, uint32_t handler_id);

    /* Conntrack, meter, bond, cache config (later additions) */
    int (*ct_dump_start)(struct dpif *, ...);
    int (*ct_flush)(struct dpif *, ...);
    int (*meter_set)(struct dpif *, ...);
    /* ... và một số method tiện ích khác */
};
```

Hai class concrete (cùng file `lib/dpif-provider.h` declare extern, implement trong file riêng):

| Trường | `dpif_netlink_class` | `dpif_netdev_class` |
|--------|---------------------|---------------------|
| File | `lib/dpif-netlink.c` | `lib/dpif-netdev.c` |
| `type` | `"system"` | `"netdev"` |
| Datapath | Linux kernel module openvswitch.ko | Userspace, in-process |
| Transport | Netlink genl (`ovs_datapath`, `ovs_flow`, `ovs_vport`) | Function call trong cùng process |
| `recv` | Netlink socket dequeue | PMD thread enqueue |
| Phù hợp | Production mặc định, kernel offload | DPDK, AF_XDP, container không có module |
| Hiệu năng | 5-10 Gbps mỗi NIC | 50+ Gbps khi pin DPDK PMD core |

**Cách kỹ sư vận hành thành thạo.** Một engineer thành thạo dpif cần phản xạ năm thao tác: (1) Nhận diện ngay provider đang dùng qua `ovs-vsctl get Bridge br0 datapath_type`; nếu trống thì mặc định `system`. Kiểm tra cross-reference với `ovs-appctl dpif/show` (dòng đầu sẽ là `system@ovs-system` hoặc `netdev@ovs-netdev`). (2) Đọc capability matrix qua `ovs-appctl dpif/show-dp-features <bridge>` trước khi push một OF rule mới có action lạ; nếu provider không hỗ trợ, xlate sẽ emulate (recirc nhiều) hoặc reject. (3) Phân biệt rõ dpif flow (in-kernel megaflow đã install) với OF flow (rule nguồn trong ofproto): `ovs-ofctl dump-flows br0` ≠ `ovs-appctl dpif/dump-flows br0`; cái sau là output của `dpif_flow_dump_*`. (4) Đọc upcall stat trong `ovs-appctl dpif/show -s` để biết `lost` (queue overflow), `hits`, `missed`; tỷ lệ `lost/missed > 1%` là báo động provider đang bị nghẽn. (5) Biết cách bật log debug riêng cho provider qua `ovs-appctl vlog/set dpif:file:dbg` và `vlog/set dpif_netlink:file:dbg` để trace từng flow_put/flow_del nếu nghi vấn corruption.

**Phân loại.** Có thể phân loại dpif theo nhiều trục: (a) **Theo type string**, `system` (kernel netlink), `netdev` (userspace netdev gồm cả DPDK), `dummy` (test in-memory, không expose ngoài unit test). (b) **Theo data path location**, kernel-resident hay userspace-resident. (c) **Theo transport**, Netlink (system) hay function call (netdev/dummy). (d) **Theo lifecycle owner**, kernel module (system tồn tại độc lập với ovs-vswitchd) hay process (netdev datapath chết khi ovs-vswitchd chết). (e) **Theo API method completeness**, full provider (đủ ct, meter, bond, cache) hay restricted provider (dummy chỉ implement subset đủ test). Theo importance: (a) > (b) > (c) > (d) > (e).

**Quy trình sử dụng.** Workflow điển hình khi engineer phải debug một mismatch giữa OF rule và hành vi thực tế trên bridge:

```
Bước 1: Xác định provider hiện tại.
        $ ovs-vsctl get Bridge br0 datapath_type
        → "system" (kernel) hoặc "netdev" (userspace) hoặc "" (default=system).
        $ ovs-appctl dpif/show
        → Dòng đầu tiên hiển thị type@name, ví dụ "system@ovs-system".

Bước 2: Đọc capability của provider hiện tại.
        $ ovs-appctl dpif/show-dp-features br0
        → Xem Recirc, CT state, CT zone, CT mark, NAT, Sample, Clone, Truncate, MPLS.
        → Nếu rule cần feature mà provider trả "No" → root cause tiềm năng.

Bước 3: So OF rule với dpif flow đã install.
        $ ovs-ofctl dump-flows br0          # OF rule (nguồn)
        $ ovs-appctl dpif/dump-flows br0    # megaflow đã install (đích)
        → Nếu OF rule có nhưng dpif không có flow tương ứng sau khi gửi packet,
          thì xlate hoặc dpif_flow_put bị lỗi (xem log).

Bước 4: Quan sát upcall flow.
        $ ovs-appctl dpif/show -s
        → hits / missed / lost. lost > 0 nghĩa upcall queue overflow.
        $ ovs-appctl coverage/show | grep -E "dpif|upcall"
        → flow_put count, recv count, miss count.

Bước 5: Bật debug log nếu nghi vấn provider corruption.
        $ ovs-appctl vlog/set dpif:file:dbg
        $ ovs-appctl vlog/set dpif_netlink:file:dbg   # nếu provider system
        → Tail file ovs-vswitchd.log, mỗi flow_put/flow_del hiện ra.
        → Tắt debug sau khi xong: ovs-appctl vlog/set dpif:file:warn.

Bước 6: Verify kernel module loaded (nếu provider system).
        $ lsmod | grep openvswitch
        → Nếu thiếu, dpif_netlink.open() đã trả về ENODEV và bridge không có data plane.
```

Best practice: không bao giờ thay đổi `datapath_type` của một bridge production đang chạy traffic; tạo bridge mới với type khác và migrate port. Anti-pattern: gọi trực tiếp `dpif-netlink-*` symbol trong patch code (vi phạm dependency inversion).

**Khi xảy ra sự cố.** `dpif` là keyword bắt buộc nhớ trong các tình huống: (a) Bridge "không forward gì cả" mặc dù OF rule đã cài đầy đủ, kiểm tra trước tiên `dpif/show` xem provider có thực sự open được không (lost queue, recv disabled). (b) `ovs-vswitchd` log spam `dpif|WARN|... could not add ...`, tức `dpif_flow_put` fail; thường do provider thiếu capability hoặc kernel module phiên bản cũ không hiểu một odp action mới. (c) Reload kernel module xong bridge mất hết flow, phải hiểu rằng provider system có lifecycle độc lập với ovs-vswitchd và `flow-restore-wait` cần được set khi restart. (d) DPDK deployment báo "datapath_type netdev not registered", thường do ovs-vswitchd build không có DPDK support nhưng OVSDB lại cấu hình netdev. (e) Mismatch capability sau khi nâng cấp OVS userspace mà không nâng kernel module cùng phiên bản, tính năng mới (ví dụ ct_label mở rộng) sẽ rớt về emulation hoặc reject.

**Liên quan mật thiết.** `dpif` đan xen với: (a) `ofproto-dpif xlate` (consumer phía trên gọi `dpif_flow_put`, đồng thời đọc capability để quyết định emulate, xem §9.32 §9.32.3). (b) `upcall` (phía dưới đẩy lên, dpif là transport, xem §9.2.4 và §9.2.10). (c) `megaflow` và `recirc_id` (entry mà dpif quản lý là megaflow có recirc_id ở đầu key, xem §9.2.2 và §9.2.14.2). (d) `openvswitch.ko` (provider system implement trên kernel module này, xem §9.2.14.3). (e) `ovs-dpctl`, `ovs-appctl dpif/*` (CLI bên ngoài cũng dùng dpif API qua control socket, xem §9.4 và §9.11). (f) `OVSDB Bridge.datapath_type` column (cấu hình chọn provider, xem §10.x). (g) `UFID` (Unique Flow ID 128-bit do dpif emit khi cài flow, xem §9.2.11).

**Khác biệt giữa các phiên bản.**

| Phiên bản OVS | Hành vi `dpif` |
|---------------|----------------|
| < 1.4 | Hard-coded linux netlink, chưa có tách dpif |
| 1.4-1.10 (2011-2013) | dpif interface stable, chỉ có `dpif-linux` |
| 2.0 (2014) | Rename `dpif-linux` → `dpif-netlink`, thêm `dpif-netdev` userspace |
| 2.4 (2015) | DPDK PMD thread integration trong dpif-netdev |
| 2.5 (2016) | Conntrack methods (`ct_dump_*`, `ct_flush`) thêm vào dpif_class |
| 2.7 (2017) | Meter methods thêm vào dpif_class |
| 2.10 (2018) | Cache levels (EMC/SMC) expose qua dpif feature |
| 2.13 (2020) | Bond methods thêm vào dpif_class cho HW bond offload |
| 2.17.9 (baseline curriculum) | dpif_class ổn định, ~30 method pointer, hỗ trợ AF_XDP qua netdev |
| 3.x | Capability negotiation refinement; `dpif/show-dp-features` mở rộng |

Lưu ý: với mỗi phiên bản, hai class concrete (`dpif_netlink_class`, `dpif_netdev_class`) có thể không implement đầy đủ method mới ngay; xlate đọc trả về của `dpif_class.method` (NULL hoặc EOPNOTSUPP) để biết và emulate.

**Cách quan sát + xác minh.** Năm cách kiểm chứng `dpif` đang hoạt động:

```
(1) Xác định provider type và datapath name:
$ ovs-appctl dpif/show
system@ovs-system:
  bridge br-int/0:
    flows: (current 1247) (avg 1250) (max 1350) (limit 200000)
    br-int 65534/<local>: (internal)
    tap-vm-web 1/1: (internal)
    ovn-huyvl-0 3/3: (geneve: remote_ip=10.200.0.12, csum=true, key=flow)

(2) Stat upcall hits/misses/lost:
$ ovs-appctl dpif/show -s
system@ovs-system: hit:45234567 missed:234561 lost:0
  flows: 2345
  port 0: ovs-system (internal)
  port 1: br-int (internal)
  port 2: tap-vm-web (system: ifindex=42)

(3) Capability matrix (dùng đọc trước khi push rule mới):
$ ovs-appctl dpif/show-dp-features br-int
Masked set action: Yes
Tunnel push pop: No
Ufid: Yes
Truncate action: Yes
Clone action: Yes
Recirc: Yes
CT state: Yes
CT zone: Yes
CT mark: Yes
CT label: Yes
CT state NAT: Yes

(4) Đối chiếu OF rule với dpif megaflow:
$ ovs-ofctl dump-flows br-int | wc -l        # OF rule count
$ ovs-appctl dpif/dump-flows br-int | wc -l  # megaflow count
                                              # (megaflow << OF rule là bình thường)

(5) Coverage counter từ chính lib/dpif.c:
$ ovs-appctl coverage/show | grep -E "^(dpif_|flow_put|flow_del|upcall)"
dpif_port_add                 0.0/sec ...
dpif_flow_put                42.5/sec ...
dpif_flow_flush               0.0/sec ...
upcall_flow_limit_hit         0.0/sec ...
```

**Source code tham chiếu.** Theo Rule 14 dùng function name anchor (line drift across version). Baseline OVS v2.17.9:

- `lib/dpif.h`, public client API. Các function chính: `dpif_open()`, `dpif_close()`, `dpif_port_add()`, `dpif_port_del()`, `dpif_flow_put()`, `dpif_flow_del()`, `dpif_flow_dump_create()`, `dpif_flow_dump_next()`, `dpif_execute()`, `dpif_recv()`, `dpif_recv_wait()`. Block comment đầu file mô tả rõ "A datapath is a collection of physical or virtual ports... a datapath has no intelligence of its own. Rather, it relies entirely on its client to set up flows."
- `lib/dpif-provider.h`, định nghĩa `struct dpif_class` (vtable provider implement) và `struct dpif` (instance handle). Hai class extern declare cuối file: `dpif_netlink_class`, `dpif_netdev_class`.
- `lib/dpif.c`, dispatch wrapper. Function `dpif_open()` lookup type qua `do_open()`, gọi `class->open()`. Function `dp_register_provider()` cho plugin add type vào registry. Function `dpif_normalize_type()` mặc định trống → `system`.
- `lib/dpif-netlink.c`, the kernel provider. The function `dpif_netlink_open()` (line 354 at v2.17.9) sends `OVS_DP_CMD_NEW` over Netlink. Flow modifications go through the class vtable callback `dpif_class->flow_put`, dispatched from `lib/dpif.c::dpif_flow_put`, not through a top-level `dpif_netlink_flow_put` function. The function `dpif_netlink_recv()` (line 3133) dequeues `OVS_PACKET_CMD_MISS` upcalls from the kernel.
- `lib/dpif-netdev.c`, the userspace provider. The function `dpif_netdev_open()` (line 1888) allocates the in-process `struct dp_netdev`. The function `dp_netdev_flow_add()` (line 4078) inserts into the in-memory classifier. Packet reception from PMD-thread queues is dispatched through the class vtable callback, not a top-level `dpif_netdev_recv` function.
- `ofproto/ofproto-dpif-upcall.c`, the consumer above the dpif layer. The function `udpif_upcall_handler()` (line 809) loops, calls `dpif_recv()`, and runs xlate.
- `ofproto/ofproto-dpif.c`, the integration layer. The class entry point is `ofproto_dpif_class.construct` (the `construct` callback at line 1649), which calls `dpif_open()` when the bridge is created. Bridge teardown goes through `dpif_close()` (already cited above), not a separate `dpif_destroy` function. All function names in this list are verified at the OVS v2.17.9 baseline (commit `0bea06d9957e3966d94c48873cd9afefba1c2677`, 2024-02-08).

> **Note on the v3.9.2 hotfix.** Earlier versions of this section listed `dpif_netlink_flow_put`, `dp_netdev_open`, `dpif_netdev_recv`, `ofproto_dpif_open`, and `dpif_destroy` as if they exist as top-level functions in v2.17.9. None of them exist as named functions; flow-put dispatch goes through the class vtable, the netdev open is `dpif_netdev_open`, the netdev recv path uses PMD threads through the class vtable, and bridge teardown uses `dpif_close`. Plan v3.9.2 R2 dropped the four fabrications and renamed `dp_netdev_open` to its real name. See `memory/sdn/v3.9.2-q7-axis16-sweep-2026-04-28.md` for the audit log.

**Real incident scenario (rewritten in English by the 2026-04-28 source-citation cleanup).** A multi-host OpenStack Neutron deployment (about 40 compute nodes) reported the following: after upgrading the `ovs-vswitchd` userspace from 2.13 to 2.17 while FORGETTING to upgrade the kernel module `openvswitch.ko` (still the distro's 2.13 builtin), some bridges showed unexpected behavior: OF rules with `actions=ct(commit,nat(src=10.0.0.5))` failed to apply NAT to packets passing through. The dpif symptom: `ovs-appctl dpif/show-dp-features br-int` reported `CT state NAT: No`. Investigation: the log contained `dpif_netlink|WARN|sending packet failed (Operation not supported)`; the capability matrix between userspace expectation and kernel reality did not match. Root cause: the 2.13 kernel module did not support the new NAT option in `OVS_CT_ATTR` that the 2.17 userspace emitted. When the flow-put dispatcher in `lib/dpif.c::dpif_flow_put` (verified at line 1022) routed the flow to the kernel through the class vtable callback `dpif_class->flow_put`, the kernel rejected it; `ovs-vswitchd` logged a warning and the rule silently had no effect. Mitigation: rebuild and load the kernel module from the same 2.17 source tree (`apt install openvswitch-datapath-dkms`), reload the module, and the bridge recovers its megaflows automatically. Lesson: when upgrading OVS, always sync userspace and kernel module together; the dpif capability matrix is the fastest tool to detect mismatch, ahead of blaming the controller or OF rule logic.

> **Note on the citation.** Earlier versions of this paragraph cited `dpif_netlink_flow_put()` as if it were a top-level function in `lib/dpif-netlink.c`. That function does not exist at v2.17.9; flow-put dispatch goes through the `dpif_class->flow_put` callback in the vtable, called from `lib/dpif.c::dpif_flow_put` at line 1022. The Netlink backend implements the callback inline as part of `dpif_netlink_class`, not as an exported top-level function. The 2026-04-28 source-citation cleanup pass corrected the narrative.

**Bài tập synthetic.**

```
Lab D1, "Hai provider cùng workload, đo capability + flow install"
       (~45 phút, cần một host có cả kernel module và userspace netdev sẵn sàng):

Topology dự kiến:
  - Host: Ubuntu 22.04 LTS với OVS 2.17.9.
  - Bridge A: br-sys, datapath_type mặc định (system, kernel).
  - Bridge B: br-userspace, datapath_type=netdev (userspace, không cần DPDK).
  - Mỗi bridge gắn 2 internal port (p1, p2) đặt netns h1, h2.

Bước 1: Tạo hai bridge với hai provider type.
        $ ovs-vsctl add-br br-sys
        $ ovs-vsctl add-br br-userspace -- set Bridge br-userspace datapath_type=netdev
        $ ovs-vsctl get Bridge br-sys datapath_type
        $ ovs-vsctl get Bridge br-userspace datapath_type

Bước 2: Quan sát hai dpif cùng tồn tại trong một ovs-vswitchd.
        $ ovs-appctl dpif/show
        Dự kiến thấy: system@ovs-system và netdev@ovs-netdev là hai dpif riêng.

Bước 3: Đối chiếu capability matrix.
        $ ovs-appctl dpif/show-dp-features br-sys
        $ ovs-appctl dpif/show-dp-features br-userspace
        Ghi nhận khác biệt: ví dụ Tunnel push pop có thể khác nhau,
        Conntrack support có thể khác nhau giữa hai provider.

Bước 4: Push cùng một OF rule, quan sát megaflow trên hai dpif.
        $ ovs-ofctl add-flow br-sys "priority=100,ip,actions=NORMAL"
        $ ovs-ofctl add-flow br-userspace "priority=100,ip,actions=NORMAL"
        $ ip netns exec h1 ping -c 3 10.0.0.2          # qua br-sys
        $ ip netns exec h1 ping -c 3 10.0.1.2          # qua br-userspace
        $ ovs-appctl dpif/dump-flows br-sys
        $ ovs-appctl dpif/dump-flows br-userspace

Bước 5: Predict, Observe, Explain.
        Predict: hai dpif có cùng số lượng flow vì cùng OF rule.
        Observe: cùng một OF rule sinh hai megaflow độc lập (mỗi dpif một),
                 có thể khác mask granularity do classifier internal khác nhau.
        Explain: dpif_class khác nhau ⇒ flow_put dispatch tới hai code path
                 khác nhau ⇒ dù cùng input vẫn có thể có internal layout khác.

Pass criteria: học viên trình bày được vì sao dpif là "cùng API, hai backend",
               và tại sao megaflow hai bên không cần khớp byte-by-byte.
```

**Lỗi thường gặp + tín hiệu chẩn đoán.**

- *Triệu chứng* Bridge tạo ra nhưng `ovs-appctl dpif/show` không liệt kê. *Nguyên nhân* Provider open fail (kernel module thiếu, hoặc type sai chính tả). *Chẩn đoán* Tail `ovs-vswitchd.log`, tìm dòng `dpif|ERR|could not create datapath`. *Khắc phục* `lsmod | grep openvswitch`, load lại module hoặc sửa `datapath_type`.
- *Triệu chứng* `ovs-appctl dpif/show -s` báo `lost: <số tăng>`. *Nguyên nhân* Upcall queue overflow, handler thread không kịp dequeue. *Chẩn đoán* `ovs-appctl coverage/show | grep upcall`, `dpif/show -s` so sánh hits vs lost. *Khắc phục* Tăng `n-handler-threads` qua `ovs-vsctl set Open_vSwitch . other_config:n-handler-threads=8`.
- *Triệu chứng* OF rule cài thành công (`ovs-ofctl dump-flows` thấy) nhưng packet không match khi đi qua. *Nguyên nhân* `dpif_flow_put` bị provider reject (capability mismatch, action không hỗ trợ). *Chẩn đoán* `ovs-appctl vlog/set dpif:file:dbg`, tail log tìm warn message khi packet đầu tiên đi qua. *Khắc phục* Đối chiếu capability với `dpif/show-dp-features`, hoặc nâng cấp provider (kernel module hoặc userspace nếu netdev) đồng bộ phiên bản.
- *Triệu chứng* Sau reload kernel module, bridge mất hết megaflow, traffic gián đoạn ~5-30 giây. *Nguyên nhân* Provider system có lifecycle độc lập với ovs-vswitchd; reload module = reset datapath. *Chẩn đoán* `dmesg | tail` thấy module unload/load sequence. *Khắc phục* Trước reload, set `ovs-vsctl set Open_vSwitch . other_config:flow-restore-wait=true`, sau khi ovs-vswitchd reconnect xong unset; trong production tránh reload module khi có traffic nóng.
- *Triệu chứng* DPDK port bị "could not add port: No such device". *Nguyên nhân* Bridge `datapath_type=system` nhưng port type DPDK chỉ tồn tại trong `dpif-netdev`. *Chẩn đoán* `ovs-vsctl get Bridge br0 datapath_type`. *Khắc phục* Tạo bridge mới với `datapath_type=netdev` và migrate DPDK port sang.

**So sánh với hệ khác.** Các hệ khác giải bài "tách interface với implementation cho data plane" theo cách khác:

- **Linux VFS** (`struct file_operations`): tương đương rất sát. Mỗi filesystem (ext4, xfs, fat) implement vtable, kernel client (`vfs_read`, `vfs_write`) gọi qua interface. dpif_class chính là VFS file_operations cho data plane switch.
- **Linux netfilter `struct nf_hook_ops`**: gần giống về vtable nhưng hướng tới hook chain, không tới data plane abstraction; netfilter không tách "kernel netfilter" và "userspace netfilter" như dpif tách kernel/userspace datapath.
- **OpenStack Neutron ML2 mechanism driver**: tương đương ở tầng cao hơn; mỗi mechanism driver (ovs, linuxbridge, sr-iov) implement một interface, controller dispatch. Khác: ML2 ở control plane ngày, dpif ở userspace data plane.
- **Cisco IOS Forwarding Information Base abstraction**: tách CEF (Cisco Express Forwarding) kernel-style với hardware ASIC FIB. Tương đương về nguyên tắc abstraction, khác về scope (Cisco là proprietary firmware).
- **VPP plugin model** (FD.io): VPP có `vlib_node` graph với plugin. Khác: VPP plugin định hình packet processing graph, dpif chỉ định hình flow table interface.
- **Linux tc `cls_*` classifier**: KHÔNG tương đương vì tc classifier không có "dispatch by type", mọi classifier đều cùng một in-kernel plugin model. dpif tách hẳn userspace với data plane backend.

---

## 9.1.AA Logical_Flow note (cross-link OVN architecture)

> **Note:** `Logical_Flow` cornerstone keyword sẽ được treat đầy đủ trong nhóm OVN architecture. Inline reference here cho cross-link integrity.

`Logical_Flow` là OVN intermediate representation (xem 13.1 + 13.16). Treat chi tiết trong nhóm OVN architecture của curriculum.

---

## Tài liệu tham khảo

1. [Open vSwitch Architecture — Official Documentation](https://docs.openvswitch.org/en/latest/intro/what-is-ovs/)
2. [man ovs-vswitchd(8), man7.org](https://man7.org/linux/man-pages/man8/ovs-vswitchd.8.html)
3. [The Design and Implementation of Open vSwitch — Pfaff et al., NSDI 2015](https://www.usenix.org/system/files/conference/nsdi15/nsdi15-paper-pfaff.pdf)
4. Jorge Crichigno et al. *Open Virtual Switch Lab Series*, University of South Carolina.
