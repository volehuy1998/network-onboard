# 9.2. Kernel datapath and megaflow cache

> **State:** Content written (Phase B, 2026-04-21)
> **Block:** IX, Open vSwitch internals
> **Part:** Part 9
> **Ebook mapping:** NSDI 2015 paper (Pfaff et al.) plus compass Ch Q plus compass Ch R plus USC Lab 9
> **Plan:** `plans/sdn-foundation-architecture.md` §3.3 Block IX
> **Prerequisites:** Part 9.1
> **Primary offline source:**
> - `sdn-onboard/doc/compass_artifact_wf-*.md`. Chapter Q (datapath introspection, `ovs-dpctl`, `upcall/set-flow-limit`), Chapter R (`ovs-appctl` reference).
> - `sdn-onboard/doc/ovs/OVS.pdf` plus `OVS.txt`. Crichigno et al. *Open Virtual Switch Lab Series*, Lab 11 (Kernel Datapath).
> - `sdn-onboard/doc/ovs/Day 4-lab 9 - Open vSwitch Kernel Datapath.pdf` plus `.txt`. WASTC 2021 Lab 9: slow path plus fast path.
> - `sdn-onboard/doc/ovs/OpenVSwitch.pdf` plus `.txt`. Companion reference for USC and WASTC.

The lab environment is Ubuntu 22.04 LTS with OVS 2.17.9. The primary references are [Pfaff et al. NSDI 2015](https://www.usenix.org/system/files/conference/nsdi15/nsdi15-paper-pfaff.pdf) and [man ovs-dpctl(8)](https://man7.org/linux/man-pages/man8/ovs-dpctl.8.html).

---

## Learning objectives

After completing this part, you can:

1. **Describe (Understand)** the difference between the microflow cache of OVS 1.x (exact match per 5-tuple) and the megaflow cache introduced in OVS 2.0 (January 2014), which uses tuple space search with wildcards.
2. **Explain (Understand)** the upcall mechanism on cache miss: the kernel sends the packet to userspace through Netlink, a handler thread runs the OpenFlow pipeline, compiles a megaflow entry with maximum wildcards (lazy wildcarding), and installs it back into the kernel.
3. **Apply (Apply)** `ovs-dpctl dump-flows -m` and `ovs-appctl upcall/show` to observe megaflow keys, wildcard masks, and flow-install pressure in a real environment.
4. **Evaluate (Evaluate)** the production cache hit rate of 99 percent or higher, what it means for real throughput, and the scenarios in which the hit rate drops below 90 percent and indicates a need for tuning.

---

Knowledge prerequisites:

- The three OVS components and the Netlink genl family (Part 9.1).
- The concept of cache hit versus cache miss in computer architecture.
- OpenFlow 1.3 match fields and actions (Part 4.2).

---

## Context: the problem the kernel datapath must solve

Consider a KVM host running 50 VMs, where each VM has 20 concurrent TCP connections. The total is 1000 TCP microflows. Each microflow is the tuple (`src_ip`, `dst_ip`, `src_port`, `dst_port`, protocol), the classic 5-tuple. The kernel datapath must decide the forwarding action for every packet at a rate of millions of packets per second.

The naive approach, where a userspace daemon decides every packet, is not viable. The kernel-to-userspace context switch costs about 2 microseconds, which limits real throughput to roughly 500K packets per second.

The opposite approach, putting the full OpenFlow pipeline inside the kernel, is also not viable. The pipeline is complex: classifier, prioritized rules, and arbitrary header modification. Kernel code should not carry logic of this kind.

The OVS solution is this. **The kernel keeps a cache of decisions that have already been computed**, while userspace computes new decisions only when the cache misses. The cache must be fast (nanosecond scale), correct (no policy violation), and memory-efficient.

---

## 9.2.1 Microflow cache: the OVS 1.x design and its limit

OVS 1.x (2011 to 2013) used a **microflow cache**. Each entry described one exact 5-tuple, with no wildcards. Lookup was O(1) through a hash table.

The advantage is that hits are extremely fast. A lookup needs only a hash computation and a full-key comparison.

The fatal drawback is that **every microflow that differs in any field needs its own entry**. For example:

- TCP connection 1: `src=10.0.0.1:50001` to `dst=10.0.0.2:80`, entry A.
- TCP connection 2: `src=10.0.0.1:50002` to `dst=10.0.0.2:80`, entry B (different `src_port`).
- TCP connection 3: `src=10.0.0.1:50003` to `dst=10.0.0.2:80`, entry C.

Even though all three connections hit the same OpenFlow rule ("allow TCP `dst=10.0.0.2:80`") and the same action (`output:5`), each one gets its own cache entry that differs only in `src_port`. With 1000 connections, the cache has 1000 entries.

The result is cache thrashing. The cache has a fixed size. When the number of microflows exceeds that size, old entries are evicted. The next packet for an evicted flow misses, triggers an upcall, reinstalls, and the cycle repeats.

NSDI 2015 measured a Rackspace production workload (2013, before megaflow) and reported a miss rate of 10 to 30 percent at peak. The overhead was too high to scale.

---

## 9.2.2 Megaflow cache: the NSDI 2015 breakthrough

A **megaflow** is a cache entry with **wildcards**. It matches a set of microflows that share the same header pattern. Instead of 1000 entries for 1000 TCP connections to port 80, a single entry `dst=10.0.0.2, dst_port=80, proto=TCP, wildcard src_ip+src_port` covers all of them.

The typical compression factor is 10 to 100. A workload of 100K microflows compresses into a cache of 2,000 to 10,000 entries.

**The key question is this: how does OVS decide which field to wildcard?** OVS answers with **lazy wildcarding**. When the userspace classifier processes a microflow for the first time, it does the following:

1. Parses the packet into the 5-tuple and other fields.
2. Walks the OpenFlow pipeline: match rules in table 0, then table 1, and so on, then execute the action.
3. While walking, **tracks** which fields have been inspected.
4. Marks any field that was NOT inspected as a wildcard.
5. Compiles a megaflow entry that wildcards every uninspected field.

For example, given the rule "match `dst_ip=10.0.0.2, dst_port=80, proto=TCP`, action `output:5`" and a packet with `src_ip=10.0.0.1, src_port=50001`:

- The classifier inspects `dst_ip` (match), `dst_port` (match), and `proto` (match).
- The classifier does NOT inspect `src_ip` or `src_port`.

The resulting megaflow entry matches `dst_ip=10.0.0.2, dst_port=80, proto=TCP` and wildcards `src_ip=*, src_port=*`.

**Correctness guarantee.** Lazy wildcarding never breaks any rule. If a different rule constrains `src_port`, the classifier inspects `src_port` while walking and does not wildcard it. The result is correct by construction.

---

## 9.2.3 Tuple Space Search for megaflow lookup

When there are many megaflows with different masks, each megaflow has its own **tuple**, which is the set of fields that are matched (not wildcarded). OVS uses **Tuple Space Search (TSS)**, an algorithm from Srinivasan et al. SIGCOMM 1998. The algorithm groups entries by tuple, and each tuple has its own hash table.

Per-packet lookup works like this: for each tuple, extract the fields per the mask, compute the hash, and look up the hash table. On a match, return the action. Otherwise, try the next tuple.

The complexity is O(T x H), where T is the number of tuples (typically fewer than 20) and H is the cost of one hash lookup (O(1) on average).

> **Key point:** Choosing TSS over TCAM or a decision tree is a pragmatic decision. TCAM is too expensive on a commodity server. A decision tree has a high build cost when rules change frequently. TSS is the best compromise for a software datapath. The NSDI 2015 benchmark shows TSS reaching more than 10M lookups per second per core on an Intel Xeon.

---

## 9.2.4 Upcall: slow path and fast path

USC Lab 9 (`doc/ovs/Day 4-lab 9 - Open vSwitch Kernel Datapath.txt`) states:

> "Kernel module directs the packet to the userspace. The userspace makes the decisions about the actions to be taken against the packet according to OpenFlow entries (slow path)."
>
> "The action entry is stored in the kernel and used to forward subsequent packet which makes the forwarding faster (fast path)."

**Fast path (cache hit):**

1. The packet arrives at the NIC, goes through the kernel stack, and enters the `openvswitch.ko` rx hook.
2. The kernel extracts the flow key from the headers.
3. The kernel looks up the megaflow cache through TSS.
4. On match, it executes the action (`output:5` calls `netdev_ops->ndo_start_xmit`).
5. The kernel updates counters. Userspace is never touched. The latency is about 1 to 5 microseconds.

**Slow path (cache miss):**

1. A packet arrives and the lookup misses.
2. The kernel sends an **upcall** through Netlink to userspace.
3. The upcall carries the packet bytes, metadata (`in_port`, timestamp), and reason.
4. An `ovs-vswitchd` handler thread receives the upcall.
5. The handler walks the OpenFlow pipeline and tracks the inspected fields.
6. It compiles a megaflow entry and an action list.
7. It sends `OVS_FLOW_CMD_NEW` back to the kernel to install the megaflow.
8. It sends `OVS_PACKET_CMD_EXECUTE` so the kernel executes the action for this packet.
9. Subsequent similar packets then hit the fast path.

USC Lab 9 puts it this way: the first packet of a flow finds that "the kernel table is empty" and so it goes through the slow path. After "action entries are installed in the kernel", subsequent packets follow the fast path.

The slow-path cost is about 50 to 200 microseconds per packet. When the hit rate is low, the slow path dominates real throughput.

---

## 9.2.5 Ukeys and revalidator threads

When a megaflow is installed into the kernel, `ovs-vswitchd` keeps a **ukey (unique key)**, a userspace copy that holds the flow key, the mask, the action, the last-known statistics counter, and the `last_used` timestamp.

**Revalidation.** When rules change, the revalidator threads (4 by default, with a 500 ms interval) scan the ukey set:

1. For each ukey, re-walk the pipeline with the flow key.
2. Compare the new action with the old action.
3. If they differ, send `OVS_FLOW_CMD_SET` to update the kernel.
4. If no rule matches, send `OVS_FLOW_CMD_DEL`.

**Timeout.** Rules carry `idle_timeout` and `hard_timeout`. The revalidator tracks the timestamp and deletes the entry on timeout.

The thread count is configured by `other_config:n-revalidator-threads`. A large cache means a CPU-heavy scan, so raise to 8 or 16 threads when needed.

---

## 9.2.6 Upcall rate limit

Compass Ch Q describes the tuning knobs:

- `upcall/show` reports the number of in-flight upcalls, the average, the maximum, and the limit (200,000 flow-installs per second by default).
- `upcall/set-flow-limit 500000` raises the limit for high-scale workloads.

Hitting the limit causes the kernel to drop packets, and the `lost` counter in `ovs-dpctl show` rises.

The relationships are:

- `hit` grows linearly: normal.
- `missed` grows: the cache is not hitting well.
- `lost` greater than zero: the rate limit is being hit and traffic is at risk.

Tune `other_config:n-handler-threads` (2 by default). Raise it when miss rate is high but `lost` is zero, because that indicates the handlers are bottlenecked.

---

## 9.2.7 Cache hit rate in production

NSDI 2015 reported the following from Rackspace production:

- Before megaflow: 70 to 90 percent hit rate, with 10 to 30 percent miss at peak.
- After megaflow: 99 percent or higher hit rate, with miss below 1 percent.

At 1M packets per second, that gives about 10,000 upcalls per second, which is within the capacity of the handler threads.

**Early warning signals:**

- Coverage miss rate above 1 percent: the compression is not effective.
- `ovs-dpctl show` shows `lost` greater than zero: the rate limit is being hit.
- `upcall/show` average is high: the handlers are bottlenecked.

**Scenarios where the hit rate drops:**

1. Highly diverse traffic, where every microflow differs in the fields that the rules match on.
2. Frequently changing rules, which causes the revalidator to invalidate many ukeys.
3. A small cache or a low `max-idle` value, which evicts entries early.

---

### Guided Exercise 3 (Predict-Observe-Explain): demonstrate that megaflow compresses microflows

**Outcomes:**
- Observe wildcarded megaflows in `ovs-dpctl dump-flows -m`.
- Demonstrate that the megaflow count is smaller than the TCP connection count.
- Understand lazy wildcarding from empirical evidence.

**Before you begin:**

```bash
[host]# apt install iperf3
[host]# ovs-vsctl add-br br-test
[host]# ovs-vsctl add-port br-test veth0 -- set Interface veth0 type=internal
[host]# ovs-vsctl add-port br-test veth1 -- set Interface veth1 type=internal
[host]# ip addr add 10.200.0.1/24 dev veth0 && ip link set veth0 up
[host]# ip addr add 10.200.0.2/24 dev veth1 && ip link set veth1 up
```

**Predict:**

Counter-hypothesis: if OVS still used the microflow cache (OVS 1.x), after creating N TCP connections that differ only in `src_port` to the same `dst_port`, we would see **N megaflow entries**. If megaflow compresses through wildcards, the entry count should be **significantly smaller**.

**Observe:**

**1.** Clear the cache: `ovs-appctl revalidator/purge`.

**2.** Create 100 TCP streams:

```bash
[host]# iperf3 -s -B 10.200.0.2 -D
[host]# iperf3 -c 10.200.0.2 -P 100 -t 30 -B 10.200.0.1 &
```

**3.** Dump megaflows and count connections:

```bash
[host]# ovs-dpctl dump-flows -m | wc -l
[host]# ss -t -n | grep ':5201' | wc -l
```

**Explain:**

A typical result is about 200 TCP connections and about 5 to 10 megaflows, a 20-fold compression ratio.

If the microflow cache were still in use, we would see 200 entries. The observed 10 entries prove that megaflow wildcards `src_port`, compressing 100 client-side flows into one megaflow.

The `dump-flows -m` output:

```
recirc_id(0),in_port(2),...,tcp(src=0/0x0,dst=5201), packets:12345
```

The token `tcp(src=0/0x0, dst=5201)` shows that the `src` mask is `0x0`, fully wildcarded. This is empirical evidence of lazy wildcarding.

**Finish:** `pkill iperf3 && ovs-vsctl del-br br-test`

---

## 9.2.8 EMC, the Exact Match Cache: the first tier in datapath lookup

Up to §9.2.7 we have discussed the megaflow cache as if it were the only cache layer. In practice, **the OVS 2.x kernel datapath has three cache tiers stacked on top of one another**, queried in this order: EMC, then SMC, then the megaflow classifier (TSS). Understanding all three tiers makes it possible to read `pmd-stats-show` correctly and to diagnose the `miss with success upcall` counter (Part 9.11 §9.11.8).

The **Exact Match Cache (EMC)** is the first tier. It is an exact-match hash table keyed by the packet 5-tuple (or a wider key), with no wildcards. Each EMC entry corresponds to one specific **microflow**.

### 9.2.8.1 EMC structure

| Property | Value |
|---|---|
| **Scope** | Per PMD thread (or per CPU core for the kernel datapath), not shared |
| **Lookup** | O(1) hash table, no wildcards |
| **Key** | Full packet header fields (both `src` and `dst`, TCP ports, and so on) |
| **Capacity** | 8192 entries per PMD by default (compile-time constant `EM_FLOW_HASH_ENTRIES`) |
| **Insertion** | After a successful lookup at a lower tier (SMC or megaflow) |
| **Eviction** | LRU-like, with overwrite on hash collision |
| **Purpose** | Fast path for hot flows: a hot packet is funneled into EMC and avoids re-walking TSS |

EMC exists in both the kernel datapath (`openvswitch.ko`) and the userspace DPDK datapath (`dpif-netdev`). The kernel implementation differs from userspace in detail but the semantics are the same: an exact-match hash for the fast path.

### 9.2.8.2 Reading EMC stats from `pmd-stats-show`

**Context.** This applies only to `datapath_type=netdev` (DPDK and AF_XDP). The kernel datapath does not expose EMC stats directly; you must read the `coverage/show` counters `emc_hit` and `emc_lookup`.

**Command and output** `[reproduced from OVS DPDK benchmark setup]`:

```shell
$ sudo ovs-appctl dpif-netdev/pmd-stats-show
pmd thread numa_id 0 core_id 2:
  packets received: 10000000
  packet recirculations: 12543
  avg. datapath passes per packet: 1.00
  emc hits: 9050231
  smc hits: 820412
  megaflow hits: 129303
  avg. subtable lookups per megaflow hit: 2.3
  miss with success upcall: 54
  miss with failed upcall: 0
  avg. packets per output batch: 12.4
```

**Anatomy of each EMC-related counter:**

| Counter | Meaning | Healthy |
|---|---|---|
| `emc hits` | Packets that matched the EMC tier | At least 85 percent of `packets received` for a stable workload |
| `smc hits` | Packets that missed EMC and hit the SMC tier | 5 to 15 percent |
| `megaflow hits` | Packets that missed both EMC and SMC and hit the megaflow classifier | 1 to 5 percent |
| `miss with success upcall` | Packets that missed megaflow and an upcall successfully installed a flow | Less than 0.1 percent in steady state |
| `miss with failed upcall` | Upcalls that failed (OVSDB lock, error) | Must be zero |

**Failure scenarios:**

- **EMC hit below 50 percent on a workload expected to be stable.** This is EMC thrashing. 8192 entries are not enough for a workload with more than 8000 concurrent distinct microflows. The fix is to enable SMC (on by default in OVS 2.15 and later), or to accept the shift down to megaflow hits as the new top tier.
- **SMC hit plus megaflow hit greater than 50 percent total.** EMC is bypassed or disabled. Check `ovs-appctl dpif-netdev/pmd-perf-show` for details.
- **`miss with success upcall` rising steadily.** New flows keep arriving. This indicates a scanner workload or a malicious traffic pattern. Check the OpenFlow classifier flow limit (Part 9.2.6) and the `coverage/show upcall_flow_add` counter.

### 9.2.8.3 When to disable EMC

EMC consumes cache memory on the PMD core. For a very low hit rate workload, where every packet is unique, EMC becomes pure overhead. Disable it like this:

```shell
$ sudo ovs-vsctl set Open_vSwitch . other_config:emc-insert-inv-prob=0
# Restart to apply; set greater than 0 to re-enable (smaller value = more insertion)
```

The value `emc-insert-inv-prob=N` is the inverse insertion probability, meaning the chance of insertion is 1 in N. The default is 100, which gives a 1 percent insertion rate. A specialized setup (a security gateway scanning every packet) can set 1000 or disable EMC entirely.

---

## 9.2.9 SMC, the Signature Match Cache: the middle tier in OVS 2.15 and later

SMC was added in OVS 2.15 (February 2021) to close the performance gap between EMC (exact microflow, 8K entries) and the megaflow classifier (TSS, with the higher O(T x H) cost). The NSDI 2020 paper *HXDP* and the OVS mailing list discuss SMC as an optimization for workloads with more than 100k concurrent microflows, where EMC alone is not sufficient.

### 9.2.9.1 SMC structure

| Property | Value |
|---|---|
| **Scope** | Per PMD thread (same as EMC) |
| **Lookup** | O(1) hash table, compact signature-based |
| **Key** | 16-bit signature of the flow hash (not the full 5-tuple) |
| **Value** | A pointer to the megaflow entry (not the action directly) |
| **Capacity** | 16384 entries per PMD (compile-time) |
| **Insertion** | After a successful lookup at the megaflow classifier |
| **Purpose** | Reduces TSS lookup cost for "moderately cold" flows (not hot enough for EMC but still regular) |

SMC **does not contain the full action**. It contains a pointer to a megaflow entry. The lookup flow is: compute the signature hash, do an SMC lookup, on hit dereference the pointer, and execute the megaflow action. The dereference step is faster than re-walking TSS because it skips the whole subtable scan.

### 9.2.9.2 The EMC, SMC, and megaflow lookup path

The lookup order in `dpif-netdev` (OVS 2.15 and later) is:

```
Packet arrives at PMD
  ↓
(1) EMC lookup: O(1) hash, full 5-tuple exact
    ├── HIT → execute action, done (~50 ns)
    └── MISS → (2)
  ↓
(2) SMC lookup: O(1) hash, 16-bit signature
    ├── HIT → follow pointer to megaflow, execute (~150 ns)
    └── MISS → (3)
  ↓
(3) Megaflow classifier TSS: O(T) subtable scan
    ├── HIT → execute action, insert into SMC (+optional EMC) (~300-500 ns)
    └── MISS → (4)
  ↓
(4) Upcall to userspace
    → handler walks OpenFlow pipeline
    → compile megaflow
    → install into kernel classifier
    → (~50-200 µs)
```

The numbers come from NSDI 2020 and OVS benchmarks. The three-tier design lets you trade memory per entry against lookup cost:
- **EMC:** small (8K entries), extremely fast (50 ns), for the hottest flows only.
- **SMC:** medium (16K entries), moderately fast (150 ns), for regular flows.
- **Megaflow:** large (200K entries by default), slower (300 to 500 ns) but wildcard-capable, covering the bulk of the workload.

### 9.2.9.3 SMC tuning

SMC is on by default. Disable it with `other_config:smc-enable=false` for troubleshooting or benchmarking.

In current OVS (3.x) there is no runtime option for SMC size. Recompile OVS with `#define SMC_ENTRIES N` if you need to change it. Production rarely needs adjustment.

---

## 9.2.10 The upcall Netlink protocol: the message anatomy between kernel and userspace

When §9.2.4 introduced upcalls, it did not examine the **message wire format** between kernel and userspace. Upcalls travel over **Generic Netlink** (genl), the Linux mechanism for a kernel subsystem to expose an API to userspace. OVS registers the genl families `ovs_vport`, `ovs_flow`, `ovs_datapath`, and `ovs_packet` with the kernel.

Understanding the wire format helps when debugging: `vlog/set dpif:file:dbg` dumps Netlink headers that are otherwise hard to interpret.

### 9.2.10.1 Generic Netlink header structure

```
+-----------------------------------------------+
|           Netlink header (16 byte)             |
|  ┌─────────────────────────────────────────┐  |
|  │ nlmsghdr {                              │  |
|  │   __u32 nlmsg_len   (total message len) │  |
|  │   __u16 nlmsg_type  (family ID, dynamic │  |
|  │                      for ovs_packet)    │  |
|  │   __u16 nlmsg_flags (NLM_F_REQUEST, ...) │  |
|  │   __u32 nlmsg_seq   (sequence #)         │  |
|  │   __u32 nlmsg_pid   (source PID)         │  |
|  │ }                                        │  |
|  └─────────────────────────────────────────┘  |
+-----------------------------------------------+
|       Generic Netlink header (4 byte)          |
|  ┌─────────────────────────────────────────┐  |
|  │ genlmsghdr {                            │  |
|  │   __u8 cmd    (OVS_PACKET_CMD_MISS,      │  |
|  │                OVS_PACKET_CMD_EXECUTE, ..) │  |
|  │   __u8 version                            │  |
|  │   __u16 reserved                          │  |
|  │ }                                        │  |
|  └─────────────────────────────────────────┘  |
+-----------------------------------------------+
|       OVS attribute TLV list (variable)        |
|  ┌─────────────────────────────────────────┐  |
|  │ nlattr {                                │  |
|  │   __u16 nla_len                         │  |
|  │   __u16 nla_type  (OVS_PACKET_ATTR_*)    │  |
|  │ }                                        │  |
|  │ + payload                                │  |
|  │ ... (multiple attributes)                │  |
|  └─────────────────────────────────────────┘  |
+-----------------------------------------------+
```

### 9.2.10.2 OVS_PACKET_CMD_MISS upcall anatomy

When the kernel misses the cache, it sends an upcall message with:

| Attribute | Type | Meaning |
|---|---|---|
| `OVS_PACKET_ATTR_PACKET` | raw bytes | Full packet data (Ethernet frame) |
| `OVS_PACKET_ATTR_KEY` | nested TLV | Extracted flow key (`tun_id`, `in_port`, `eth_src/dst`, `nw_src/dst`, and so on) |
| `OVS_PACKET_ATTR_USERDATA` | raw | Optional context (when the packet went through a `userspace` action) |
| `OVS_PACKET_ATTR_EGRESS_TUN_KEY` | nested | Tunnel metadata for egress |
| `OVS_PACKET_ATTR_MRU` | u16 | Maximum Receive Unit for fragmentation handling |

**Debugging the upcall wire format:**

```shell
# Enable DPIF debug log (prints Netlink messages in detail)
$ sudo ovs-appctl vlog/set dpif:file:dbg
$ sudo tail -f /var/log/openvswitch/ovs-vswitchd.log

# Trigger a miss: ping an IP that the datapath has never seen
$ ping -c 1 <fresh_ip>

# Log sample:
2026-04-24T11:15:23.412Z|00123|dpif(handler1)|DBG|system@ovs-system: \
  miss upcall:
  recirc_id(0),in_port(3),eth(src=aa:bb:cc:00:00:01,dst=aa:bb:cc:00:00:02),\
  eth_type(0x0800),ipv4(src=10.1.0.5,dst=10.1.0.10,proto=1,frag=no), \
  icmp(type=8,code=0)
2026-04-24T11:15:23.413Z|00124|dpif(handler1)|DBG|system@ovs-system: \
  execute: actions:4 packets:1 (86B)
```

**Anatomy of each log field:**

| Part | Meaning |
|---|---|
| `|dpif(handler1)|` | Log subsystem and thread name |
| `|DBG|` | Log level (turned on by `vlog/set dpif:file:dbg`) |
| `system@ovs-system:` | Datapath name |
| `miss upcall:` | Upcall reason: cache miss |
| `recirc_id(0)` | Recirculation ID is 0; the packet has not gone through `ct()` or conjunction |
| `in_port(3)` | Ingress datapath port |
| `eth(src=..., dst=...)` | L2 extract |
| `eth_type(0x0800)` | EtherType is IPv4 |
| `ipv4(src=..., dst=..., proto=1, frag=no)` | L3 extract, `proto=1` is ICMP |
| `icmp(type=8, code=0)` | ICMP Echo Request |
| `execute: actions:4` | Handler reply: execute action `output:4` |

### 9.2.10.3 OVS_FLOW_CMD_NEW: installing the megaflow back to the kernel

After the upcall handler compiles a megaflow, it sends `OVS_FLOW_CMD_NEW` back to the kernel:

| Attribute | Meaning |
|---|---|
| `OVS_FLOW_ATTR_KEY` | Masked flow key (the match fields that need exact comparison) |
| `OVS_FLOW_ATTR_MASK` | Mask for each field (0 means wildcard, all-1 means exact) |
| `OVS_FLOW_ATTR_UFID` | Unique Flow ID (UUID128, OVS 2.7 and later) |
| `OVS_FLOW_ATTR_ACTIONS` | Datapath action list (`output`, `ct`, `set`, `recirc`, and so on) |
| `OVS_FLOW_ATTR_STATS` | Initial stats (N = 0) |

The kernel applies the change atomically through RCU protection: lookup reads do not block install writes.

---

## 9.2.11 Ukey lifecycle and revalidator RCU

§9.2.5 introduced ukeys and revalidators at a high level. This section goes into the ukey state machine and the synchronization guarantee that the revalidator provides.

### 9.2.11.1 State machine of a single ukey

Each ukey (`struct udpif_key` in `ofproto/ofproto-dpif-upcall.c`) goes through the following lifecycle:

```
           create
              ↓
        ┌──────────┐
        │ CREATED  │  (tentative insert, not yet applied to kernel)
        └──────────┘
              ↓  flow installed successfully
        ┌──────────┐
        │  VISIBLE │  (kernel holds the megaflow, ukey tracks counters)
        └──────────┘
              ↓  revalidator sweep sees the ukey
        ┌──────────┐
        │ OPERATIONAL │  (steady state, stats updated each sweep)
        └──────────┘
              ↓  rule change, re-walk pipeline
        ┌──────────┐
        │ CHECKING │  (revalidator verifies the action is still correct)
        └──────────┘
         ↓ same action          ↓ action differs / no match
     (back to OPERATIONAL)    ┌────────────┐
                              │ EVICTING   │  (OVS_FLOW_CMD_DEL)
                              └────────────┘
                                    ↓
                              ┌────────────┐
                              │ DELETED    │  (memory freed after RCU grace)
                              └────────────┘
```

### 9.2.11.2 Revalidator RCU guarantee

The revalidator threads (4 by default, controlled by `n-revalidator-threads`) run periodically (500 ms by default). Each sweep does:

1. **Snapshot phase (RCU read-side):** the thread acquires the RCU read lock and iterates the ukey list.
2. **Per-ukey check:** for each ukey, re-walk the OpenFlow pipeline with the flow key.
3. **Compare action:** compare the new pipeline result with the old action stored in the ukey.
4. **Write phase:** if the action differs, mark the ukey `EVICTING` and send `OVS_FLOW_CMD_DEL` to the kernel.
5. **Grace period:** the ukey is not freed immediately. The thread waits for the RCU grace period (about 25 ms) to make sure no other thread is still reading this ukey.

RCU (Read-Copy-Update) is the Linux primitive that lets reads happen without blocking writes. The revalidator uses RCU so that the dispatcher thread (which handles upcalls) can read ukeys while the revalidator updates them.

### 9.2.11.3 Reading `revalidator/wait` output

**Context.** When debugging a race condition between OpenFlow programming and datapath state, run the `revalidator/wait` barrier to make sure the revalidator has processed the current state.

```shell
$ sudo ovs-appctl dpctl/dump-flows | wc -l
1247
$ sudo ovs-ofctl -O OpenFlow15 del-flows br-int
$ sudo ovs-appctl revalidator/wait
waited <N> ms
$ sudo ovs-appctl dpctl/dump-flows | wc -l
0
```

The `waited <N> ms` output shows how long the call blocked. More than 1000 ms means the revalidator is overloaded.

**Failure scenarios:**

- **`revalidator/wait` blocks for more than 5 seconds.** The revalidator threads have a backlog of ukeys to check. Check `upcall/show` for `dump duration` and `avg-updates` per thread. Raise `n-revalidator-threads`.
- **The `coverage/show facet_revalidate` rate is above 10k per second.** The revalidator is overloaded. This is the signature of the Part 9.26 revalidator storm.

### 9.2.11.4 Ukey count versus rule count

`memory/show udpif keys` shows the ukey count. Compare with `rules`:

```
rules:12847
udpif keys:1247
```

The ukey count is usually less than the rule count because many rules have never matched any packet, so no ukey was created. The ratio `ukey / rule` reflects coverage: a ukey is a flow that is currently active in the kernel.

**Pathology:** when `udpif keys` is greater than `rules`, you have a ukey leak (Part 9.26). Commit `180ab2fd635e` fixed exactly this bug.

---

## 9.2.12 Summary of the three cache tiers and a monitoring cheat sheet

### 9.2.12.1 Summary table

| Tier | Capacity | Lookup cost | Expected hit rate | Key type | Monitoring |
|---|---|---|---|---|---|
| EMC | 8K entries per PMD | About 50 ns | 85 to 95 percent | Exact microflow | `pmd-stats-show emc hits` |
| SMC | 16K entries per PMD | About 150 ns | 5 to 15 percent | Signature hash | `pmd-stats-show smc hits` |
| Megaflow (TSS) | 200K by default | 300 to 500 ns | 1 to 5 percent | Masked wildcard | `pmd-stats-show megaflow hits`, `dpctl/dump-flows` |
| Upcall | Not applicable | 50 to 200 microseconds | Less than 0.1 percent steady | Not applicable | `upcall/show`, `coverage/show upcall_*` |

### 9.2.12.2 Datapath health checklist

```
[ ] EMC hit > 85%                     (pmd-stats-show)
[ ] Total hit (EMC+SMC+megaflow) > 99%
[ ] `miss with success upcall` < 100/sec
[ ] `miss with failed upcall` = 0
[ ] `dump_flows_fallback` = 0         (coverage/show)
[ ] `lost` counter = 0                (ovs-dpctl show)
[ ] udpif keys <= rules               (memory/show)
[ ] dump duration < 500 ms            (upcall/show)
[ ] avg-updates per revalidator < 5   (upcall/show)
[ ] revalidate rate < 1/sec           (coverage/show)
```

A production script runs every 5 minutes, logs the deltas, and alerts when any metric crosses its threshold.

---

First, the megaflow cache is the most important architectural contribution of NSDI 2015. It moves from exact match to wildcard match through lazy wildcarding. The 10 to 100 times compression is what lets OVS scale to a cloud production workload.

Second, Tuple Space Search is the right algorithm for a software datapath. It has a good constant factor and supports dynamic insert and delete. The choice is pragmatic, driven by commodity hardware constraints.

Third, slow path plus fast path is the core mechanism. USC Lab 9 shows it clearly: the first packet goes the slow path, installs the action into the kernel, and subsequent packets go the fast path. Understanding the mechanism helps debug "the first packet is slow but later ones are fast" or "the missed counter is rising".

Fourth, a 99 percent or higher cache hit rate is the operational goal. Below that, you hit cache thrashing, handler bottleneck, and traffic loss risk. Monitor with `ovs-dpctl show`, `ovs-appctl coverage/show`, and `ovs-appctl upcall/show`.

---

## 9.2.13 Additional lab steps (session 27 Phase D expansion)

> This section adds a concrete lab workflow based on Lab 11 by Crichigno (OVS.pdf pages 228 to 243, document dated 09-15-2021). The megaflow and TSS theory was covered in §9.2.1 to §9.2.5 above. This section focuses on practical commands for directly observing the kernel datapath at work.

### 9.2.6.1 Lab 11 topology

The Lab 11 topology is the simplest in Block IX: one OVS bridge `s1` plus two hosts `h1` (10.0.0.1/8) and `h2` (10.0.0.2/8). The goal is to observe how the kernel flow table changes as traffic passes through, without building a complex pipeline.

```
             ┌──────────────┐
    h1 ──────│   s1 (OVS)   │────── h2
 10.0.0.1    │              │     10.0.0.2
             └──────────────┘
```

Load topology:

```bash
sudo mn --switch ovsk --topo single,2
```

### 9.2.6.2 `ovs-dpctl show`, datapath summary

The first command to run when debugging the kernel datapath:

```bash
sudo ovs-dpctl show
```

Expected output (Lab 11 Figure 22, page 13):

```
system@ovs-system:
  lookups: hit:42,324 missed:156 lost:0
  flows: 8
  masks: hit:42,480 total:5 hit/pkt:1.00
  port 0: ovs-system (internal)
  port 1: s1 (internal)
  port 2: s1-eth1
  port 3: s1-eth2
```

Seven readable indicators:

- **`lookups: hit`:** number of packets that match a cache hit (fast path). High is good.
- **`lookups: missed`:** number of packets that miss and are pushed to userspace (slow path). High is bad.
- **`lookups: lost`:** number of packets dropped because the cache is full. Must be 0 in a production environment.
- **`flows`:** number of megaflow entries currently in the cache.
- **`masks: hit`:** total number of mask probes.
- **`masks: total`:** number of masks currently in the cache.
- **`masks: hit/pkt`:** average number of masks each packet probes. Low (1 to 5) is good; high (greater than 40) means the flow table is fragmented.

### 9.2.6.3 `ovs-dpctl dump-flows`, the kernel flow table

```bash
sudo ovs-dpctl dump-flows
```

Expected output (Lab 11 Figure 23, page 14):

```
recirc_id(0),in_port(2),eth(src=00:00:00:00:00:01,dst=00:00:00:00:00:02),
eth_type(0x0800),ipv4(src=10.0.0.1,dst=10.0.0.2,frag=no),
packets:1234, bytes:123456, used:0.123s, actions:3
```

The datapath format is completely different from OpenFlow. Match fields appear in parentheses; there is no `priority`, no `cookie`, no `table`. This is a **megaflow** that `ovs-vswitchd` has compiled down to the kernel for a specific packet.

### 9.2.6.4 POE: refuting "kernel flow equals OpenFlow flow"

**Counter-hypothesis to refute:**

> *"The kernel flow table and the OpenFlow flow table are the same thing. The commands `ovs-dpctl dump-flows` and `ovs-ofctl dump-flows` return the same data."*

### Step 1, compare the output of the two commands

```bash
sudo ovs-ofctl dump-flows s1
```

```
cookie=0x0, duration=60s, table=0, priority=0 actions=NORMAL
```

Only one flow exists: the default `NORMAL` action (the built-in learning switch).

```bash
sudo ovs-dpctl dump-flows
```

Eight entries with full field matches plus wildcarded megaflows. The hypothesis is refuted: the kernel and OpenFlow tables are two completely different tables in count and format.

### Step 2, explanation

OVS has a two-layer architecture:

1. **Userspace (OpenFlow):** the rule table installed by the controller or administrator through `ovs-ofctl`. Usually few entries; each entry covers many different packets through wildcards.
2. **Kernel (megaflow):** the compiled cache of userspace, one entry per megaflow. Each entry is valid for a specific set of packets already seen.

For example, the OpenFlow flow `priority=0 actions=NORMAL` covers every packet. The kernel does not cache that flow. The first packet of each kind misses, is pushed to userspace; userspace processes the `NORMAL` action (MAC learning plus flood or forward), compiles the result down to the kernel as one specific megaflow entry. Similar packets afterwards match the megaflow directly in the kernel without going to userspace.

This is why installing one OpenFlow flow can produce N megaflows in the kernel.

### 9.2.6.5 `ovs-appctl dpif/show`, statistics for capacity planning

The `dpif/show` command shows more detail than `ovs-dpctl show`:

```bash
sudo ovs-appctl dpif/show
```

Documentation-plausible output:

```
system@ovs-system: hit:45234567 missed:234561
  flows: 2345
  port 0: ovs-system (internal)
  port 1: s1 (internal)
  port 2: s1-eth1 (system: ifindex=42)
  port 3: s1-eth2 (system: ifindex=43)
```

`dpif/show-dp-features` lists datapath capabilities:

```bash
sudo ovs-appctl dpif/show-dp-features s1
```

Expected output (Lab 11 Figure 15, page 10):

```
Masked set action: Yes
Tunnel push pop: No
Ufid: Yes
Truncate action: Yes
Clone action: Yes
Sample nesting: 10
Conntrack eventmask: Yes
Conntrack clear: Yes
Max VLAN headers: 2
Max MPLS depth: 3
Recirc: Yes
CT state: Yes
CT zone: Yes
CT mark: Yes
CT label: Yes
CT state NAT: Yes
CT orig tuple: Yes
IPv6 ND Extensions: No
```

Read these features when planning a kernel upgrade. For example, if Conntrack is not supported, the `ct()` action cannot be used (see §9.24).

### 9.2.6.6 `ovs-appctl upcall/show`, capacity planning for handler threads

```bash
sudo ovs-appctl upcall/show
```

Documentation-plausible output:

```
system@ovs-system:
  flows         : (current 2345) (avg 1234) (max 4567) (limit 200000)
  dump duration : 12ms
  ufid enabled : true
  0: (keys 456)
  1: (keys 432)
  2: (keys 123)
  3: (keys 234)
```

Three important fields:

- **`current`:** the current number of megaflows.
- **`limit`:** the megaflow cache limit (default 200,000).
- **`avg` and `max`:** historical statistics.

If `current` stays close to `limit`, there are two options:

```bash
# Increase the limit
sudo ovs-appctl upcall/set-flow-limit 500000

# Or increase the number of handler threads (default equals the number of CPU cores)
sudo ovs-vsctl set Open_vSwitch . other_config:n-handler-threads=8
sudo ovs-vsctl set Open_vSwitch . other_config:n-revalidator-threads=4
```

Handler threads process upcalls (slow path). Revalidator threads scan the cache and remove unused flows. Both affect actual throughput.

### 9.2.6.7 Guided Exercise 14, measure cache hit rate with iperf3

**Goal:** Observe how the cache hit/miss ratio changes as traffic runs. Verify that the cache hit rate reaches 99 percent or higher when the workload is steady.

### Step 1, baseline before traffic

```bash
sudo ovs-dpctl show
# Record hit, missed, lost
```

### Step 2, run iperf3 for 30 seconds

On h2:

```bash
h2$ iperf3 -s
```

On h1:

```bash
h1$ iperf3 -c 10.0.0.2 -t 30
```

### Step 3, observe after the test

```bash
sudo ovs-dpctl show
# Record hit, missed, lost a second time.
# Compute cache hit rate = hit_delta / (hit_delta + missed_delta)
```

Expectation: cache hit rate of 99 percent or higher for iperf3 steady state. The first packet of a TCP session misses (slow path); after that, every packet matches a megaflow.

### Step 4, create a cache-unfriendly workload

Run many short TCP sessions in parallel to create many new flows:

```bash
# Shell script on h1
for i in {1..100}; do
  (curl -s http://10.0.0.2:8000/ -o /dev/null) &
done
wait
```

Measure `ovs-dpctl show` again. The miss rate rises significantly.

### Lesson

The cache hit rate is the most important health indicator of the kernel datapath. Below 95 percent is a warning; below 90 percent is critical. Common causes:

- A workload with too many different flows (high connection churn).
- The cache limit is hit (`upcall/set-flow-limit` too low).
- Flow expiration is too short (low `idle_timeout`).
- Revalidator threads cannot keep up (raise `n-revalidator-threads`).

---

## 9.2.14 OVS datapath in-depth supplement (2026-04 expansion)

> **Purpose:** Add the remaining aspects for four core keywords already substantively covered in §9.2.1 to §9.2.13: `megaflow`, `recirc_id`, `openvswitch.ko` (kernel datapath), and `conntrack execute`. The register keyword group is supplemented in Part 13.18.

### 9.2.14.1 megaflow, additional aspects

**Importance.** `megaflow` is an **absolute tier-1 pillar** of OVS scaling. Justification: before OVS 2.0 (2014), the kernel datapath had only microflow per 5-tuple. Production traffic with 100K-plus flows per second caused a flow explosion (millions of kernel cache entries, O(N) classifier traversal, `ovs-vswitchd` at 100 percent CPU). Megaflow consolidates entries through wildcard masks and reduces entry count by 100 to 1000 times. An engineer who does not understand megaflow cannot debug scaling issues, revalidator storms, or cache-miss spikes. Tier-1 absolute, on par with `ct_state` in importance.

**Classification.** Type: kernel datapath flow entry abstraction. Parent: the `dp_netdev_flow` struct (kernel) or `udpif_key` (revalidator userspace). Scope: per-bridge, persisted in the kernel until idle or hard timeout fires, or until the revalidator evicts. Idempotency: re-installing the same flow has no effect (deduplicated). Type: data-plane caching, not control-plane state.

**Modify and tune procedure:**

```
Step 1: Identify high cache miss rate
  $ ovs-appctl coverage/show | grep -E "miss|upcall"
  → upcall rate > 1000/sec = potential megaflow churn

Step 2: Inspect existing megaflow
  $ ovs-appctl dpctl/dump-flows | head -20
  → Look for flow with very narrow mask (close to microflow)

Step 3: Tune via OF rule design
  → Add a conjunction action to reduce cartesian flow rules
  → Use a generic match (for example, `ip` instead of `ip,nw_src=10.0.0.5`)
    for an ACL that does not need per-IP granularity

Step 4: Force microflow (debug only)
  $ ovs-vsctl set Open_vSwitch . other_config:flow-limit=1000
  → Limit megaflow count to observe wildcard generalization

Step 5: Verify post-tune
  $ ovs-appctl coverage/show | grep upcall
  → upcall rate should drop after wildcard generalize works
```

**Version differences:**

| Version | Behavior |
|---------|----------|
| Pre-OVS 2.0 | Microflow only, flow explosion problem |
| OVS 2.0 (2014) | Megaflow introduced, TSS classifier |
| OVS 2.4 | Subtable mask consolidation |
| OVS 2.6 | SMC (Signature Match Cache) added as a tier between EMC and megaflow |
| OVS 2.9 | Depth-wise lookup optimization |
| OVS 2.15 | SMC tunable through `other_config` |
| OVS 3.0+ | Lookup optimization continues, behavior stable |

Curriculum baseline: OVS v2.17.9. Production may run 3.3 or later; check the version for the SMC tunable.

**Comparison with other systems.**

- **Linux bridge MAC learning:** wildcard concept (1 entry per MAC, not per flow). Difference: bridge is L2 only, no L3/L4 mask. OVS megaflow is flexible.
- **Cisco TCAM ACL aggregation:** TCAM cells aggregate similar patterns. Difference: hardware-level (fast, fixed); OVS megaflow is software (slower, flexible).
- **Intel DPDK rte_flow API:** modern hardware offload uses similar wildcards. OVS hardware offload (see Part 9.5) combines megaflow with rte_flow.

### 9.2.14.2 recirc_id, the recirculation identifier for multi-pass pipelines

> **Scope:** In-depth treatment (2026-04-26 expansion) for the keyword `recirc_id`. Related sections: §9.2.10.2 (megaflow rows have `recirc_id(0)` in `dump-flows`), §9.22 multi-table pipeline (compared with `goto_table` and `resubmit`), §9.24 conntrack (the `ct()` action triggers recirc), §9.32 classifier xlate.

**Concept.** `recirc_id` is a 32-bit metadata field in OVS that marks the recirculation *round* a packet is going through inside the datapath. Every packet just received from a port (before any `ct()`, `recirc()`, or translation breakpoint) has `recirc_id = 0`. When xlate hits a situation where it cannot statically compose the action set in a single lookup (for example, it needs the conntrack result before deciding the next OF table), userspace `ofproto-dpif-xlate` assigns a new `recirc_id` to the packet (a non-zero uint32 allocated by `recirc_alloc_id()`), freezes the frozen state, and the datapath re-enters the classifier with the key updated to `recirc_id = <new id>`. Type: a pseudo-field metadata in the kernel datapath, an NXM extension match field (NXM-only; no OXM equivalent), and the key to every multi-pass behaviour in OVS.

**History and background.** Before OVS 2.2 (2014), the kernel datapath had no recirculation mechanism; every pipeline had to be linearised into a single action chain. When conntrack integration was planned, the problem appeared immediately: the result of `ct()` (NEW versus ESTABLISHED, NAT mapping) is only known *after* the packet has gone through the netfilter hook, but OF rules need to *match on that result* to decide forward or drop. `NXM_NX_RECIRC_ID(36)` was introduced in OVS 2.2 (source pool in `ofproto/ofproto-dpif-rid.c`, copyright Nicira 2014 to 2017) as a generic mechanism to bookmark the translation state and resume from a different OF table after the packet has been reprocessed. OVS 2.5 (2016) officially brought `ct()` into the kernel datapath, making `recirc_id` the foundation of the entire stateful-firewall pipeline. OVS 2.6 and later extended this to tunnel decapsulation, MPLS push and pop, and the `dec_ttl` exception path.

**Position in the architecture.** `recirc_id` sits at the intersection of three layers: (1) the classifier keys megaflows by `recirc_id` together with `in_port` and header fields; (2) userspace `ofproto-dpif-xlate` translates OF flow; on encountering an action that cannot be linearised, it calls `recirc_alloc_id()` and produces a frozen state; (3) the datapath (kernel `net/openvswitch/actions.c` or userspace `lib/dpif-netdev.c`) executes the `OVS_ACTION_ATTR_RECIRC` action by setting `key->recirc_id = N` and re-entering `ovs_execute_actions()` recursively. The megaflow in `ovs-dpctl dump-flows` shows `recirc_id(0x<hex>)` at the start of the match list because it is the first dimension on which the TSS classifier branches.

**Role.** Four core roles: (a) **state continuation token**, the frozen translation state is stored in the `ofproto-dpif-rid` pool so that translation can resume after the packet returns to the classifier; (b) **classifier partition**, each different `recirc_id` creates an independent logical sub-table, so a first-pass packet (`recirc_id=0`) never matches flows for a later pass, preventing cross-contamination; (c) **action-chain breakpoint**, when an action set cannot be statically composed (because it depends on a ct or decap result), recirc creates a new join point; (d) **megaflow caching key**, `recirc_id` is a field in the cached key, so the same flow tuple at pass-0 and pass-1 produces two independent megaflow entries with no aliasing.

**Why it exists.** The design motivation is to solve the problem of stateful matches that depend on runtime state, which the original OpenFlow model could not solve. Standard OF allows `goto_table` to linearise forward, but it has no concept of returning to the classifier after the packet has been modified by a result outside the flow table (for example, the nf_conntrack result, or a tunnel decap that changes the inner header). Nicira found that having the controller manage multi-pass through barrier messages plus multiple flow_mods would break the fast path with latency and state divergence. `recirc_id` is the dataplane-internal solution: the controller installs static OF rules once, and the datapath automatically loops through the classifier with different metadata per pass.

**Problems it solves.** Specifically, `recirc_id` solves four classes of problem: (1) **Stateful firewall**, knowing whether a packet belongs to an ESTABLISHED or NEW connection is only possible after a conntrack lookup, which is a costly action with side effects, so a recirc is required to match on `ct_state` in the next pass. (2) **Tunnel decapsulation**, when a VXLAN or Geneve packet is decapsulated, the inner header becomes a new packet; recirc lets the classifier reclassify on the inner header instead of the outer. (3) **MPLS and NSH push and pop**, label-stack operations change packet structure; recirc creates a classifier checkpoint to handle the modified portion. (4) **`dp_hash` and `select_via_hash` group bucket**, the kernel must compute the hash before choosing a bucket; recirc combined with `recirc_update_dp_hash` defers bucket selection to the next pass.

**Importance.** This is an absolute core concept of OVS internals. An engineer who does not understand `recirc_id` cannot debug: (a) any pipeline that uses `ct()` (which is 100 percent of production OVS with security groups); (b) any OVN pipeline (OVN logical pipelines typically generate 5 to 15 recirc passes per packet through `lr_in_arp_resolve`, `ls_in_acl`, `ls_in_pre_acl`, `ls_in_pre_lb`, `ls_in_lb_aff_check`, and so on); (c) megaflow-explosion debugging, because one of the most common anti-patterns is an OF rule that produces combinatorial recirc and grows entries exponentially. The level of importance is on par with `ct_state`, `megaflow`, and `OFPACT_OUTPUT` in the OVS core group.

**How it works.** Per-packet sequence when the pipeline hits an action that requires recirc (for example, `ct(zone=5)` at table 0):

1. Userspace xlate receives the packet and translates flow table 0. It encounters `OFPACT_CT`.
2. `ofproto-dpif-xlate.c` calls `recirc_alloc_id_ctx(frozen_state)` to allocate `recirc_id = 0xabcd1234` (a non-zero uint32). `frozen_state` holds the metadata needed for the next pass: the ofproto pointer, the table_id to resume at, register values, and stack values.
3. The state is stored in a hash table in `ofproto/ofproto-dpif-rid.c` keyed by `recirc_id`.
4. xlate emits the datapath action sequence: `ct(zone=5,commit), set(recirc_id=0xabcd1234), recirc()` (in concrete form, the odp action format `OVS_ACTION_ATTR_RECIRC`).
5. The datapath (kernel `net/openvswitch/actions.c` or userspace `lib/dpif-netdev.c`) executes: it runs the conntrack hook, sets `key->recirc_id = 0xabcd1234`, then enqueues the packet on a deferred FIFO (kernel) or recursively calls `dp_netdev_recirculate()` (userspace).
6. New pass: the classifier looks up again with the key carrying `recirc_id=0xabcd1234`. If a cached megaflow matches, it is fast path. If miss, upcall; userspace looks up `frozen_state` through `recirc_find_id()` and resumes xlate from the saved `table_id`. By now `ct_state` carries the real value.
7. The new pass continues to output or to another recirc. Each pass has its own megaflow; the kernel distinguishes them through the `recirc_id` field in the key.

Important note: `recirc_id` is not a counter that increments per pass for that packet; it is a *handle* to a specific frozen state. Two packets going through the same `ct()` action can receive the same `recirc_id` if the frozen state is identical (hash dedup), which conserves pool slots.

**How an operator masters this.** A proficient engineer with `recirc_id` should have five reflexes: (1) Read `ovs-dpctl dump-flows` and immediately recognise `recirc_id(0)` (first pass) versus `recirc_id(0x<hex>)` (later pass), not confuse it with a register match. (2) Run `ofproto/trace` with the `--no-megaflow` option to see the full recirc path, including the line "thawed from frozen state at table N". (3) Count the number of passes per packet in production through `ovs-appctl coverage/show | grep -i recirc` and understand the warning threshold (rule of thumb: an average above 6 recirc per packet means the OF rule pipeline is not yet optimised). (4) Read `ovs-appctl dpif/show -s` for the cumulative counter "packet recirculations: N", combine with packet count to compute the ratio. (5) When writing a new OF rule, predict the recirc cost before commit: each `ct()`, each `dec_ttl()` exception, each `pop_mpls`, and each tunnel `decap` produces at least one recirc.

**Classification.** `recirc_id` can be classified by the source that produces it: (a) **conntrack-induced**, the most common, produced by `ct()`, `ct(commit)`, `ct(nat)`, or `ct_clear`; (b) **decap-induced**, by `decap()` for Geneve, VXLAN, STT, NSH, by `pop_mpls`, or `pop_vlan` (in some cases); (c) **hash-induced**, the `dp_hash` and `hash_l4` actions for a `select_via_hash` group; (d) **clone or sample induced**, the `clone()` and `sample()` actions split the flow so the main branch and the sample branch continue independently; (e) **OVN-pipeline-induced**, not a new mechanism but the pattern in which OVN logical flows generate many recircs through the `ct_*` and `lr_*` stages. By production prevalence: group (a) > (e) > (b) > (c) > (d).

**Usage workflow.** A typical workflow when an engineer must debug a pipeline that uses recirc:

```
Step 1: Identify the desired packet path (paper diagram first, do not guess).
        - List recirc-producing actions: ct, decap, pop_mpls, dp_hash.
        - Count the expected number of passes.

Step 2: Run a trace with concrete input.
        $ ovs-appctl ofproto/trace br-int \
          'in_port=1,tcp,nw_src=10.0.0.5,nw_dst=10.0.0.10,tp_dst=80'
        - Read each "Final flow" line after every recirc.
        - Compare actual against the Step 1 expectation.

Step 3: Observe the corresponding megaflow that was installed.
        $ ovs-dpctl dump-flows | grep "recirc_id"
        - One flow per pass; verify recirc_id increments correctly.

Step 4: Measure the cost.
        $ ovs-appctl coverage/show | grep -i recirc
        $ ovs-appctl dpif/show -s | grep "packet recirculations"
        - A recirc-per-packet ratio below 6 is good; above 10 needs refactor.

Step 5: If the cost is high, find which OF rule produces the most recirc.
        $ ovs-ofctl dump-flows br-int | grep -E "ct\(|decap|dp_hash"
        - Consider merging ct(commit) with ct() lookup where possible.
```

**When something goes wrong (rewritten in English by the 2026-04-28 source-citation cleanup).** `recirc_id` is a keyword to keep in mind in the following situations: (a) the OVS upcall rate spikes for no obvious reason; check first whether a new rule generates recirc. (b) The megaflow count grows non-linearly with new traffic; recirc combinations are the number-one suspect. (c) Packets drop after a `ct()` rule is installed; the frozen state pool may be full, or the rule may generate a recirc loop. (d) p99 latency jumps without an increase in bandwidth; each recirc adds another classifier traversal, and if the pass misses the megaflow it adds upcall latency. (e) A kernel oops with a stack trace containing recursive `do_execute_actions`: the recursion limit `OVS_RECURSION_LIMIT=4` (verified in `datapath/actions.c` of the OVS out-of-tree at v2.17.9) can be hit when a rule produces a recirc loop with no exit condition.

**Tightly related to.** `recirc_id` is intertwined with: (a) `ct_state`, `ct_zone`, `ct_mark`, `ct_label`, all of which carry their real values only after a pass-0 recirc through `ct()` (see §9.24). (b) `frozen_state`, the structure stored in the `ofproto-dpif-rid` pool (see §9.32 classifier xlate). (c) `goto_table` and `resubmit`, which are FUNDAMENTALLY DIFFERENT from recirc: those two stay in *the same pass* (linearised in xlate); recirc is a *new pass* with the key updated (see §9.22). (d) `megaflow`, `recirc_id` is the first field in the key (see §9.2.2). (e) `dp_hash` and `select_via_hash`, group-bucket selection requires recirc (see §9.18, §9.22). (f) OVN logical flows, each stage in the logical pipeline is usually equivalent to one recirc (see §13.7, §13.8).

**Version differences.**

| OVS version | Behaviour of `recirc_id` |
|-------------|--------------------------|
| < 2.2 | Not present; the pipeline had to be fully linearised |
| 2.2 (2014) | Introduced `NXM_NX_RECIRC_ID(36)`; the pool mechanism in `ofproto-dpif-rid.c` |
| 2.4 (2015) | Pool dedup improved; `recirc_alloc_id_ctx` hashes the frozen state |
| 2.5 (2016) | `ct()` action production-ready; recirc became mandatory for stateful |
| 2.6 (2016) | Extended to tunnel decap and MPLS push and pop |
| 2.8 (2017) | NSH supports recirc after decap |
| 2.10 (2018) | `recirc_update_dp_hash` for `select_via_hash` group |
| 2.13 (2020) | Pool eviction optimised; `recirc_id` no longer leaks when an ofproto is removed |
| 2.17.9 (curriculum baseline) | Stable behaviour; `OVS_RECURSION_LIMIT=4`, `MAX_DEPTH=64` |
| 3.x | Public semantics unchanged; only internal pool refinements |

Note: `recirc_id` has never been standardised into the official OpenFlow OXM. In `include/openvswitch/meta-flow.h` the comment is explicit: `OXM: none`, `Access: read-only`, `Internal use only, not programmable from controller`, meaning the controller cannot set it directly; only the kernel datapath allocates it.

**Observation and verification.** Five ways to verify that `recirc_id` is at work:

```
(1) A megaflow row contains recirc_id:
$ ovs-dpctl dump-flows | head -3
recirc_id(0),in_port(2),eth(...),eth_type(0x0800),ipv4(...),tcp(...), packets:N
recirc_id(0xa1b2c3d4),in_port(2),ct_state(+est+trk),...           packets:M

(2) Counter cumulative:
$ ovs-appctl dpif/show -s
  flows: N
  packet recirculations: 12543

(3) Coverage:
$ ovs-appctl coverage/show | grep -iE "recirc|frozen"
  xlate_actions_too_many_output                0.0/sec ...
  packet_recirculations                        87.4/sec ...

(4) ofproto/trace shows the thawed line:
$ ovs-appctl ofproto/trace br-int 'in_port=1,tcp,...'
...
Final flow: recirc_id=0,...
Megaflow: recirc_id=0,in_port=1,...,actions:ct(zone=5),recirc(0xa1b2c3d4)
---- thawing from frozen state at table 1 ----
Final flow: recirc_id=0xa1b2c3d4,ct_state=0x21,...
Megaflow: recirc_id=0xa1b2c3d4,...,actions:output:3

(5) Pool inspection (debug build):
$ ovs-appctl dpif-netdev/pmd-perf-show
(does not show recirc_id directly but shows the "Recirculations" stat)
```

**Source code reference.** Per Rule 14, use the function-name anchor (line numbers drift across versions). Baseline OVS v2.17.9:

- `include/openvswitch/meta-flow.h` defines `MFF_RECIRC_ID`. The comment block records `NXM: NXM_NX_RECIRC_ID(36) since v2.2`, `OXM: none`, `Access: read-only`, `Internal use only, not programmable from controller`.
- `ofproto/ofproto-dpif-rid.h` exposes the public API: `recirc_alloc_id()`, `recirc_alloc_id_ctx(struct frozen_state *)`, `recirc_find_id()`, `recirc_free_id()`. The file header is copyright Nicira 2014 to 2017.
- `ofproto/ofproto-dpif-rid.c` is the pool hash-table implementation. The internal doc reads: "A recirculation id can be any uint32_t value except 0, which is reserved for the no-recirculation case".
- `ofproto/ofproto-dpif-xlate.c`, the `xlate_ctx` struct holds the recirc-related fields: `freezing` (bool), `recirc_update_dp_hash` (bool), `dp_hash_alg`, `dp_hash_basis`, `frozen_actions` (struct ofpbuf). The function `ctx_trigger_recirculate_with_hash()` triggers recirculation; line numbers are dropped per Rule 14 §14.4 Option C because they drift across releases. The constant `MAX_DEPTH = 64` caps recursion during translation.
- `lib/odp-execute.c`, the userspace datapath executor for `OVS_ACTION_ATTR_RECIRC`.
- `lib/dpif-netdev.c`, the function `dp_netdev_recirculate()` re-runs the classifier per packet inside the userspace datapath.
- Linux kernel `net/openvswitch/actions.c` (the OVS out-of-tree datapath; the in-tree mainline kernel path mirrors this code), the `OVS_ACTION_ATTR_RECIRC` handler inside `do_execute_actions()`. The constant `OVS_RECURSION_LIMIT = 4` (verified at the OVS out-of-tree `datapath/actions.c` v2.17.9; mainline kernel value may differ, check the running kernel) and `DEFERRED_ACTION_FIFO_SIZE = 10` cap the recirc depth and the deferred-action FIFO buffer.
- Linux kernel `net/openvswitch/flow.h`, the struct `sw_flow_key` carries the `recirc_id` field at the start of the key.

> **Note on the v3.9.2 hotfix.** An earlier version of this section cited `OVS_RECURSION_LIMIT = 5` and an inline line number "approx. line 766" for `ctx_trigger_recirculate_with_hash`. Verified at the OVS v2.17.9 baseline (commit `0bea06d9957e3966d94c48873cd9afefba1c2677`, 2024-02-08): the real value of `OVS_RECURSION_LIMIT` in `datapath/actions.c:77` is `4`, and the function is at `ofproto/ofproto-dpif-xlate.c:474`, not 766. Plan v3.9.2 R2 corrected the value and dropped the inline line number per Rule 14 §14.4 Option C. See `memory/sdn/v3.9.2-q7-axis16-sweep-2026-04-28.md`.

**Real-world incident.** Based on the revalidator-storm forensic pattern that is common in production OpenStack and OVN environments (see §9.26 for the detailed anatomy template): a Neutron deployment with security groups using `ct()` for stateful firewall; after the tenant count grew from 200 to 1000, `ovs-vswitchd` CPU rose to 100 percent and the packet-drop rate climbed. Symptoms: `ovs-appctl dpif/show -s` showed `packet recirculations` jumping from 50K/sec to 800K/sec; `ovs-dpctl dump-flows | wc -l` showed the megaflow count growing from about 5K to about 80K. The root cause was usually the OF rules generated by the Neutron OVS agent that use the pattern `ct(commit,zone=N)` with a different zone per port, combined with a recirc at the pipeline head for ARP resolve; the combination `(zone × in_port × ct_state)` exploded the megaflow count. Mitigation: consolidate zones to per-tenant instead of per-port, and reduce the number of recirc points from 4 to 2 in the pipeline. Lesson: each added recirc point multiplies the megaflow count by (the number of metadata values matched in the next pass), it does not just add them.

**Synthetic exercise.**

```
Lab R1, "Measure the recirc cost of one ct() rule" (about 30 minutes):

Topology: ovs br0, two netns h1 (10.0.0.1) and h2 (10.0.0.2), veth pair.
Baseline rule: action=normal (no recirc).
Test rule: table=0,priority=100,ip,actions=ct(zone=1),resubmit(,1)
           table=1,priority=100,ip,ct_state=+trk+new,actions=ct(commit,zone=1),normal
           table=1,priority=100,ip,ct_state=+trk+est,actions=normal

Step 1: Install the baseline rule. Run iperf3 for 30 seconds from h1 to h2.
        $ ovs-appctl dpif/show -s | grep recirculation
        Record the recirc count.

Step 2: Install the test rule. Run iperf3 for 30 seconds again.
        $ ovs-appctl dpif/show -s | grep recirculation
        Record the new recirc count.

Step 3: Compute the delta. Each packet adds 1 recirc (through ct).
        Verify: ovs-dpctl dump-flows | grep recirc_id
        At least two lines must appear: recirc_id(0) and recirc_id(0x<hex>).

Step 4: Predict, Observe, Explain.
        Predict: the recirc count equals the number of packets through the bridge.
        Observe: in practice it can be lower because EMC/SMC hits do not
                 trigger an upcall, but the recirculation counter still
                 increments because the pass really happens.
        Explain: recirc is a datapath-level event, not an xlate event;
                 the megaflow cache does not bypass recirc.

Pass criteria: the learner explains why recirc count equals packet count
               but the upcall count is less than the recirc count.
```

**Common failures and diagnostic signals.**

- *Symptom* The megaflow count spikes after committing a new OF rule. *Cause* Recirc combinations with different metadata per port or per zone. *Diagnosis* `ovs-dpctl dump-flows | awk '{print $1}' | sort | uniq -c | sort -rn`, group by `recirc_id` to see which pass has the most entries. *Fix* Consolidate zones or split rules to reduce the metadata dimensions in that pass.
- *Symptom* Kernel log `nf_conntrack: table full, dropping packet` together with an OVS upcall storm. *Cause* Each recirc through `ct()` triggers a conntrack lookup; if the pipeline has 2 to 3 recirc passes through ct, the nf_conntrack table is under 2 to 3 times the expected pressure. *Diagnosis* `cat /proc/sys/net/netfilter/nf_conntrack_count` compared with `nf_conntrack_max`. *Fix* Raise `nf_conntrack_max` or remove redundant recirc through `ct()`.
- *Symptom* `ofproto/trace` reports `recursion too deep`, or a kernel oops `OVS_RECURSION_LIMIT exceeded`. *Cause* An OF rule produces a recirc loop without an exit condition (pass N recircs back to a table from an earlier pass N-1). *Diagnosis* Trace each pass and find a rule whose recirc action targets a table earlier than the current one. *Fix* Add a match condition so the later pass does not re-trigger the same recirc point.
- *Symptom* The frozen-state pool grows unbounded; `ovs-vswitchd` leaks memory. *Cause* An old bug before OVS 2.13 where the `recirc_id` was not freed when an ofproto was removed. *Diagnosis* Compare `ovs-appctl memory/show` before and after `ovs-vsctl del-br`. *Fix* Upgrade OVS to 2.13 or later.
- *Symptom* p99 latency spikes after conntrack ACL is enabled. *Cause* Each packet adds 1 to 2 recirc passes; if the cache misses, 1 to 2 upcall latencies are added. *Diagnosis* Compare `ovs-appctl coverage/show | grep upcall` before and after enabling. *Fix* Pre-warm the cache or reduce the number of recirc points.

**Comparison with other systems.** Other systems solve the multi-pass packet-processing problem differently:

- **Linux netfilter NF_HOOK** chains (PREROUTING, FORWARD, POSTROUTING): each hook is a "pass" in the pipeline sense, but the metadata is not persisted explicitly like `recirc_id`. The fundamental difference: the netfilter pipeline is statically defined by the kernel; OVS recirc is dynamic and defined by OF rules.
- **Cisco IOS-XR feature pipeline** on ASIC (egress recirculation for VXLAN encap): equivalent in principle but implemented in hardware. `recirc_id` in OVS is close to the notion of "loopback FIFO" on Trident or Tomahawk chips.
- **P4 architecture parser-deparser-parser** (re-parse after a header rewrite): differs in that P4 requires the programmer to declare recirc explicitly in P4 code; OVS recirc is implicit and generated by xlate.
- **VPP graph-node re-enqueue** (FD.io): equivalent. Each graph node is a stage; the packet is re-enqueued into the graph with updated metadata. `recirc_id` is roughly equivalent to the VPP `vnet_buffer` opaque field.
- **Pure OpenFlow `goto_table`** (no recirc): only linearises within the same xlate pass; it does not change the packet key. This is the most common point of confusion with recirc, because `goto_table` is cheap whereas recirc is expensive.

### 9.2.14.3 openvswitch.ko, the kernel module that underlies the entire kernel datapath

> **Scope.** Deep treatment (2026-04-26 expansion) of the keyword `openvswitch.ko`. Related sections: §9.1 (the three OVS components and the `dpif-netlink` userspace counterpart), §9.2.4 (upcall slow and fast path), §9.2.10 (Netlink wire format), §9.2.14.2 (`recirc_id`, one of the action attributes the module executes), §9.24 (the conntrack hook in `actions.c`), §11.0 (tunnel modules on top of `openvswitch.ko`).

**Concept.** `openvswitch.ko` is a loadable Linux kernel module (`.ko` stands for "kernel object") that contains the entire kernel side of OVS: the kernel-side megaflow flow table, the TSS classifier, the action executor, the conntrack glue, and the Netlink generic genl families (`ovs_datapath`, `ovs_vport`, `ovs_flow`, `ovs_packet`, `ovs_meter`, `ovs_ct_limit`) that bridge to the userspace `ovs-vswitchd`. When we say "the OVS kernel datapath", we specifically mean this module; the entire fast-path forwarding logic (about 1 to 5 microseconds per packet) runs inside it. Type: a loadable Linux kernel module in the `net/` subsystem, with source tree `net/openvswitch/`, built with `CONFIG_OPENVSWITCH=m`, GPL licensed (`MODULE_LICENSE("GPL")` in `datapath.c`). Without this module, the command `ovs-vsctl add-br br0` still runs, but the datapath becomes userspace (`netdev`) or `dummy`, not kernel; production stability is not achievable.

**History and background.** Nicira wrote the module starting in 2009 under the name `openvswitch_mod.ko` together with userspace; for the first two years it existed only out-of-tree (`openvswitch.git/datapath/`), and distributions had to DKMS build it for each kernel. After multiple review rounds on `linux-netdev`, Jesse Gross sent the patch series mainline in November 2011, and Linus merged it into Linux **3.3** (released 18 March 2012). From then on it lived in-tree under `net/openvswitch/`, and the module name was shortened to `openvswitch.ko`. Userspace continued to ship a `datapath/` copy for out-of-tree builds (necessary for distribution kernels lacking newer features such as NSH, MPLS, or conntrack zone limit), but the production best practice from 2018 onward is to use the in-tree version. Each kernel release added new features: 3.10 (2013) added the `ovs_vport_ops` framework; 4.3 (2015) added conntrack glue (`OVS_ACTION_ATTR_CT`); 4.6 (2016) added the conjunction action; 4.13 (2017) added hardware-offload TC integration; 5.6 (2020) added `OVS_ACTION_ATTR_CHECK_PKT_LEN`; 5.15 (2021, the curriculum baseline) stabilised the entire action set. The out-of-tree backport stayed bundled with the OVS userspace tarball through OVS 2.17; OVS 3.0 (2022) dropped the kernel-module backport and supports only the in-tree version.

**Position in the architecture.** The module lives in the `net/` layer of the Linux kernel, beneath `netfilter` in hook order and parallel to `bridge`. Layered structure: (1) the bottom layer is the `vport` abstraction (`net/openvswitch/vport.c`) that binds the module into a Linux `net_device` through `netdev_rx_handler_register()` to intercept packets; (2) the middle layer is the flow table plus classifier (`flow_table.c`, `flow.c`) that looks up megaflows by `sw_flow_key`; (3) the action-executor layer (`actions.c`) executes the datapath action set; (4) the Netlink layer (`datapath.c`) registers four genl families with userspace to receive `OVS_DP_CMD_*`, `OVS_VPORT_CMD_*`, and `OVS_FLOW_CMD_*` commands, and to send upcalls through `OVS_PACKET_CMD_MISS`. The userspace counterpart is `ovs-vswitchd` through `lib/dpif-netlink.c` (Part 9.1). The module sits above the tunnel modules (`vport-vxlan.ko`, `vport-geneve.ko`, `vport-gre.ko`); the tunnel modules register their `vport_ops` with `openvswitch.ko`. Both `nf_conntrack` and `openvswitch.ko` share the same conntrack table (Part 9.24).

**Role.** Four core roles in the OVS architecture: (a) **fast-path executor**, every packet through an OVS bridge passes the module hook at `netdev_rx_handler_register()`, looks up the flow key, executes the action set, and never returns to the Linux IP stack unless the action is `output:LOCAL` or `normal`; (b) **Netlink server**, declares the genl families `ovs_datapath`, `ovs_vport`, `ovs_flow`, `ovs_packet`, `ovs_meter`, and `ovs_ct_limit` (see the `MODULE_ALIAS_GENL_FAMILY` declarations in `datapath.c`) so that `ovs-vswitchd` can install flows, query statistics, send packets, and receive upcalls; (c) **flow-cache custodian**, owns the `dp_netdev_flow` struct and the RCU-protected flow table, ensuring that lookups do not block installs through the RCU read-side critical section; (d) **conntrack-hook integrator**, calls `nf_conntrack_in()` (`net/openvswitch/conntrack.c`) for the `ct()` action, integrating the Linux netfilter conntrack into the OF pipeline and preserving atomicity between `ct(commit)` and the stateful match.

**Why it exists.** Userspace-only switching (Open vSwitch v0.x in early 2009) was limited to about 500K packets per second because each packet required a kernel-userspace-kernel context switch for the forwarding decision, at about 2 microseconds per switch. With a KVM cloud (50 or more VMs per host, thousands of microflows per VM), real throughput could not exceed 1 Gbps. Nicira chose a "split datapath" approach: the kernel caches the already-computed decision, while userspace only computes on a cache miss. To keep the cache in the kernel without patching the entire kernel core (which Linux maintainers would not accept), the only option was a loadable module. The module had to be generic enough to handle the OF action set (output, set, push_vlan, ct, recirc, and so on) yet small enough for Linus to merge mainline. Jesse Gross's 2011 to 2012 patch series succeeded because the module stood alone (no patches to the L2 bridge or netfilter), the feature was isolated, and the source was about 5,000 lines (compared with about 50,000 lines for the userspace `ovs-vswitchd`).

**Problems it solves.** Five foundational problems: (1) **context-switch overhead**, every packet had to pay about 2 microseconds for a kernel-userspace round trip, capping real throughput; an in-kernel cache eliminates that cost for about 99 percent of packets (cache hit). (2) **classifier complexity in kernel**, an OF pipeline with prioritised rules and arbitrary header modification is too complex to write as kernel code; megaflow plus TSS keep the classifier in userspace, leaving the kernel to do only a hash lookup. (3) **multi-bridge and multi-tenant scaling**, a host needs dozens of bridges, thousands of ports, and millions of flows; the module uses RCU plus a per-bridge `struct datapath` to isolate tenants. (4) **conntrack integration**, stateful firewalling requires sharing the conntrack table with Linux netfilter; the module calls `nf_conntrack_in()` directly instead of duplicating state. (5) **tunnel offload**, VXLAN, Geneve, and GRE encap and decap need to run at line rate, which requires kernel-level processing; the module provides the `vport_ops` framework for tunnel modules to plug into.

**Importance.** This is an absolute core concept, on par with `megaflow` and `OVS_ACTION_ATTR_OUTPUT`. An engineer who does not master `openvswitch.ko` cannot debug: (a) any failure that happens before the packet reaches `ovs-vswitchd` (kernel-only path); (b) module load and unload race conditions during an OVS or kernel upgrade; (c) conntrack helper module dependencies (`nf_nat`, `nf_conntrack_ftp`, and so on); (d) the hardware-offload chain (`tc-flower` pinned to this module); (e) module parameter tuning (`flow_lookup` debug, `vport-internal_dev` MTU). Production OpenStack and OVN deployments use the kernel datapath by default, and every `neutron-ovs-agent` crash log includes an `openvswitch.ko` frame in the stack trace. The level of importance is on par with `recirc_id` and `ofproto-dpif xlate` in the core group.

**How it works.** Module life cycle plus per-packet flow:

1. **Module load** (`modprobe openvswitch`): the kernel calls `dp_init()` in `datapath.c`. Sequence: `action_fifos_init()`, then `ovs_internal_dev_rtnl_link_register()` (registers Netlink RTNL ops for the internal port), then `ovs_flow_init()` (allocates the slab cache for `sw_flow`), then `ovs_vport_init()` (initialises the vport table), then `register_pernet_device(&ovs_net_ops)` (per-network-namespace state), then `register_netdevice_notifier(&ovs_dp_device_notifier)` (watches netdev changes), then `ovs_netdev_init()`, then `dp_register_genl()` (registers four genl families with generic netlink). The kernel log `pr_info("Open vSwitch switching datapath\n")` appears in `dmesg`.
2. **`ovs-vswitchd` connect**: userspace opens a Netlink socket on family `NETLINK_GENERIC`, resolves the family ID for `ovs_datapath`, and sends `OVS_DP_CMD_NEW` with attribute `OVS_DP_ATTR_NAME="ovs-system"`. The kernel function `ovs_dp_cmd_new()` allocates a `struct datapath`, creates the internal port `ovs-system`, and replies with `OVS_DP_CMD_NEW`.
3. **Bridge create**: for each OF bridge (`br-int`, `br-ex`), userspace sends a separate `OVS_DP_CMD_NEW`, and the kernel allocates a separate `struct datapath`. Userspace then sends `OVS_VPORT_CMD_NEW` to add ports (of type `OVS_VPORT_TYPE_NETDEV`, `_INTERNAL`, `_VXLAN`, and so on). The kernel calls `netdev_rx_handler_register()` to intercept packets coming into that netdev.
4. **Per-packet fast path**: NIC RX, then the driver's `napi_gro_receive()`, then Linux's `__netif_receive_skb_core()`, then the kernel checks the `rx_handler` bit and calls `netdev_frame_hook()`, then `ovs_vport_receive()`, then `ovs_dp_process_packet()`, then `ovs_flow_key_extract()` extracts the `sw_flow_key` (`recirc_id`, `in_port`, eth, ip, tp, `ct_state`, and so on), then `ovs_flow_tbl_lookup_stats()` performs the TSS lookup, then on a match `ovs_execute_actions()` (in `actions.c`) iterates the action set and executes each `OVS_ACTION_ATTR_*` (output through `do_output()`, set through `execute_set_action()`, ct through `ovs_ct_execute()`, recirc through `ovs_recirc()`). The whole chain is about 1 to 5 microseconds.
5. **Per-packet slow path (cache miss).** `ovs_dp_process_packet()` does not find a flow, so `ovs_dp_upcall()` builds the Netlink message `OVS_PACKET_CMD_MISS` and sends it through the socket registered by the handler thread. The `do_execute_actions()` recursion limit `OVS_RECURSION_LIMIT = 4` (in `datapath/actions.c` of the OVS out-of-tree at v2.17.9) prevents infinite loops; the deferred FIFO `DEFERRED_ACTION_FIFO_SIZE = 10` buffers long action chains.
6. **Module unload** (`rmmod openvswitch`): the kernel calls `dp_cleanup()` (registered through `module_exit(dp_cleanup)` in `datapath.c`) in the reverse sequence: `dp_unregister_genl()`, then `ovs_netdev_exit()`, then `unregister_netdevice_notifier()`, then `unregister_pernet_device()`, then `ovs_vport_exit()`, then `ovs_flow_exit()`, then `ovs_internal_dev_rtnl_link_unregister()`, then `action_fifos_exit()`. If a datapath is still active and `refcount` is greater than 0, unload fails with `Module openvswitch is in use`.

**How an operator masters this.** An `openvswitch.ko`-fluent engineer needs five reflex skills: (1) verify the module is loaded with `lsmod | grep ^openvswitch`, immediately seeing `openvswitch` along with the dependent modules `nf_conncount`, `nf_nat`, and `nf_conntrack` in the Used-by column. (2) Read `modinfo openvswitch` to verify version, vermagic, license, dependencies, and parameters. (3) Observe the kernel log at module load with `dmesg | grep -i openvswitch`, expecting the line `Open vSwitch switching datapath` and no warnings. (4) When upgrading the kernel or OVS, follow the right order: stop the `openvswitch-switch` service, then `rmmod` the vport modules, then `rmmod openvswitch`, upgrade, then `modprobe openvswitch`, then start the service. (5) When a module bug is suspected, enable debugging: `echo 8 > /proc/sys/kernel/printk` together with `modprobe openvswitch dyndbg=+p` for dynamic debug logging of each function.

**Classification.** The related artifacts and operations can be grouped as: (a) **module artifact**, the `.ko` file at `/lib/modules/$(uname -r)/kernel/net/openvswitch/openvswitch.ko`; (b) **dependency modules**, `nf_conntrack`, `nf_nat`, `nf_defrag_ipv4`, `nf_defrag_ipv6`, `libcrc32c`, `vport-vxlan`, `vport-geneve`, `vport-gre`, and `vport-stt` (out-of-tree); (c) **per-net state**, `struct ovs_net` per network namespace; (d) **per-bridge state**, `struct datapath`; (e) **per-port state**, `struct vport`; (f) **per-flow state**, `struct sw_flow` plus `struct sw_flow_actions` (RCU-protected); (g) **Netlink family**, six families registered with generic netlink.

**Usage workflow.** A typical workflow when an engineer must verify module health or tune the build:

```
Step 1: Verify module presence and version
        $ lsmod | grep ^openvswitch
        openvswitch           172032  6
        nf_conncount           24576  1 openvswitch
        nf_nat                 57344  3 openvswitch,nft_chain_nat,xt_MASQUERADE
        nf_conntrack          172032  6 openvswitch,nf_nat,nf_conncount,...
        nf_defrag_ipv6         24576  2 nf_conntrack,openvswitch
        nf_defrag_ipv4         16384  2 nf_conntrack,openvswitch
        libcrc32c              16384  4 nf_conntrack,nf_nat,openvswitch,raid456

Step 2: Read the module metadata
        $ modinfo openvswitch
        filename:       /lib/modules/5.15.0-101-generic/kernel/net/openvswitch/openvswitch.ko
        license:        GPL
        description:    Open vSwitch switching datapath
        srcversion:     B6F3D5C... (depends on build)
        depends:        nf_conntrack,nf_nat,libcrc32c,nf_defrag_ipv6,nf_defrag_ipv4,nf_conncount
        retpoline:      Y
        intree:         Y
        name:           openvswitch
        vermagic:       5.15.0-101-generic SMP mod_unload modversions
        alias:          net-pf-16-proto-16-family-ovs_ct_limit
        alias:          net-pf-16-proto-16-family-ovs_meter
        alias:          net-pf-16-proto-16-family-ovs_packet
        alias:          net-pf-16-proto-16-family-ovs_flow
        alias:          net-pf-16-proto-16-family-ovs_vport
        alias:          net-pf-16-proto-16-family-ovs_datapath

Step 3: Verify the kernel log at load
        $ dmesg | grep -i openvswitch
        [   12.345678] openvswitch: Open vSwitch switching datapath

Step 4: Verify the kernel build option (for kernel package developers)
        $ grep CONFIG_OPENVSWITCH /boot/config-$(uname -r)
        CONFIG_OPENVSWITCH=m
        CONFIG_OPENVSWITCH_GRE=m
        CONFIG_OPENVSWITCH_VXLAN=m
        CONFIG_OPENVSWITCH_GENEVE=m

Step 5: Verify the Netlink genl family IDs
        $ genl ctrl list | grep ovs
        Name: ovs_datapath
        Name: ovs_vport
        Name: ovs_flow
        Name: ovs_packet
        Name: ovs_meter
        Name: ovs_ct_limit

Step 6: Run menuconfig (kernel rebuild scenario)
        $ make menuconfig
        Networking support  --->
          Networking options  --->
            <M> Open vSwitch
              <M>   Open vSwitch GRE tunneling support
              <M>   Open vSwitch VXLAN tunneling support
              <M>   Open vSwitch Geneve tunneling support
```

**When something goes wrong.** `openvswitch.ko` is a keyword to keep in mind in the following situations: (a) `ovs-vsctl` reports `database connection failed` but `ovs-vswitchd` is running; check `lsmod | grep openvswitch` to see whether the module is loaded; if the module failed to load, the Netlink genl family does not exist and `ovs-vswitchd` cannot connect. (b) `dmesg` reports `kernel BUG at net/openvswitch/...` or an oops with a function frame inside the module, signalling a kernel-side bug; collect the full stack trace and `cat /proc/version` to send upstream. (c) After a kernel upgrade, the `openvswitch-switch` service fails to start with `Failed to load module openvswitch`, usually because the `linux-modules-extra-$(uname -r)` package is missing (Ubuntu) or the DKMS build has not been rebuilt (RHEL out-of-tree). (d) `rmmod openvswitch` fails with `Module openvswitch is in use` even though `ovs-vswitchd` is stopped; check whether any datapath is still active through `cat /sys/kernel/debug/openvswitch/` (if debugfs is mounted) or `ip -d link show type openvswitch`. (e) FTP or TFTP conntrack helpers stop working after a security group is enabled; check whether `nf_conntrack_ftp` is loaded and whether `openvswitch.ko` sees it, by running `modprobe nf_conntrack_ftp`.

**Tightly related.** `openvswitch.ko` is woven together with: (a) the userspace `ovs-vswitchd` daemon (Part 9.1) through Netlink; without the module, the daemon has no datapath to install flows into. (b) `dpif-netlink` (Part 9.1, `lib/dpif-netlink.c` in userspace), the driver-provider abstraction that talks to the module through genl. (c) `nf_conntrack`, `nf_nat`, and `nf_conncount`, mandatory dependencies for the `ct()` action (Part 9.24); the module declares the dependency through `MODULE_DEPS`. (d) `vport-vxlan`, `vport-geneve`, `vport-gre`, and `vport-stt` plug into the module through the `vport_ops` framework (Part 11.0). (e) the `sw_flow_key` struct that contains `recirc_id` at the front (Part 9.2.14.2) is the shared datatype between the module and userspace through the `OVS_KEY_ATTR_*` Netlink attributes. (f) `tc-flower` hardware offload (Part 9.5) registers with the module through `flow_offload_setup_block_cb()`. (g) the megaflow cache (Part 9.2.2) lives in kernel-side RAM and does not persist across reboots or module unloads.

**Version differences.**

| Linux kernel | `openvswitch.ko` behaviour |
|--------------|----------------------------|
| < 3.3 | Not in-tree; the out-of-tree version from the OVS userspace tarball was required (DKMS) |
| 3.3 (2012) | Mainline initial; basic megaflow plus TSS classifier |
| 3.10 (2013) | `vport_ops` framework for tunnel-module plug-in |
| 3.12 (2013) | `OVS_ACTION_ATTR_HASH` for `select_via_hash` groups |
| 4.3 (2015) | `OVS_ACTION_ATTR_CT` mainlined (conntrack glue) |
| 4.6 (2016) | `OVS_ACTION_ATTR_CT_CLEAR` plus conjunction action |
| 4.9 (2016) | Conntrack zone limit (`nf_conncount` integration) |
| 4.13 (2017) | Hardware-offload TC flower integration |
| 4.19 (2018) | NSH support, `OVS_ACTION_ATTR_PUSH_NSH` and `POP_NSH` |
| 5.4 (2019) | LTS; conntrack expectation for the FTP ALG |
| 5.6 (2020) | `OVS_ACTION_ATTR_CHECK_PKT_LEN` for MTU enforcement |
| 5.15 (2021, curriculum baseline) | Stable; `OVS_RECURSION_LIMIT=4` (in the OVS out-of-tree datapath at v2.17.9), `DEFERRED_ACTION_FIFO_SIZE=10`, six genl families |
| 6.x | Refinement; no change to the public Netlink ABI |

Important note: **the kernel module version is independent of the OVS userspace version.** Ubuntu 22.04 with kernel 5.15 runs `openvswitch.ko` version "5.15.0-101-generic" (tied to the kernel build), while `ovs-vswitchd` can be OVS 2.17.9 from apt. Out-of-tree OVS 3.x has dropped the kernel module and only uses the in-tree version provided by the host kernel. Therefore `ovs-vswitchd --version` is not equal to the kernel module version; check both when debugging compatibility issues.

**Observation and verification.** Five ways to verify that `openvswitch.ko` is at work:

```
(1) Module loaded:
$ lsmod | grep ^openvswitch
openvswitch    172032  6

(2) Module metadata + signature:
$ modinfo openvswitch | head -10
filename:    /lib/modules/5.15.0-101-generic/kernel/net/openvswitch/openvswitch.ko
license:     GPL
description: Open vSwitch switching datapath
depends:     nf_conntrack,nf_nat,libcrc32c,nf_defrag_ipv6,nf_defrag_ipv4,nf_conncount
intree:      Y
name:        openvswitch

(3) Datapath alive (uses module):
$ sudo ovs-dpctl show
system@ovs-system:
  lookups: hit:42324 missed:156 lost:0
  flows: 8

(4) Netlink family registered:
$ genl ctrl list | awk '/Name: ovs_/ {print $2}'
ovs_datapath
ovs_vport
ovs_flow
ovs_packet
ovs_meter
ovs_ct_limit

(5) Kernel log load event:
$ dmesg -T | grep -i openvswitch
[Sun Apr 26 03:42:11 2026] openvswitch: Open vSwitch switching datapath
```

**Source code reference.** Per Rule 14 §14.4 Option C, this section uses function-name anchors instead of inline line numbers. Inline line numbers drift across releases, and the kernel datapath path here spans two trees (the OVS out-of-tree `datapath/` shipped with OVS v2.17.9, and the mainline Linux kernel tree) that have different line numbers. The user's local OVS clone has the OVS out-of-tree, so all anchors below were verified at OVS v2.17.9 against `datapath/`, with the understanding that the mainline kernel tree mirrors this structure but uses different line numbers.

- `net/openvswitch/datapath.c` (mainline kernel) or `datapath/datapath.c` (OVS out-of-tree), the main module file. The function `dp_init()` is the module entry point registered by `module_init(dp_init)`; it runs the full init sequence: `action_fifos_init`, `ovs_internal_dev_rtnl_link_register`, `ovs_flow_init`, `ovs_vport_init`, `register_pernet_device`, `register_netdevice_notifier`, `ovs_netdev_init`, `dp_register_genl`. The function `dp_cleanup()` is referenced in `module_exit(dp_cleanup)` and runs the reverse cleanup. The function `ovs_dp_cmd_new()` handles `OVS_DP_CMD_NEW` from userspace, and `ovs_dp_cmd_del()` handles `OVS_DP_CMD_DEL`. The module declares `MODULE_LICENSE("GPL")`, `MODULE_DESCRIPTION("Open vSwitch switching datapath")`, and six `MODULE_ALIAS_GENL_FAMILY()` aliases.
- `net/openvswitch/datapath.h`, the type definitions: `struct datapath` (per bridge), `struct ovs_net` (per namespace), and `struct ovs_skb_cb` (per packet metadata).
- `net/openvswitch/flow.h`, the type definitions: `struct sw_flow_key` (the `recirc_id` field is at the start), `struct sw_flow`, and `struct sw_flow_actions`.
- `net/openvswitch/flow_table.c` (mainline) or `datapath/flow_table.c` (OVS out-of-tree), the function `ovs_flow_alloc()` allocates a slab entry, and `ovs_flow_tbl_lookup_stats()` is the TSS lookup hot path. Note: an earlier version of this section cited these functions inside `flow.c`; the actual file is `flow_table.c`.
- `net/openvswitch/actions.c` (mainline) or `datapath/actions.c` (OVS out-of-tree), the function `do_execute_actions()` is the per-packet executor. The constants `DEFERRED_ACTION_FIFO_SIZE = 10` and `OVS_RECURSION_LIMIT = 4` (verified at OVS out-of-tree v2.17.9; mainline kernel may differ) cap the recursion depth and the deferred-action FIFO buffer. The recursion check is `if (unlikely(level > OVS_RECURSION_LIMIT))` inside `do_execute_actions`.
- `net/openvswitch/vport.c` plus `vport.h`, the vport abstraction; hooks into `net_device` through `netdev_rx_handler_register()`.
- `include/uapi/linux/openvswitch.h` (mainline) or `datapath/linux/compat/include/linux/openvswitch.h` (OVS out-of-tree), the Netlink UAPI: `#define OVS_DATAPATH_FAMILY "ovs_datapath"`, `OVS_DATAPATH_VERSION 2`, `OVS_PACKET_FAMILY "ovs_packet"`, `OVS_VPORT_FAMILY "ovs_vport"`, `OVS_FLOW_FAMILY "ovs_flow"`, `OVS_METER_FAMILY "ovs_meter"`, `OVS_CT_LIMIT_FAMILY "ovs_ct_limit"`. The `OVS_ACTION_ATTR_*` enum defines every action that the datapath executes.
- Userspace counterpart: `lib/dpif-netlink.c` (OVS userspace tree v2.17.9), the function `dpif_netlink_init()` and `dpif_netlink_open()` open the `NETLINK_GENERIC` socket family and resolve the genl family ID through `nl_lookup_genl_family()` in `lib/netlink-socket.c`.

> **Note on the v3.9.2 hotfix.** An earlier version of this section cited mainline Linux kernel v5.15 line numbers (for example `dp_init` at line 2711, `do_execute_actions` at line 1241, `OVS_RECURSION_LIMIT` at line 61). The user does not have the mainline kernel source on disk; the available source is the OVS out-of-tree `datapath/` at v2.17.9, which uses different line numbers. Plan v3.9.2 R2 dropped every inline line number for this section per Rule 14 §14.4 Option C, corrected the file path for `ovs_flow_alloc` and `ovs_flow_tbl_lookup_stats` from `flow.c` to `flow_table.c`, and corrected the `OVS_RECURSION_LIMIT` value from `5` to `4`. See `memory/sdn/v3.9.2-q7-axis16-sweep-2026-04-28.md`.

**Real-world incident.** An OpenStack Neutron deployment with security groups using the OVS firewall driver was running stably on Ubuntu 20.04 (kernel 5.4.0-150-generic) and lost internal connectivity between VMs after a kernel upgrade to 5.15.0-101-generic (no live patch was triggered; the node was restarted). Symptoms: `ovs-vswitchd` started, but `ovs-vsctl show` reported the bridge with no `datapath_id`, and every ping between VMs was dropped. First diagnostic: `lsmod | grep openvswitch` showed no module loaded. Next: `journalctl -u openvswitch-switch` reported `modprobe: ERROR: could not insert 'openvswitch': Exec format error`, and `dmesg` showed `openvswitch: version magic '5.4.0-150-generic SMP mod_unload modversions ' should be '5.15.0-101-generic SMP mod_unload modversions '`. Root cause: the package `linux-modules-extra-5.15.0-101-generic` was not installed (only the basic `linux-modules-5.15.0-101-generic` was). Resolution: `apt install linux-modules-extra-$(uname -r)`, then `modprobe openvswitch` succeeds, then `systemctl restart openvswitch-switch`, and the datapath is restored; VMs ping again. Lesson: the kernel module version magic must match the running kernel exactly; each kernel package has two parts (modules core plus modules extra), and `openvswitch.ko` lives in `extra`, which is easy to miss when automation installs only the main kernel.

**Synthetic exercise.**

```
Lab K1, "Observe the openvswitch.ko life cycle" (about 25 minutes):

Goal: Understand module load to datapath active to module unload requirements.

Step 1: Baseline. Verify the module and service are running.
        $ lsmod | grep ^openvswitch
        $ systemctl status openvswitch-switch
        $ sudo ovs-dpctl show
        Record the current number of bridges and ports.

Step 2: Stop the service but keep the module loaded.
        $ sudo systemctl stop openvswitch-switch
        $ lsmod | grep ^openvswitch
        Predict: module still loaded, refcount = 0 (Used-by column).
        Verify: the module is still visible but has no user.

Step 3: Try rmmod when no service is running.
        $ sudo rmmod openvswitch
        Predict: succeeds because refcount = 0.
        Observe: if "in use", check `ip -d link show type openvswitch`
                 to see if any internal port remains, and remove it first.

Step 4: Verify the Netlink families disappear.
        $ genl ctrl list | grep ovs
        Predict: empty.
        Explain: module unload auto-deregisters all 6 families.

Step 5: Reload the module and the service.
        $ sudo modprobe openvswitch
        $ dmesg | tail -3
        Verify: the line "openvswitch: Open vSwitch switching datapath" appears.
        $ sudo systemctl start openvswitch-switch
        Verify: the bridges and ports recover from the OVSDB persisted state.

Step 6: Falsification, module unload while the service is running.
        $ sudo systemctl start openvswitch-switch
        $ sudo rmmod openvswitch
        Predict: fails with "Module openvswitch is in use".
        Explain: ovs-vswitchd holds the Netlink socket, so refcount > 0.

Pass criteria: the learner explains why modprobe load auto-registers
              the Netlink families, and why the service requires the
              module to be loaded before it can start.
```

**Common failures and diagnostic signals.**

- *Symptom* `modprobe openvswitch` reports `Exec format error`. *Cause* The module was compiled for a different kernel than the one running (vermagic mismatch). *Diagnosis* `dmesg | tail` shows `version magic '...' should be '...'`; compare `uname -r` with `modinfo openvswitch | grep filename`. *Fix* Install `linux-modules-extra-$(uname -r)` (Ubuntu) or rebuild DKMS (`dkms autoinstall`).
- *Symptom* `ovs-vswitchd` logs `dpif|ERR|could not create datapath system: Address family not supported`. *Cause* The module is not loaded, so the genl family `ovs_datapath` does not exist. *Diagnosis* `lsmod | grep openvswitch` is empty. *Fix* `modprobe openvswitch` and verify `dmesg`.
- *Symptom* `rmmod openvswitch` reports `Module openvswitch is in use by: nf_nat`. *Cause* The dependency direction is misread; in fact `nf_nat` depends on many things, and conntrack helper modules may also be in use. *Diagnosis* `lsmod | grep openvswitch` and read the Used-by column; if 1 or more, there is a user. *Fix* `systemctl stop openvswitch-switch`, ensure no bridge is active, then `rmmod`.
- *Symptom* After a live kernel patch (kpatch), `openvswitch.ko` crashes with the oops "BUG: unable to handle page fault". *Cause* The live patch did not patch the module symbol correctly. *Diagnosis* `dmesg` full stack trace with a function frame in `net/openvswitch/`. *Fix* Reboot into an unpatched kernel, or rebuild the module with the patch.
- *Symptom* The FTP conntrack helper does not work (passive FTP fails) after an OVN security group is enabled. *Cause* `nf_conntrack_ftp` does not auto-load through `openvswitch.ko`. *Diagnosis* `lsmod | grep nf_conntrack_ftp` is empty; `cat /proc/sys/net/netfilter/nf_conntrack_helper` is 0. *Fix* `modprobe nf_conntrack_ftp` and add it to `/etc/modules-load.d/openvswitch.conf`.

**Comparison with other systems.** Other systems solve kernel-level packet forwarding differently:

- **Linux native bridge** (`bridge.ko`, source `net/bridge/`): equivalent in layered placement (the kernel module hooks into `netdev_rx_handler`) but with a much narrower feature scope. The bridge is L2 only, with no flow table, no wildcard classifier, and no Netlink genl programmable interface. The bridge uses STP and fixed MAC learning; OVS uses programmable OF rules. When a node runs both Linux bridge and OVS, the two modules hook into `netdev_rx_handler` in parallel, but each netdev can bind only one handler at a time.
- **macvlan and ipvlan**: kernel modules at the same layer that provide L2 or L3 multiplexing on a single netdev, but they are not switching and have no flow table.
- **VPP graph nodes** (FD.io, userspace): equivalent in graph-based packet processing, but it runs entirely in userspace with a DPDK polling driver, not as a kernel module. VPP `vnet/` is at the same abstraction level as `openvswitch.ko` `net/openvswitch/` (a graph node corresponds to an action), but the implementation environment is very different (kernel bypass versus in-kernel).
- **Cisco IOS-XR forwarding code** (proprietary, ASIC-assisted): operates at the same abstraction (flow table plus action) but runs on ASIC with hardware acceleration; `openvswitch.ko` is the software equivalent for commodity x86 servers.
- **`nftables` and `iptables`** (`nf_tables.ko` and `ip_tables.ko`): both live in `net/netfilter/` and are kernel modules for L2-L7 packet processing, but the paradigm differs. Netfilter has a fixed hook structure (PREROUTING, FORWARD, POSTROUTING) plus rule chains, with no flow cache or wildcard classifier. The OVS conntrack action calls into `nf_conntrack` and shares the table with netfilter, an important intersection (Part 9.24).
- **DPDK userspace datapath** (the OVS `netdev` datapath running inside `ovs-vswitchd`): an alternative to `openvswitch.ko`; same OVS userspace, but the datapath runs fully in userspace with polling threads. It is not a kernel module. Trade-off: higher throughput (10 to 50 Gbps line rate is feasible) but a dedicated CPU core, and it does not integrate natively with Linux netfilter conntrack. The curriculum baseline uses the kernel datapath through `openvswitch.ko`; the userspace datapath internals are out of scope.

### 9.2.14.4 conntrack execute, additional aspects

**Concept.** `conntrack_execute()` is the kernel function in `net/openvswitch/conntrack.c` that executes the conntrack action (`ct()` in an OF flow). Per-packet conntrack lookup plus state update.

**Importance.** Core of the OVS conntrack pipeline. Cross-link `9.24 ct_state` (the consumer of the state set by execute).

**How it works.** Triggered by the `OFPACT_CONNTRACK` action:
1. Look up the nf_conntrack table with the 5-tuple.
2. If this is the first packet, create a new entry (state=NEW).
3. If an entry exists, update the state (NEW to ESTABLISHED, and so on).
4. Set the packet's `ct_state`, `ct_zone`, `ct_mark`, and `ct_label` registers.
5. Optionally commit the entry, perform NAT rewrite, or invoke a helper.

**Source code reference.** `net/openvswitch/conntrack.c` `ovs_ct_execute()`. Linux `nf_conntrack_in()` underlies it. `lib/conntrack.c` is the userspace fallback (DPDK datapath).

**Common failures and diagnostic signals.**
- Mode A: Conntrack table full. Diagnostic: `nf_conntrack_count > nf_conntrack_max`. Recovery: increase the max sysctl.
- Mode B: Conntrack zone collision. Diagnostic: `ct_state` is INVALID. Recovery: zone partitioning.

**Comparison with other systems.** Linux iptables `-m conntrack` consumes the same kernel conntrack. Difference: iptables is an expression-chain hook; the OVS conntrack action is an OF flow stage.

---

## References

1. [The Design and Implementation of Open vSwitch, Pfaff et al., NSDI 2015](https://www.usenix.org/system/files/conference/nsdi15/nsdi15-paper-pfaff.pdf)
2. [Packet Classification using Tuple Space Search, Srinivasan et al., SIGCOMM 1998](https://dl.acm.org/doi/10.1145/285237.285282)
3. [man ovs-dpctl(8), man7.org](https://man7.org/linux/man-pages/man8/ovs-dpctl.8.html)
4. [Open vSwitch datapath documentation](https://docs.openvswitch.org/en/latest/topics/datapath/)
5. Jorge Crichigno et al. *Open Virtual Switch Lab Series*, USC, NSF Award 1829698. Local: `sdn-onboard/doc/ovs/OVS.pdf`, Lab 11.
6. Jorge Crichigno et al. *WASTC 2021 Workshop*, Lab 9. Local: `sdn-onboard/doc/ovs/Day 4-lab 9 - Open vSwitch Kernel Datapath.pdf`.
7. Offline source: `sdn-onboard/doc/compass_artifact_wf-*.md`, Chapter Q (datapath introspection), Chapter R (ovs-appctl reference).

---

## 9.2.15 OVS internals secondary group, in-depth supplement (2026-04 expansion)

> **Scope.** Five userspace components of `ovs-vswitchd` that act as the "brain" feeding the kernel datapath: `revalidator thread`, `handler thread`, `ukey`, `UFID`, and the conceptual pair `fast path / slow path`. These five have been mentioned in §9.2.4, §9.2.5, §9.2.6, §9.2.6.6, and §9.2.11; this section consolidates them and adds, for each keyword, a separate full treatment of concept, mechanism, observation, operation, real-world incidents, exercises, common failures, and comparison with other systems. Upstream sources: `ofproto/ofproto-dpif-upcall.c` (revalidator, handler, ukey), `lib/odp-util.c` (UFID), OVS v2.17.9 baseline.

---

### 9.2.15.1 `revalidator thread`

#### Concept

The revalidator thread is a userspace thread group inside the `ovs-vswitchd` process whose task is to periodically walk every megaflow installed in the kernel datapath, reconcile their behaviour with the current OpenFlow tables, and synchronise the state: update new actions, delete megaflows that have expired, or delete megaflows that no longer have any rule producing them. The name "revalidator" reflects the task of "revalidating" cache correctness, because a megaflow is a re-compiled result at install time and can become incorrect when controller or OVN rules change.

#### History and background

The revalidator mechanism was split into its own thread group around OVS 2.0 (2014), in step with the new userspace-cache model after the NSDI 2015 paper "The Design and Implementation of Open vSwitch". Earlier, OVS used the "facet" and "subfacet" mechanism inside the main thread, which scaled poorly. The number of revalidator threads has been configurable through `other_config:n-revalidator-threads` since OVS 2.4. The default is `ceil(CPU/4)` on a modern host, with a minimum of 1.

#### Position in the architecture

The revalidator lives in the `ofproto-dpif-upcall.c` userspace layer, between the OF rule store (`classifier`) and the datapath interface (`dpif`). The userspace thread group consists of: the main thread (OVSDB IDL processing), N handler threads (upcall processing), and M revalidator threads (sweep processing). Revalidators do NOT process packets directly; they only read and write megaflow metadata.

#### Role

Four main roles: (1) maintain correctness so megaflows in the kernel match the current OF pipeline; (2) reclaim idle megaflows and free kernel memory; (3) update the `n_packets` and `n_bytes` counters from kernel back to userspace; (4) force the kernel to delete a megaflow when the related rule is removed by the OF controller or a higher-priority rule replaces it.

#### Why it exists

The kernel datapath knows nothing about OF semantics; it only executes the already installed actions. When rules change, the kernel cannot be expected to update by itself. Userspace needs a background "vacuum cleaner" sweeping the cache. Putting that into the main thread would block upcall handling when the cache is large (hundreds of thousands of megaflows). Splitting it into a separate thread group is a natural scale-out decision.

#### How it works

Each sweep cycle (default about 500 ms): the thread acquires the RCU read lock, snapshots the full ukey list, and shards it evenly across the M revalidator threads (round-robin by ukey hash). For each ukey in its shard, the thread calls `revalidate_ukey()`: it dumps `OVS_FLOW_CMD_GET` for the current stats, re-walks the OF pipeline (`xlate_actions()`), and compares the new result with the existing action. If different, it sends `OVS_FLOW_CMD_SET` (or `_DEL`); if the same, it updates the `last_used` timestamp and stats. Main functions: `revalidator_run()` and `revalidator_sweep()` in `ofproto/ofproto-dpif-upcall.c`.

#### How an operator masters this

A level-2 engineer needs to know: (a) the tunable `n-revalidator-threads`; (b) how to interpret `upcall/show` to see whether the revalidator is keeping up; (c) running `revalidator/wait` as a barrier when debugging a race; (d) reading the counter `coverage/show facet_revalidate` to quantify the load; (e) knowing when to raise the thread count (average updates per thread above 5 per second, or dump duration above 1 second).

#### Observation and verification

```
$ sudo ovs-appctl upcall/show
system@ovs-system:
  flows         : (current 2345) (avg 1234) (max 4567) (limit 200000)
  dump duration : 12ms
  ufid enabled : true
  0: (keys 456)   ← revalidator 0 manages 456 ukey
  1: (keys 432)
  2: (keys 123)
  3: (keys 234)
```

`dump duration: 12ms` is the time for one sweep cycle to complete. Above 1000 ms is a warning. Detailed counters:

```
$ sudo ovs-appctl coverage/show | grep -E "revalidate|ukey"
flow_dumps_done                 4567 / sec
revalidate                      4501 / sec
ukey_invalid                    23 / sec
```

#### Source code reference

The functions `revalidator_run()` and `revalidator_sweep()` are in `ofproto/ofproto-dpif-upcall.c` (OVS branch-2.17). The function `revalidate_ukey()` performs the action comparison; `delete_op_init()` builds the batched `OVS_FLOW_CMD_DEL`. Main struct: `struct revalidator` holds the per-thread state.

#### Version differences

- OVS 2.0 (2014): the revalidator was split out of the main thread.
- OVS 2.4 (2015): the tunable `n-revalidator-threads`.
- OVS 2.7 (2017): UFID enforced when communicating with the kernel, reducing the Netlink message size for the dump.
- OVS 2.13 (2020): batched delete optimised, syscall count reduced.
- OVS 2.17 (2022, baseline): default 4 threads on a 16-core host; integer-overflow fix in `udpif_revalidator()`.

#### Real-world incident

A production case in Part 9.26 forensic "revalidator storm": the OF controller (Faucet) pushed 5000 rule updates in 1 second; the revalidator was forced to re-walk all approximately 50K ukeys, and dump duration jumped from 12 ms to 8500 ms. As a result, the handler threads were starved by contention on the ofproto mutex, packet rate dropped by 30 percent, and some TCP retransmits occurred. Mitigation: raise `n-revalidator-threads` from 4 to 12 and batch rule pushes through `ovs-ofctl --bundle replace-flows`.

#### Common failures and diagnostic signals

| Symptom | Likely cause | Verification |
|---------|--------------|--------------|
| `dump duration` above 5 seconds | Many ukeys, too few threads | `upcall/show` and `memory/show udpif keys` |
| Low `flow_dumps_done` rate | Revalidator backlog | `coverage/show flow_dumps_done` |
| Kernel megaflow stale (wrong action) | Revalidator crashed or deadlocked | `ovs-appctl revalidator/wait` blocks forever |
| High `ukey_invalid` rate | RCU race when rules change quickly | `coverage/show ukey_invalid` |

#### Comparison with other systems

Linux conntrack has a `gc_worker` running every 1/8 second to sweep the `nf_conn` table, similar to a revalidator: same "background sweeper updating cache state" pattern. Cilium eBPF has no equivalent because BPF maps do not need to be revalidated; an LRU map self-evicts instead. Cisco Nexus 9000 NX-OS has a `vshd` thread that sweeps the hardware MAC table in a similar way.

---

### 9.2.15.2 `handler thread`

#### Concept

The handler thread is a userspace thread group inside `ovs-vswitchd` that specifically handles upcalls: when the kernel datapath encounters a packet that does not match a megaflow, the kernel sends the packet (with metadata) up to userspace through a Netlink socket. The handler thread receives it, runs the OpenFlow pipeline, compiles a new megaflow, installs it back into the kernel, and (depending on context) re-injects the packet to avoid dropping the first packet.

#### History and background

Split into its own thread group from OVS 2.0, in the same wave as the revalidator. Previously, upcalls were handled in the main thread, a clear single-thread bottleneck when miss rate rose. The tunable `other_config:n-handler-threads` exists since OVS 2.4. Default was the number of CPU cores (until OVS 2.13), then changed to `ceil(CPU/2)` to leave cores for the revalidator and main thread.

#### Position in the architecture

The handler runs in parallel with the revalidator in `ofproto-dpif-upcall.c`. Each handler registers a Netlink PID with the kernel; the kernel hashes the flow key to choose which handler receives the upcall (load balanced). The handler calls `xlate_actions()` (the same module the revalidator uses) to run the OF pipeline.

#### Role

(1) Receive the upcall from the kernel; (2) parse the Netlink message into `struct dp_packet` plus `struct flow`; (3) classify through the TSS classifier to get the matching OF rule; (4) execute the action chain in the xlate context; (5) compile into a megaflow with the mask widened as far as possible (lazy wildcarding); (6) install the megaflow into the kernel through `OVS_FLOW_CMD_NEW`; (7) re-inject the packet through `OVS_PACKET_CMD_EXECUTE` so that the first packet is not lost.

#### Why it exists

The slow path is an unavoidable consequence of the cache architecture: every flow misses on first use. If the main thread were to handle this, a miss rate around 10K/sec would be enough to saturate it. Splitting into a thread group and load-balancing by flow-key hash allows scaling with CPU count.

#### How it works

```
Kernel openvswitch.ko          Userspace ovs-vswitchd
─────────────────────          ──────────────────────
ovs_dp_process_packet()
    │ (cache miss)
    ▼
ovs_dp_upcall()
build OVS_PACKET_CMD_MISS
    │ Netlink (genl)
    ▼
                              udpif_upcall_handler()  ← thread N
                                    │ recv_upcalls()
                                    │ parse → struct upcall
                                    │ xlate_actions()
                                    │ ukey_create_from_upcall()
                                    │ dpif_flow_put() OVS_FLOW_CMD_NEW
                                    │ dpif_execute() OVS_PACKET_CMD_EXECUTE
                                    ▼
                              megaflow installed in kernel
                              packet re-injected
```

Main functions: `udpif_upcall_handler()` (loop), `recv_upcalls()` (drain Netlink), `process_upcall()` (per-packet xlate plus install).

#### How an operator masters this

An engineer needs to know: (a) the tunable `n-handler-threads`; (b) reading the per-thread keys count in `upcall/show` to see load balance; (c) distinguishing the symptom "high miss rate" (normal for new traffic) from "high lost rate" (abnormal, kernel drop because handlers are blocked); (d) when to raise the thread count (average upcalls in flight above 100, or lost above 0).

#### Observation and verification

```
$ sudo ovs-appctl upcall/show
system@ovs-system:
  flows         : (current 2345) (avg 1234) (max 4567) (limit 200000)
  dump duration : 12ms
  ufid enabled : true
  0: (keys 456)   ← handler 0 has installed 456 megaflow
  1: (keys 432)
  2: (keys 123)
  3: (keys 234)

$ sudo ovs-appctl coverage/show | grep -E "upcall|miss"
miss_upcall                     1247 / sec
flow_install                    1198 / sec
upcall_flow_limit_hit           0 / sec
```

The `miss_upcall` rate should be roughly equal to the `flow_install` rate. If `upcall_flow_limit_hit` is greater than 0, the `set-flow-limit` (default 200K) has been hit; raise it with `upcall/set-flow-limit 500000`.

#### Source code reference

`udpif_upcall_handler()` and `recv_upcalls()` are in `ofproto/ofproto-dpif-upcall.c` (branch-2.17). `process_upcall()` performs xlate; `handle_upcalls()` batches the install. The struct `struct handler` holds the per-thread state plus the Netlink PID.

#### Version differences

- OVS 2.0: handler split out of the main thread.
- OVS 2.4: tunable `n-handler-threads`.
- OVS 2.7: kernel batch upcall (`OVS_PACKET_ATTR_MRU`).
- OVS 2.13: default changed to `CPU/2`.
- OVS 2.17 (baseline): batched flow install through an `OVS_FLOW_CMD_NEW` array, reducing syscalls.

#### Real-world incident

Part 9.32 "handler starvation case": a 32-core host running DPDK PMD on 8 cores, with OVS userspace defaulting to 16 handler threads, became stuck on `ofproto_mutex` contention when the controller pushed updates simultaneously with rising traffic. Coverage showed `miss_upcall` at 50K/sec but `flow_install` only at 12K/sec, indicating a backlog. The kernel `lost` counter rose. Mitigation: split into 2 bridges, one per port group, each with its own handler pool.

#### Common failures and diagnostic signals

| Symptom | Cause | Verification |
|---------|-------|--------------|
| `lost` greater than 0 in `dpctl show` | Handlers not draining in time | `coverage/show miss_upcall` much greater than `flow_install` |
| Abnormally high first-packet latency | Too few threads | `upcall/show` shows key imbalance |
| One core at 100 percent while others idle | Hash collision on the Netlink PID | `top -H -p $(pgrep ovs-vswitchd)` |
| TCP SYN retry on a new flow | Handlers blocked during a burst | `tcpdump` shows duplicate SYN |

#### Comparison with other systems

Equivalent to Cilium's BPF tail-call dispatch but in the opposite direction: BPF runs in the kernel, while the OVS handler runs in userspace. The `ipset` hash table in nftables does not have an upcall because rules are fixed; OVS, by contrast, programs rules dynamically through OF and needs a slow path for the first packet. Hardware ASIC switches use an "exception path" to the control-plane CPU on miss; the role is similar to the handler's.

---

### 9.2.15.3 `ukey` (unique key)

#### Concept

A ukey is a userspace data structure (`struct udpif_key`) that represents one megaflow installed in the kernel. It is the "shadow copy" of that megaflow in userspace, storing the key, mask, action, counters (last seen, n_packets, n_bytes), and lifecycle state. The ukey is the object that the revalidator thread walks; without ukeys, the revalidator has nothing to check.

#### History and background

Appeared in the same "facet/subfacet to ukey" refactor in OVS 2.0. Before that, facet and subfacet were more complex and scaled poorly with a large cache. The ukey was designed to be lean: one ukey per kernel megaflow, looked up through a hashmap keyed by the flow-key hash.

#### Position in the architecture

The ukey lives in userspace, separate per `dpif`. It is organised inside `struct umap` (a hash table sharded to avoid contention between N handlers and M revalidators). It is accessed through `ukey_install()` (handler creates), `ukey_lookup()` (revalidator queries), and `ukey_delete()` (cleanup).

#### Role

(1) Track the megaflow life cycle between userspace and the kernel; (2) store the metadata needed for revalidation (xlate context, OF rule reference); (3) cache stats for rate limiting and monitoring; (4) coordinate between the handler (creator) and the revalidator (consumer) through an atomic state machine.

#### Why it exists

The kernel only knows "key plus mask plus action plus counter". It does not know which OF rule produced a given megaflow or how it should be re-validated. Userspace needs a counterpart copy to hold that information. This is an implicit kernel-userspace bridge structure, completely invisible to the OF controller but critical for correctness.

#### How it works

A five-state state machine: `INITIAL -> VISIBLE -> CHECKING -> EVICTING -> FREED` (details in §9.2.11.1). Transitions:

```
        INITIAL  ── handler install kernel ──▶ VISIBLE
                                                  │
                 revalidator sweep ───────────────┤
                                                  ▼
                                              CHECKING
                                                  │ pipeline match? ─ no ─▶ EVICTING ─▶ FREED
                                                  │ ─ yes ─▶ VISIBLE (stay)
```

An RCU grace period (about 25 ms) after EVICTING ensures that no thread is still reading the ukey (a handler may still be running xlate on the same key).

#### How an operator masters this

An engineer needs to know: (a) `memory/show udpif keys` shows the global ukey count; (b) the ratio `udpif keys / rules` measures cache coverage; (c) `udpif keys` greater than `rules` is a pathology (ukey leak; commit `180ab2fd635e` fixed it); (d) read the state machine to understand race conditions when debugging.

#### Observation and verification

```
$ sudo ovs-appctl memory/show
udpif keys:1247
rules:892
ports:23
ofconns:3
```

A ratio of 1247/892 = 1.4 is normal (each rule on average spawns about 1.4 megaflows because of different wildcard fields). A ratio below 0.5 means a cold cache (traffic is not yet diverse). A ratio above 5 indicates fragmentation, possibly from an action chain that depends on a wide field.

```
$ sudo ovs-appctl coverage/show | grep ukey
ukey_invalid                    23 / sec
ukey_dp_change                  4 / sec
```

#### Source code reference

The `udpif_key` struct is defined in `ofproto/ofproto-dpif-upcall.c` (branch-2.17). Functions: `ukey_create_from_upcall()` (handler creates), `ukey_install()` (insert into the hashmap), `ukey_lookup()` (revalidator), `ukey_delete()` and `ukey_destroy()` (cleanup with RCU).

#### Version differences

- OVS 2.0: introduced.
- OVS 2.4: sharded `umap` to reduce contention.
- OVS 2.7: use UFID as a secondary key (in addition to the flow-key hash) to make dumps faster.
- OVS 2.13: optimised batched delete.
- OVS branch-2.17: leak fix `180ab2fd635e` for the corner case where the handler crashes between install and register.

#### Real incident

Part 9.1.Z OOM case: an OpenStack cluster with 200 chassis, each running `ovs-vswitchd`; OVN port_binding update spam caused a slow ukey leak (one ukey per hour per chassis); after 30 days, `ovs-vswitchd` RSS reached 8 GB and was OOM-killed by the kernel. Fix: upgrade to branch-2.17 containing commit `180ab2fd635e`, and add a cron `ovs-appctl revalidator/purge` every 24 hours as defence in depth.

#### Common failures and diagnostic signals

| Symptom | Cause | Diagnostic |
|---------|-------|------------|
| `udpif keys` much greater than `rules` | Ukey leak | `memory/show` ratio above 5 |
| `ovs-vswitchd` RSS rises monotonically | Slow leak | `ps -p $(pgrep ovs-vswitchd) -o rss` over 7 days |
| Revalidator does not remove idle ukey | Old-version bug | Compare version against fix `180ab2fd635e` |

#### Comparison with other systems

The Linux `nf_conn` struct in conntrack is similar: per-flow shadow state, RCU-protected, with a cleanup thread. An eBPF LRU map has no userspace shadow because the map lives in the kernel; instead `bpftool map dump` is used. A Cisco TCAM entry has no shadow because the hardware manages it; the software mirror is an NMS (Netconf collector).

---

### 9.2.15.4 `UFID` (Unique Flow ID)

#### Concept

UFID is a 128-bit identifier (UUID style) assigned to each megaflow when the handler thread installs it into the kernel. Goal: replace sending the entire flow key (which can be a few hundred bytes) in every Netlink message; only the UFID (16 bytes) needs to be sent, and both sides (kernel and userspace) look up their corresponding structures. This significantly reduces the Netlink traffic of the revalidator dump cycle.

#### History and background

Introduced in OVS 2.4 (2015) and enabled by default in OVS 2.7 (2017). Before UFID, each `OVS_FLOW_CMD_GET` dump had to send the full `OVS_FLOW_ATTR_KEY` (the flow-key TLV). With 50K megaflows, the dump cycle sent about 10 MB through Netlink every 500 ms, a bottleneck. UFID reduces this to about 800 KB.

#### Position in the architecture

UFID is an attribute in the Netlink protocol between userspace and `openvswitch.ko`. Specifically, `OVS_FLOW_ATTR_UFID` (TLV type) in `<linux/openvswitch.h>`. Kernel side: UFID is stored in `struct sw_flow_id` field `ufid`. Userspace side: stored in `struct udpif_key` field `ufid`.

#### Role

(1) Compress the Netlink payload of the dump cycle; (2) allow kernel-side hashing of the flow by UFID instead of computing the hash from the flow key; (3) simplify `OVS_FLOW_CMD_DEL` (only the UFID is needed, no need to resend the full key); (4) support batch operations more efficiently (an array of UFIDs instead of an array of full keys).

#### Why it exists

When the cache size grew from a few thousand (OVS 1.x facet) to hundreds of thousands (OVS 2.x megaflow), Netlink overhead became a bottleneck. UFID is a clear optimisation: 16 bytes versus 200 bytes is a 92 percent reduction on the wire.

#### How it works

When the handler creates a megaflow, it calls `dpif_flow_hash()` to generate a UFID equal to the SHA-1 hash of (flow key plus mask) truncated to 128 bits. It sends `OVS_FLOW_CMD_NEW` with both `OVS_FLOW_ATTR_KEY` and `OVS_FLOW_ATTR_UFID`. The kernel caches both but uses UFID as the hashmap secondary key. The revalidator then dumps with the `OVS_UFID_F_OMIT_KEY` and `OVS_UFID_F_OMIT_ACTIONS` flags, so the kernel returns stats by UFID and does not resend the key or actions when they have not changed.

#### How an operator masters this

An engineer needs to know: (a) UFID is enabled when `ufid enabled : true` appears in `upcall/show`; (b) host kernels older than 4.0 do not support UFID and need the fallback (rare today); (c) `ovs-appctl dpctl/dump-flows --ufid` shows the UFID; (d) `ofproto/trace --ufid <UFID>` is faster than the full key in scripting.

#### Observation and verification

```
$ sudo ovs-appctl upcall/show | grep ufid
  ufid enabled : true

$ sudo ovs-appctl dpctl/dump-flows --more
ufid:9b8a7c6d-1234-5678-9abc-def012345678,recirc_id(0),in_port(2),...
  packets:1247, bytes:91234, used:0.123s, actions:output(3)
```

UFID is shown as a UUID. With `dpctl/del-flows --ufid 9b8a7c6d-...` you can delete an entry without copy-pasting the entire flow key.

#### Source code reference

The function `dpif_flow_hash()` in `lib/dpif.c` (computes UFID). The functions `odp_ufid_from_string()` and `odp_ufid_to_string()` in `lib/odp-util.c` (serialise and deserialise). The struct `ovs_u128` is defined in `lib/util.h`. Kernel side: `struct sw_flow_id` in `net/openvswitch/flow.h`.

#### Version differences

- OVS 2.4 (2015): introduced, opt-in through kernel feature detection.
- OVS 2.7 (2017): enabled by default when the kernel supports it.
- Linux kernel 4.0 and later: `OVS_FLOW_ATTR_UFID` is recognised.
- OVS 2.13: added the `--ufid` flag to the `dpctl` CLI.
- OVS 2.17 (baseline): UFID is mandatory for dpif-netdev (DPDK), optional for dpif-netlink (kernel) for backward compatibility.

#### Real incident

Rackspace 2018 production case: a host kernel upgrade from 3.10 to 4.4 while keeping OVS 2.5; UFID was disabled because the code path still checked the kernel version and did not fully recognise 4.4. Consequence: the dump cycle consumed about 30 percent CPU instead of about 5 percent. Diagnosed through `upcall/show` showing `ufid enabled : false`; fixed by upgrading OVS to 2.7 or later. Lesson: UFID enablement depends on both userspace and kernel.

#### Common failures and diagnostic signals

| Symptom | Cause | Diagnostic |
|---------|-------|------------|
| `ufid enabled : false` | Old kernel or old OVS | `uname -r` and `ovs-vsctl --version` |
| High dump-cycle CPU | UFID not active | `coverage/show flow_dumps_done` per cycle |
| `dpctl/dump-flows` does not show UFID | CLI did not pass `--more` | Re-run with `--more` |

#### Comparison with other systems

Linux conntrack has `nf_conn->ext` carrying a 32-bit "id", but it does not serve a wire protocol the way UFID does. An eBPF map uses the key directly and does not need UFID-style indirection because the map lives in the kernel. SR-IOV NIC offload uses a 32-bit "flow handle" assigned by firmware, a role close to UFID but at hardware scope. P4Runtime uses a 64-bit `MatchEntry` ID with the same motivation of reducing wire overhead.

---

### 9.2.15.5 `fast path / slow path`

#### Concept

This concept pair splits OVS packet processing into two routes: the **fast path** processes the packet entirely inside the kernel datapath (`openvswitch.ko`) once the megaflow is cached, without leaving kernel context; the **slow path** processes the packet inside userspace `ovs-vswitchd` through a handler thread, activated on a cache miss or when the action requires it (for example, the `controller` or `userspace` action). Fast path is measured in nanoseconds and microseconds; slow path is measured in tens to hundreds of microseconds.

#### History and background

The concept pair has its roots in traditional cache design (CPU L1 versus main memory). It was applied to networking starting with Linux NAPI (1990s) and later the `nf_conntrack` fast path. OVS inherited and clarified the model from day one (2009): the kernel datapath is the fast path, userspace is the slow path. Part 9.2.4 already introduced it; this section consolidates the full view.

#### Position in the architecture

```
                        ┌───────────────────────────┐
Userspace               │  ovs-vswitchd             │
                        │   handler thread (SLOW)   │
                        │   xlate_actions, cache    │
                        └────────────▲──────────────┘
                                     │ Netlink upcall
                        ┌────────────┴──────────────┐
Kernel                  │  openvswitch.ko (FAST)    │
                        │   megaflow lookup, exec   │
                        └───────────────────────────┘
```

The fast path is the lower box. The slow path is the upper box plus the Netlink channel plus the lower box (round trip).

#### Role

Fast path: handles the vast majority of steady-state packets at low cost. Slow path: handles the first packet of every flow, special packets that need the controller, or packets that miss the cache for any reason. The fast-to-slow path ratio is the hit rate, the headline KPI.

#### Why it exists

The slow path cannot be completely avoided: the first packet of every flow misses because the cache has no entry yet. The fast path cannot be completely avoided either: if every packet went into userspace, throughput would drop 10 to 100 times because of context switches and Netlink overhead. The two-layer architecture is the right compromise.

#### How it works

Typical fast-path latency: 1 to 3 microseconds per packet (kernel datapath lookup plus execute). Typical slow-path latency: 50 to 200 microseconds per packet (Netlink up, xlate, Netlink down, execute). Ratio: about 50 times. Steady-state target: hit rate above 99 percent, miss rate below 1 percent, lost = 0.

USC Lab 9 illustrates this clearly: on the first `ping h1 -> h2`, the ICMP echo goes through the slow path (about 150 microseconds); from the second packet onward it goes through the fast path (about 2 microseconds). `ovs-dpctl dump-flows` shows that the megaflow appears only after the first packet.

#### How an operator masters this

An engineer needs to know: (a) read `ovs-dpctl show` to see hit, missed, lost; (b) compute the hit rate; (c) understand why the first TCP connection has unusually high RTT (slow path) and is normal afterwards (fast path); (d) when a rising miss rate is normal (new traffic) versus abnormal (rule churn, undersized cache).

#### Observation and verification

```
$ sudo ovs-dpctl show
system@ovs-system:
        lookups: hit:9876543 missed:12345 lost:0
        flows: 1247
        masks: hit:9876543 total:9888888 hit/pkt:1.00
```

Hit rate = 9876543 / (9876543 + 12345) = 99.875 percent, which is fine. If `lost > 0`, the kernel is dropping because the handler cannot drain in time.

```
$ sudo ovs-appctl coverage/show | grep -E "(hit|miss|lost)"
flow_dumps_done                 4567 / sec
miss_upcall                     12 / sec
flow_lookup_extracted_kernel    9876 / sec
```

#### Source code reference

Fast path: `ovs_dp_process_packet()` in `net/openvswitch/datapath.c` (Linux kernel tree, not the OVS userspace tree). Slow path: `udpif_upcall_handler()` in `ofproto/ofproto-dpif-upcall.c`. Hand-off: `ovs_dp_upcall()` in the kernel and `recv_upcalls()` in userspace.

#### Version differences

The fast/slow path concept is invariant. Implementation has improved across versions:

- OVS 1.x (microflow): fast path was too narrow, with a high miss rate in production.
- OVS 2.0 (megaflow): fast path expanded significantly, hit rates of 99 percent or more became common.
- OVS 2.x DPDK (PMD): fast path is fully userspace (dpif-netdev), slow path is still the handler thread in the same process.
- OVS 2.7+ AF_XDP: an alternative datapath that bypasses the kernel.
- OVS 2.17 (baseline): fast path is kernel or DPDK; slow path is conceptually unchanged.

#### Real incident

Cloud provider 2020 case "first-packet TCP RTT jitter": a latency-sensitive application complained that the TCP handshake was sometimes 50 ms instead of 2 ms. Diagnosis: the chassis had a cron job flushing conntrack every 5 minutes, which dragged in `ovs-appctl revalidator/purge` and cleared all megaflows. After the purge, every new flow missed and accumulated 150 microseconds of slow-path latency across the three handshake packets. Fix: stop the cron, let the revalidator clean up via `idle_timeout`.

#### Common failures and diagnostic signals

| Symptom | Cause | Diagnostic |
|---------|-------|------------|
| Hit rate below 95 percent in steady state | Small cache size, diverse traffic | `ovs-dpctl show` and `upcall/show` |
| Unusually high first-packet latency | Slow path saturated | `tcpdump` SYN RTT versus SYN-ACK |
| `lost > 0` | Handler bottlenecked | `coverage/show miss_upcall` rate |
| Throughput does not scale to NIC line rate | Slow-path bottleneck | Raise `n-handler-threads` |

#### Comparison with other systems

- Linux conntrack has a "fast path" (cached `nf_conn` lookup) and a "slow path" (`nf_conntrack_in` resolve), but the granularity is per connection (5-tuple), not per megaflow.
- Cilium eBPF has no explicit slow path: a BPF map miss means drop or fallback policy, with no upcall.
- Cisco ASIC switch: the fast path is the TCAM and SDK lookup, the slow path is the CPU control plane (CoPP). Similar architecture.
- VPP has a similar dual path: vector graph nodes, but with no cache-miss-style upcall.
