# v3.9.4 Baseline Reconfirmation (Phase R0)

> **Local OVS repo:** C:\Users\voleh\Documents\ovs (checked out at v2.17.9).
> **Commit:** 0bea06d9957e3966d94c48873cd9afefba1c2677 (2024-02-08).
> **Plan:** plans/sdn/v3.9.4-ovs-block-comprehensive-resolution.md Phase R0.
> **Purpose:** Confirm the v3.9.1 Q0 baseline plus the v3.9.2 R0 plus v3.9.3 R0 reconfirmations still apply for the v3.9.4 comprehensive resolution.
> **Date:** 2026-04-28.

---

## 1. Repo state

### 1.1. OVS repo

```
$ cd C:/Users/voleh/Documents/ovs
$ git rev-parse HEAD
0bea06d9957e3966d94c48873cd9afefba1c2677
```

The OVS repo is at the v2.17.9 release commit (annotated tag `v2.17.9`, commit dated 2024-02-08, message "Set release date for 2.17.9"). Matches the baseline locked in plan v3.9.1 Q0 and reconfirmed in v3.9.2 R0 plus v3.9.3 R0.

### 1.2. Curriculum repo

```
$ cd C:/Users/voleh/Documents/network-onboard
$ git log --oneline -5
212bac0 docs(plans): R-1 v3.9.4 plan-file save
af75b5d chore(meta): R-1 v3.9.4 v3.9.3 plan-file closure callout
67cb23b chore(meta): R-1 v3.9.4 v3.9.3 final-audit report partial closure
dfbfbf9 chore(meta): R1 v3.9.3 audit log 9.4 and 9.11 axis-16
f6d2178 chore(meta): R0 v3.9.3 baseline reconfirmation

$ git branch --show-current
docs/sdn-foundation-rev2

$ git status --short | grep -v "^??"
(no output: zero modified or staged tracked files at R0 entry)
```

Curriculum repo HEAD is on `docs/sdn-foundation-rev2` at commit `212bac0`, the v3.9.4 plan-save commit. Working tree clean of tracked-file changes.

---

## 2. Spot-check grep results

All three greps run against the OVS repo at v2.17.9 (HEAD `0bea06d9957e3966d94c48873cd9afefba1c2677`).

### 2.1. classifier_insert and classifier_lookup

```
$ grep -nE "^classifier_(insert|lookup)" lib/classifier.c
690:classifier_insert(struct classifier *cls, const struct cls_rule *rule,
938:classifier_lookup__(const struct classifier *cls, ovs_version_t version,
1166:classifier_lookup(const struct classifier *cls, ovs_version_t version,
```

Match: `classifier_insert` line 690, `classifier_lookup` line 1166. PASS.

### 2.2. raft_run

```
$ grep -nE "^raft_run" ovsdb/raft.c | head -2
1930:raft_run(struct raft *raft)
3755:raft_run_reconfigure(struct raft *raft)
```

Match: `raft_run` line 1930, `raft_run_reconfigure` line 3755. PASS.

### 2.3. ovsdb_rbac_insert and ovsdb_rbac_update

```
$ grep -nE "^ovsdb_rbac_(insert|update)" ovsdb/rbac.c
149:ovsdb_rbac_insert(const struct ovsdb *db, const struct ovsdb_table *table,
308:ovsdb_rbac_update(const struct ovsdb *db,
```

Match: `ovsdb_rbac_insert` line 149, `ovsdb_rbac_update` line 308. PASS.

---

## 3. Pre-commit hook status

The pre-commit hook at `.git/hooks/pre-commit` was verified active during the v3.9.4 R-1 commits (`67cb23b`, `af75b5d`, `212bac0`); each commit ran the four staged-only checks (anti_gaming, rubric_leak, em_dash, lang) and reported PASS.

Hook references:
- `python scripts/em_dash_check.py --staged`
- `python scripts/lang_check.py --staged`
- `python scripts/anti_gaming_check.py --staged`
- `python scripts/rubric_leak_check.py --staged`

---

## 4. Verdict

PASS. The v3.9.1 Q0 baseline plus the v3.9.2 R0 plus v3.9.3 R0 reconfirmations still apply for the v3.9.4 comprehensive resolution. No drift detected. Plan v3.9.4 may proceed to Phase R1.A (9.4 §1 to §10 fix per §9.A decision matrix).

The corrected method per plan v3.9.1 §0.3 (`git checkout v2.17.9` plus `grep -nE "^<fn>" <file>`, anchored at column 0 per OVS coding style) is preserved and operative. The wider-grep fallback for FABRICATED disambiguation (`grep -rn "<fn>" lib/ ofproto/ utilities/ include/`) per plan v3.9.4 §3.5 is also operative.
